# Revisión independiente del backend17

**APPROVED para el paquete core/PG/migración/wiring congelado**, sin cerrar17 ni acreditar frontend, smoke o campañas. Lectura de COMMON, delta desde7e7c8ff y fuentes finales; no ejecución de tests ni cambios productivos. Verificación8bc66b: los26 hashes coinciden, XML preservado contiene175 tests en13 suites, cero fallos/errores/saltos. EXIT0 dc5c90 procede de la ejecución de A; este juez verificó los XML, no repitió Gradle.

- `ExtendWorkSession:21–61`, `WorkSessionEnd.requireTime` y `WorkSessionState.requireClose/requireTime`: cantidad antes del puerto; revisión/estado/máximo antes del Clock; captura única truncada aµs; max(fin,now)+minutos; año superior rechazado y año0000 menor que changedAt válido. EXTEND sólo modifica revisión en State6, conservando igualdad runningSince/changedAt y trabajo. La marca separada controla todas las decisiones, no relaja el dominio histórico. Acumulación2880 y RESUME en la misma marca tienen oráculos nuevos concretos.
- `PostgresWorkSessionStore:68–134` mantiene propiedad→identidad→replay→operación bajo lock de sesión. Actualiza sólo fin/revisión/marca y confirma recibo/evento en la misma transacción. P/R/C: la guarda de marca se aplica al occurredAt ya capturado antes de sus escrituras, y UPDATE conserva el fin. No añade locks de planificación, otra captura de reloj o intervalos de ampliación.
- Colisión: ON CONFLICT apunta sólo a owner/key; un resultado distinto de1 provoca excepción y rollback antes del callback de lectura nuevo. @s20 en StoreTest:614 y2017 usa SQL real, txid diferentes, ganador durable y comparación posterior. El éxito inyecta deliberadamente snapshot anterior y primera búsqueda vacía después de adquirir lock real; es un test de la frontera excepcional, no una carrera natural. Conflicto y ausencia durable están comprobados por separado. Ese límite es honesto y suficiente para el contrato; no hace falta inventar dos abiertas.
- Carreras: StoreTest:423–548 y helper742 observan conexiones esperando Lock antes de liberar, y aceptan cualquier ganador. Hay misma intención y cuatro pares de decisiones; el oráculo verifica revisión, fin, marca, intervalos y únicos recibo/evento. Fallos de rowcount, restricción distinta y COMMIT diferido comparan la fila completa y efectos del agregado. No se confunde un fallo de almacenamiento con replay.
- E usa la plantilla read-only RR; StoreTest:560 confirma writer terminado antes del Clock, aislamiento observable y segunda lectura antigua, seguida de consulta fresca. El fallo al finalizar lectura no produce snapshot válido. Wrapper de errores y propiedad se reutilizan con conexiones HTTP probadas por C.
- V18 añade dos columnas nullable y CHECK fin>=plan, sin UPDATE ni cambiosV14–V17. EndMigration conserva columnas/filas, keys, intervalos y JSONB históricos de los tres estados; GET resuelve fallback sin backfill. `receiptJson:421` retira únicamente campos null de otras variantes. El ajuste de ClosureMigration siembra State6/receipt6/evento12 válidos del esquemaV16, en vez de ejecutar Store17 contra columnas inexistentes; conserva comparaciones de todos los valores previos y cantidad de filas. No rebaja los oráculos históricos.

No hallazgo bloqueante demostrado. La reutilización específica de las ramas closed/máximo/año0000 y errores SQL es razonable: mismas guardas y métodos, conexiones17 probadas y regresión compartida repetida; no se exigen duplicados por cada fila Gherkin. El mapa final A aporta nombres concretos y distingue límites de@s23: C/K tras retirada outbox está integrado, reinicio real y ampliación posterior siguen para smoke/E2E.

Pendientes fuera del dictamen: primer E2E aún RED por panel no integrado, freeze/review B, smoke de pérdida de ACK/reinicio y gates globales/PIT/Stryker. Los175 no son175 tests nuevos ni una campaña global. Revisiones HTTP/publicador de C las realizó raíz; este juez no se autoaprueba esos paquetes.
