# Prediccion de huecos de mutacion de frontend — 10 de septiembre de 2026

Workflow de 10 agentes: tres predicen leyendo el codigo, dos refutan cada
prediccion (uno buscando racimos inflados y otro buscando huecos omitidos), y
uno sintetiza el plan.

**Validacion del metodo**: predijo la feature 25 en "62-72 %" y la medicion real
dio **71,40 %**.

---

{
  "summary": "Predict the frontend mutation gaps of features 25, 28 and 29 by reading code, and adversarially verify each prediction",
  "agentCount": 10,
  "logs": [
    "3 features analizadas, 40 racimos predichos, 25 refutados, 51 huecos anadidos"
  ],
  "result": {
    "vivos": [
      {
        "feature": "25-webhooks",
        "prediccion": {
          "feature": "25 · webhooks (frontend/src/webhooks-client.ts + frontend/src/webhooks.tsx)",
          "puntuacionEstimada": "62-72 %",
          "racimos": [
            {
              "causa": "Los guardas de decodeEndpoint sólo se ejercen en su rama verdadera (el DTO bueno) o por dos casos malos muy concretos (status \"paused\", propiedad extra); ninguna prueba entrega un id no-uuid, una url http://, una descripción de 81 caracteres, eventTypes vacío, un tipo desconocido, ni un instante inválido.",
              "fichero": "frontend/src/webhooks-client.ts",
              "lineas": "79-116 (y los helpers 66-77)",
              "mutantesEstimados": 25,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Una tabla de rechazo por cada cláusula, al estilo de la que ya existe para status: it.each con id de 35 caracteres, url \"http://example.com/h\", descripción de 81 puntos de código (y una de 80 con emojis, que distingue [...s].length de s.length), eventTypes: [], eventTypes: [\"NoExiste.v1\"], createdAt: \"2026-09-08 10:00:00\", updatedAt no instante, disabledReason: \"OTRO\" y un endpoint disabled con disabledAt: \"ayer\". Cada una debe esperar \"Confirmación incompatible\".",
              "posibleDefecto": "El decodificador acepta hoy cualquier url que empiece por https:// pero nunca se ha probado que rechace una que no lo haga; con el literal \"https://\" mutado a \"\" el guarda desaparece sin que nada se ponga rojo. Más grave: si el backend devolviera un eventType que el catálogo no conoce, la vista pinta eventLabels[indexOf(...)] = undefined en el <span> de la lista, así que el guarda es lo único que impide mostrar «undefined» al usuario."
            },
            {
              "causa": "Todos los endpoints decodificados en las tres suites llevan exactamente UN eventType ([\"TaskCreated.v1\"]). Con un solo elemento .some() y .every() son indistinguibles y la segunda mitad del predicado (index > 0 && indexOf(anterior) >= indexOf(actual)) no se ejecuta jamás.",
              "fichero": "frontend/src/webhooks-client.ts",
              "lineas": "93-99",
              "mutantesEstimados": 8,
              "tipo": "asercion_que_no_puede_fallar",
              "queOraculoFalta": "Tres casos con listas de dos o más tipos: uno válido en orden de catálogo (que debe pasar y además prueba el pintado de varias etiquetas en la vista), uno repetido [\"TaskCreated.v1\",\"TaskCreated.v1\"] y uno desordenado [\"TaskCreated.v1\",\"ProjectCreated.v1\"], los dos últimos esperando \"Confirmación incompatible\". Sin eso, .some→.every, indexOf→lastIndexOf y >=→> sobreviven todos.",
              "posibleDefecto": "La regla «sin repetidos y en orden de catálogo» es hoy un adorno: no hay prueba que la ejerza. Un endpoint con tipos repetidos pasaría el decodificador si alguien toca ese predicado, y la lista mostraría la etiqueta duplicada."
            },
            {
              "causa": "Los guardas de decodeDelivery, el helper whole() (llamado tres veces: attempt/6, httpStatus/599, latencyMs/MAX_SAFE_INTEGER) y la comprobación de ids duplicados de decodeList nunca se falsifican. Sólo attempt: 0 mata el >= 0; el <= max, el Number.isInteger y el typeof number no los mata nadie, y ninguna lista de prueba tiene dos ids iguales.",
              "fichero": "frontend/src/webhooks-client.ts",
              "lineas": "70-77, 119-160",
              "mutantesEstimados": 20,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Rechazos por: attempt: 7, attempt: 1.5, attempt: \"1\", httpStatus: 600, latencyMs: -1, eventType: \"\", status: \"queued\", errorClass: \"OTRO\", eventId no-uuid, un pending con nextAttemptAt null y un succeeded con nextAttemptAt puesto. Y una lista con dos entregas de id idéntico, esperando \"Confirmación incompatible\" (hoy la deduplicación sólo sobrevive porque el test de tres filas mata por casualidad el mutante del .map).",
              "posibleDefecto": "Un attempt fuera del 0..6 o un httpStatus de 600 entrarían en la tabla sin que ninguna prueba lo note; el contrato dice que el cliente es el que cierra el DTO, y esa promesa no está verificada en ninguno de los tres límites."
            },
            {
              "causa": "Doce sitios repiten el mismo patrón `if (signal.aborted) return` / `if (!controller.signal.aborted) set...` y en ninguna prueba una promesa llega a resolverse DESPUÉS de abortar: los tests de aborto usan promesas que no se resuelven nunca. La rama abortada existe pero nunca se ejecuta.",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "119, 122, 125, 151, 158, 161, 198, 208, 221, 233, 246, 253",
              "mutantesEstimados": 13,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Un test que guarde el `resolve` de la promesa, desmonte la vista (o cambie de owner) y SÓLO ENTONCES resuelva con un 201 y su secreto, afirmando que no aparece nada en pantalla. Repetirlo para la lista (resolver el GET tras desmontar) y para una acción (resolver el PUT de estado tras desmontar). Ojo: webhooks.test.tsx:314 afirma hoy la ausencia del secreto cuando la respuesta no ha llegado nunca — es una aserción que no puede fallar y por eso los seis mutantes de submit/fetchList siguen vivos.",
              "posibleDefecto": ""
            },
            {
              "causa": "Las celdas de la tabla de entregas se pintan pero no se afirman: ningún test mira el data-label de las ocho columnas, ni el guion «—», ni «12 ms», ni la fecha recortada. La fila exhausted lleva errorClass \"HTTP_ERROR\" y httpStatus 500 en el fixture y nadie comprueba que salgan.",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "506-536",
              "mutantesEstimados": 16,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Sobre la fila exhausted: getByRole(\"cell\", {name: \"HTTP_ERROR\"}), \"500\", \"6\"; sobre la fila pending: que Código HTTP, Latencia y Clase de error muestren «—»; sobre la succeeded: «12 ms» y la fecha «2026-09-08» (no el instante entero). Y una aserción de que cada celda conserva su data-label igual al encabezado de su columna, recorriendo deliveryColumns.",
              "posibleDefecto": "El comentario del propio código dice que cada celda lleva su etiqueta porque a anchos estrechos la hoja de estilos apila la tabla y el encabezado desaparece visualmente. Si un data-label se vacía o se descoloca, a 320 px el usuario ve una columna de números sin nombre y nada se pone rojo — es exactamente la familia del aria-live que no se actualizaba."
            },
            {
              "causa": "Las doce etiquetas humanas y su emparejamiento con el catálogo sólo se ejercen a través de una: «Crear tarea» (y «Seleccionar todos»). En la lista, el span que traduce eventTypes a etiquetas no se afirma en ninguna prueba.",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "17-30, 385, 413-420",
              "mutantesEstimados": 13,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Un test que afirme los doce nombres accesibles de las casillas en orden exacto (getAllByRole(\"checkbox\").map(name) toEqual [...]) — el mismo patrón que ya se usa con deliveryColumns y que funciona — y otro que, con un endpoint de dos o tres tipos, afirme el texto exacto del span: \"Crear proyecto, Crear tarea\". Sin eso sobreviven once literales de etiqueta, el separador \", \" del join y indexOf→lastIndexOf.",
              "posibleDefecto": "eventLabels y webhookEventTypes son dos listas paralelas emparejadas por índice y sólo el índice 3 está verificado. Un desalineamiento entre ambas (al insertar un tipo nuevo, por ejemplo) haría que el usuario marque «Crear subtarea» y se suscriba a otra cosa; la suite seguiría verde."
            },
            {
              "causa": "report() se escribió para la creación pero act() lo reutiliza para ping, activar/desactivar, eliminar, ver entregas y reenviar. Ninguna prueba hace fallar una de esas cinco acciones, así que el catch de act() y las dos últimas ramas de report() (WEBHOOK_INVALID y el Response genérico) no se ejecutan nunca.",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "175-199",
              "mutantesEstimados": 9,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Un test por acción fallida: «Desactivar» que devuelve 409 y otro 500, «Eliminar» que devuelve 404, «Reenviar» que devuelve 409, afirmando qué ve el usuario y DÓNDE. Más un caso de creación con un código que no está en la lista (500 con {code:\"BOOM\"}) para ejercer «No se ha podido crear el webhook.», y otro con WEBHOOK_INVALID.",
              "posibleDefecto": "Defecto real y visible: si desactivar un webhook devuelve un problema, el usuario lee «No se ha podido crear el webhook.»; si devuelve WEBHOOK_LIMIT lee «Ya tienes cinco webhooks. Elimina uno antes de crear otro.»; y si devuelve WEBHOOK_URL_BLOCKED se enciende el error del campo URL del formulario de creación, con aria-describedby apuntando al campo equivocado. El mensaje aparece además pegado al formulario, lejos del control que falló, y la región role=status no se entera."
            },
            {
              "causa": "Todas las pruebas de la vista tienen UN solo endpoint en la lista, así que abrir las entregas de un segundo endpoint nunca ocurre. openDeliveries fija deliveriesOf de forma síncrona y sólo sustituye las filas cuando llega la respuesta.",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "218-229, 242-248, 531",
              "mutantesEstimados": 4,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Con dos endpoints en la lista: abrir «Ver entregas» de A, esperar sus filas, pulsar «Ver entregas» de B con la respuesta retenida y afirmar que la tabla no muestra ya las entregas de A (ni permite reenviarlas). Y un segundo ping seguido, para ejercer el filtro de deduplicación de ping (hoy el .filter corre siempre sobre una lista vacía, así que d.id !== sent.id sobrevive entero).",
              "posibleDefecto": "Sí, y es un defecto de producto: entre el clic en «Ver entregas» de B y la llegada de su respuesta —o para siempre, si esa respuesta falla— la tabla sigue mostrando las entregas de A mientras deliveriesOf ya vale B. El botón «Reenviar» de esas filas hace POST a /webhooks/B/deliveries/{id-de-una-entrega-de-A}/redeliver. Lo mismo con ping(B), que antepone la entrega nueva a las filas de A."
            },
            {
              "causa": "Los literales de atributos que sostienen la accesibilidad no se afirman: aria-live=\"polite\" (el test sólo comprueba que el atributo existe con closest(\"[aria-live]\"), lo cual sigue casando con el valor vacío), aria-atomic=\"true\", el aria-label del diálogo (getByRole(\"dialog\") va sin name), scope=\"col\", el texto «Secreto» de la etiqueta y los cuatro className.",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "273-286, 304, 461, 493-500",
              "mutantesEstimados": 13,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Afirmar el valor, no la presencia: toHaveAttribute(\"aria-live\", \"polite\") y (\"aria-atomic\", \"true\") sobre la región de estado; getByRole(\"dialog\", {name: \"Confirmar eliminación\"}); getByRole(\"textbox\", {name: \"Secreto\"}) en vez de getByDisplayValue; toHaveAttribute(\"scope\", \"col\") sobre los encabezados.",
              "posibleDefecto": "Un aria-live vacío deja de anunciar los resultados de @s39 y @s40 sin que ninguna prueba lo note — es literalmente el defecto de la región aria-live que se cazó esta noche en otra feature, aquí protegido por una aserción de presencia en vez de valor."
            },
            {
              "causa": "problemCode nunca recibe una respuesta de error cuyo cuerpo no sea JSON, o sea JSON sin \"code\": el .catch(() => null) y las tres ramas de la comprobación de forma no se ejecutan.",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "61-70",
              "mutantesEstimados": 4,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Dos casos de creación fallida: uno que devuelve new Response(\"<html>502</html>\", {status: 502}) y otro Response.json({detalle:\"x\"}, {status: 400}), afirmando que el usuario ve «No se ha podido crear el webhook.» y no una pantalla en blanco ni el mensaje de resultado incierto.",
              "posibleDefecto": "Un proxy o un gateway que devuelva HTML en vez del problem+json contractual cae hoy en un camino que nadie ha ejecutado; si el .catch se rompe, la promesa rechaza dentro de report() y el usuario se queda sin mensaje alguno."
            },
            {
              "causa": "Las cuatro cadenas opcionales sobre refs y sobre navigator.clipboard sólo se ejercen con el objeto presente; el mutante que quita el ?. no cambia nada observable.",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "238, 263, 268, 318",
              "mutantesEstimados": 4,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Un caso sin portapapeles (vi.stubGlobal sobre navigator.clipboard = undefined) que afirme que pulsar «Copiar» no revienta la vista y el secreto sigue visible. Para las tres refs el mutante es probablemente equivalente y conviene documentarlo en la bitácora en vez de perseguirlo.",
              "posibleDefecto": "En un navegador sin API de portapapeles (o en http://), navigator.clipboard es undefined; sin el ?. el clic lanzaría y el panel del secreto quedaría inservible justo cuando el secreto no vuelve a mostrarse."
            },
            {
              "causa": "El test de reintento afirma que la lista llega, pero no que la alerta de fallo desaparezca; nada verifica el setLoadFailed(false) de load(), ni el estado inicial de la casilla «Seleccionar todos».",
              "fichero": "frontend/src/webhooks.tsx",
              "lineas": "132-136, 288-295, 360-368",
              "mutantesEstimados": 3,
              "tipo": "asercion_que_no_puede_fallar",
              "queOraculoFalta": "Tras pulsar «Reintentar» con éxito: expect(screen.queryByRole(\"alert\")).not.toBeInTheDocument(). Y sobre la casilla maestra: que empiece desmarcada, que se marque sola al marcar los doce tipos uno a uno y que se desmarque al quitar uno (toBeChecked/not.toBeChecked, que hoy no aparece en ninguna prueba de esta feature).",
              "posibleDefecto": "La alerta de «no se ha podido cargar» podría quedarse en pantalla junto a la lista ya cargada y ninguna prueba lo vería."
            }
          ]
        },
        "falsos": [
          "Racimo 3, sub-afirmacion «el <= max no lo mata nadie»: FALSA. La fila exhausted del fixture de webhooks.test.tsx:496-503 lleva attempt: 6, que es justo el limite; con whole() mutado a `value < max` esa entrega lanza, listWebhookDeliveries rechaza y la asercion de 4 filas (webhooks.test.tsx:539) se pone roja. Ese mutante YA muere. Igual el `value >= 0`: attempt: 0 aparece en el ping (client:207, view:374) y en el redeliver (client:245), asi que `>= 0`→`> 0` tambien muere. De las cuatro clausulas de whole() solo sobreviven typeof number y Number.isInteger; los 20 mutantes estimados son mas bien 10-12.",
          "Racimo 1, la prueba propuesta «id de 35 caracteres»: no mata lo que cree. `identifier()` es uuid(value) && length === 36, y la regex de uuid (schedule-block-api.ts:275) esta anclada y ya obliga a 36 caracteres exactos. El `=== 36` es tautologico: su mutante a true es EQUIVALENTE y ningun oraculo lo matara. Un id no-uuid si mata la clausula `!identifier(value.id)`, pero conviene documentar el `.length === 36` como equivalente en vez de perseguirlo (o borrarlo, que es codigo muerto).",
          "Racimo 2, sub-afirmacion «indexOf→lastIndexOf sobrevive [y la prueba propuesta lo mata]»: el mutante sobrevive, si, pero es EQUIVALENTE y la tabla de rechazo propuesta no lo matara. webhookEventTypes no tiene duplicados (client:5-18, y hay una prueba que fija la lista entera en client:281), asi que indexOf y lastIndexOf devuelven siempre lo mismo, tanto en webhooks-client.ts:97-98 como en el span de webhooks.tsx:417. Lo mismo aplica al indexOf que el racimo 6 promete matar.",
          "Racimo 8, la premisa «Todas las pruebas de la vista tienen UN solo endpoint en la lista»: FALSA. webhooks.test.tsx:158-169 renderiza DOS endpoints (uno activo y uno disabled/MANUAL) y :320-361 anade el de Bea. La conclusion del racimo sigue en pie —nadie abre «Ver entregas» de un segundo endpoint— pero el artesano no tiene que inventar el fixture: ya existe y solo hay que reutilizarlo.",
          "Racimo 5, el «no se afirman» en bloque: matizado. Estado SI se afirma por las tres filas (Entregada/Agotada/Pendiente, :540-542), la celda de intento se afirma una vez tras el reenvio (getByRole(\"cell\", {name: \"0\"}), :578) y el eventType se afirma en el ping (getByText(\"webhook.ping.v1\"), :391). Lo que de verdad esta huerfano es data-label x8, el guion, «12 ms», la fecha recortada, HTTP_ERROR y 500: mas cerca de 8-10 mutantes que de 16.",
          "Racimo 12, la casilla maestra: el mutante interesante ya muere. `types.length === webhookEventTypes.length` mutado a `!==` hace que «Seleccionar todos» arranque marcada, el clic la desmarque y el POST viaje con cero tipos, contra la asercion de doce tipos de webhooks.test.tsx:221. Solo sobrevive la variante checked={false}. La parte de setLoadFailed(false) del racimo si es correcta: :151-156 no comprueba que la alerta desaparezca.",
          "Racimo 3, «los ids duplicados»: cierto que ninguna lista los tiene, pero el mutante `!==`→`===` del Set (webhooks-client.ts:155-157) ya muere con la lista de tres entregas distintas de webhooks.test.tsx:494-513. Solo sobreviven la eliminacion del if y las variantes que hacen la condicion siempre falsa.",
          "RACIMO 3 (decodeDelivery / whole) — «Sólo attempt: 0 mata el >= 0; el <= max no lo mata nadie». FALSO para el primero de los tres usos de whole(): webhooks.test.tsx:501 mete `attempt: 6` en la fila exhausted del listado de entregas, y ese 6 es exactamente el máximo. El mutante `value <= max` → `value < max` hace que whole(6,6) devuelva false, decodeDelivery lanza, act() lo traga, la tabla no pinta filas y revientan las líneas 538-539 (`toHaveLength(4)`) y 541 (`within(exhaustedRow).getByText(\"Agotada\")`). El <= sólo queda huérfano para httpStatus/599 (probado con 200 y 500) y para latencyMs/MAX_SAFE_INTEGER (probado con 12).",
          "RACIMO 3 (deduplicación de decodeList) — «ninguna lista de prueba tiene dos ids iguales… la deduplicación sólo sobrevive porque el test de tres filas mata por casualidad el mutante del .map». FALSO en su parte importante: el mutante EqualityOperator `size !== items.length` → `size === items.length` muere en CUALQUIER test de lista, incluso con un solo elemento (Set de tamaño 1 === length 1 → lanzaría y rompería webhooks-client.test.ts:66-76, :78-81 y :223-230). Lo único que sobrevive ahí es el ConditionalExpression→false (borrar la guarda entera), que son 1-2 mutantes, no el racimo que sugiere. Escribir la lista de ids duplicados sigue mereciendo la pena, pero rinde mucho menos de lo prometido.",
          "RACIMO 8 — «Todas las pruebas de la vista tienen UN solo endpoint en la lista». FALSO literalmente: webhooks.test.tsx:158-179 («@s36 lists each webhook with its url, description, types and status») renderiza DOS endpoints (id y second, uno activo y uno desactivado manualmente). La conclusión operativa del racimo sí se sostiene (nadie abre «Ver entregas» de un segundo endpoint, porque ese test no toca ningún botón), pero la premisa como está escrita lleva a creer que hace falta construir el escenario de dos endpoints desde cero cuando ya existe un fixture que se puede reutilizar.",
          "RACIMO 5 — «ningún test mira… ni el guion «—», ni «12 ms», ni la fecha recortada… sobre la fila exhausted: … «6»» presentado como si la columna Intento estuviera desnuda. Parcialmente FALSO: webhooks.test.tsx:578 afirma `within(succeededRow).getByRole(\"cell\", { name: \"0\" })` después del reenvío, o sea que la celda Intento SÍ tiene un oráculo de contenido (y de rol «cell») en al menos una fila. Lo que de verdad está desnudo son Código HTTP, Latencia, Clase de error, Fecha y los ocho data-label; el resto del racimo es correcto.",
          "RACIMO 12 (casilla maestra) — «nada verifica… el estado inicial de la casilla Seleccionar todos» presentado como mutantes vivos garantizados. Parcialmente FALSO: el mutante `types.length === webhookEventTypes.length` → `!==` muere hoy por accidente. Con `!==` la maestra se pinta MARCADA de salida, así que el clic de webhooks.test.tsx:213 la DESMARCA (event.target.checked = false → setTypes([])) y entonces revienta la línea 221 (`bodyOf(other).eventTypes` con length 0, no 12). Lo que sobrevive de ese racimo es la rama `: []` del ternario (nadie desmarca nunca la maestra) y la ausencia de toBeChecked, no el `===`."
        ],
        "omitidos": [
          "setWebhookStatus, webhooks-client.ts:218: `if (endpoint.id !== id || endpoint.status !== status) throw incompatible()`. Ninguna prueba devuelve un endpoint cuyo id o estado no case con lo pedido (client:169 y view:398/424 siempre casan), asi que la condicion nunca es verdadera: sobreviven el `||`→`&&`, la eliminacion del if y el mensaje. Es ademas la unica defensa contra que el backend confirme el cambio del webhook equivocado.",
          "Los `signal.throwIfAborted()` de arranque de SEIS de las siete funciones exportadas (lineas 186, 210, 223, 239, 248, 258). Solo listWebhooks tiene la prueba de senal ya abortada (client:272). Las demas nunca se llaman con una senal abortada, asi que la eliminacion de esa sentencia sobrevive entera. Y el segundo throwIfAborted de json() (linea 175), tras `await response.json()`, no se ejecuta abortado en ningun sitio.",
          "webhookUrl (linea 162-165) y redeliverWebhook (linea 259): `if (!identifier(id)) throw new Error(\"Identidad incompatible\")`. Cero pruebas con un id malo —grep de «Identidad incompatible» en las suites no devuelve nada—: sobreviven el guarda y su literal en dos sitios.",
          "deleteWebhook, linea 229: `if (response.status !== 204) throw response`. Ni el cliente (client:191, 204) ni la vista (view:447) devuelven nunca un DELETE distinto de 204, asi que el 204 literal y la rama verdadera no se ejercen. Se solapa con el racimo 7 solo si el test de «Eliminar que devuelve 404» se escribe de verdad.",
          "createWebhook, linea 194: `if (!exact(body, \"endpoint secret\")) throw incompatible()`. Los tres secretos malos de client:149-167 conservan ambas claves, asi que la rama falsa de este exact nunca corre; falta un 201 con una clave de mas o de menos.",
          "webhooks.tsx:153, `setItems((current) => [created.endpoint, ...current])`. Las tres pruebas de creacion arrancan con la lista vacia (stubApi([])), asi que anteponer o anadir al final es indistinguible: el mutante que invierte el orden sobrevive y el usuario podria ver su webhook recien creado al final de la lista.",
          "webhooks.tsx:480-483, el boton «Actualizar» del panel de entregas: `const endpoint = items.find(...); if (endpoint) openDeliveries(endpoint)`. La rama de no-encontrado (el endpoint eliminado mientras su panel sigue abierto) no se toma jamas, asi que `if (true)` sobrevive; y es ademas un estado alcanzable de verdad, porque eliminar no cierra el panel de entregas.",
          "Atributos nunca afirmados fuera del racimo 9: `tabIndex={-1}` del <main> (view:99 comprueba el id y la contencion, no el tabIndex, y el enlace de salto depende de el), `rows={2}` del textarea del secreto, `required` del campo URL, y sobre todo el manejador `onFocus={(e) => e.currentTarget.select()}` de webhooks.tsx:314, que ninguna prueba llega a invocar: es una funcion entera sin cobertura.",
          "webhooks.tsx:341, `aria-describedby={urlError ? urlErrorId : undefined}`. Solo se afirma la rama con error (view:646), y con un waitFor que pasa igual si el atributo estuvo puesto desde el principio; falta afirmar que ANTES del fallo no hay aria-describedby, o el mutante que lo pone siempre sobrevive apuntando a un elemento inexistente.",
          "EL HUECO MÁS GRANDE, INVISIBLE PARA LA PREDICCIÓN — La lógica de selección por tipo del formulario (webhooks.tsx:369-387) no tiene NINGÚN oráculo. Comprobado por grep: en webhooks.test.tsx sólo hay UNA afirmación sobre el cuerpo del POST de creación, la línea 221 `expect(bodyOf(other).eventTypes).toHaveLength(12)`, y viene del camino «Seleccionar todos». Las OCHO pruebas que pulsan la casilla «Crear tarea» (líneas 257, 283, 307, 344, 618, 643, 663, 705) jamás miran qué eventTypes se envían: sólo cuentan llamadas. Además NO existe ni un `toBeChecked` en todo el fichero ni un solo clic que DESMARQUE una casilla. Consecuencias: (a) `webhookEventTypes.filter(candidate => candidate === type || current.includes(candidate))` —el inserto en orden de catálogo— sobrevive al MethodExpression que borra el .filter, al `===`→`!==` y al `||`→`&&`; (b) la rama falsa del ternario `event.target.checked ? ... : current.filter(c => c !== type)` NO SE EJECUTA NUNCA (sin cobertura pura), con su .filter y su `!==` dentro; (c) el ConditionalExpression→true/false del ternario sobrevive en un sentido. Son ~10 mutantes vivos de una sola causa, y encima es la interacción central de @s37. Oráculo que falta: pulsar «Crear tarea» y «Crear proyecto» en ese orden inverso y afirmar `expect(bodyOf(other).eventTypes).toEqual([\"ProjectCreated.v1\",\"TaskCreated.v1\"])` (prueba orden Y emparejamiento), luego desmarcar «Crear tarea» y afirmar que el cuerpo queda en [\"ProjectCreated.v1\"], y afirmar toBeChecked/not.toBeChecked en cada paso.",
          "CORRECCIÓN AL ALZA DEL RACIMO 6 — La predicción dice «sólo el índice 3 está verificado» del emparejamiento eventLabels[i] ↔ webhookEventTypes[i]. Es demasiado generosa: NI SIQUIERA el índice 3 está verificado. Pulsar la casilla llamada «Crear tarea» y no mirar el cuerpo del POST no demuestra que esa etiqueta corresponda a TaskCreated.v1; si eventLabels se reordenase, «Crear tarea» seguiría existiendo, seguiría siendo clicable por nombre y la suite seguiría verde entera. El defecto de producto que el racimo 6 describe (el usuario marca «Crear subtarea» y se suscribe a otra cosa) no está protegido por NINGUNA prueba, ni una. Los doce literales de etiqueta están vivos, no once.",
          "GUARDAS DE IDENTIDAD DEL CLIENTE, no mencionadas en ninguno de los doce racimos — webhooks-client.ts:162-165 (`webhookUrl`: `if (!identifier(id)) throw new Error(\"Identidad incompatible\")`) y :259 (la misma guarda sobre deliveryId en redeliverWebhook). Ninguna prueba pasa jamás un id malformado a setWebhookStatus, deleteWebhook, pingWebhook, listWebhookDeliveries ni redeliverWebhook, así que la rama verdadera de las dos guardas no se ejecuta nunca y el literal «Identidad incompatible» no se afirma en ningún sitio (grep: cero apariciones en las tres suites). Sobreviven el StringLiteral del mensaje ×2, el ConditionalExpression→false ×2 y el BlockStatement ×2: ~6-8 mutantes sin cobertura. Oráculo que falta: `await expect(deleteWebhook(\"no-soy-uuid\", signal)).rejects.toThrow(\"Identidad incompatible\")` y lo mismo con redeliverWebhook(id, \"tampoco\").",
          "POSCONDICIÓN DE setWebhookStatus, no mencionada — webhooks-client.ts:218: `if (endpoint.id !== id || endpoint.status !== status) throw incompatible()`. Los tres tests que la recorren (client:169-189, view:398-422 y :424-444) devuelven siempre un endpoint cuyo id y estado CONCUERDAN con lo pedido, así que las dos comparaciones sólo se evalúan en falso. El mutante LogicalOperator `||`→`&&` (false && false = false) sobrevive intacto, igual que el ConditionalExpression→false. Es la única defensa del cliente contra que el servidor conteste con OTRO webhook, y no está verificada. Oráculo que falta: un PUT que devuelva el endpoint con status \"active\" cuando se pidió \"disabled\", y otro que devuelva un id distinto, ambos esperando «Confirmación incompatible».",
          "LAS OCHO LLAMADAS A signal.throwIfAborted() DEL CLIENTE — La predicción trata el aborto sólo en la vista (racimo 4) y da por cubierto el del cliente porque existe webhooks-client.test.ts:272 («refuses to start once the signal is already aborted»), pero ese test sólo cubre listWebhooks. Quedan sin ejercer con señal abortada: createWebhook:186, setWebhookStatus:210, deleteWebhook:210+228, pingWebhook:239, listWebhookDeliveries:248, redeliverWebhook:258, y —la más interesante— el SEGUNDO throwIfAborted de json():175, el que corre DESPUÉS de `await response.json()` y es justamente el que impide procesar un cuerpo que llegó tarde. Ningún test aborta entre la llegada de la respuesta y su parseo. Son ~8-9 mutantes de borrado de sentencia, todos vivos, y el de json():175 es el mismo defecto de familia que el racimo 4 pero en la capa de abajo. Oráculo que falta: repetir el patrón de la línea 272 para cada exportada, y un caso con un Response cuyo .json() sea una promesa que se resuelva después de controller.abort().",
          "EL REGEX DEL SECRETO — webhooks-client.ts:199, `/^whsec_[A-Za-z0-9_-]{43}$/`. Stryker mutila regexes y el test de rechazo (client:149-167) sólo prueba tres casos, todos con la letra «A»: «whsec_short», prefijo «owp_» y 44 aes. Por tanto sobreviven: quitar el ancla `^` (ningún caso de prueba lleva basura ANTES de un «whsec_» válido, así que el mutante sin ancla se comporta igual con los tres), y encoger la clase de caracteres —quitar `0-9`, `a-z` o `_-`— porque ningún secreto de prueba contiene un dígito, una minúscula, un guion o un guion bajo. Son 3-5 mutantes vivos y además un riesgo real: si la clase se estrecha, el cliente rechazaría secretos legítimos del backend y el usuario perdería el único momento en que puede copiarlo. Oráculo que falta: un secreto válido con `whsec_aA0_-` + relleno hasta 43, y un rechazo con un prefijo pegado por delante («ruido-whsec_AAA…43»).",
          "REINICIOS Y LIMPIEZA DEL FORMULARIO — webhooks.tsx:142-144 (`setFormError(null)`, `setUrlError(null)`, `setUncertain(false)` al empezar cada envío) y :154-156 (`setUrl(\"\")`, `setDescription(\"\")`, `setTypes([])` tras el éxito). Ningún test envía DOS veces con resultados distintos, así que los tres reinicios de la cabecera nunca se observan; y ningún test comprueba que el formulario quede limpio tras crear (la única afirmación sobre el valor del campo URL, línea 624, es la del caso de ERROR, donde debe conservarse). Sobreviven los seis borrados de sentencia y los StringLiteral de las dos cadenas vacías. Defecto de producto latente: tras crear un webhook con éxito la URL anterior seguiría en el campo y el usuario crearía el mismo dos veces; nada se pondría rojo.",
          "EL CAMPO «Descripción» NO EXISTE PARA LA SUITE DE LA VISTA — grep confirma cero apariciones de «Descripción» en webhooks.test.tsx: ninguna prueba escribe en él. Su `onChange={event => setDescription(event.target.value)}` (webhooks.tsx:352-355) no se ejecuta jamás y el `useState(\"\")` de la línea 77 admite el StringLiteral mutado sin que nada lo note (el cuerpo del POST tampoco se afirma en la vista). ~3 mutantes. Es además el campo que alimenta el guarda de 80 puntos de código del racimo 1, así que hoy no hay ni una sola prueba de extremo a extremo de esa cadena.",
          "DETALLES MENORES PERO REALES QUE TAMPOCO APARECEN: (a) el botón «Actualizar» del panel de entregas, webhooks.tsx:480-483 — la rama falsa de `if (endpoint)` (el endpoint ya no está en la lista) no se ejecuta nunca y su ConditionalExpression→false sobrevive; (b) `onFocus={e => e.currentTarget.select()}` del textarea del secreto (:314) — nadie enfoca el campo, borrado de bloque vivo, y es justo la ayuda que hace copiable el secreto a mano; (c) el ternario `aria-describedby={urlError ? urlErrorId : undefined}` (:341) — sólo se afirma la rama verdadera (test :646), así que el mutante →true (describedby SIEMPRE presente, apuntando a un id inexistente) sobrevive; (d) `identifier()` (:66-68) encadena `uuid(value) && length === 36`, pero el regex de uuid ya está anclado a 36 caracteres, así que el `&&`→`||` es indistinguible salvo con una cadena de 36 caracteres que no sea uuid: conviene documentarlo como probablemente equivalente en la bitácora en vez de perseguirlo."
        ]
      },
      {
        "feature": "28-calendario-externo",
        "prediccion": {
          "feature": "28 — calendario externo (frontend: external-calendar-api.ts, external-calendar.tsx, today-external-calendar.tsx)",
          "puntuacionEstimada": "65-75 %",
          "racimos": [
            {
              "causa": "La cadena de 22 guardas de subscriptionOf solo tiene fixture roto para 10 de ellas; las otras 12 se evaluan siempre en falso, asi que el mutante que las apaga sobrevive",
              "fichero": "frontend/src/external-calendar-api.ts",
              "lineas": "91-122 (mas los anclajes ^ y $ del regex de :95-97)",
              "mutantesEstimados": 24,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Ampliar el it.each de external-calendar-api.test.ts:81-98 con una fila por guarda hoy sin fixture: label \"\" (vacia), label 7 (no cadena), urlHost 7, urlTail 7, id 7 (no cadena), lastAttemptAt \"ayer\", snapshotZoneId 7, truncated \"si\", updatedAt \"ayer\", skippedRecurring -1, skippedCancelled 1.5, skippedInvalid -1. Anadir ademas id con prefijo y con sufijo (\"x11111111-2222-...\" y \"...-555555555555x\") para matar los dos mutantes de anclaje del regex: en la campana de automatizaciones ese mismo regex genero 12 mutantes y sobrevivieron exactamente los dos que quitan ^ y $.",
              "posibleDefecto": "Los cuatro contadores comparten la misma guarda counter() pero solo imported tiene fixture; si el backend enviara skippedRecurring negativo o fraccionario la vista lo pintaria tal cual en el resumen de @s37. Y sin fixture de updatedAt invalido, un instante mal formado en el ultimo campo del DTO cerrado pasaria la validacion de forma."
            },
            {
              "causa": "Tres operaciones (save, synchronise, confirmRemoval) repiten las mismas tres guardas —reentrada, catch abortado y finally abortado— y solo una de las nueve tiene oraculo: el doble clic de synchronise",
              "fichero": "frontend/src/external-calendar.tsx",
              "lineas": "190, 203, 207, 212, 227, 232, 237, 246, 250",
              "mutantesEstimados": 16,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Replicar el patron de external-calendar.test.tsx:312-351 (retener la respuesta, segundo clic, calls.filter(...).toHaveLength(1)) para Guardar y para Si-eliminar. Y anadir tres pruebas de aborto en vuelo: con el PUT / POST / DELETE retenidos, desmontar la vista y afirmar que la respuesta tardia no cambia el DOM ni deja los controles desbloqueados (hoy ninguna prueba aborta una escritura: el unico unmount, :422-444, solo tiene el GET de montaje en vuelo).",
              "posibleDefecto": "Si un aborto durante el DELETE dejara de cortocircuitar, confirmRemoval llamaria a forget() sobre una vista desmontada o, peor, tras navegar a otra ruta; @s39 fila 3 exige justo lo contrario («la peticion se cancela y su respuesta tardia no modifica la vista destino») y hoy solo esta probado para el GET."
            },
            {
              "causa": "El catch de loadEvents tiene tres salidas y solo una esta ejercida: falta el aborto, falta ConnectorsDisabledError y falta el rechazo que no es Error (una Response de 500)",
              "fichero": "frontend/src/external-calendar.tsx",
              "lineas": "124-137",
              "mutantesEstimados": 9,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Tres pruebas sobre ExternalCalendar con suscripcion: (a) GET /events responde 500 -> afirmar que NO aparece «No hay eventos en la ventana guardada» y que si aparece el aviso de fallo; (b) GET /events responde 503 CONNECTORS_DISABLED -> afirmar el texto que corresponda; (c) desmontar con el GET /events retenido y afirmar que no se repinta.",
              "posibleDefecto": "Si. Con un 500 o un 503 en GET /events la vista cae al else de :393 y afirma «No hay eventos en la ventana guardada»: dice al usuario que su calendario esta vacio cuando en realidad no ha podido leerlo. Es el mismo patron de los tres defectos de esta noche: la interfaz afirma un hecho que no conoce."
            },
            {
              "causa": "Ninguna prueba hace fallar el GET de montaje, asi que todo el catch del efecto inicial —incluido el mensaje de conectores deshabilitados— nunca se ejecuta",
              "fichero": "frontend/src/external-calendar.tsx",
              "lineas": "147-158",
              "mutantesEstimados": 8,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Dos pruebas: GET /api/v1/me/external-calendar responde 503 CONNECTORS_DISABLED -> se ve «Los conectores externos no estan disponibles.» y NO se ve el mensaje generico; GET responde 500 -> se ve «No se ha podido cargar tu calendario externo.» y ademas la pantalla queda cargada (setLoaded(true)), es decir se ve el formulario y no un vacio permanente.",
              "posibleDefecto": "Si. @s8 exige que las cinco rutas devuelvan 503 CONNECTORS_DISABLED cuando falta APP_CONNECTOR_KEY, y ese es el estado de un despliegue real sin clave. Hoy nadie ha comprobado que /calendario-externo diga algo en ese estado; si el ternario se equivocara el usuario veria «No se ha podido cargar tu calendario externo» sin la pista de que faltan los conectores, o peor, la pantalla en blanco si setLoaded(true) se perdiera."
            },
            {
              "causa": "Las tres guardas de refuse() son conjunciones estado+codigo y jamas se prueba con solo una mitad cierta (un 503 con otro codigo, un 404 con otro codigo, un 400 que no es VALIDATION_ERROR), asi que el mutante && -> || sobrevive en las tres",
              "fichero": "frontend/src/external-calendar-api.ts",
              "lineas": "158-189",
              "mutantesEstimados": 8,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Seis casos en external-calendar-api.test.ts: 503 con code \"OTRA_COSA\" -> rechaza con la propia Response, no con ConnectorsDisabledError; 404 con code \"OTRA_COSA\" -> Response; 400 con code \"MALFORMED_JSON\" -> Response; y los simetricos: 500 con code \"CONNECTORS_DISABLED\", 400 con code \"EXTERNAL_CALENDAR_NOT_CONFIGURED\", 404 con code \"VALIDATION_ERROR\" -> Response en los tres.",
              "posibleDefecto": "@s10 fija 400 MALFORMED_JSON y 415 como respuestas posibles de la misma ruta. Si la guarda del 400 se relajara, un MALFORMED_JSON se traduciria en un ExternalCalendarValidationError con fields vacio y la vista pintaria un error de campo inexistente en vez del estado incierto."
            },
            {
              "causa": "La prueba de fallo de red en Hoy solo comprueba que existe el enlace, no que el texto sea el de «no se ha podido consultar»; los dos mensajes comparten enlace, asi que ni el literal del startsWith ni el mensaje de unreachable pueden fallar",
              "fichero": "frontend/src/today-external-calendar.tsx",
              "lineas": "22-33, 76-82, 92-99",
              "mutantesEstimados": 8,
              "tipo": "asercion_que_no_puede_fallar",
              "queOraculoFalta": "En today-external-calendar.test.tsx:184-192 anadir la asercion positiva del texto («No se ha podido consultar el calendario externo.») y la negativa del otro («no se ha podido leer»), y en :194-210 la simetrica. Anadir ademas una prueba con zoneId invalido (p.ej. \"Marte/Base\") para ejercer el catch de clock() y otra con lastSyncAt null para ejercer su rama de cadena vacia; hoy ninguna de las dos se ejecuta.",
              "posibleDefecto": "La seccion lleva aria-live=\"polite\" en el propio <section> que se inserta en el DOM cuando llega la lectura. Una region viva que aparece —en vez de actualizarse— no se anuncia en muchos lectores de pantalla; es exactamente el defecto de region aria-live encontrado esta noche en automatizaciones, y jsdom no puede detectarlo. Ademas, si clock() lanzara (zona no resoluble llegada de snapshotZoneId), hoy devuelve el instante crudo UTC y @s35 pide hora local."
            },
            {
              "causa": "Ninguna prueba afirma jamas una fecha formateada por formatMoment; el unico oraculo es «no es cadena vacia», que pasaria igual con el objeto de opciones vacio o con la zona ignorada",
              "fichero": "frontend/src/external-calendar.tsx",
              "lineas": "63-70 (oraculo en external-calendar.test.tsx:164-165)",
              "mutantesEstimados": 6,
              "tipo": "asercion_que_no_puede_fallar",
              "queOraculoFalta": "Afirmar la cadena exacta: con lastAttemptAt \"2030-01-07T12:00:00Z\" y snapshotZoneId \"Europe/Madrid\", que el <dd> de «Ultimo intento» contenga «13:00» (y no «12:00»), y repetirlo con snapshotZoneId null para fijar la diferencia. Eso mata a la vez el mutante de objeto vacio, el del ternario de zona y el de dateStyle/timeStyle.",
              "posibleDefecto": "Si. Hoy nadie comprueba que las dos fechas de la ficha se pinten en la zona de instantanea; podrian estar saliendo en la zona del navegador y la suite seguiria verde. @s37 pide «la lista de eventos en hora local» y la ficha de estado va en el mismo criterio."
            },
            {
              "causa": "Cuatro de los siete mensajes de FEED_MESSAGES no se renderizan en ninguna prueba, y la conjuncion lastStatus===FAILED && lastError nunca se prueba con una mitad sola",
              "fichero": "frontend/src/external-calendar.tsx",
              "lineas": "18-34, 352-355",
              "mutantesEstimados": 6,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Un it.each de siete filas que monte la vista con lastStatus FAILED y cada lastError, afirmando un fragmento distintivo de cada mensaje (hoy solo hay FEED_HTTP_ERROR, SECRET_UNREADABLE y FEED_UNREACHABLE). Y dos filas mas: lastStatus \"OK\" con lastError \"FEED_MALFORMED\" -> no se pinta ningun role=\"alert\"; lastStatus \"FAILED\" con lastError null -> tampoco.",
              "posibleDefecto": "Los siete codigos son contrato cerrado (@s12 y linea 15 del .feature). Si FEED_MESSAGES perdiera una clave, FEED_MESSAGES[lastError] daria undefined y el parrafo de alerta saldria vacio: un role=\"alert\" sin texto, que es peor que no mostrarlo."
            },
            {
              "causa": "Ninguna prueba inspecciona las cabeceras enviadas, ni Accept ni Content-Type ni la mezcla con options.headers",
              "fichero": "frontend/src/external-calendar-api.ts",
              "lineas": "190-205",
              "mutantesEstimados": 5,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "En las pruebas que ya capturan fetcher.mock.calls, afirmar new Headers(options.headers).get(\"Accept\") === \"application/json\" en el GET y en el PUT, y get(\"Content-Type\") === \"application/json\" en PUT y POST /sync, comprobando ademas que el X-CSRF-TOKEN que anade api-client no se pierde al mezclar (el PUT con setCsrfToken ya montado en :136).",
              "posibleDefecto": "Si el spread de options.headers se perdiera, el PUT viajaria sin X-CSRF-TOKEN y el backend respondaria 403 (@s10 fila 2). Ninguna prueba de frontend lo notaria: el stub de fetch responde 200 pase lo que pase."
            },
            {
              "causa": "En failed(), el ternario que elige campo a enfocar solo se ejerce por la rama url, y la guarda instanceof Response && status===401 nunca ve una Response de otro estado",
              "fichero": "frontend/src/external-calendar.tsx",
              "lineas": "171-186",
              "mutantesEstimados": 5,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Una prueba de @s38 con 400 VALIDATION_ERROR en label (code TOO_LONG): afirmar que el mensaje se asocia al campo Etiqueta, que este queda aria-invalid=\"true\" y que recibe el foco. Y otra con un PUT 500: afirmar que se ve el estado incierto y que NO desaparecen host y cola (es decir, que forget() no se dispara con cualquier Response).",
              "posibleDefecto": "Si. Con el mutante && -> || cualquier Response de error borraria la suscripcion de pantalla; y sin la prueba de label, un error de etiqueta (@s4 tiene cinco filas de label) enfocaria el campo de direccion, que es el que @s38 fila 2 quiere dejar corregible."
            },
            {
              "causa": "El resumen vacio nunca se pinta: ninguna prueba de componente mete un evento con summary \"\", aunque la API declara explicitamente que es valido",
              "fichero": "frontend/src/external-calendar.tsx",
              "lineas": "397 (y today-external-calendar.tsx:113)",
              "mutantesEstimados": 5,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "En external-calendar.test.tsx y en today-external-calendar.test.tsx, un evento con summary \"\" en la lista: afirmar que se ve «Sin titulo» y que el <li> no queda con solo la hora. La API ya lo declara valido en external-calendar-api.test.ts:306-316, asi que el hueco esta solo en la vista.",
              "posibleDefecto": "@s19 fija que un VEVENT sin SUMMARY se guarda como \"\". Es un caso real de feed, no un borde teorico: si el fallback se rompiera, en Hoy apareceria una fila con la hora y ningun texto, sin nombre accesible."
            },
            {
              "causa": "Tres caminos de fallo de la capa API no se ejecutan nunca: cuerpo 200 que no es JSON, PUT que responde configured false, y DELETE con la senal ya abortada",
              "fichero": "frontend/src/external-calendar-api.ts",
              "lineas": "199, 218, 222-227",
              "mutantesEstimados": 7,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Tres pruebas: (a) fetch devuelve new Response(\"no soy json\", {status:200}) -> readExternalCalendar rechaza con «Respuesta de calendario externo invalida»; (b) PUT responde {configured:false, subscription:null} -> saveExternalCalendar rechaza con el mismo error (hoy nadie ejerce esa guarda); (c) deleteExternalCalendar con un AbortController ya abortado -> rechaza sin llamar a fetch, y con la senal abortada durante la respuesta 204 -> rechaza con AbortError, como ya se hace para el GET en :329-347.",
              "posibleDefecto": "El PUT que devuelve configured false es la unica proteccion contra que saveExternalCalendar devuelva null y la vista haga setSubscription(null) tras un guardado con exito, dejando al usuario en el formulario de alta como si nada se hubiera guardado."
            },
            {
              "causa": "Guardas de forma y de presentacion que nunca se afirman: el exact() y el typeof del snapshot, el titulo que cambia con la suscripcion y el aria-busy del formulario",
              "fichero": "frontend/src/external-calendar-api.ts",
              "lineas": "137-151 y external-calendar.tsx:266-272",
              "mutantesEstimados": 6,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "En la API: un snapshot con campo de mas ({configured:false, subscription:null, extra:1}) y otro con configured:\"si\" -> ambos rechazados. En la vista: afirmar el h2 «Suscribirte a un calendario» sin suscripcion y «Cambiar la suscripcion» con ella, y afirmar aria-busy=\"true\" en el <form> mientras el PUT esta retenido (la prueba de :181-210 ya retiene la respuesta, solo falta la asercion) y \"false\" al resolverse.",
              "posibleDefecto": "aria-busy es la unica senal programatica de que el formulario esta ocupado; @s40 exige que los estados guardando/guardado se anuncien sin mover el foco y hoy solo se comprueba la region role=\"status\", no el aria-busy del formulario."
            }
          ]
        },
        "falsos": [
          "Racimo 7 (formatMoment), sub-afirmacion falsa: «el unico oraculo es no es cadena vacia». external-calendar.test.tsx:125-130 («deja sin fecha la ultima sincronizacion correcta cuando nunca hubo») afirma term.nextElementSibling toHaveTextContent(\"\") con lastSyncAt null, asi que la guarda `if (value === null) return \"\"` de external-calendar.tsx:64 tiene oraculo por los dos lados y sus mutantes mueren. Lo que si sobrevive, y ahi el racimo es valido, es el objeto de opciones {dateStyle,timeStyle} y el ternario de zona de :68 (ninguna prueba fija una cadena exacta). Cuenta realista ~3-4 mutantes, no 6.",
          "Racimo 6 (today), sub-afirmacion falsa: «ni el literal del startsWith ni el mensaje de unreachable pueden fallar». today-external-calendar.test.tsx:194-210 afirma positivamente /no se ha podido leer el calendario externo/i con un item invalido, lo que mata cualquier mutante que fuerce el ternario de today-external-calendar.tsx:101-105 a \"unreachable\" o el startsWith a falso constante. Solo sobreviven la direccion contraria (el argumento del startsWith a \"\", la condicion a verdadero constante) y el literal «No se ha podido consultar el calendario externo.», que efectivamente nadie afirma: la prueba de fallo de red de :184-192 solo comprueba el href del enlace. El nucleo del racimo se sostiene; la frase que lo describe, no.",
          "Racimo 5, el posibleDefecto es falso (el mutante si sobrevive, pero la consecuencia de producto no existe): un 400 MALFORMED_JSON que entrara en el bloque por culpa del mutante && -> || NO produciria un ExternalCalendarValidationError con fields vacio, porque external-calendar-api.ts:185 solo lanza `if (Object.keys(fields).length > 0)`. La prueba external-calendar-api.test.ts:174-187 («no inventa un error de campo si el problema no trae errores reconocibles») ya demuestra exactamente eso: con errors no reconocibles rechaza con la propia Response. El oraculo que falta sigue siendo util para matar el mutante, pero no hay defecto de producto que vender.",
          "Racimo 9, el posibleDefecto es falso: «si el spread de options.headers se perdiera, el PUT viajaria sin X-CSRF-TOKEN». api-client.ts:22-27 construye su propio `new Headers(options.headers)` y hace headers.set(\"X-CSRF-TOKEN\", csrfToken) para todo metodo distinto de GET, con independencia del spread de external-calendar-api.ts:195. Perder ese spread quita Content-Type (y en el otro sentido Accept), nunca el token CSRF. El hueco de cobertura es real —ninguna prueba de ningun fichero inspecciona cabeceras— pero el 403 del backend no es la consecuencia.",
          "Racimo 6 (today, «ni el literal del startsWith ni el mensaje de unreadable pueden fallar»): FALSO a medias. today-external-calendar.test.tsx:194-210 afirma /no se ha podido leer el calendario externo/i sobre una lectura inválida, y eso SÍ mata el mutante que apaga el startsWith de today-external-calendar.tsx:84 (con la guarda invertida saldría «No se ha podido consultar» y la aserción fallaría). Lo que de verdad no tiene oráculo es solo el literal de unreachable: la prueba de red (:184-192) afirma únicamente el enlace. El racimo vale 2-3 mutantes, no 8. Lo que sí es cierto y confirmo: el catch de clock() (:30-32) y la rama de lastSyncAt null nunca se ejecutan — el único fixture con lastSyncAt null (:262) pertenece a la prueba de desmontaje, que nunca llega a pintar.",
          "Racimo 9 (cabeceras), su posibleDefecto: FALSO. «Si el spread de options.headers se perdiera, el PUT viajaría sin X-CSRF-TOKEN» no puede ocurrir: api-client.ts:22-27 construye `new Headers(options.headers)` y hace `headers.set(\"X-CSRF-TOKEN\", ...)` DESPUÉS de que external-calendar-api.ts:195 haya mezclado, así que el token se añade pase lo que pase. Perder el spread pierde el Content-Type de writing() (:203), no el CSRF. El hueco (ninguna prueba mira las cabeceras) es real; la consecuencia narrada no lo es, y un oráculo escrito sobre esa premisa daría verde con el producto roto.",
          "Racimo 3, el oráculo (a) que propone: FALSO tal y como está escrito. «GET /events responde 500 -> afirmar que sí aparece el aviso de fallo» fallaría contra el producto correcto: refuse() relanza la propia Response (api:188), que NO es instanceof Error, así que external-calendar.tsx:132 nunca ejecuta setInvalidList(true) y no aparece ningún aviso. El posibleDefecto que describe el racimo (la vista cae en «No hay eventos en la ventana guardada», :391) es exactamente correcto y lo confirmo como defecto real de producto; lo que hay que afirmar es la ausencia del texto de vacío, no la presencia del aviso."
        ],
        "omitidos": [
          "Envoltorio de readExternalEvents sin fixtures (external-calendar-api.ts:256-265). Las pruebas rompen los ITEMS (7 filas en :273-291) y anaden un campo de mas al nivel superior (:293-304), pero ninguna rompe el resto del sobre: `typeof body.configured !== \"boolean\"`, `optionalInstant(body.lastSyncAt)`, la pertenencia de body.lastStatus a STATUSES y `!Array.isArray(body.items)` jamas se evaluan en verdadero. Son 4 guardas mudas, ~6-8 mutantes, el mismo patron del racimo 1 pero en otra funcion. Faltan: configured \"si\", lastSyncAt \"ayer\", lastStatus \"RARO\", items {} o null.",
          "syncExternalCalendar: `exact(body, \"performed subscription\")` de external-calendar-api.ts:239 solo se llama con el cuerpo correcto; la unica prueba de forma es `performed: \"si\"` (:237-242). Falta un cuerpo con campo de mas o con subscription ausente para matar el exact.",
          "external-calendar.tsx:220-225, dos huecos que se refuerzan. (a) El literal «Sincronizacion fallida.» del ternario de :221-224 no se afirma en ninguna prueba: la de :353-372 solo comprueba el mensaje FEED_UNREACHABLE. (b) La guarda `if (outcome.subscription.lastStatus === \"OK\") await loadEvents(signal)` de :225 no puede fallar: el stub de external-calendar.test.tsx:81 reutiliza la unica respuesta encolada (`list.length > 1 ? shift() : list[0]`), asi que si el mutante quitara la guarda, loadEvents volveria a traer [meeting] y el expect de :370 («Reunion» sigue) pasaria igual. La conducta «una sincronizacion fallida no refresca la lista» esta sin oraculo.",
          "external-calendar.tsx:110, `inFlight.current?.abort()` dentro de start(). Ninguna prueba lanza una operacion mientras hay otra peticion en vuelo (el unico solapamiento del fichero es el doble clic, que ni siquiera llega a dispararse), asi que la cancelacion de la peticion anterior no tiene oraculo: el mutante que borra ese abort sobrevive entero. Hoy si lo tiene, en today-external-calendar.test.tsx:212-246.",
          "today-external-calendar.tsx:117, `{pendingSync ? <p>Sincronizacion pendiente.</p> : null}`. La rama verdadera se afirma en :181, pero ninguna prueba del camino feliz afirma la AUSENCIA del aviso, asi que el mutante que lo pinta siempre sobrevive. Y en el mismo fichero, el `if (signal.aborted) return` del catch de sincronizacion (:61) nunca se ejecuta: la prueba de desmontaje :248-276 deja el /sync resolver y solo retiene el GET de eventos.",
          "today-external-calendar.test.tsx:96, `expect(within(section).queryAllByRole(\"button\")).toHaveLength(0)` es una asercion que no puede fallar: la seccion no renderiza ningun boton en ninguna de sus ramas (solo un RouteLink, que es role=link). Pasa igual con el producto roto; es exactamente la categoria 2 del encargo.",
          "external-calendar.tsx:116-122 y :249-251. El `setConfirming(false)` de forget() no se afirma nunca (tras un borrado con exito el dialogo desaparece de todos modos porque subscription pasa a null), y el `if (!signal.aborted) setBusy(\"\")` del finally de confirmRemoval sobrevive en ambas direcciones: la prueba de :405-420 comprueba que el formulario queda vacio, cosa que sigue siendo cierta con busy atascado en \"deleting\". Falta afirmar que tras eliminar los controles vuelven a estar habilitados y la Etiqueta deja de ser readonly.",
          "SINCRONIZACIÓN FALLIDA, la rama entera sin oráculo (external-calendar.tsx:220-225, ~6 mutantes). La prueba :353-372 es la única con lastStatus FAILED y solo afirma el texto de FEED_UNREACHABLE y que «Reunión» sigue ahí. (a) El anuncio «Sincronización fallida.» no aparece en NINGÚN test (grep en todo frontend/src: solo existe el stamp de Hoy). (b) El guardián `if (outcome.subscription.lastStatus === \"OK\") await loadEvents(signal)` (:225) es inmatable: el stub reutiliza la misma respuesta de /events (external-calendar.test.tsx:81, `list.length>1 ? shift : list[0]`), así que tanto el mutante que recarga siempre como el que no recarga nunca dejan «Reunión» en pantalla. @s38 fila 7 exige «la lista anterior permanece» y hoy nadie comprueba que no se vuelva a pedir.",
          "LOS DOS MENSAJES DE FALLO DE GUARDADO SON INDISTINGUIBLES (external-calendar.tsx:177-185, ~4 mutantes). external-calendar.test.tsx:244-264 es un it.each de dos filas (503 CONNECTORS_DISABLED y fallo de red) que usa la MISMA aserción /no sabemos si se guardó/i para las dos. Borra entero el bloque `if (error instanceof ConnectorsDisabledError) { setFailure(`${error.message} ${UNCERTAIN}`); return; }` y las dos filas siguen verdes, porque el 503 cae en el setFailure(UNCERTAIN) de :185 y produce el mismo texto. El prefijo «Los conectores externos no están disponibles.» no se afirma en ninguna prueba. Es literalmente «una aserción que pasaría igual con el producto roto», y @s38 fila 3 pide un mensaje distinto del genérico.",
          "LAS GUARDAS DE FORMA DE readExternalEvents Y syncExternalCalendar, sin un solo fixture roto (external-calendar-api.ts:256-265 y :238-242, ~10 mutantes). El it.each de :273-291 rompe el ITEM, y :293-304 rompe exact(); pero `typeof body.configured !== \"boolean\"`, `!optionalInstant(body.lastSyncAt)`, la conjunción de lastStatus y `!Array.isArray(body.items)` no se falsifican jamás: los cuatro se evalúan siempre en falso, así que el mutante que los apaga sobrevive. Lo mismo con `exact(body, \"performed subscription\")` en sync (:239): la única fila rota (:237) ataca `typeof body.performed`. La predicción listó los 12 fixtures que faltan en subscriptionOf (correcto, lo verifiqué guarda por guarda) pero no vio que la misma enfermedad está en los otros dos validadores del fichero.",
          "EL RACIMO DE LOS SIETE REINICIOS DE ESTADO (external-calendar.tsx:129, 194, 195, 204, 216, 228, 247; ~8 mutantes de una sola causa). No existe ninguna prueba de error-y-luego-reintento ni de inválido-y-luego-válido. Consecuencia: se pueden borrar `setInvalidList(false)` (:129), `setFieldErrors({})` y `setFailure(\"\")` (:194-195), `setFailure(\"\")` (:216) y los tres `setAnnouncement(\"\")` de los catch (:204, :228, :247) sin que caiga un solo test. Es el patrón del récord de esta noche: una causa (falta la prueba de recuperación) explica ocho supervivientes seguidos. Y hay defecto detrás: si `setAnnouncement(\"\")` se perdiera, la región role=status seguiría diciendo «Guardando…» mientras el role=alert dice que falló.",
          "TEXTOS DE VISTA QUE NINGUNA PRUEBA AFIRMA (~7 mutantes). Grep sobre todos los tests: no aparecen «No hay eventos en la ventana guardada» (:391), «Todavía no tienes ningún calendario externo» (:409), «Eliminando…» (:240), «Suscripción eliminada.» (:244). Los cuatro literales y el ternario `loaded ? ... : null` de :407 sobreviven a cualquier mutación. Es doblemente grave en el primero: es el mismo texto que la propia predicción quiere usar como aserción NEGATIVA en el racimo 3, y hoy no está fijado ni en positivo.",
          "LAS RAMAS FALSAS DE LAS TRES CONDICIONALES DE PRESENTACIÓN (~5 mutantes). `subscription.truncated ? <p>Solo se conservan los 500…</p> : null` (:347): la prueba :145 afirma que aparece con truncated true, pero NADIE afirma que no aparece con truncated false, así que mutar la condición a `true` sobrevive. Igual con `confirming ?` (:374): la prueba de Cancelar (:393-403) solo mira que no se envía DELETE, no que el alertdialog se cerró, así que el mutante que borra `setConfirming(false)` de :380 sobrevive. Y en Hoy, `pendingSync ? <p>Sincronización pendiente.</p> : null` (today:117) se afirma presente (:181) pero nunca ausente.",
          "LAS TRES GUARDAS DE REENTRADA NO TIENEN OCHO DE NUEVE ORÁCULOS: TIENEN CERO DE TRES (external-calendar.tsx:190, 212, 237). La predicción da por oraculado el doble clic de synchronise, y no lo está: en external-calendar.test.tsx:343 el segundo `user.click(sync)` es sobre un botón que ya tiene `disabled={locked}` (:361), y user-event no despacha click sobre un elemento deshabilitado, así que synchronise() ni siquiera se ejecuta y la rama verdadera de `if (busy) return` nunca se toma. El `toHaveLength(1)` de :344 lo garantiza el atributo disabled, no el guardián. Mutar los tres `if (busy) return` a `if (false)` sobrevive. Añade además start() (:109-114): `inFlight.current?.abort()` jamás aborta nada vivo (precisamente porque la guarda impide dos escrituras simultáneas), así que borrar esa línea tampoco rompe nada. ~5 mutantes extra sobre lo previsto.",
          "DOS GUARDAS DE ABORTO DE HOY QUE NUNCA SON CIERTAS (today-external-calendar.tsx:61 y :66, ~3 mutantes). En la prueba de desmontaje (:248-276) lo retenido es el GET de eventos, no el POST de sync, así que el `if (signal.aborted) return` del catch de sync (:61) nunca se toma. Y el de :66 tampoco: cuando la señal se aborta, json() lanza en su propio throwIfAborted (api:197) y el flujo entra por el catch de :78, nunca vuelve a la línea 66. Las dos se pueden borrar sin romper nada; la de :66 es sospechosa de ser código muerto.",
          "eventOf, tres guardas sin fixture (external-calendar-api.ts:126-132, ~4 mutantes): `typeof value.uid !== \"string\"`, `!instant(value.endAt)` con un endAt malformado, y `typeof value.allDay !== \"boolean\"`. Ojo con la última: el fixture `{...item, allDay: undefined}` que usan external-calendar.test.tsx:450, today:200 y api:274 pierde la clave al serializarse con Response.json, así que cae en exact() y jamás llega al typeof. Ese mismo detalle es el que hace que `truncated: \"sí\"` de la predicción sea correcto — lo confirmo.",
          "describeEvent/formatClock con zona nula (external-calendar.tsx:71-83, ~3 mutantes). La predicción marcó el ternario de zona de formatMoment (:68) pero no el gemelo de formatClock (:76): TODAS las pruebas que llegan a pintar un evento usan snapshotZoneId «Europe/Madrid» (synced :40 y ZONE en Hoy), así que la rama falsa del ternario nunca se ejerce en la vista y el mutante que ignora la zona en las horas del listado sobrevive por esa vía.",
          "ASERCIONES QUE NO PUEDEN FALLAR, dos localizadas. (a) external-calendar.test.tsx:119 `expect(document.body.textContent).not.toContain(\"https://\")` es el guardián del And de @s37 («ningún nodo del DOM contiene la URL completa») y es estructuralmente infalible: el DTO solo transporta urlHost y urlTail, la URL completa no existe en el fixture, así que NINGÚN mutante de external-calendar.tsx puede hacerla fallar. (b) today-external-calendar.test.tsx:96 `queryAllByRole(\"button\")).toHaveLength(0)`: la sección no renderiza botones en ninguna rama, la cuenta es cero por construcción. Ninguna de las dos protege nada.",
          "problem(): quitar el `.clone()` (external-calendar-api.ts:154) sobrevive, porque ninguna prueba vuelve a leer el cuerpo de la Response relanzada. (El `.catch(() => null)` y el `body?.` sí están ejercidos, por el 200 sin cuerpo de :207-211 del test de borrado — lo comprobé, no lo cuento como hueco.) ~1-2 mutantes."
        ]
      },
      {
        "feature": "29-conectores",
        "prediccion": {
          "feature": "29 — conectores adicionales (additional_connectors)",
          "puntuacionEstimada": "72-82 %",
          "racimos": [
            {
              "causa": "La guarda live(controller) = mounted.current && !signal.aborted se llama tres veces por operacion en cuatro operaciones (try, catch, finally), y ningun oraculo puede distinguir que se haya quitado: con React 19 un setState sobre un arbol desmontado es un no-op silencioso, sin aviso en consola. Las cuatro pruebas @s37 afirman inFlight.aborted (que mata el abort del cleanup) y console.error vacio (que ya no puede fallar), nunca que la escritura tardia no ocurrio.",
              "fichero": "frontend/src/gitlab-connector.tsx",
              "lineas": "102-103, 110, 113, 122, 186, 192, 199, 215, 219, 242, 256, 264, 271",
              "mutantesEstimados": 13,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Hacer observable la escritura tardia SIN desmontar: hoy no hay ningun camino en el que se aborte una peticion con la pantalla viva, porque pending.current = controller se sobrescribe sin abortar la anterior. La prueba que muerde: lanzar «Importar issues» y, con la peticion en vuelo, pulsar «Desconectar» + «Confirmar»; resolver DESPUES el recibo de la importacion y afirmar que la pantalla sigue sin conexion y sin la region «Resultado de la importacion». Eso exige que la produccion aborte la peticion anterior al empezar una nueva, y de paso mata los doce mutantes. Si se decide no cambiar la produccion, hay que declararlos supervivientes equivalentes por escrito antes de la campana, como el juez exige en la seccion 7.",
              "posibleDefecto": "Si. Dos operaciones solapadas escriben las dos: pending.current se sobrescribe sin abortar, asi que una importacion lenta puede repintar «Creadas/Omitidas/Fallidas» sobre una pantalla ya desconectada, o una lectura de conexion antigua puede resucitar «Conectado» despues de un 204. Gana el que llega ultimo, no el que el propietario pidio ultimo."
            },
            {
              "causa": "ERROR_TEXT tiene nueve codigos y solo uno (CONNECTION_INVALID) llega a pintarse en alguna prueba. Los ocho restantes —DELIVERY_EXHAUSTED, los seis FEED_* y SECRET_UNREADABLE— son las traducciones de los errores de webhooks y del calendario ICS, y ninguna prueba construye una fila del catalogo con esos codigos. Ninguna prueba ejercita tampoco el ?? de describeError.",
              "fichero": "frontend/src/connectors-catalog.tsx",
              "lineas": "50-68",
              "mutantesEstimados": 9,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Una prueba parametrica de nueve filas: para cada codigo del contrato, catalogo con esa fila en status error y lastError con ese codigo, y afirmar el texto exacto en la fila correspondiente Y que el codigo crudo no aparece (queryByText(codigo) === null). Mas una decima fila con un codigo desconocido que afirme el texto de reserva «Hay un problema con esta integracion».",
              "posibleDefecto": "Si. Ocho traducciones que nadie ha visto nunca renderizadas: un codigo mal escrito en la clave hace caer silenciosamente en el texto generico, y el propietario de un calendario ICS caido lee «Hay un problema con esta integracion» en vez de «El calendario no responde». El fallback tapa el defecto en vez de delatarlo."
            },
            {
              "causa": "Los dos oraculos @s7 del catalogo hacen `await screen.findByRole(\"alert\")` y nada mas: no afirman ni una palabra del aviso. Un <p role=\"alert\"></p> vacio satisface los dos. Eso deja sin matar el texto de STORAGE_UNAVAILABLE, el texto de reserva, el objeto CATALOG_ERROR_TEXT entero y las cinco mutaciones de la extraccion del codigo en el catch.",
              "fichero": "frontend/src/connectors-catalog.tsx",
              "lineas": "62-64, 103-113",
              "mutantesEstimados": 8,
              "tipo": "asercion_que_no_puede_fallar",
              "queOraculoFalta": "Afirmar el texto: con 503 STORAGE_UNAVAILABLE, alert.textContent === «No se pudo consultar el estado. Intentalo mas tarde»; con un catalogo ilegible (error sin `code`), el texto generico «No se pudo consultar el estado de las integraciones». Dos afirmaciones de una linea que matan las ocho.",
              "posibleDefecto": "Si, y es de la familia exacta de la region aria-live de esta noche: un role=alert vacio no anuncia nada. Un lector de pantalla se queda sin saber que ha pasado y la pantalla muestra un parrafo en blanco donde el contrato promete una explicacion."
            },
            {
              "causa": "El estado «conectado pero roto» no se decodifica nunca. Ninguna prueba del cliente ni del componente trae una conexion con status \"error\" ni con lastError distinto de null: todas las fixtures llevan lastError: null. Por eso decodeFailure sale siempre por el return de la linea 110 y las lineas 111-117 no las ejecuta nadie, y el literal \"error\" de la lista blanca de la linea 124 no lo mata ninguna prueba.",
              "fichero": "frontend/src/gitlab-connector-client.ts",
              "lineas": "109-118, 124, 142",
              "mutantesEstimados": 8,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Un fixture `broken` = connected con status \"error\" y lastError {code: \"CONNECTION_INVALID\", at: AT}, y cuatro pruebas: (1) se decodifica y conserva code y at; (2) se rechaza un lastError con un tercer campo `detail` que lleve texto del proveedor —la propiedad @s5, hoy probada solo para el cliente del catalogo—; (3) se rechaza un lastError con code vacio o con `at` que no es un instante; (4) se acepta una conexion connected con lastActivityAt null.",
              "posibleDefecto": "Si, y doble. Uno: la pantalla pinta STATUS_TEXT.error = «Error» y NO renderiza connection.lastError en ningun sitio, asi que el propietario ve «Error» sin motivo aunque el servidor le mande el codigo. Dos: la propiedad de seguridad de @s5 (que el lastError nunca lleve texto del proveedor) esta probada en el cliente del catalogo pero no en el de GitLab, que es el que decodifica la respuesta donde el token podria colarse."
            },
            {
              "causa": "Ninguna prueba que provoca un error afirma las palabras del aviso; afirman el efecto lateral (aria-invalid, la aparicion del boton «Actualizar estado», que la conexion sigue). Cinco de las ocho entradas de MESSAGES no se leen nunca: CONNECTION_NOT_FOUND, PROJECT_COMPLETED y RESOURCE_NOT_FOUND no aparecen en ninguna prueba, y STORAGE_UNAVAILABLE (@s35 desconexion fallida) y VALIDATION_ERROR (@s34 campo marcado) se disparan pero sin afirmar el texto. Sumo el texto de reserva de describeFailure y STATUS_TEXT.not_connected, que es codigo muerto: el panel solo se pinta si status !== \"not_connected\".",
              "fichero": "frontend/src/gitlab-connector.tsx",
              "lineas": "29-56",
              "mutantesEstimados": 7,
              "tipo": "asercion_que_no_puede_fallar",
              "queOraculoFalta": "Una parametrica de ocho filas codigo -> texto exacto sobre describeFailure (es exportada, no hace falta renderizar), mas afirmar en las dos pruebas que ya disparan STORAGE_UNAVAILABLE y VALIDATION_ERROR que alert.textContent es el texto del contrato y no contiene el codigo crudo. Y decidir sobre STATUS_TEXT.not_connected: o se borra la entrada o se pinta el estado sin conexion en el panel.",
              "posibleDefecto": "Si. Misma familia que el racimo del catalogo: un role=alert vacio tras un fallo de desconexion o de validacion. El propietario ve el campo marcado en rojo sin ninguna frase que le diga que revisar."
            },
            {
              "causa": "En la rama `absent` de decodeConnection los siete `!== null` se encadenan con `||` y ninguna prueba manda un not_connected que se deje un campo puesto: la fixture notConnected trae los siete nulos. Con todos los operandos en falso, cambiar cualquier `||` por `&&` da el mismo resultado. Se suma nonEmpty, cuyo `> 0` no lo mata nadie porque no hay ninguna prueba con una cadena vacia en ningun campo.",
              "fichero": "frontend/src/gitlab-connector-client.ts",
              "lineas": "105-107, 127-144",
              "mutantesEstimados": 7,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Una parametrica de siete filas: not_connected con exactamente uno de los siete campos no nulo (apiBase, projectPath, projectId, tokenHint, lastActivityAt, lastError, version) debe rechazarse con «Confirmacion incompatible». Y una fila mas con projectPath: \"\" y otra con tokenHint: \"\" sobre una conexion connected.",
              "posibleDefecto": "Si. Una fila not_connected que arrastre el projectPath o el tokenHint de la conexion anterior se aceptaria. Como la pantalla solo pinta el panel cuando status !== not_connected no se veria hoy, pero es exactamente el residuo que @s32 y @s37 prohiben que sobreviva a un cambio de propietario, y el decodificador es la unica guarda que hay."
            },
            {
              "causa": "Un recibo en status \"running\" no lo decodifica ninguna prueba: las dos fixtures son \"completed\". Eso deja vivos los tres literales \"running\" (lista blanca de la linea 158 y las dos comparaciones de 166-167), \"failed\" de la lista blanca, la guarda de errorCode de la 167 y las dos fronteras de la 168-170, que nunca se ejercitan como rechazo (no hay ninguna prueba con finishedAt anterior a startedAt ni igual).",
              "fichero": "frontend/src/gitlab-connector-client.ts",
              "lineas": "151-174",
              "mutantesEstimados": 6,
              "tipo": "sin_cobertura",
              "queOraculoFalta": "Cuatro filas: (1) recibo running con finishedAt null y errorCode null se acepta; (2) running con errorCode no nulo se rechaza; (3) running con finishedAt no nulo se rechaza; (4) completed con finishedAt anterior a startedAt se rechaza, y una quinta con finishedAt == startedAt para fijar la frontera (hoy `<` acepta el empate y nadie lo dice).",
              "posibleDefecto": "Si. El componente Receipt pinta cuatro cifras y nunca el status: un recibo running (creadas 0, omitidas 0, falladas 0, truncado No) se presenta identico a una importacion terminada sin resultados. Y readGitlabImport, que seria quien releyera ese recibo, esta exportada y no la llama nadie en produccion —solo su propia prueba—, asi que la importacion en curso no tiene forma de completarse en pantalla."
            },
            {
              "causa": "Nada afirma la exclusion mutua entre el panel y el formulario ni el estado de carga. showPanel y showForm encadenan cuatro y tres condiciones con &&; ninguna prueba afirma que el formulario esta AUSENTE con una conexion viva, ni que el panel esta ausente mientras se reemplaza el token, ni que durante la carga inicial no se pinta nada. La unica prueba que afirma la ausencia del campo de token es @s29, y alli manda `disabled`.",
              "fichero": "frontend/src/gitlab-connector.tsx",
              "lineas": "66, 298-300",
              "mutantesEstimados": 6,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Tres afirmaciones: (1) con la conexion connected resuelta, queryByLabelText(/Token de acceso personal/) === null; (2) tras pulsar «Actualizar token», queryByRole(\"button\", {name: \"Importar issues\"}) === null y queryByText(\"••••WXYZ\") === null; (3) con el GET de la conexion todavia en vuelo, ni formulario ni panel: queryByLabelText(/Token/) === null y queryByRole(\"button\") === null.",
              "posibleDefecto": "Si, dos. Con loading arrancando en false el propietario ya conectado veria parpadear el formulario «Conectar» antes de que llegue el GET —una invitacion a reescribir el token—. Y mientras reemplaza el token el panel viejo seguiria ofreciendo «Importar issues» y «Desconectar» sobre una conexion que esta a punto de cambiar."
            },
            {
              "causa": "El mensaje del Error tipado no lo afirma nadie: las pruebas comprueban `.code`, `.retryAfterSeconds`, `.importId` y `instanceof`, nunca `.message` ni `.name`. La linea 58 (el super() con su ternario) queda entera sin oraculo, y `this.name = \"GitlabConnectorError\"` tampoco, porque instanceof no mira el nombre. Se suma el ternario de importId, cuya rama de rechazo no ejercita ninguna prueba (no hay ningun cuerpo con un importId malformado).",
              "fichero": "frontend/src/gitlab-connector-client.ts",
              "lineas": "57-67",
              "mutantesEstimados": 6,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "En la prueba del cuerpo ilegible y en la de RATE_LIMITED, afirmar tambien error.message y error.name; y una fila que mande importId: \"NO-ES-UN-UUID\" (o el mismo en mayusculas, que identifier tambien rechaza) y afirme que error.importId es null. El mismo agujero, calcado, esta en CatalogError (connectors-catalog-client.ts:44-52, unos 4 mutantes mas): alli tampoco se afirma el mensaje.",
              "posibleDefecto": "Menor, pero real: un Error cuyo message quede vacio no dice nada en un volcado ni en un log del navegador, y es lo unico que se ve cuando el error escapa de la pantalla."
            },
            {
              "causa": "El unico cuerpo con `errors` que se prueba trae una sola entrada bien formada sobre projectPath. Las tres condiciones del filtro de fieldErrors se cumplen a la vez, asi que cambiar cualquier && por || da el mismo resultado, y forzar la condicion a true tampoco cambia nada. En la pantalla, el aria-invalid del campo `token` nunca llega a ser true en ninguna prueba: solo se prueba que NO lo es.",
              "fichero": "frontend/src/gitlab-connector-client.ts",
              "lineas": "70-85",
              "mutantesEstimados": 6,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Cuerpos de error con: una entrada que no es objeto, una con field vacio, una con code vacio, dos entradas validas a la vez (ambos campos marcados), y `errors` como objeto en vez de array. Y en gitlab-connector.tsx (lineas 390 y 409) la simetrica de @s34: un VALIDATION_ERROR sobre `token` que afirme aria-invalid=\"true\" en el campo de token y que el de la ruta NO lo tiene.",
              "posibleDefecto": "Si. Hoy nadie prueba que el campo del token se pueda marcar; si el mapeo estuviera roto, un token rechazado por el servidor senalaria el campo equivocado o ninguno, que es justo lo que @s34 promete evitar."
            },
            {
              "causa": "Las tres banderas de foco se apagan con `= false` inmediatamente antes de mover el foco, y ninguna prueba afirma que el foco se mueva UNA sola vez. Dejarlas en true reenfoca en cada render y las tres pruebas @s38 siguen verdes, porque solo miran donde esta el foco justo despues del cambio de estado. Los tres `?.focus()` opcionales tampoco los mata nadie.",
              "fichero": "frontend/src/gitlab-connector.tsx",
              "lineas": "139-154",
              "mutantesEstimados": 6,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Despues del cambio de estado que mueve el foco, provocar OTRO render que no deba moverlo (escribir en el selector de proyecto, o pulsar «Desconectar» para abrir la confirmacion) y afirmar que el foco sigue donde el usuario lo dejo, no de vuelta en el h1 o en el boton «Actualizar token».",
              "posibleDefecto": "Si, y es un defecto de accesibilidad clasico: una bandera que no se apaga roba el foco en cada repintado. Quien navegue con teclado no podria ni recorrer el selector de proyectos: cada render lo devolveria al encabezado."
            },
            {
              "causa": "Ninguna prueba del componente afirma QUE se envia al conectar. Las @s34/@s32 escriben el token, pulsan y afirman lo que sale en pantalla; el cuerpo del PUT solo lo afirma gitlab-connector-client.test.ts, que no renderiza el componente y por tanto no mata sus mutantes. Quedan vivos el `?? \"\"` del valor del campo, el encadenamiento opcional de la ref y el objeto {token, projectPath} entero. Se suma la guarda `if (connecting) return`, que ninguna prueba ejercita porque el boton esta deshabilitado.",
              "fichero": "frontend/src/gitlab-connector.tsx",
              "lineas": "174-204",
              "mutantesEstimados": 5,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "En la prueba que ya escribe el token y pulsa «Conectar», leer fetcher.mock.calls y afirmar JSON.parse(body) toEqual {token: TOKEN, projectPath: \"grupo/proyecto\"} — atar lo tecleado con lo enviado. Y una prueba de doble envio: enviar el formulario dos veces (submit del form, que no pasa por el boton deshabilitado) y afirmar que solo hay un PUT.",
              "posibleDefecto": "Si. Hoy la pantalla podria enviar el token vacio y todas las pruebas del componente seguirian verdes: el servidor doble responde `connected` pase lo que pase. Y un submit por teclado repetido mandaria el token dos veces."
            },
            {
              "causa": "Atributos de accesibilidad que ninguna prueba mira: aria-describedby de los dos campos y los id de los dos parrafos de ayuda (la prueba @s34 afirma que el texto existe, no que este asociado), el id=\"proyectos\" del landmark —destino del enlace de salto que App.test.tsx:303 afirma, pero para el main de App, no para el de esta pantalla— y el className.",
              "fichero": "frontend/src/gitlab-connector.tsx",
              "lineas": "303, 383-414",
              "mutantesEstimados": 5,
              "tipo": "asercion_que_no_puede_fallar",
              "queOraculoFalta": "toHaveAccessibleDescription sobre los dos campos con el texto de su ayuda (eso ata aria-describedby con el id de una vez), y expect(screen.getByRole(\"main\")).toHaveAttribute(\"id\", \"proyectos\") — el patron ya existe en import-data.test.tsx:407. Lo mismo en connectors-catalog.tsx:122 y su lista (aria-label=\"Conectores\", hoy sin oraculo: las pruebas usan getAllByRole(\"listitem\")).",
              "posibleDefecto": "Si. Sin id en el main el enlace «Saltar al contenido» de App no lleva a ninguna parte en las dos pantallas nuevas, y sin la descripcion asociada un lector de pantalla no anuncia «con la forma grupo/proyecto» ni «alcance read_api» al entrar en el campo. Es exactamente el hueco de navegador que el juez marca como C2."
            },
            {
              "causa": "El efecto que carga los proyectos: su limpieza `() => controller.abort()` no la afirma nadie (ninguna prueba desmonta con la lista de proyectos en vuelo), su guarda triple nunca se evalua con un solo operando cierto, y el `catch {}` se traga cualquier fallo. La cadena `open[0]?.id` tampoco: cuando la lista viene vacia el TypeError del mutante cae dentro de ese mismo catch.",
              "fichero": "frontend/src/gitlab-connector.tsx",
              "lineas": "156-172",
              "mutantesEstimados": 4,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Desmontar con la peticion de /proyectos en vuelo y afirmar que su signal quedo abortado (el mismo patron que ya usan las @s37 para las otras peticiones). Y una prueba con la lista vacia: el selector sin opciones y el boton «Importar issues» que no debe enviar nada.",
              "posibleDefecto": "Si. Salir de la pantalla no cancela la peticion de proyectos —solo se abortan las que pasan por pending.current—, y con la lista vacia `selected` se queda en \"\", pero el boton «Importar issues» sigue habilitado: pulsarlo con la guarda relajada mandaria un POST con projectId vacio."
            },
            {
              "causa": "En startImport y confirmDisconnect hay ramas que ninguna prueba recorre: la guarda `importing || !selected` (ninguna prueba importa sin proyecto ni pulsa dos veces), el ternario `current ? {...} : current` con current nulo, el objeto lastError que se sintetiza y que la pantalla no pinta en ningun sitio, y setProjectPath(\"\")/setReplacing(false)/setConfirming(false) tras la desconexion, que ninguna asercion comprueba.",
              "fichero": "frontend/src/gitlab-connector.tsx",
              "lineas": "206-275",
              "mutantesEstimados": 4,
              "tipo": "oraculo_debil",
              "queOraculoFalta": "Tras el 204, afirmar que el campo «Ruta del proyecto» del formulario reaparecido esta vacio (hoy solo se afirma tras un cambio de propietario, que remonta el arbol y por tanto no puede fallar). Y tras una desconexion fallida, afirmar si el grupo «Confirmar desconexion» debe seguir abierto o cerrado: el contrato lo fija, la prueba no.",
              "posibleDefecto": "Si. Al desconectar, la ruta del proyecto anterior podria quedarse escrita en el formulario que se reabre —residuo de la conexion anterior, justo lo que @s32/@s37 prohiben—, y nadie lo veria. Y el lastError que startImport sintetiza no se pinta nunca: la pantalla dice «Error» sin decir por que."
            }
          ]
        },
        "falsos": [
          "R5 — la causa esta mal enunciada, aunque la cuenta se sostiene. Dice «Ninguna prueba que provoca un error afirma las palabras del aviso; afirman el efecto lateral». Es falso: CUATRO pruebas afirman el texto literal. gitlab-connector.test.tsx:173 «expect(alert.textContent).toContain(\"La conexion ya no es valida\")» (y :174 que el codigo crudo no aparece), :493 toContain(\"30 segundos\"), :525 toContain(\"importacion en curso\"), :548 toContain(\"GitLab no responde\"). Es decir, MESSAGES.CONNECTION_INVALID, MESSAGES.IMPORT_IN_PROGRESS, MESSAGES.GITLAB_UNAVAILABLE y la plantilla de RATE_LIMITED mueren. Lo que SI es cierto, y es lo que hay que escribir: sobreviven las cinco restantes (CONNECTION_NOT_FOUND, PROJECT_COMPLETED, RESOURCE_NOT_FOUND sin ninguna prueba; STORAGE_UNAVAILABLE disparada en :439 y VALIDATION_ERROR en :184 pero sin afirmar texto), mas el `?? 60` de la 54, mas el texto de reserva de la 55 (lo alcanza la prueba de fallo de red de :554, que no mira el texto), mas STATUS_TEXT.not_connected. La cuenta de 7 se mantiene; la frase de cabecera no.",
          "R9, apendice sobre CatalogError — «alli tampoco se afirma el mensaje» solo vale para el mensaje: el NOMBRE si tiene oraculo. connectors-catalog-client.test.ts:172-175 hace «rejects.toMatchObject({ name: \"CatalogError\", code: \"STORAGE_UNAVAILABLE\" })», asi que `this.name = \"CatalogError\"` muere. De los ~4 mutantes que el apendice promete quedan 2 (el literal del super() y su ternario). No merece una prueba propia: basta anadir .message a esa misma asercion.",
          "R14, la parte de la lista vacia — «una prueba con la lista vacia» ya existe como CAMINO, no como oraculo. connectors-catalog-routing.test.tsx:27-38 monta App con un fetch que devuelve {items: [], nextCursor: null} para /api/v1/projects, y :55 renderiza la pantalla de GitLab; ahi `open[0]?.id ?? \"\"` se ejecuta con el array vacio. Lo que falta no es cobertura sino asercion (y ademas en esa prueba el GET de la conexion da 404, asi que el selector ni se pinta). Lo mismo vale para la ultima frase de su posibleDefecto: el boton «Importar issues» NO enviaria un POST con projectId vacio, porque startImport:207 corta con `!selected` — solo pasaria si ademas se relaja esa guarda, que es justo el mutante de R15.",
          "PARCIAL, racimo 3 (connectors-catalog.tsx:62-64 + 103-113, 8 mutantes): los DOS textos que el racimo cuenta SI tienen oraculo, por accidente. El aviso se pinta con `{failure ? <p role=\"alert\">{failure}</p> : null}` (connectors-catalog.tsx:126); Stryker convierte un literal de cadena en \"\", `setFailure(\"\")` es falsy y el <p role=\"alert\"> desaparece entero, asi que los dos `await screen.findByRole(\"alert\")` (connectors-catalog.test.tsx:225 y 235) se quedan sin encontrar nada y fallan. Sobreviven de verdad los CINCO de la extraccion del codigo (el `&&` de la 106, las dos ramas del ternario, \"code\"->\"\" y \"CATALOG_ERROR\"->\"\") mas el ObjectLiteral de CATALOG_ERROR_TEXT: ~6, no 8. El hueco de oraculo es real y merece arreglarse; la cifra esta inflada.",
          "PARCIAL, racimo 9: «El mismo agujero, calcado, esta en CatalogError... alli tampoco se afirma el mensaje» — el `name` SI se afirma: connectors-catalog-client.test.ts:172-175 hace `rejects.toMatchObject({name: \"CatalogError\", code: \"STORAGE_UNAVAILABLE\"})`, y el `code` se afirma en las dos ramas (linea 186 para el cuerpo ilegible). De CatalogError solo sobrevive la expresion del `super()` (connectors-catalog-client.ts:48): ~5 mutantes, y todos por el message, ninguno por el name. En GitlabConnectorError, en cambio, la prediccion acierta: gitlab-connector-client.test.ts solo usa `toBeInstanceOf`, asi que `this.name` (linea 59) tambien sobrevive alli.",
          "PARCIAL, racimo 10 (6 mutantes): dos de los que cuenta estan muertos por via del componente. `!Array.isArray(value)` (gitlab-connector-client.ts:71) y `typeof entry === \"object\"` (linea 76) los mata @s34 (gitlab-connector.test.tsx:179-198), porque el mapa `fields` alimenta el `aria-invalid` de la ruta y la prueba lo afirma en la 195. Sobreviven los tres `&&`->`||` (74-79, todos los operandos ciertos a la vez) y el `if(true)` del filtro: ~4. Se compensa con los dos ternarios de aria-invalid (lineas 390 y 409): el `true` de la ruta y el `false` del token no los mata nadie."
        ],
        "omitidos": [
          "El catch de loadConnection (gitlab-connector.tsx:113-120) no lo cubre mas que UNA prueba de fallo. Solo @s29 (:255) hace fallar el GET de la conexion, con CONNECTORS_DISABLED; la otra unica lectura fallida del repo es el 404 de connectors-catalog-routing.test.tsx:55, que no afirma nada mas que el h1. Consecuencia: `error instanceof GitlabConnectorError && error.code === \"CONNECTORS_DISABLED\"` mutado a `||` (o a true) sobrevive, y con el sobrevive un defecto de producto de la familia de esta noche: cualquier lectura fallida —STORAGE_UNAVAILABLE, un 503 cualquiera, un cuerpo ilegible— pintaria «Falta configuracion del servidor para usar los conectores» y ocultaria el formulario, mandando al propietario a molestar a quien administra la instalacion por un fallo pasajero. Oraculo que falta: un GET que falle con STORAGE_UNAVAILABLE y afirmar que NO aparece el aviso de configuracion y SI el formulario de conectar.",
          "La guarda `connection.status !== \"not_connected\"` de la linea 319 no tiene oraculo. Con una conexion not_connected el objeto NO es nulo, asi que showPanel ya vale true y lo unico que impide pintar el panel es ese `!==`. Mutado a `===` el panel se pinta con «No conectado», «••••» (maskHint de la cadena vacia), proyecto vacio e identificador vacio, junto al formulario — y la suite entera sigue verde: la unica asercion que podria pillarlo, `expect(screen.queryByText(\"Conectado\")).toBeNull()` de :92, compara texto exacto y «No conectado» no es «Conectado». R8 pide la exclusion mutua en la direccion contraria (formulario ausente con conexion viva) y no mata este. Oraculo: con notConnected resuelto, queryByRole(\"region\", {name: \"Conexion\"}) === null y queryByText(\"••••\") === null.",
          "La region viva ociosa (438-444) no la afirma nadie en silencio. Se afirma que contiene «Guardando…» (:117) y «Importando» (:366), pero ninguna prueba afirma que este VACIA cuando no hay nada en vuelo, asi que el literal \"\" de la 443 y el mutante que deja `importing ? ... : \"\"` siempre en la rama del texto sobreviven: una pantalla que anuncia «Importando issues. Esto puede tardar un poco…» de forma permanente pasa la suite. Es exactamente el mismo sitio del que salio el defecto de la region aria-live de esta noche, mirado por el otro lado. Ademas aria-atomic=\"true\" no lo afirma nadie (solo aria-live, en :116).",
          "Atributos con cero aserciones que R13 no llega a nombrar y que caen en el mismo racimo: aria-label=\"Conexion\" de la seccion del panel (grep: no aparece en ninguna prueba; solo se consulta por rol «region» el «Resultado de la importacion»), autoComplete=\"off\" del campo de la ruta (solo se afirma el del token, :72), name=\"projectPath\" y name=\"token\", y el tabIndex={-1} de los dos <main>. Son mutantes baratos de matar en las pruebas que ya existen: basta consultar el panel por getByRole(\"region\", {name: \"Conexion\"}) en vez de por getByText(\"••••WXYZ\").",
          "EL GRANDE: connectors-catalog.tsx:96-119 tiene el MISMO racimo de escritura tardia que el racimo 1, y la prediccion no lo vio: al catalogo solo le asigno racimos de texto. `let live = true` (98), `if (live) setRows(catalog)` (102), `if (!live || controller.signal.aborted) return` (104) y la limpieza `live = false; controller.abort()` (115-118). La unica prueba que los toca es @s37 (connectors-catalog.test.tsx:241-262), que desmonta y afirma `failures.toHaveLength(0)` y `queryAllByRole(\"listitem\")` a cero: las dos pasan igual con el guardia quitado, porque un setState sobre un arbol desmontado es un no-op silencioso y las filas no estan por el desmontaje, no por el guardia. Sobreviven `if(true)` en la 102, `if(false)` y `||`->`&&` en la 104, `live = true` en la 116 y el vaciado del bloque de limpieza (que ademas deja la peticion sin abortar): ~5. Misma familia, mismo remedio, otro fichero.",
          "gitlab-connector.tsx:319 — el estado sin conexion pinta el panel y nadie lo prohibe. Con `notConnected`, `connection` NO es null (loadConnection guarda el objeto decodificado), asi que `showPanel` es TRUE y lo unico que oculta el panel es el literal `connection.status !== \"not_connected\"`. Mutalo a \"\" y la pantalla le ofrece a un propietario sin conexion el panel entero: «No conectado», Proyecto vacio, «••••», y los botones «Importar issues» / «Actualizar token» / «Desconectar». Las pruebas de notConnected (gitlab-connector.test.tsx:65-93) solo afirman tipo, autocomplete, valor del campo y `queryByText(\"Conectado\")` a null — y «No conectado» no es «Conectado», asi que pasan. Falta: con la conexion notConnected resuelta, `queryByRole(\"button\", {name: \"Importar issues\"}) === null` y `queryByText(/••••/) === null`. 1 mutante, pero es el que el racimo 5 dio por muerto al llamar «codigo muerto» a STATUS_TEXT.not_connected: esta muerto por ese guardia, y el guardia no tiene oraculo.",
          "gitlab-connector.tsx:473-479 — el aviso de truncado no tiene oraculo de ausencia. `{receipt.truncated ? <p>...quedaron issues sin traer...</p> : null}`: la rama `true` sobrevive porque la prueba del recibo normal (gitlab-connector.test.tsx:373-392) solo mira las cuatro cifras dentro de la region y nunca afirma que el parrafo NO esta. Es exactamente el defecto del «oraculo de recorte que no veia el recorte» de esta noche: una importacion completa le diria al propietario que quedaron issues sin traer. Falta una linea en la prueba de 373: `expect(screen.queryByText(/quedaron issues sin traer/i)).toBeNull()`.",
          "gitlab-connector.tsx:438-444 — la region aria-live no tiene oraculo de silencio. Nadie afirma que `<p role=\"status\">` este vacio cuando no pasa nada, ni que vuelva a callarse cuando la operacion termina; solo se afirma `toContain(\"Guardando…\")` (117) y `toContain(\"Importando\")` (366) mientras la peticion viaja. Sobreviven: el `\"\"` de la 443 (Stryker lo cambia por «Stryker was here» y la region anuncia esa frase en todo momento, en las dos pantallas y siempre) y la rama `true` del segundo ternario (anuncia «Importando issues» sin importacion). Ademas `aria-atomic=\"true\"` (438) no lo mira nadie. ~3 mutantes, y es la familia literal de la region aria-live que no se actualizaba.",
          "Nombres accesibles y atributos, mas alla del racimo 13 (~10 mutantes que ese racimo no cuenta): `aria-label=\"Conexión\"` de la seccion del panel (gitlab-connector.tsx:320) no lo afirma nadie — sin nombre, un <section> deja de exponerse como region; el par `id=\"gitlab-project\"` / `htmlFor` del selector (333-335) tampoco, porque las pruebas usan `getAllByRole(\"option\")` y nunca `getByLabelText(\"Proyecto de destino\")` — el selector se queda sin nombre accesible; `name=\"projectPath\"` (386) y `name=\"token\"` (405); `autoComplete=\"off\"` del campo de ruta (387), que solo se afirma para el token (gitlab-connector.test.tsx:72); y los cinco `tabIndex={-1}` de las dos pantallas (gitlab-connector.tsx:303, 304, 461 y connectors-catalog.tsx:122, 123), que el mutador unario convierte en `+1` y meten el <main>, el <h1> y el recibo en el orden de tabulacion sin que ninguna prueba lo note.",
          "gitlab-connector.tsx:307-309 — la miga de pan `<RouteLink href=\"/conectores\">Conectores</RouteLink>` no tiene NINGUN oraculo: gitlab-connector.test.tsx no usa `getByRole(\"link\")` ni una sola vez (comprobado por grep) y connectors-catalog-routing.test.tsx:76 afirma el enlace del menu de App, no este. `href` a \"\" sobrevive y deja al propietario sin camino de vuelta al catalogo desde la pantalla de GitLab.",
          "gitlab-connector.tsx:334-352 — el selector de proyecto no se cambia NUNCA en ninguna prueba (`user.selectOptions` no aparece en todo el fichero; «Segundo» solo se comprueba como etiqueta de opcion en la 340). Consecuencias: la flecha del `onChange` (338) sobrevive como `() => undefined`; el `useState(\"\")` de `selected` (74) sobrevive convertido en «Stryker was here», porque `current || (open[0]?.id ?? \"\")` conserva el valor basura, el servidor doble responde el recibo pase lo que pase y ninguna prueba del componente lee el cuerpo del POST (no hay un solo `JSON.parse` en gitlab-connector.test.tsx). Es el racimo 12 otra vez, pero para la importacion: falta atar el proyecto elegido con lo que se envia, y falta importar al SEGUNDO proyecto. ~3 mutantes.",
          "gitlab-connector.tsx:368-370 — el boton «Cancelar» de la confirmacion de desconexion no lo pulsa ninguna prueba (grep de «Cancelar» en el arbol: solo aparece en appearance y availability). Su `onClick` sobrevive como `() => undefined`: hoy la confirmacion podria no poder cerrarse y la suite seguiria verde. Camino de producto declarado por @s35 y sin recorrer.",
          "gitlab-connector.tsx:294 y connectors-catalog.tsx:93 — encadenamientos opcionales sin oraculo que el racimo 11 solo cuenta para los tres focos del efecto: `connection?.projectPath` en replaceToken y `heading.current?.focus()` del useLayoutEffect del catalogo. ~2 mutantes.",
          "gitlab-connector-client.ts:97-103 — el segundo `&&` de `identifier()` (`value === value.toLowerCase() && value.length === 36`) sobrevive al pasar a `||` porque no hay ninguna prueba con un uuid de 36 caracteres en mayusculas ni con longitud equivocada. La prediccion lo insinua en el remedio del racimo 9 pero no lo cuenta; se suma tambien el `{\" \"}` de connectors-catalog.tsx:145, que al vaciarse pega el glifo al estado («●Conectado») sin que nadie lo vea."
        ]
      }
    ],
    "plan": "# PLAN DE ATAQUE — features 25, 28, 29 (frontend)\n\nLeído y verificado sobre el código real (no el sandbox de Stryker). Todo lo que marco **(verificado)** lo he confirmado abriendo el fichero; lo que marco *(heredado)* viene del análisis + refutación y no lo he vuelto a comprobar línea a línea.\n\n---\n\n## 0. VEREDICTO DE ORDEN — léelo antes de repartir\n\n**Ninguna de las tres está en el 80 %. Pero no están igual de lejos, y el orden de la predicción original se invierte tras la refutación.**\n\n| Feature | Prod. LOC | Vivos que explico leyendo | Oráculos que hay que escribir | Distancia al 80 % |\n|---|---|---|---|---|\n| **29 conectores** | 1011 (4 ficheros) | ~130 | **~35** | **La más cerca.** 2-3 carriles y cruza. |\n| **28 calendario externo** | 830 (3 ficheros) | ~160 | ~50 | Media. 3 carriles. |\n| **25 webhooks** | 810 (2 ficheros) | ~157 | **~55** | **La más lejos**, y encima con el mayor racimo huérfano de las tres. |\n\nPor qué se invierte: la refutación **recortó** poco en 29 (los recortes de R3/R9/R10 suman ~8 mutantes menos) y **añadió mucho** en 25 y 28 — en 25 apareció un racimo de ~10 mutantes que la predicción no vio (la selección de tipos del formulario, `webhooks.tsx:369-387`, cero oráculos), más ~8 de `throwIfAborted` y ~7 de guardas de identidad; en 28 aparecieron ~8 de los reinicios de estado y ~7 de textos de vista que nadie afirma nunca.\n\n**Recomendación al orquestador:** arranca los 8 carriles a la vez si tienes máquina, pero si tienes que priorizar, **29 primero** (victoria barata y cierre de feature), **25 con más carriles** (es la que más trabajo tiene y la que tiene los dos defectos de producto más feos).\n\n---\n\n## 1. DEFECTOS DE PRODUCTO — VAN PRIMEROS, SIEMPRE\n\nEstos cinco tienen prueba que se pone **ROJA hoy** contra el producto actual. No son mutantes: son bugs. Cada uno debe ir al `tdd_craftsman` como prueba que falla → arreglo → verde, **no** como \"añadir oráculo\".\n\n### D1 · webhooks — todas las acciones fallidas dicen «No se ha podido crear el webhook» (verificado)\n`frontend/src/webhooks.tsx:175-199` (`report`) y `:201-208` (`act`).\n`report()` se escribió para el formulario de creación y `act()` lo reutiliza para **desactivar, activar, eliminar, ping, ver entregas y reenviar**. Consecuencias reales, las tres visibles:\n- Un 500 al desactivar → el usuario lee **«No se ha podido crear el webhook.»** (`:196`).\n- Un `WEBHOOK_LIMIT` al reenviar → lee **«Ya tienes cinco webhooks. Elimina uno antes de crear otro.»** (`:186-190`).\n- Un `WEBHOOK_URL_BLOCKED` en cualquier acción → **se enciende el error del campo URL del formulario de creación** (`:177-180`), con `aria-describedby` (`webhooks.tsx:341`) apuntando a un campo que no tiene nada que ver.\n- En los tres casos el mensaje aparece pegado al formulario, lejos del control que falló, y la región `role=\"status\"` (`:273-286`) no se entera.\n\nPrueba roja: «Desactivar» que responde 409 → afirmar que el usuario ve algo sobre desactivar y **no** el texto de creación.\n\n### D2 · webhooks — la tabla de entregas muestra las de otro webhook, y «Reenviar» las manda al endpoint equivocado (verificado)\n`frontend/src/webhooks.tsx:218-224` (`openDeliveries`) y `:208-216` (`ping`), pintadas en `:531`.\n`openDeliveries` hace `setDeliveriesOf(endpoint.id)` **síncrono** y sólo sustituye `deliveries` cuando llega la respuesta. Entre el clic en «Ver entregas» de B y la llegada de su GET —o **para siempre**, si ese GET falla, porque el `catch` de `act()` no vacía la tabla— la tabla enseña las entregas de A bajo el rótulo de B. El botón «Reenviar» de esas filas hace `POST /webhooks/B/deliveries/{id-de-una-entrega-de-A}/redeliver`. `ping(B)` hace lo mismo: antepone la entrega nueva a las filas de A.\nEl fixture de dos endpoints **ya existe** (`webhooks.test.tsx:158-179`), no hay que inventarlo.\n\n### D3 · calendario externo — un 500 en GET /events le dice al usuario que no tiene eventos (verificado)\n`frontend/src/external-calendar.tsx:124-135` + `frontend/src/external-calendar-api.ts:188` + render en `:389-393`.\nEl `catch` de `loadEvents` hace `if (error instanceof Error && !(error instanceof ConnectorsDisabledError)) setInvalidList(true)`. Pero `refuse()` **relanza la propia `Response`** (`external-calendar-api.ts:188`), que **no es `instanceof Error`**. Resultado: cualquier 4xx/5xx en `/events` cae en `setEvents([])` con `invalidList` en `false`, y la pantalla pinta **«No hay eventos en la ventana guardada.»** Es exactamente la familia de los tres defectos de esta noche: la interfaz afirma un hecho que no conoce.\nPrueba roja: con 500 en GET /events, `expect(screen.queryByText(\"No hay eventos en la ventana guardada\")).toBeNull()`. **Ojo:** el oráculo correcto es la **ausencia del texto de vacío**, no la presencia del aviso — afirmar la presencia del aviso fallaría contra el producto *correcto* si se arregla de otra manera.\n\n### D4 · conectores GitLab — gana la última respuesta, no la última orden (verificado)\n`frontend/src/gitlab-connector.tsx:96-122` y siguientes: cada operación hace `pending.current = controller` **sin abortar el anterior**; `live(controller)` sólo mira `mounted` y la señal *propia*. Dos operaciones solapadas escriben las dos. Una importación lenta repinta «Creadas/Omitidas/Fallidas» sobre una pantalla ya desconectada; una lectura de conexión vieja resucita «Conectado» después de un 204.\nPrueba roja: lanzar «Importar issues», con la petición en vuelo pulsar «Desconectar» + «Confirmar», resolver **después** el recibo y afirmar que la pantalla sigue sin conexión y sin la región «Resultado de la importación».\n**Nota para el juez:** si se decide NO tocar producción, los ~13 mutantes de `live()` + los ~5 gemelos de `connectors-catalog.tsx:96-119` hay que **declararlos supervivientes equivalentes por escrito antes de la campaña**. Con React 19 un `setState` sobre un árbol desmontado es un no-op silencioso: ningún oráculo de desmontaje puede distinguirlos.\n\n### D5 · conectores GitLab — «Error» sin motivo (verificado)\n`gitlab-connector-client.ts:109-118` decodifica `connection.lastError {code, at}`, y `gitlab-connector.tsx:320-330` **no lo pinta en ningún sitio**: el `<dl>` del panel tiene Estado / Proyecto / Identificador / Token y nada más. El propietario ve `STATUS_TEXT.error` = «Error» aunque el servidor le esté mandando el código. Lo mismo con el `lastError` que `startImport` sintetiza (`:206-275`): se guarda y no se muestra.\n\n**Segundo nivel (defectos latentes, prueba VERDE al escribirla pero riesgo real):**\n- `webhooks.tsx:153` — `setItems([created.endpoint, ...current])`: las tres pruebas de creación arrancan con lista vacía, anteponer y añadir al final son indistinguibles.\n- `webhooks.tsx:154-156` — nadie comprueba que el formulario quede limpio tras crear; hoy el `setUrl(\"\")` podría perderse y el usuario crearía el mismo webhook dos veces.\n- `gitlab-connector.tsx:319` — `connection.status !== \"not_connected\"` es **lo único** que impide pintarle el panel entero (con «••••», proyecto vacío y los tres botones) a un propietario sin conexión. Cero oráculos: `queryByText(\"Conectado\")` de `gitlab-connector.test.tsx:92` compara texto exacto y «No conectado» no es «Conectado».\n- `gitlab-connector.tsx:113-120` — `error instanceof GitlabConnectorError && error.code === \"CONNECTORS_DISABLED\"`: mutado a `||`, **cualquier** fallo pasajero de lectura pinta «Falta configuración del servidor» y esconde el formulario.\n- `gitlab-connector.tsx:473-479` — el aviso «quedaron issues sin traer» se afirma presente (`gitlab-connector.test.tsx:405`) pero **nunca ausente** (verificado por grep). Es literalmente el oráculo de recorte que no veía el recorte. **Una línea de arreglo.**\n- `external-calendar.tsx:204, 228, 247` — si se perdiera `setAnnouncement(\"\")`, la región `role=status` seguiría diciendo «Guardando…» mientras el `role=alert` dice que falló.\n\n---\n\n## 2. RACIMOS SUPERVIVIENTES POR FEATURE (refutados fuera, omitidos dentro)\n\n### FEATURE 25 — webhooks · ~157 vivos · **~55 oráculos** · la más cara\n\nOrdenados por mutantes que explican.\n\n| # | Causa | Fichero:líneas | Vivos | Tipo |\n|---|---|---|---|---|\n| 1 | **Selección de tipos del formulario, CERO oráculos.** Las 8 pruebas que pulsan «Crear tarea» (`webhooks.test.tsx:257,283,307,344,618,643,663,705`) nunca miran los `eventTypes` del POST; la única afirmación del cuerpo (`:221`) viene de «Seleccionar todos». No hay ni un `toBeChecked` ni un clic que DESMARQUE (verificado por grep). Sobreviven el `.filter` de inserción en orden, el `===`→`!==`, el `\\|\\|`→`&&` y **toda la rama falsa del ternario**. | `webhooks.tsx:369-387` | ~10 | sin cobertura |\n| 2 | Tabla de rechazo de `decodeEndpoint` sin fixture: id no-uuid, `http://`, descripción de 81 puntos de código, `eventTypes: []`, tipo desconocido, instantes inválidos, `disabledReason` desconocido. | `webhooks-client.ts:79-116` (+ helpers `66-77`) | ~20 | sin cobertura |\n| 3 | Las doce etiquetas humanas y su emparejamiento con el catálogo. **Corrección al alza:** ni siquiera el índice 3 está verificado — pulsar la casilla «Crear tarea» y no mirar el cuerpo del POST no demuestra nada. Son **12** literales vivos, no 11. El defecto que protege (marcar «Crear subtarea» y suscribirse a otra cosa) no lo cubre **ninguna** prueba. | `webhooks.tsx:17-30, 385, 413-420` | ~12 | oráculo débil |\n| 4 | Doce `if (signal.aborted) return` de la vista: ninguna promesa se resuelve DESPUÉS de abortar (los tests de aborto usan promesas que no resuelven nunca). Y `webhooks.test.tsx:314` afirma la ausencia del secreto cuando la respuesta no ha llegado nunca — **aserción que no puede fallar**. | `webhooks.tsx:119,122,125,151,158,161,198,208,221,233,246,253` | 13 | sin cobertura |\n| 5 | Atributos de accesibilidad afirmados por presencia y no por valor: `aria-live` (hoy `closest(\"[aria-live]\")`, que casa con el valor vacío), `aria-atomic`, el `aria-label` del diálogo, `scope=\"col\"`. Misma familia que la región aria-live cazada esta noche. | `webhooks.tsx:273-286, 304, 461, 493-500` | ~13 | oráculo débil |\n| 6 | `decodeDelivery` + `whole()` + dedupe. **Recortado por la refutación:** el `<= max` muere con `attempt: 6` (`webhooks.test.tsx:501`), el `>= 0` muere con el ping, y el `size !== items.length` muere en cualquier lista. Sobreviven `typeof number`, `Number.isInteger`, el `<= max` de httpStatus/latencyMs y el `ConditionalExpression→false` de la dedupe. | `webhooks-client.ts:70-77, 119-160` | ~10 | sin cobertura |\n| 7 | Ocho `signal.throwIfAborted()` sin ejercer: sólo `listWebhooks` tiene la prueba (`webhooks-client.test.ts:272`). El más interesante es el **segundo** de `json()` (`:175`), el que corre tras `await response.json()`. | `webhooks-client.ts:175,186,210,228,239,248,258` | ~8 | sin cobertura |\n| 8 | Guardas de identidad: `if (!identifier(id)) throw new Error(\"Identidad incompatible\")`. Grep: **cero apariciones** del literal en las tres suites (verificado). | `webhooks-client.ts:162-165, 259` | ~7 | sin cobertura |\n| 9 | `report()`/`act()` — el racimo de D1. Ninguna acción falla nunca en ninguna prueba. | `webhooks.tsx:175-199` | 9 | sin cobertura |\n| 10 | Ocho `data-label`, el guion «—», «12 ms», la fecha recortada, `HTTP_ERROR`, `500`. **Recortado:** Estado sí se afirma (`:540-542`), Intento sí (`:578`), eventType sí (`:391`). | `webhooks.tsx:506-536` | ~9 | oráculo débil |\n| 11 | Reinicios y limpieza del formulario: nadie envía dos veces con resultados distintos, nadie comprueba que el formulario quede limpio tras crear. | `webhooks.tsx:142-144, 154-156` | ~6 | sin cobertura |\n| 12 | Postcondición de `setWebhookStatus`: `if (endpoint.id !== id \\|\\| endpoint.status !== status)`. Los tres tests devuelven siempre lo que se pidió → el `\\|\\|`→`&&` sobrevive. Única defensa contra que el servidor confirme el webhook equivocado. | `webhooks-client.ts:218` | ~4 | sin cobertura |\n| 13 | Regex del secreto `/^whsec_[A-Za-z0-9_-]{43}$/`: los tres rechazos usan sólo la letra «A». Sobreviven quitar el ancla `^` y encoger la clase (`0-9`, `a-z`, `_-`). Riesgo real: el cliente rechazaría secretos legítimos y el usuario pierde el único momento en que puede copiarlo. | `webhooks-client.ts:199` | ~4 | oráculo débil |\n| 14 | `problemCode` nunca ve un cuerpo no-JSON ni un JSON sin `code`. | `webhooks.tsx:61-70` | 4 | sin cobertura |\n| 15 | Entregas de un segundo endpoint (D2) + el `.filter` de dedupe del ping, que hoy corre siempre sobre lista vacía. | `webhooks.tsx:218-229, 242-248` | 4 | sin cobertura |\n| 16 | El campo «Descripción» **no existe para la suite de la vista** (grep: cero apariciones). Su `onChange` no se ejecuta jamás. | `webhooks.tsx:77, 352-355` | ~3 | sin cobertura |\n| 17 | Menores: `createWebhook` `exact()` con clave de más/menos (`client:194`), `deleteWebhook` `status !== 204` (`client:229`), botón «Actualizar» con endpoint ya borrado (`view:480-483`), `onFocus` del textarea del secreto (`view:314`, función entera sin cobertura), `aria-describedby` sólo afirmado en la rama verdadera (`view:341`), `tabIndex={-1}` del `<main>`, `rows={2}`, `required`. | varias | ~8 | mixto |\n\n**DESCARTADOS (no los toques, gasto sin retorno):**\n- `identifier()` → `.length === 36` es **tautológico** (el regex de uuid ya está anclado a 36): mutante equivalente. Documentarlo en la bitácora o borrar el código muerto.\n- `indexOf`→`lastIndexOf` en `webhooks-client.ts:97-98` y `webhooks.tsx:417`: `webhookEventTypes` no tiene duplicados → **equivalente**. Ninguna tabla de rechazo lo matará.\n- Las tres cadenas opcionales sobre refs (`view:238, 263, 268`): probablemente equivalentes. Sí merece la pena la de `navigator.clipboard` (`:318`).\n- La casilla maestra: el `===`→`!==` **ya muere** por accidente en `webhooks.test.tsx:213-221`. Sólo falta la rama `: []` del ternario y los `toBeChecked`.\n- Premisa falsa a corregir en el brief del artesano: **sí existe** fixture de dos endpoints (`webhooks.test.tsx:158-179`).\n\n---\n\n### FEATURE 28 — calendario externo · ~160 vivos · **~50 oráculos**\n\n| # | Causa | Fichero:líneas | Vivos | Tipo |\n|---|---|---|---|---|\n| 1 | Los 22 guardas de `subscriptionOf` sólo tienen fixture roto para 10. Faltan: `label` vacía, `label` no-cadena, `urlHost`, `urlTail`, `id` no-cadena, `lastAttemptAt` inválido, `snapshotZoneId`, `truncated: \"sí\"`, `updatedAt` inválido, y los tres contadores `skipped*` negativos/fraccionarios. Más los dos anclajes `^`/`$` del regex de `:95-97` (en automatizaciones ese mismo regex dejó vivos exactamente esos dos). | `external-calendar-api.ts:91-122` | ~22 | oráculo débil |\n| 2 | **Las tres guardas de reentrada no tienen 8 de 9 oráculos: tienen 0 de 3.** El doble clic de `external-calendar.test.tsx:343` es sobre un botón con `disabled={locked}` y user-event no despacha click sobre él: `synchronise()` ni se ejecuta. El `toHaveLength(1)` lo garantiza el atributo, no el guarda. Sumar `start()` (`:109-114`): `inFlight.current?.abort()` no aborta nunca nada vivo. Y **ninguna prueba aborta una escritura**: el único `unmount` (`:422-444`) sólo tiene el GET de montaje en vuelo. | `external-calendar.tsx:109-114, 190, 203, 207, 212, 227, 232, 237, 246, 250` | ~18 | sin cobertura |\n| 3 | Los siete reinicios de estado: no existe ninguna prueba de error-y-luego-reintento ni de inválido-y-luego-válido. Una sola causa, ocho supervivientes seguidos. | `external-calendar.tsx:129, 194, 195, 204, 216, 228, 247` | ~8 | sin cobertura |\n| 4 | Envoltorio de `readExternalEvents` y `exact()` de `syncExternalCalendar` sin un solo fixture roto: `typeof body.configured`, `optionalInstant(lastSyncAt)`, pertenencia de `lastStatus`, `!Array.isArray(items)`. La misma enfermedad del racimo 1 en otros dos validadores. | `external-calendar-api.ts:256-265, 238-242` | ~9 | sin cobertura |\n| 5 | El catch de `loadEvents` (D3) y el catch del efecto de montaje: nadie hace fallar el GET inicial, así que el mensaje de conectores deshabilitados nunca se pinta. | `external-calendar.tsx:124-137, 147-158` | ~9+8 | sin cobertura |\n| 6 | Las tres guardas de `refuse()` son conjunciones estado+código y nunca se prueban con media verdad → el `&&`→`\\|\\|` sobrevive en las tres. Seis casos cruzados. | `external-calendar-api.ts:158-189` | 8 | oráculo débil |\n| 7 | **Sincronización fallida, rama entera sin oráculo.** «Sincronización fallida.» no aparece en NINGÚN test. Y el guarda `if (lastStatus === \"OK\") await loadEvents` es **inmatable con el stub actual** (ver nota de infraestructura abajo). | `external-calendar.tsx:220-225` | ~6 | asérción que no puede fallar |\n| 8 | Textos de vista que ninguna prueba afirma (verificado por grep, cero apariciones): «No hay eventos en la ventana guardada», «Todavía no tienes ningún calendario externo», «Eliminando…», «Suscripción eliminada.», `aria-busy`, «Los conectores externos no están disponibles». Más el ternario `loaded ? ... : null` de `:407`. | `external-calendar.tsx:240, 244, 391, 409, 266-272` | ~7 | sin cobertura |\n| 9 | Ramas **falsas** de las condicionales de presentación: `truncated ? ... : null` (`:347`, se afirma presente en `:145`, nunca ausente), `confirming ?` (`:374`, la prueba de Cancelar sólo mira que no se envía DELETE), y en Hoy `pendingSync ?` (`today:117`). | `external-calendar.tsx:347, 374, 380` + `today:117` | ~5 | oráculo débil |\n| 10 | Los dos mensajes de fallo de guardado son **indistinguibles**: el `it.each` de `:244-264` usa la misma aserción `/no sabemos si se guardó/i` para el 503 y para el fallo de red. Borra entero el bloque de `ConnectorsDisabledError` (`:177-180`) y las dos filas siguen verdes. | `external-calendar.tsx:177-185` | ~4 | aserción que no puede fallar |\n| 11 | Cuatro de siete `FEED_MESSAGES` no se renderizan nunca, y `lastStatus === \"FAILED\" && lastError` no se prueba con media verdad. | `external-calendar.tsx:18-34, 352-355` | 6 | sin cobertura |\n| 12 | Tres caminos de fallo de la API: 200 que no es JSON, PUT que responde `configured:false`, DELETE con señal abortada. El del PUT es la única protección contra `setSubscription(null)` tras un guardado con éxito. | `external-calendar-api.ts:199, 218, 222-227` | 7 | sin cobertura |\n| 13 | `failed()`: el ternario de campo a enfocar sólo se ejerce por `url`; falta el caso `label`. Y `instanceof Response && status===401` nunca ve otra `Response` → con `\\|\\|` cualquier error borraría la suscripción de pantalla. | `external-calendar.tsx:171-186` | 5 | sin cobertura |\n| 14 | `summary: \"\"` nunca se pinta: la API lo declara válido (`api.test:306-316`) pero ninguna vista lo monta. `@s19` fija que un VEVENT sin SUMMARY se guarda como \"\". | `external-calendar.tsx:397` + `today:113` | 5 | sin cobertura |\n| 15 | Cabeceras: ninguna prueba de ningún fichero inspecciona `Accept`/`Content-Type`. | `external-calendar-api.ts:190-205` | 5 | oráculo débil |\n| 16 | `eventOf`: `typeof uid`, `endAt` malformado, `typeof allDay`. **Ojo:** el fixture `{...item, allDay: undefined}` pierde la clave al serializar con `Response.json` y cae en `exact()`, nunca llega al `typeof`. | `external-calendar-api.ts:126-132` | ~4 | sin cobertura |\n| 17 | `formatMoment` (**recortado**: la rama `null` sí tiene oráculo en `:125-130`; sobreviven el objeto de opciones y el ternario de zona) y su gemelo **no visto**, `formatClock` (`:76`): todas las pruebas que pintan un evento usan «Europe/Madrid». | `external-calendar.tsx:63-70, 71-83` | ~7 | aserción que no puede fallar |\n| 18 | Hoy: el catch de `clock()` (`:30-32`) y la rama `lastSyncAt: null` no se ejecutan nunca; las dos guardas de aborto (`:61` y `:66`) nunca son ciertas — la de `:66` es **sospechosa de código muerto**. `guardas de forma` + `problem()` sin `.clone()` (`api:154`). | `today-external-calendar.tsx:30-32, 61, 66` | ~6 | sin cobertura |\n\n**DESCARTADOS:**\n- Racimo 9 original: «sin el spread el PUT viajaría sin X-CSRF-TOKEN» es **falso** — `api-client.ts:22-27` hace `headers.set(\"X-CSRF-TOKEN\")` después. Un oráculo escrito sobre esa premisa daría verde con el producto roto. El hueco de cabeceras es real, la consecuencia no.\n- Racimo 5, su `posibleDefecto`: falso, `external-calendar-api.ts:185` sólo lanza `if (Object.keys(fields).length > 0)`; ya está probado en `api.test:174-187`.\n- Racimo 6 de Hoy: `today-external-calendar.test.tsx:194-210` **sí** mata el `startsWith`. Sólo sobrevive el literal de *unreachable*. Vale 2-3 mutantes, no 8.\n- `external-calendar.test.tsx:119` (`not.toContain(\"https://\")`) y `today:96` (`queryAllByRole(\"button\")` a 0) son **estructuralmente infalibles**: el DTO no transporta la URL completa y la sección no renderiza botones en ninguna rama. No protegen nada — anótalo en la bitácora, no las \"arregles\" con más de lo mismo.\n\n> **BLOQUEO DE INFRAESTRUCTURA para el carril de la vista de 28.** El stub de `external-calendar.test.tsx:81` hace `list.length > 1 ? list.shift()! : list[0]`: con **una** respuesta encolada para `/events` la devuelve para siempre, así que «¿volvió a pedir la lista?» es inobservable y los mutantes del guarda `:225` son inmatables. El artesano tiene que **encolar dos respuestas distintas** de `/events` (la segunda distingue el refetch) o contar llamadas en `calls`. Sin esto, tres oráculos del racimo 7 nacen muertos.\n\n---\n\n### FEATURE 29 — conectores · ~130 vivos · **~35 oráculos** · LA MÁS CERCA DEL 80 %\n\n| # | Causa | Fichero:líneas | Vivos | Tipo |\n|---|---|---|---|---|\n| 1 | `live()` + su gemelo del catálogo (D4). **La predicción no vio el del catálogo**: `let live = true` / `if (live) setRows` / `if (!live \\|\\| aborted) return` / limpieza. La única prueba (`connectors-catalog.test.tsx:241-262`) pasa igual con el guarda quitado. | `gitlab-connector.tsx:102-271` (13 sitios) + `connectors-catalog.tsx:96-119` | ~18 | oráculo débil / equivalentes |\n| 2 | Nombres accesibles y atributos, **más de los que contaba R13**: `aria-label=\"Conexión\"` (`gl:320`), el par `id=\"gitlab-project\"`/`htmlFor` (`gl:333-335`) — las pruebas usan `getAllByRole(\"option\")`, el selector no tiene nombre accesible comprobado —, `name=\"projectPath\"`, `name=\"token\"`, `autoComplete=\"off\"` de la ruta, los cinco `tabIndex={-1}` (`gl:303,304,461`, `cat:122,123`) que el mutador unario convierte en `+1`, `aria-label=\"Conectores\"` del catálogo, y `aria-describedby` de los dos campos. | `gitlab-connector.tsx:303, 320, 333-335, 383-414, 461` + `connectors-catalog.tsx:122` | ~13 | aserción que no puede fallar |\n| 3 | `ERROR_TEXT`: nueve códigos, sólo `CONNECTION_INVALID` llega a pintarse. Los ocho restantes (`DELIVERY_EXHAUSTED`, seis `FEED_*`, `SECRET_UNREADABLE`) son las traducciones de webhooks y del ICS y nadie las ha visto renderizadas. Más el `??` de reserva. | `connectors-catalog.tsx:50-68` | 9 | sin cobertura |\n| 4 | El estado «conectado pero roto» no se decodifica nunca: todos los fixtures llevan `lastError: null`. Con él, la propiedad de seguridad de @s5 (que `lastError` no lleve texto del proveedor) está probada en el cliente del catálogo pero **no** en el de GitLab, que es el que decodifica la respuesta donde podría colarse el token. Ata con **D5**. | `gitlab-connector-client.ts:109-118, 124, 142` | 8 | sin cobertura |\n| 5 | Rama `absent` de `decodeConnection`: los siete `!== null` encadenados con `\\|\\|`, todos falsos siempre → cualquier `\\|\\|`→`&&` da lo mismo. Más `nonEmpty` con cadena vacía. Es la única guarda contra que un `not_connected` arrastre el `projectPath` o el `tokenHint` del propietario anterior (@s32/@s37). | `gitlab-connector-client.ts:105-107, 127-144` | 7 | sin cobertura |\n| 6 | `MESSAGES` — **la causa está mal enunciada en la predicción**: cuatro textos SÍ se afirman (`gitlab-connector.test.tsx:173, 493, 525, 548`). Sobreviven las cinco restantes: `CONNECTION_NOT_FOUND`, `PROJECT_COMPLETED`, `RESOURCE_NOT_FOUND` (sin ninguna prueba), `STORAGE_UNAVAILABLE` (disparada en `:439` sin afirmar texto) y `VALIDATION_ERROR` (`:184` ídem), más el `?? 60` de `:54`, el texto de reserva de `:55` y `STATUS_TEXT.not_connected`. | `gitlab-connector.tsx:29-56` | 7 | oráculo débil |\n| 7 | Recibo `running`: las dos fixtures son `completed`. Vivos los tres literales `\"running\"`, `\"failed\"`, la guarda de `errorCode` y las dos fronteras de `finishedAt`/`startedAt` (nadie prueba el empate). El componente `Receipt` no pinta el status: un recibo en curso es idéntico a una importación terminada sin resultados. | `gitlab-connector-client.ts:151-174` | 6 | sin cobertura |\n| 8 | Las tres banderas de foco: nadie afirma que el foco se mueva **una** sola vez. Dejarlas en `true` reenfoca en cada render y las tres pruebas @s38 siguen verdes. Defecto de a11y clásico: no podrías ni recorrer el selector de proyectos. | `gitlab-connector.tsx:139-154` | 6 | oráculo débil |\n| 9 | Exclusión mutua panel/formulario y estado de carga: nadie afirma que el formulario está AUSENTE con conexión viva, ni que el panel está ausente mientras se reemplaza el token, ni que durante la carga inicial no se pinta nada. Ata con el hueco de `:319`. | `gitlab-connector.tsx:66, 298-300, 319` | 7 | oráculo débil |\n| 10 | **Nadie afirma qué se envía.** Cero `JSON.parse` y cero `selectOptions` en `gitlab-connector.test.tsx` (verificado por grep). El `?? \"\"` del campo, el objeto `{token, projectPath}`, el `useState(\"\")` de `selected` y la flecha del `onChange` del selector (`:338`) sobreviven todos: el servidor doble responde `connected` pase lo que pase. Falta también importar al **segundo** proyecto. | `gitlab-connector.tsx:74, 174-204, 334-352` | ~8 | oráculo débil |\n| 11 | Región viva ociosa: se afirma que contiene «Guardando…» (`:117`) y «Importando» (`:366`) pero **nunca que está vacía**. El `\"\"` de `:443` mutado hace que la pantalla anuncie «Importando issues…» de forma permanente y la suite pasa. Más `aria-atomic` sin oráculo. Familia literal del defecto de esta noche. | `gitlab-connector.tsx:438-444` | ~3 | sin cobertura |\n| 12 | El aviso de truncado sin oráculo de **ausencia** (verificado: `:405` afirma la presencia, nada la ausencia). **Una línea.** | `gitlab-connector.tsx:473-479` | 1 | oráculo de recorte |\n| 13 | Catch de `loadConnection` con una sola prueba de fallo (`@s29:255`, `CONNECTORS_DISABLED`) → el `&&`→`\\|\\|` sobrevive con el defecto latente descrito arriba. | `gitlab-connector.tsx:113-120` | ~3 | sin cobertura |\n| 14 | `fieldErrors` — **recortado**: `!Array.isArray` y `typeof entry === \"object\"` ya mueren vía @s34 (`:179-198`). Sobreviven los tres `&&`→`\\|\\|` y el `if(true)`, **más los dos ternarios de `aria-invalid`** (`:390` y `:409`): nadie prueba nunca que el campo del **token** se pueda marcar. | `gitlab-connector-client.ts:70-85` + `gitlab-connector.tsx:390, 409` | ~6 | oráculo débil |\n| 15 | Menores pero baratos: el botón «Cancelar» de la confirmación **no lo pulsa ninguna prueba** (grep) → su `onClick` sobrevive como `() => undefined`; la miga de pan `RouteLink href=\"/conectores\"` (`:307-309`) sin ningún `getByRole(\"link\")` en todo el fichero; `identifier()` `&&`→`\\|\\|` (`client:97-103`); el `{\" \"}` de `catalog:145`; `connection?.projectPath` de `:294`; `heading.current?.focus()` del catálogo. | varios | ~7 | mixto |\n| 16 | `GitlabConnectorError`: nadie afirma `.message` ni `.name` (sólo `toBeInstanceOf`). Más el ternario de `importId` sin rama de rechazo. | `gitlab-connector-client.ts:57-67` | 6 | oráculo débil |\n\n**DESCARTADOS:**\n- `CatalogError`: `name` **sí** tiene oráculo (`connectors-catalog-client.test.ts:172-175`) y `code` en las dos ramas. Sólo sobrevive el `super()` — basta con **añadir `.message` a esa misma aserción**, no hace falta prueba propia.\n- Racimo 3 del catálogo inflado: los dos textos SÍ mueren por accidente (Stryker vacía el literal, `setFailure(\"\")` es falsy y el `<p role=\"alert\">` de `catalog:126` desaparece entero, así que los dos `findByRole(\"alert\")` fallan). Sobreviven **~6**, no 8: los cinco de la extracción del código (`:106`) y el `ObjectLiteral` de `CATALOG_ERROR_TEXT`.\n- Racimo 14, «lista vacía»: el camino ya existe (`connectors-catalog-routing.test.tsx:27-38`); falta la aserción, no la cobertura. Y «el botón mandaría un POST con projectId vacío» es **falso**: `startImport:207` corta con `!selected`.\n\n---\n\n## 3. CARRILES PARALELOS — ocho, sin un solo choque de fichero\n\nRegla: **un fichero, un carril.** Cada carril declara los ficheros que posee; nadie más los abre.\n\n| Carril | Feature | Ficheros que POSEE | Oráculos | Contenido |\n|---|---|---|---|---|\n| **W-A** | 25 | `frontend/src/webhooks-client.test.ts` | ~22 | Racimos 2, 6, 7, 8, 12, 13 + `exact()` de create + `204` de delete. Todo cliente, cero React. |\n| **W-B** | 25 | `frontend/src/webhooks.test.tsx` **+** `frontend/src/webhooks.tsx` | ~18 | **D1 y D2 (rojos → arreglo de producción)**, racimo 4 (abortos tardíos), 14 (`problemCode`), 15, 11 (reinicios). Es el único carril que toca producción de webhooks. |\n| **W-C** | 25 | `frontend/src/webhooks.mutation.test.tsx` (**NUEVO**, convención ya usada en el repo) | ~15 | Racimos 1 (selección de tipos — **el más grande**), 3 (doce etiquetas), 5 (a11y por valor), 10 (`data-label` y celdas), 16 (Descripción), 17. Sólo lectura de `webhooks.tsx`. |\n| **C-A** | 28 | `frontend/src/external-calendar-api.test.ts` | ~22 | Racimos 1, 4, 6, 12, 15, 16 + `problem()` sin `.clone()`. |\n| **C-B** | 28 | `frontend/src/external-calendar.test.tsx` **+** `frontend/src/external-calendar.tsx` | ~18 | **D3 (rojo → arreglo)**, racimos 2, 3, 5, 7, 10, 13, 17. **Empieza arreglando el stub de `:81`** (ver bloqueo). Único carril que toca producción de calendario. |\n| **C-C** | 28 | `frontend/src/today-external-calendar.test.tsx` + `frontend/src/external-calendar.mutation.test.tsx` (**NUEVO**) | ~12 | Racimo 18 (Hoy: `clock()`, `lastSyncAt` null, guardas de aborto, `pendingSync` ausente), 8 (textos de vista), 9 (ramas falsas), 11 (`FEED_MESSAGES`), 14 (`summary: \"\"` en las dos vistas). Sólo lectura de los `.tsx`. |\n| **G-A** | 29 | `frontend/src/gitlab-connector-client.test.ts` | ~12 | Racimos 4, 5, 7, 14 (parte cliente), 16, `identifier()`. |\n| **G-B** | 29 | `frontend/src/gitlab-connector.test.tsx` **+** `frontend/src/gitlab-connector.tsx` | ~15 | **D4 y D5 (rojos → arreglo)**, racimos 6, 8, 9+`:319`, 10, 11, 12, 13, 15, `aria-invalid` del token. |\n| **G-C** | 29 | `frontend/src/connectors-catalog.test.tsx` + `connectors-catalog-client.test.ts` + `connectors-catalog.tsx` | ~10 | Racimo 3 (los nueve códigos), el `live` del catálogo, extracción del código en el catch, `aria-label=\"Conectores\"`, `tabIndex`, `{\" \"}`, `.message` de `CatalogError`. |\n\n**Colisiones que hay que vigilar (sólo tres, y son de producción):**\n- `webhooks.tsx` → **sólo W-B**. W-C lee pero no escribe.\n- `external-calendar.tsx` → **sólo C-B**. C-C lee pero no escribe (si C-C necesita el fallback de `summary: \"\"` en la vista grande, que lo pida a C-B; el de `today-external-calendar.tsx` es suyo).\n- `gitlab-connector.tsx` → **sólo G-B**. `connectors-catalog.tsx` → **sólo G-C**.\n\nLos tres carriles «-B» son los que llevan defectos rojos y arreglo de producción: son los **más lentos** y los que deben arrancar primero. Los «-A» (clientes puros, sin React, sin fixtures compartidos) son los más rápidos y los más independientes: pueden empezar sin esperar a nadie.\n\n**Restricción de máquina (memoria de sesión):** ningún carril lanza la suite completa ni una campaña. Cada uno corre `pnpm --dir frontend exec vitest run src/<su-fichero>` y nada más. La campaña de mutación por feature va al final, en serie, cuando se libere la máquina.\n\n---\n\n## 4. TAMAÑO HONESTO Y ORDEN DE ATAQUE\n\n- **29 conectores — ~35 oráculos, 3 carriles, la más cerca.** Tiene la mayor superficie de producción (1011 líneas en 4 ficheros) y el menor número de vivos que explico leyendo. Con G-A + G-B + G-C bien hechos, más D4/D5 arreglados, **es plausible que cruce el 80 % en una sola campaña**. Es la victoria barata. **Atácala primero si sólo puedes con una.**\n- **28 calendario externo — ~50 oráculos, 3 carriles.** Media distancia, pero con un bloqueo de infraestructura (el stub de `:81`) que hay que resolver antes de escribir tres de los oráculos. El grueso es mecánico: dos tablas grandes de `it.each` (racimos 1 y 4) que un solo artesano escribe seguidas.\n- **25 webhooks — ~55 oráculos, 3 carriles, la más lejos.** No es sólo la que más oráculos pide: es la que tiene la interacción **central de @s37** (marcar tipos y enviarlos) con **cero oráculos**, los dos defectos de producto más visibles, y la mayor proporción de \"sin cobertura\" pura. **No la programes para cerrar esta noche a la vez que las otras dos**; dale los tres carriles desde el minuto uno y asume que su campaña se corre la última.\n\n**Antes de cada campaña, el juez tiene que ver por escrito la lista de equivalentes declarados**, o volverán como supervivientes sin explicación: `identifier().length === 36` y `indexOf`/`lastIndexOf` en 25; las tres cadenas opcionales sobre refs en 25; los ~18 mutantes de `live()`/`live` en 29 si se decide no tocar producción; `today-external-calendar.tsx:66` en 28 (sospechoso de código muerto); y las dos aserciones estructuralmente infalibles de 28 (`external-calendar.test.tsx:119` y `today:96`), que **no se arreglan escribiendo más de lo mismo** — o se rediseñan o se borran."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Prediccion"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Refutacion"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Plan"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "predice:25-webhooks",
      "phaseIndex": 1,
      "phaseTitle": "Prediccion",
      "agentId": "adc71a61b1d9c0a62",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030254574,
      "queuedAt": 1789030254532,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "25 · webhooks (frontend/src/webhooks-client.ts + frontend/s…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789030640450,
      "tokens": 98551,
      "toolCalls": 12,
      "durationMs": 385869,
      "resultPreview": "{\"feature\":\"25 · webhooks (frontend/src/webhooks-client.ts + frontend/src/webhooks.tsx)\",\"puntuacionEstimada\":\"62-72 %\",\"racimos\":[{\"causa\":\"Los guardas de decodeEndpoint sólo se ejercen en su rama verdadera (el DTO bueno) o por dos casos malos muy concretos (status \\\"paused\\\", propiedad extra); ninguna prueba entrega un id no-uuid, una url http://, una descripción de 81 caracteres, eventTypes vac…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "predice:28-calendario-externo",
      "phaseIndex": 1,
      "phaseTitle": "Prediccion",
      "agentId": "a41710a8df302cdbc",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030254577,
      "queuedAt": 1789030254532,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "28 — calendario externo (frontend: external-calendar-api.ts…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789030750541,
      "tokens": 149576,
      "toolCalls": 28,
      "durationMs": 494113,
      "resultPreview": "{\"feature\":\"28 — calendario externo (frontend: external-calendar-api.ts, external-calendar.tsx, today-external-calendar.tsx)\",\"puntuacionEstimada\":\"65-75 %\",\"racimos\":[{\"causa\":\"La cadena de 22 guardas de subscriptionOf solo tiene fixture roto para 10 de ellas; las otras 12 se evaluan siempre en falso, asi que el mutante que las apaga sobrevive\",\"fichero\":\"frontend/src/external-calendar-api.ts\",\"l…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "predice:29-conectores",
      "phaseIndex": 1,
      "phaseTitle": "Prediccion",
      "agentId": "a70ba24f7a62a3816",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030254580,
      "queuedAt": 1789030254532,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "29 — conectores adicionales (additional_connectors)",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789030892052,
      "tokens": 147303,
      "toolCalls": 16,
      "durationMs": 635624,
      "resultPreview": "{\"feature\":\"29 — conectores adicionales (additional_connectors)\",\"puntuacionEstimada\":\"72-82 %\",\"racimos\":[{\"causa\":\"La guarda live(controller) = mounted.current && !signal.aborted se llama tres veces por operacion en cuatro operaciones (try, catch, finally), y ningun oraculo puede distinguir que se haya quitado: con React 19 un setState sobre un arbol desmontado es un no-op silencioso, sin aviso …"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "refuta:25-webhooks:0",
      "phaseIndex": 2,
      "phaseTitle": "Refutacion",
      "agentId": "a91f157abb457b6e8",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030640473,
      "queuedAt": 1789030640455,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "La predicción NO queda refutada: los doce racimos existen y…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789030908181,
      "tokens": 99275,
      "toolCalls": 11,
      "durationMs": 267707,
      "resultPreview": "{\"porque\":\"La predicción NO queda refutada: los doce racimos existen y los he confirmado uno a uno leyendo produccion y pruebas. Lo confirmado en firme: (1) decodeEndpoint solo se falsifica por status \\\"paused\\\" (client:97), propiedad extra (client:83) y disabled-sin-razon (client:104); no hay ninguna prueba con id no-uuid, url http://, descripcion >80, eventTypes vacio, tipo desconocido ni instan…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "refuta:25-webhooks:1",
      "phaseIndex": 2,
      "phaseTitle": "Refutacion",
      "agentId": "ac0b31b4866a2fee3",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030640474,
      "queuedAt": 1789030640456,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "La predicción acierta en el diagnóstico general (los doce r…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789030983451,
      "tokens": 105650,
      "toolCalls": 12,
      "durationMs": 341195,
      "resultPreview": "{\"porque\":\"La predicción acierta en el diagnóstico general (los doce racimos existen y casi todos son reales), pero la refuto por tres motivos verificables: (1) contiene sub-afirmaciones falsas sobre oráculos que SÍ existen, (2) omite al menos siete huecos de tamaño comparable a los que sí vio —incluido el más grande de la feature: la lógica de selección de casillas del formulario, que no tiene NI…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "refuta:28-calendario-externo:0",
      "phaseIndex": 2,
      "phaseTitle": "Refutacion",
      "agentId": "a289d1330a40f762f",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030750548,
      "queuedAt": 1789030750545,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "La predicción NO queda refutada: los 13 racimos existen en …",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789031084028,
      "tokens": 116593,
      "toolCalls": 17,
      "durationMs": 333479,
      "resultPreview": "{\"porque\":\"La predicción NO queda refutada: los 13 racimos existen en el código y en ningún caso encontré la prueba que los cubriría. Comprobaciones clave: (1) el it.each de external-calendar-api.test.ts:81-98 tiene exactamente 10 filas y las 12 guardas que lista la predicción (label vacía/no-cadena, urlHost/urlTail/id no-cadena, lastAttemptAt, snapshotZoneId, truncated, updatedAt, los tres skippe…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "refuta:28-calendario-externo:1",
      "phaseIndex": 2,
      "phaseTitle": "Refutacion",
      "agentId": "aab263f9aa7ed4652",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030750549,
      "queuedAt": 1789030750545,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "La predicción es mayoritariamente correcta en lo que afirma…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789031158238,
      "tokens": 119511,
      "toolCalls": 18,
      "durationMs": 407689,
      "resultPreview": "{\"porque\":\"La predicción es mayoritariamente correcta en lo que afirma —comprobé guarda por guarda los 13 racimos y 11 se sostienen literalmente contra el código— pero es CORTA y su cifra (65-75 %) no se sostiene. Encontré ~12 racimos reales que no vio, varios de ellos del tipo más caro (una causa que explica media docena de mutantes) y uno que es un defecto de oráculo del mismo patrón que los tre…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "refuta:29-conectores:0",
      "phaseIndex": 2,
      "phaseTitle": "Refutacion",
      "agentId": "a03770cea7997cd55",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030892061,
      "queuedAt": 1789030892056,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "He comprobado los quince racimos contra los cuatro ficheros…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789031311483,
      "tokens": 134485,
      "toolCalls": 18,
      "durationMs": 419419,
      "resultPreview": "{\"porque\":\"He comprobado los quince racimos contra los cuatro ficheros que la campana va a mutar (frontend/stryker.additional-connectors.config.json: connectors-catalog-client.ts, connectors-catalog.tsx, gitlab-connector-client.ts, gitlab-connector.tsx) y contra sus cuatro ficheros de prueba. TRECE de los quince se sostienen tal cual; dos llevan una afirmacion falsa dentro pero el hueco que descri…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "refuta:29-conectores:1",
      "phaseIndex": 2,
      "phaseTitle": "Refutacion",
      "agentId": "a142f61c9c50ef633",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789030892063,
      "queuedAt": 1789030892056,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Los quince racimos son REALES: los he recorrido uno a uno c…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789031497034,
      "tokens": 140726,
      "toolCalls": 19,
      "durationMs": 604967,
      "resultPreview": "{\"racimosFalsos\":[\"PARCIAL, racimo 3 (connectors-catalog.tsx:62-64 + 103-113, 8 mutantes): los DOS textos que el racimo cuenta SI tienen oraculo, por accidente. El aviso se pinta con `{failure ? <p role=\\\"alert\\\">{failure}</p> : null}` (connectors-catalog.tsx:126); Stryker convierte un literal de cadena en \\\"\\\", `setFailure(\\\"\\\")` es falsy y el <p role=\\\"alert\\\"> desaparece entero, asi que los dos…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "plan",
      "phaseIndex": 3,
      "phaseTitle": "Plan",
      "agentId": "ac4fe764cad8bbf6c",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789031497040,
      "queuedAt": 1789031497037,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /c/Users/vhurt/OneDrive/Escritorio/Proyectos/Organizacio…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites ningun fichero. NO ejecutes campanas de mutacion\nni la suite completa: hay dos campanas corriendo y 55 contenedores vivos en esa\nmaquina. Puedes leer todo lo que quieras y ejecutar como mucho UN fichero de\npruebas con `pnpm --dir frontend exec vitest run <fichero>`.\n\nCONTEXTO QUE N…",
      "promptFramed": false,
      "lastProgressAt": 1789031828856,
      "tokens": 111393,
      "toolCalls": 8,
      "durationMs": 331815,
      "resultPreview": "# PLAN DE ATAQUE — features 25, 28, 29 (frontend)\n\nLeído y verificado sobre el código real (no el sandbox de Stryker). Todo lo que marco **(verificado)** lo he confirmado abriendo el fichero; lo que marco *(heredado)* viene del análisis + refutación y no lo he vuelto a comprobar línea a línea.\n\n---\n\n## 0. VEREDICTO DE ORDEN — léelo antes de repartir\n\n**Ninguna de las tres está en el 80 %. Pero no …"
    }
  ],
  "totalTokens": 1223063,
  "totalToolCalls": 159
}