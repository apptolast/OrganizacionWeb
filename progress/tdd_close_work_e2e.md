# E2E funcional16

Primer caso preparado, todavía **sin ejecución**. Propiedad exclusiva nuevo `e2e/close-work-session.spec.mjs` y bitácora; frontend/backend no se modifican. Se reutilizan authenticated-test, create/sql y saveTask, sin nuevos helpers, servicios ni respuestas de éxito simuladas. Los selectores fueron confirmados por B antes de redactar.

Caso1 running @s1/@s26/@s32: inicio25min desde UI; enlace de cierre establece la URL propia antes de cualquier POST; notas con espacios/salto de línea; POST201 y CLOSE7; fin previsto e inicio originales; trabajo exacto contrastado en SQL con intervalo y tiempo entre inicio/cierre; estado, recibo/evento únicos y tarea pending. Recarga consulta F y recupera el recibo completo por URL, sin otro POST. La comparación de microsegundos usa numeric/BIGINT de PostgreSQL, no Date/Number del navegador.

Node --check y Prettier verdes en preparación (f416b9 y cierre de formato posterior), sin atribuir GREEN E2E. No se arrancó Docker ni runner18080; root espera fuentes congeladas de A/B para autorizar la imagen real. No copiar snapshots a COMMON ni tocar datos fuera del lifecycle aislado del runner.

Pendientes después del primer resultado: un caso paused que conserva el neto y distingue una nueva activa; después un caso de ACK perdido únicamente tras POST real201 y recarga sin key. No se añaden antes de cerrar el primer caso. Smoke/publicación corresponden a A; matrizUX a B. Este archivo no acredita motores, responsive, pérdidas de respuesta ni gate global aún.

Primer resultado real: GREEN1/1 d73a29/26b9da, caso6.3s y Playwright10.0s, sin cambio al test para pasar. Log `progress/close_e2e_running.log`; runner aislado organizationweb-e2e-18452 retiró contenedores/volumen/red al terminar EXIT0. La producción integrada estaba congelada por root. Los oráculos se alcanzaron; sin atribuir paused/ACK/UX todavía. Root priorizó después corregir comprobaciones históricas de Today en el arnés, sin cancelar esta ejecución ya terminada.

Segundo caso, añadido después del primero verde: paused @s2/@s34, inicialmente GREEN c8f915 EXIT0, 1/1 (caso 3,1 s; Playwright 4,8 s), log `close_e2e_paused.log`. El cierre conserva exactamente el acumulado de PAUSE y un único intervalo; después se inicia otra sesión real en otra pestaña del mismo contexto. La consulta de sesión abierta muestra esa sesión distinta junto al cierre histórico original, cuyo recibo F permanece idéntico. El runner retiró su stack44740. No hubo cambio de producción para obtener este resultado.

Tercer caso, añadido después de paused verde: ACK perdido @s28/@s33, inicialmente GREEN 1/1 (Playwright 5,3 s), log `close_e2e_ack.log`, lectura final8cc115. `route.fetch()` obtiene el POST201 real antes de abortar su entrega. La UI queda incierta; la recarga de la URL estable recupera F con notas, sin POST adicional ni consulta por una key retenida. SQL conserva un cierre, un intervalo y un evento. El log acredita retirada completa del stack54388. La sesión de proceso83487 dejó de estar disponible al recuperar contexto; no se atribuye un EXIT no recuperado. No se simula una respuesta de éxito ni se repite el caso por esa pérdida de metadatos.

El runner18080 queda cedido a B para UX44px. Se prepara individualmente el recorrido @s39 del GET activo del padre pendiente durante navegación y cierre. Ese recorrido acredita la composición alcanzable del padre; no sustituye los oráculos unitarios de todas las generaciones S/A. No hay cambios Java/TS ni ejecución global propia.

Cuarto caso @s39: la actualización manual del padre obtiene un GET activo real y retiene su entrega. El enlace sigue accesible, navega al lector y permite cerrar por POST201. Después se libera la respuesta anterior: permanecen URL, cierre, notas y ausencia actual, sin restaurar Pausar; F coincide con el recibo. Primer intento6c596a EXIT1 fue error del oráculo: la respuesta heredada14 es `{session}`, no SessionStart directo. Se corrigió esa única envoltura conforme al parser existente; log preservado `close_e2e_parent_lookup_fixture_red.log`. Segundo intento inicialmente GREEN de producto, 3d103a EXIT0, 1/1 (caso2,0 s; total4,1 s), log `close_e2e_parent_lookup.log`. Stack18532 retirado y runner cedido a B. No se presenta el fallo de fixture como RED productivo ni se afirma que una respuesta abortada por navegación llegue al componente desmontado.

Preparación del cuarto caso: el primer comando Prettier desde la raíz no encontró el ejecutable (87ea0f); se corrigió su ubicación con `pnpm --dir frontend exec prettier`, c0a618, y Node check63a50c. No se añadió tooling. Cuatro casos funcionales ejecutados individualmente, sin repetir suite completa ni acreditar aún otros motores/UX. El paquete queda congelado para revisión e integración por root.
