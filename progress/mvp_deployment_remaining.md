# Despliegue MVP: pendientes verificables

Revisión local de sólo lectura, 7 de septiembre de 2026. Sin conexión al servidor, ejecución de gates ni cambios de infraestructura. Fuentes: `docs/deployment-readiness.md`, `docs/mvp-delivery-plan.md`, `progress/current.md`, Compose, `deploy/`, workflows y el repositorio local `DockerSwarmInfrastrcture`.

- [x] Existe empaquetado local web/API/PostgreSQL con salud y volumen persistente; el overlay `deploy/compose.publisher.yml` añade RabbitMQ persistente y activa el publicador. El Compose base lo desactiva y expone web únicamente en loopback. Esto todavía no constituye una definición Swarm del nuevo servicio.
- [x] Existen gates reproducibles de aplicación: init, build, E2E y smoke del publicador en CI. `current.md` acredita CI34076019493 verde con 108 E2E y smoke; CI posterior detectó el fixture ya corregido. CI34077907038 seguía en curso al leer el documento: comprobar su resultado antes de publicar ese corte. El smoke15 local 0886c3 acreditó recuperación tras ACK perdido, reinicio y publicación real; no acredita el servidor.
- [ ] Completar el alcance MVP pendiente: cierre16, aviso17 e historial18, con sus gates. La propuesta16 aprobada no equivale a implementación; `current.md` impide habilitar uso habitual antes de cerrar16.
- [ ] Resolver acceso SSH y hostname/origin definitivos, ya preguntados al usuario. El último intento documentado rechazó la clave pública; no existe nueva medición remota. No repetir las preguntas ni inferir credenciales o dominio.
- [ ] Verificar capacidad y estado operativo actuales. Los 45 MiB documentados son margen del presupuesto agregado de límites, no RAM libre. Los certificados y servicios descritos por infraestructura son una instantánea de julio, no prueba de disponibilidad actual para esta aplicación.
- [ ] Incorporar OrganizacionWeb al catálogo y despliegue Swarm: imágenes con identidad inmutable, recursos, almacenamiento PostgreSQL/RabbitMQ, secrets, origen público y configuración de autenticación. La CI de aplicación construye y prueba; no publica imágenes ni despliega. Reutilizar el mecanismo de infraestructura, sin trasladar `build:` de Compose como si ya fuera una imagen distribuible.
- [ ] Configurar ruta HTTPS/DNS mediante infraestructura revisada y comprobar proxy/origin/login. Resolver primero la adopción del estado DNS que documenta infraestructura si afecta al cambio. No ejecutar Terraform a ciegas ni presentar los gates históricos como fallos vivos confirmados.
- [ ] Incorporar los datos nuevos a respaldo y restauración verificables, con destino externo y procedimiento de recuperación. Revisar los gates de secrets, redes y marcadores del stack existente; no regenerarlos ni eludirlos por añadir la aplicación.
- [ ] Sobre el corte final, usar los comandos existentes de aplicación: `node .harness/harness.mjs init`, `pnpm build`, `xvfb-run -a pnpm test:e2e` en Linux y `pnpm test:publisher`; conservar las puertas de mutación por feature ya exigidas. No repetir ahora campañas aprobadas por esta auditoría documental.
- [ ] Para el cambio de infraestructura: `./scripts/validate-iac.sh`, `./scripts/lint.sh` y `scripts/validate-workloads.sh`; después el dry-run documentado `./scripts/deploy-ansible.sh --playbook workloads --check --ask-become-pass` sobre el catálogo ya preparado. Revisar el resultado antes de la aplicación manual mediante el mismo wrapper y `--confirm-production`; edge/DNS requieren su propio cambio revisado, no una orden de despliegue inventada aquí.
- [ ] Tras desplegar, acreditar salud, HTTPS/autenticación, recorrido persistente y recuperación tras reinicio en el servidor, más restauración y reversión de imagen/configuración compatibles con las migraciones. Los éxitos locales/CI no sustituyen esta comprobación.

Conclusión: hay empaquetado y validación local/CI reutilizables. Faltan la integración de infraestructura y su comprobación remota, además del alcance funcional restante. El acceso, dominio y estado real del servidor impiden dar hoy una fecha de despliegue fiable.
