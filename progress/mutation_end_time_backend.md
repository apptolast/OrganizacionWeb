# Mutación backend17 — resultado original

**Gate numérico superado con límites explícitos.** PIT terminó con EXIT0 recuperado por root (`b598f3`). XML original preservado en `progress/end_time_pit_final/mutations.xml`; lectura independiente `7e139b` confirma620 registros: **616 KILLED,4 NO_COVERAGE,0 SURVIVED,0 TIMED_OUT y0 errores**. Los cuatro NC tienen numberOfTestsRun=0 y ningún killingTest. No se reclasifica ninguno.

Puntuación estricta: **616/620 ×100 =99,35483870967742%**, superior al umbral80 de build.gradle.kts. El denominador incluye los4 NC. El100% test strength de PIT no sustituye esta puntuación. Duración Gradle38m55s; PIT informa38m46s, con35m59s de análisis de mutaciones y2m46s de cobertura/dependencias. No se inició otra campaña ni se modificó código/tests/config.

XML SHA256: `5006D78E0A06DED7B153EC1E6480E5CD4703704ED5ABBB978B8A8275487FC681`.

Freeze: `end_time_pit_before_hashes.json` y `end_time_pit_after_hashes.json` contienen348 entradas cada uno, sin diferencias de ruta/SHA (`a37f7e`); ambos manifiestos SHA256 `6734BF7640A5753A4AFA63DFC495BA55FF94A534C1D4ED5E30C9A4381F60B210`. La comparación es del snapshot original de campaña, no de cambios posteriores ajenos. Raw XML y log permanecen intactos.

## Inventario de los cuatro NO_COVERAGE

| Identidad original | Ruta y diagnóstico | Oráculo viable / límite |
| --- | --- | --- |
| `com.apptolast.organization.domain.OutboxMessage.validationCode`, línea196, `EmptyObjectReturnValsMutator`, índice870/bloque138 | Retorno INVALID_EVENT cuando previousEndAt o effectiveEndAt no son String. El esquema exterior sólo exige las claves; este rechazo es alcanzable con valor numérico/null. Los tests17 existentes prueban fracción temporal, fórmula, extremos y otros tipos, pero no entran en esta devolución. | Un evento EXTEND completo con previousEndAt numérico, manteniendo los otros diez campos válidos, debe quedar blocked/INVALID_EVENT y no publicarse. Usar el helper de payload JSON coherente y assertStartedBlocked de PublishOutboxTest. Es un hueco de oráculo real, no equivalencia. Un único campo no string alcanza este retorno; no hace falta matriz por cada tipo. |
| `com.apptolast.organization.domain.OutboxMessage.validationCode`, línea207, `EmptyObjectReturnValsMutator`, índice961/bloque165 | Catch DateTimeException en parseo/fórmula. workSessionTimestamp sólo comprueba forma con regex; una fecha imposible con cuatro dígitos y forma UTC pasa esa guarda y falla al parsearse. No es una rama inalcanzable. | Evento válido salvo effectiveEndAt=`2026-02-30T10:20:00.123456Z`: forma aceptada, calendario imposible. Debe quedar blocked/INVALID_EVENT sin envío. Alternativamente previousEndAt imposible llega a la misma rama. No confundirlo con desbordamiento del rango de Instant: los años acotados y minutos<=1440 hacen innecesaria esa justificación especulativa. |
| `com.apptolast.organization.adapter.persistence.PostgresWorkSessionStore.lambda$commit$0`, línea253, `NullReturnValsMutator`, índice37/bloque3 | Sustituye por null el resultado normal de readOnly.execute en la recuperación posterior a TransitionInsertCollision de PAUSE/RESUME/CLOSE. Las pruebas existentes de esta rama terminan por intención ajena o ausencia; no cubren el retorno exitoso. Es la ruta heredada de commit, **no** extend. | La misma intención concurrente natural se serializa por FOR UPDATE: el segundo observa replay antes de INSERT, por lo que no genera naturalmente este fallback. Se puede instrumentar snapshot/primera lookup como `end_s20_sameIntentRecoveryUsesTheDurableWinnerAfterRollback`, pero para una transición heredada válida: ganador durable real, UNIQUE real, rollback y txid nuevo; assert recibo/replayed y efectos intactos. No afirmar una carrera natural ni crear dos sesiones abiertas. |
| `com.apptolast.organization.adapter.persistence.PostgresWorkSessionStore.lambda$commit$2`, línea259, `NullReturnValsMutator`, índice32/bloque5 | Devuelve null en vez de WorkSessionTransitionConfirmation(receipt,true) dentro del mismo fallback heredado. Comparte la carencia de retorno exitoso de la fila anterior; las excepciones probadas no alcanzan esta devolución. | El mismo oráculo controlado anterior ejercitaría ambos retornos; comprobar confirmación no-null y recibo ganador es observable. La rama EXTEND equivalente en estructura sí tiene su oráculo17, pero no se atribuye su kill a estos dos lambdas distintos. |

Los métodos lambda se identifican por clase/método/línea/mutador e índice originales, no por ordinal cambiante del informe. El descriptor de línea253 devuelve WorkSessionTransitionConfirmation y recibe owner/session/revisión/key/action/notas/Function; el de259 incluye TransitionInsertCollision y TransactionStatus, distinguibles en el XML conservado.

## Juicio y propuesta acotada

No se ha encontrado un bug de producto al leer estos residuos: los rechazos de tipo/calendario están implementados y la recuperación heredada devuelve correctamente el ganador cuando alcanza su rama de éxito. Sí hay **dos rechazos diferenciables sin oráculo directo17** y **una ruta heredada de éxito excepcional sin cobertura**, representada por dos mutantes. La serialización explica por qué ésta no aparece en una carrera normal; no demuestra equivalencia universal del código mutado y no se usa para excluirla.

Si root decide reforzar, los candidatos mínimos son dos casos individuales en PublishOutboxTest (tipo temporal y calendario) y, sólo si se considera pertinente reforzar la recuperación heredada, un caso controlado que cubra ambos retornos de commit. No hace falta perseguir100%, duplicar la matriz ni repetir38min de campaña completa automáticamente. Cualquier nueva medición se conservará aparte: el resultado original seguirá siendo616K/4NC sobre620.

Este dictamen mide el backend congelado17. No cierra por sí solo la feature ni acredita frontend, smoke o UX. La decisión de gates posteriores corresponde al juez/root.

## Refuerzo posterior

Los dos rechazos de Outbox196/207 ahora tienen oráculos y medición dirigida: replay64/64 KILLED, EXIT0 1e6077,349 hashes idénticos ee30d9. Véase tdd_end_time_publisher_refinement.md e inventario separado. Se preserva el resultado original616/620; no se modifica ningún estado original ni se reclasifica como Killed la ruta heredada Store253/259. El juez acepta ese límite excepcional ya justificado por la serialización, sin afirmar equivalencia universal.
