# Revisión del refuerzo del publicador17

APPROVED para los dos oráculos y el replay acotado. Root0222bb revisó16 líneas de tests: payload válido salvo el tipo del fin anterior o fecha de calendario imposible, usando el helper existente que exige rechazo sin entrega. No modifica producción.211 pruebas verdes del autor e837e3; XML y hash del test contrastados por root.

El init script sólo limita clase a OutboxMessage, mutador EMPTY_RETURNS y carpeta de informe independiente. Conserva candidatos JUnit, umbral80 y cuatro workers. El replay medirá todos sus mutantes y comprobará expresamente las dos firmas originales196/207, sin sustituir620originales.
