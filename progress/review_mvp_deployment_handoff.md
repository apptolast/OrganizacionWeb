# Handoff de despliegue MVP — revisión acotada

Lectura local de82e97a/560677/2ff590/40e2a0. No SSH, secretos, comandos de despliegue ni suites ejecutados. No certifica servidor ni fecha de entrega.

- **Artefactos existentes:** backend/Dockerfile construye el bootJar y ejecuta Java25 como usuario sin privilegios; deploy/web.Dockerfile genera el frontend con lockfile y lo sirve mediante Nginx. docker-compose.yml proporciona PostgreSQL persistente, web en loopback y API; deploy/compose.publisher.yml añade RabbitMQ persistente y activa el publicador. El Compose base lo desactiva: desplegar sólo ese archivo omitiría publicación.
- **Gates reutilizables:** la CI ejecuta install, init, build, E2E con Xvfb y smoke del publicador; no contiene publicación de imágenes ni deploy. El corte17 tiene init2076Java/1795frontend/40Node y build verdes; E2E global7009 y Stryker estaban activos al revisar. Los resultados antiguos de deployment-readiness/mvp_deployment_remaining son históricos, no gates actuales pendientes por sí mismos. Historial18 sigue pendiente para el alcance MVP completo.
- **Bloqueo operativo confirmado:** el último SSH documentado terminó Permission denied(publickey); alias/clave y hostname/origin ya están solicitados. No hay acceso autenticado ni medición remota nueva. No se repiten preguntas ni se infieren credenciales/dominio.
- **Integración que falta:** registrar OrganizationWeb en el catálogo y workloads Swarm de DockerSwarmInfrastrcture, con imágenes publicadas y referencia inmutable, recursos, almacenamiento, secrets y origen/autenticación. Los Dockerfiles y build: local no son imágenes ya distribuibles al servidor. El catálogo fija digests y sus marcadores; respetar ese contrato.
- **Comprobaciones remotas pendientes:** capacidad real actual, ruta DNS/HTTPS revisada y posible adopción de estado DNS, configuración de proxy/origin/login, respaldos externos y restauración de PostgreSQL/RabbitMQ. Los45MiB del documento son margen de límites presupuestados, no RAM libre; quorum de un nodo no protege ante pérdida del disco.
- **Secuencia mínima ya disponible:** cerrar gates del corte final con node .harness/harness.mjs init, pnpm build, xvfb-run -a pnpm test:e2e y pnpm test:publisher, conservando mutación exigida. En infraestructura, preparar el cambio y ejecutar scripts/validate-iac.sh, scripts/lint.sh, scripts/validate-workloads.sh; revisar el dry-run del wrapper deploy-ansible.sh --playbook workloads --check --ask-become-pass antes de su aplicación controlada --confirm-production. Este informe no ejecuta ni autoriza un apply.
- **Aceptación tras desplegar:** salud y login por HTTPS, recorrido persistente y recuperación tras reinicio, publicación/reintento y restauración/reversión compatibles con migraciones. /healthz devuelve204 desde Nginx; por sí solo no acredita API, base de datos ni broker. Reutilizar los procedimientos de outbox-publishing.md sin confundir published con consumo o entrega exactamente una vez.

Próximo trabajo útil: paquete de infraestructura revisable e imágenes del corte final; la verificación del servidor queda condicionada al acceso y dominio pendientes. Sin ampliar funcionalidades ni instalar infraestructura durante esta revisión.
