# Handoff contractual16 — cierre de sesión

Normativa promovida en project-spec.md desde la propuesta aprobada77D864…CA5C6, sin cambios de decisiones. Gherkin final: features/close_work_session.feature,41 escenarios y113 casos expandidos (17 escenarios simples y96 filas),41 tags únicos. SHA2563E45F26004E2656E97443451E2D0368CBC9DE734D859725BD07896E206A44285. Recuento fe6f6e; delta editorial d8f57e aclara migración posterior aV16 y aritmética de cierre desdepaused. Sin status16, código, pruebas ejecutables ni Git. El juez frontend recibió el corte; el contrato todavía necesita gate root antes de TDD.

## Mapa de revisión

@s1–5: cierre running/paused/cero, precisión y notas Unicode. @s6–14: forma/precedencia, tiempo y atribución histórica, terminalidad y revisión. @s15–22: compatibilidad de recibos, intención completa, keys entre sesiones alcanzables, concurrencia y atomicidad. @s23–29: upgrade, snapshot, recuperación por sesión/reinicio/publicación real. @s30–41: decoder, fecha persistida sin Intl, URL anterior al POST, recuperación sin key, activa separada, incertidumbre/412/CSRF y UX. Las matrices de seguridad/JSON/revisión comunes remiten a14/15; no se atribuyen nuevas pruebas por referenciarlas.

## Propuesta de firmas Java para preparar el reparto

Estas firmas son diseño de implementación pendiente de TDD, no clases que deban precrearse. A+B conserva núcleo/Store/migración y fija el primer corte compilable; C sólo usa ese corte real para HTTP/publicador. No stubs, defaults que simulen éxito ni copia completa de un árbol mutable.

- `WorkSessionCloseNotes(String progressNote, String nextStep)` en domain: valor concreto de intención, normaliza null a vacío y valida sólo las notas16 mediante ValidationException/FieldError existentes. JSON de tipo incorrecto/propiedad desconocida sigue en HTTP. No reglas temporales ni catálogo dentro del constructor.
- `WorkSessionClosure(String progressNote, String nextStep, LocalDate workDate, String closeZoneId)` en application: cuatro datos históricos ya calculados. No añade campos a SessionStart ni WorkSessionState.
- Evolucionar `WorkSessionTransitionReceipt` añadiendo `WorkSessionClosure closure` como séptimo componente interno, null para PAUSE/RESUME. Conservar constructor público15 de seis argumentos delegando a null. El mapper HTTP conserva seis campos exactos para PAUSE/RESUME y siete para CLOSE; no serializar un closure:null adicional en respuestas15. Los JSONB15 existentes, sin ese campo, deben seguir deserializando y no se reescriben.
- Conservar `requireIntent(session,action,expected)` y añadir sobrecarga con `WorkSessionCloseNotes`. Para CLOSE compara ambas notas normalizadas además de los tres componentes anteriores; una llamada sin notas no puede aceptar por accidente un replay CLOSE. Las operaciones15 conservan su comparación previa.
- Añadir a `ChangeWorkSessionUseCase`: `close(String owner, UUID session, UUID key, WorkSessionRevision expected, WorkSessionCloseNotes notes)` con retorno `WorkSessionTransitionConfirmation` existente. `ChangeWorkSession` implementa el método y conserva su constructor `(WorkSessionChanging, Clock)`; java.time puede resolver la zona histórica y fallback UTC sin nuevo puerto de preferencias ni cambiar pausa/reanudar.
- Evolucionar el método abstracto de `WorkSessionChanging` a `commit(owner,session,key,action,expected,notes,Function<WorkSessionState,WorkSessionTransition>)`, con notes null sólo para PAUSE/RESUME. Conservar sobrecarga anterior delegando al nuevo método con null, no con un resultado ficticio. Los fakes funcionales15 deberán adaptar mecánicamente la aridad al mismo contrato; Store sigue siendo el único adaptador. Las notas llegan antes del callback para que replay y colisión comparen intención antes de negocio.
- `WorkSessionClosed` en application tiene los once componentes normados: UUID eventId/aggregateId, String ownerId, Instant occurredAt, int schemaVersion, String type/revision/fromStatus/workedMicroseconds, LocalDate workDate, String closeZoneId. No modificar el record ni el esquema serializado de WorkSessionStateChanged15.
- Para mantener el accessor y constructor15 de `WorkSessionTransition` sin introducir un evento genérico: añadir componente concreto `WorkSessionClosed closedEvent` junto a `receipt` y `WorkSessionStateChanged event`. Conservar constructor15 `(receipt,event)` con closedEvent null; una factoría concreta de cierre entrega event null y closedEvent informado. El valor exige exactamente el evento correspondiente a la acción. Store persiste el único evento presente; no serializa este bundle ni introduce un envelope nuevo.
- Añadir `closure(String owner, UUID session)` a `ReadWorkSessionChangesUseCase`, retornando el mismo `WorkSessionTransitionReceipt`. En `WorkSessionTransitionQueries`, método homónimo retorna Optional de recibo, con propiedad verificada primero en Store: sesión ajena/ausente lanza WorkSessionNotFoundException; sesión propia abierta retorna vacío y aplicación traduce WorkSessionChangeNotFoundException. Consulta dentro de plantilla read-only RR existente, sin Clock. detail/byRequest no cambian firmas.

La alternativa de un port/evento genérico o nuevas tablas de cierre no aporta una necesidad actual. La propuesta de bundle usa dos tipos concretos y preserva los consumidores15; el juez puede evaluar su simplicidad en el primer ciclo real, antes de que C dependa de esa firma. Ningún record nuevo ni sobrecarga se implementa sin el test activo que lo exige.

C conserva WorkSessionStateController/WorkSessionStateApiTest y OutboxMessage/RabbitBrokerPublisher con sus pruebas. Puede planificar los DTO/eventos desde estos datos, pero debe esperar la entrega compilable A+B antes de escribir mocks contra firmas aún no creadas. Root conserva gate, Git y reconciliación de15. El selector de mutación16 no pertenece a esta fase documental.
