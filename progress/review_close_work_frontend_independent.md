# Revisión independiente frontend16

APPROVED en el alcance estático revisado, sin hallazgos accionables nuevos. No es dictamen final de gates, geometría ni cobertura literal de todos los ejemplos. Lecturas: review_close_work_implementation.md, tdd_close_work_frontend.md, API16/lector/panel15, integración de ruta y SessionGate; herramientas4b01c3/73a3d0/7e3321/4208a3. No se ejecutaron pruebas ni se modificó código.

- @s30: work-session-state-api.ts exige shapes exactos: CLOSE7/closure4 y PAUSE/RESUME6. Valida notas por puntos de código, NUL y surrogates aislados; workDate usa calendario UTC validado, sin Intl. Comprueba identidad de sesión, before/after, revisión siguiente, aritmética exacta BigInt y neto cerrado fijo. POST y K añaden comparación de intención; F valida recurso independientemente. Los negativos de notas se trasladaron a F para evitar que sameNotes ocultase defectos del validador; se conserva honestamente la historia RED anterior.
- @s31–33: el enlace del panel establece la ruta con sessionId antes de montar el formulario. App y WorkSessionReader usan key por contexto; el lector comprueba proyecto/tarea tanto en S como en F. La fecha atribuida se muestra tal como fue persistida; SnapshotTime sólo aplica fallback visual explícito. Notas son texto React, con preservación visual por SCSS, sin almacenamiento privado local.
- @s34–37: la intención retenida conserva key/token/textos durante incertidumbre y no se reenvía automáticamente. K404 reconocido habilita decisión manual;503/errores desconocidos no prueban ausencia. Un412 retira snapshot/consentimiento pero conserva borrador, y exige consulta/nueva confirmación. Un closed consultado lleva a F, no identifica por sí solo la intención pendiente. CSRF utiliza recuperación global existente. La nueva consulta active monta después del cierre confirmado y no reemplaza su recibo; error no se trata como null.
- @s38: desmontaje/contexto abortan comando y lectura. Tras await de éxito, F y clasificación hay comprobación del AbortSignal antes de setters; apiRequest evita observer401 cuando la petición está abortada antes de entregar Response. SessionGate retira App al perder acceso. Las pruebas diferidas distinguen401 HTTP de JSON200 y clasificación no401; no dependen de un401 imposible dentro de JSON ya entregado.
- @s39: dentro del lector no existe un GETS simultáneo con envío porque el formulario requiere snapshot y se retira al consultar tras conflicto. Active se monta después de confirmar. La composición desde el padre anterior sigue siendo una frontera de integración distinta; B confirmó que no atribuye todavía esa prueba a su corteUI y que C prepara su evidencia. No se inventa un resultado desde inspección estática.
- @s40: guardas de petición por ref junto a aria-disabled impiden duplicados, mensajes role=status anuncian carga y la restauración al encabezado sólo se conserva mientras el iniciador retiene foco. onBlurCapture cancela la restauración cuando el usuario desplaza foco. Se revisaron los oráculos de blur deliberado y desaparición del botón ya registrados. No se certifican400ms físicos ni tamaño44px desde JSDOM o fuente.

Límites: la validación HTTP/PG, reinicio y Rabbit pertenecen a sus paquetes independientes. El ajuste posterior de tamaño de «Volver» y los motores/zoom/dispositivos pertenecen a UX; no se reabre ese trabajo por esta revisión. La evidencia compuesta @s39 se mantiene pendiente de C/root hasta su resultado real. No se exige duplicar las matrices14/15 ni fabricar pruebas de acceso privado.
