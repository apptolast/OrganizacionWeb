# Revisión independiente de lecturas y recibos13

**APPROVED parcial** para el paquete congelado de13 Java. Root revisó SQL/puerto/controlador/decoder y pruebas en `ab837e`, `119c15`, `25b7ce` y `0c24de`; verificó los13 hashes del freeze en `ae4c03`. No escribió producción/tests ni ejecutó otro Gradle. El autor registra33/33 GREEN72171d, XML99762b (5app/11PG/17HTTP). Al intentar leerlos después, Gradle de core ya había sustituido esos XML: no se presenta `ae4c03` como verificación independiente del conteo33. La regresión integrada pendiente dará evidencia del corte común.

- La consulta comprueba propiedad/contexto antes de buscar datos dentro de la misma transacción read-only REPEATABLE_READ. No convierte contexto ajeno en historial vacío o ausencia de recibo. ID/key se restringen a proyecto/tarea; no requieren catálogo, reloj ni outbox retenido.
- SQL aplica orden occurredAt/id DESC y lookahead21 antes del recorte20 de aplicación. El cursor nace del último elemento servido sólo cuando hay elemento21; terminal20 es null. La frontera usa pareja estrictamente menor, sin repetir el recibo.
- La extracción de BlockCursor conserva lógica11 y sus parámetros originales blocks/createdAt. El controlador nuevo usa blockChanges/occurredAt, JSON cerrado/base64url canónico y contexto ligado. Su conexión, cursor repetido y query desconocida están probados; las21 variantes del decoder se reutilizan desde la regresión173API11, sin atribuirlas como tests nuevos.
- Las rutas literales ID/key resuelven antes de interpretar UUID y validan query antes de IDs. DTO7/Block9 reutilizados y errores locales404 usan problem+json; filtros compartidos conservan no-store y seguridad. Se observa privacidad entre propietarios y tareas, ausencia ID/key y SQL503 con tabla efímera restaurada en finally.
- La captura de TransactionException rodea execute, incluyendo terminación de lectura. El test del adaptador inyecta fallo tras commit read-only real; no demuestra rollback de una escritura, ni se anuncia como segunda prueba HTTP de commit. Los fixtures de lectura no prueban atomicidad de comandos.

No se encontró hallazgo bloqueante en este alcance. Mantener la revisión de ApplicationConfiguration/contextos en core, mutación explícita de las clases compartidas/nuevas y validación integrada. La recreación de adaptador en s25 es sólo persistencia histórica; el reinicio real corresponde al E2E separado. No marca feature13 done ni autoriza fusionar snapshots aislados como backend final.
