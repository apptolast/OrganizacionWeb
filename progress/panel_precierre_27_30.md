# Panel de jueces antes de cerrar las features 27 y 30 — 10 de septiembre de 2026

Tres jueces independientes por feature, con lentes distintas -cobertura del
contrato, seguridad, y que se toco deprisa-, mas una sintesis. Su encargo NO era
aprobar: era encontrar el motivo por el que un juez rechazaria el cierre.

Se lanzo porque las dos features tenian ya sus tres puertas medidas y por encima
del umbral, y despues de una noche en la que se cayeron cuatro ambitos de
mutacion, diez literales sin oraculo y cuatro errores del orquestador, fiarse de
que los numeros salen era justo el error que se estaba corrigiendo en otros.

**Encontro un bloqueante real en la 27.**

---

{
  "summary": "Independent judge panel over the two features closest to done, to find any rejection reason before marking them",
  "agentCount": 7,
  "logs": [
    "27-github: 3 de 3 jueces rechazarian, 6 motivos bloqueantes",
    "30-automatizaciones: 3 de 3 jueces rechazarian, 4 motivos bloqueantes"
  ],
  "result": {
    "vivos": [
      {
        "feature": "27-github",
        "votos": [
          {
            "feature": "Feature 27 — conector de GitHub (features/github_connector.feature, 42 escenarios)",
            "lente": "Cobertura del contrato: recorrido escenario a escenario del .feature buscando cláusulas del Then sin oráculo que pueda fallar, más verificación profunda de tres escenarios elegidos (s16 paginación, s24 proyecto terminado, s37/s38 formulario y resumen).",
            "rechazaria": true,
            "motivos": [
              {
                "que": "@s41, primera fila: el contrato exige que al navegar a otra pantalla y volver con la misma sesión «repository conserva \"octocat/Hello-World\" y el campo token está vacío». El producto NO conserva el repositorio, y la única prueba de esa fila afirma sólo la mitad del token. El nombre de la prueba —«forgets the token but keeps the repository when the screen is remounted»— declara la mitad que ningún expect() comprueba: tras view.unmount() + render() sólo hay dos aserciones, toHaveValue(\"\") sobre el token y un innerHTML sin el token. Añadir hoy expect(getByLabelText(/repositorio/i)).toHaveValue(\"octocat/Hello-World\") falla: App.tsx:98-99 renderiza <GithubConnector> dentro de un ternario por ruta, así que salir de /integraciones/github lo desmonta, y github-connector.tsx:61 vuelve a nacer con useState(\"\"). El juez dio la fila por buena (progress/judge_github_connector.md:307, «@s41 [x] las cuatro filas»).",
                "donde": "features/github_connector.feature:525 (fila); frontend/src/github-connector.test.tsx:540-557 (prueba sin oráculo); frontend/src/github-connector.tsx:61 y frontend/src/App.tsx:98-99 (producción)",
                "porQueBloquea": "Es una cláusula del Then declarada cerrada cuya conducta no existe y cuyo oráculo no puede fallar por construcción. No es un oráculo débil: es un Then que hoy sería rojo si alguien lo escribiera. Marcar done con esto dentro repite exactamente el patrón que la noche ya ha cazado siete veces: ramas que ninguna prueba ejecuta y filas de contrato que nadie mide.",
                "gravedad": "bloqueante"
              },
              {
                "que": "@s14 (dos filas) y @s15 (una fila) nombran U+00A0 y «sólo Unicode White_Space», y ninguna prueba mete jamás un U+00A0 —ni un U+2003— en ExternalIssue. ExternalIssueTest.java:16-23 usa sólo espacios ASCII (\"  Arreglar login \", \"   \", \"b\"*155+\"      \"); ExternalIssueCriterionTest.java:23 usa \"\", \"   \" y \"  \\n\\r\\n\\t\". El recorte de producción es un literal de expresión regular, ExternalIssue.java:28: replaceAll(\"(?U)^\\\\s+|\\\\s+$\", \"\"). Sustituirlo por String.strip() deja verdes las 14 aserciones existentes y rompe las tres filas del contrato, porque Character.isWhitespace(' ') es false y strip() no quita el espacio duro. PIT no muta literales de cadena, así que la campaña tampoco puede delatarlo. Y el proyecto ya sabe que este caso importa: el gemelo del mismo regex en GithubRepository.parse SÍ tiene su oráculo con   y   (GithubRepositoryTest.java:12-14, fila @s5:76). Falta justo donde el gemelo lo tiene.",
                "donde": "features/github_connector.feature:191-192 y :206 (filas); backend/src/test/java/com/apptolast/organization/domain/ExternalIssueTest.java:16-23 y ExternalIssueCriterionTest.java:23 (oráculos que no discriminan); backend/src/main/java/com/apptolast/organization/domain/ExternalIssue.java:28 (el literal sin oráculo)",
                "porQueBloquea": "Tres filas del contrato cuyo oráculo no distingue la implementación correcta de una que las incumple, en un punto que ninguna puerta puede cubrir (PIT no muta cadenas). Es el mismo hueco que produjo los diez literales de SQL y de códigos de error de esta misma noche.",
                "gravedad": "serio"
              },
              {
                "que": "El ámbito de mutación github_connector no muta una clase de la 27 y sí muta una de la 28. GithubIssueConnections —la clase que fija source()=\"github\" (que acaba en cada recibo y en cada fila de task_external_links), que traduce la fila almacenada a IssueConnection decidiendo projectPath, reference y valid, y cuyo invalidate(ownerId, errorCode, at) tira a propósito errorCode y at— no aparece en NINGÚN targetClasses del repositorio: el único glob que la alcanzaría es el «else ->» de la corrida completa, y bin/harness mutate github_connector pasa -PmutationScope=github_connector (scripts/project.mjs:212). Su gemela GitlabIssueConnections sí está declarada, en el ámbito de la 29 (build.gradle.kts:89). En sentido contrario, ImportGuard está DENTRO del ámbito de la 27 (:121) pero sólo lo usan ConnectGitlab.java:46 y DisconnectGitlab.java:28. La cifra 467/502 no mide, por tanto, lo que dice medir.",
                "donde": "backend/build.gradle.kts:111-136 (githubConnectorClasses, sin GithubIssueConnections y con ImportGuard) frente a :89; backend/src/main/java/com/apptolast/organization/application/GithubIssueConnections.java; scripts/project.mjs:212",
                "porQueBloquea": "El veredicto vigente está condicionado a la puerta de mutación, y su punto 7 exige que ninguna clase del ámbito quede a cero mutantes por «glob muerto». Una clase de la feature que ni siquiera está nombrada es peor que una a cero: no aparece en el informe, así que nadie la echa de menos. Es el cuarto ámbito mal apuntado de la noche, y el precedente de ImportGithubIssues —anotado en el propio build.gradle.kts:117-119— dice que aquí se rechaza por esto. (No he podido contrastarlo contra los XML: tanto pitest-noche-cinco/mutations.xml como pitest-github-connector/mutations.xml están truncados a media escritura, campañas en curso; el hallazgo es estático y se lee en el build.)",
                "gravedad": "serio"
              },
              {
                "que": "Cláusulas de Then declaradas y no medidas en ninguna parte: (a) @s3 «el resto de rutas de proyectos y tareas responde con normalidad» — GithubConnectorWiringTest y GithubConnectorApiTest sólo afirman las cinco rutas del conector; (b) @s29 «no se crea ningún recibo nuevo en ninguna fila y el servidor falso recibe cero peticiones» — de las seis filas, sólo dos lo comprueban; las cuatro pruebas s29_* de ImportGithubIssuesTest.java:432-462 afirman únicamente la excepción lanzada, sin mirar fakes.receipts.size() ni fakes.source.calls(); (c) @s27 «no se relanza ninguna importación automáticamente ni se contacta con el servidor falso» — sin oráculo (hoy es cierto porque no hay ningún @Scheduled del conector, pero nada lo sujeta); (d) @s41 fila 4, «la sesión vence en mitad de la importación» — ninguna prueba del conector la ejerce; los tres 401 de github-connector.test.tsx llevan código GITHUB_TOKEN_REJECTED, que es otra cosa. Descansa en el mecanismo global de api-client.ts:32.",
                "donde": "features/github_connector.feature:38, :383-392, :434-436, :528; backend/src/test/java/com/apptolast/organization/application/ImportGithubIssuesTest.java:432-462; frontend/src/github-connector.test.tsx:737,789,953",
                "porQueBloquea": "Cada una es una frase del contrato que hoy no puede ponerse roja. Ninguna sola justifica el rechazo, pero juntas dicen que el recuento de filas del juez se hizo por escenario y no por cláusula.",
                "gravedad": "menor"
              },
              {
                "que": "El Then de @s12 «finishedAt no anterior a startedAt» lo sostiene un literal de SQL sin oráculo: PostgresIssueImportReceiptStore.java:135 escribe finished_at=GREATEST(?, started_at), y la única prueba que cierra un recibo (GithubConnectorPersistenceTest.java:500-516) usa NOW.plusSeconds(4), muy por encima de started_at. Quitar el GREATEST deja verde toda la suite. PIT no muta la cadena SQL.",
                "donde": "backend/src/main/java/com/apptolast/organization/adapter/persistence/PostgresIssueImportReceiptStore.java:135; backend/src/test/java/com/apptolast/organization/adapter/persistence/GithubConnectorPersistenceTest.java:500-516; features/github_connector.feature:157",
                "porQueBloquea": "Defensa contra desajuste de reloj que ninguna prueba distingue de su ausencia, con la restricción de la V25 esperando al otro lado. Es el mismo molde que el evento perdido en el mismo microsegundo, aunque el daño aquí sea menor.",
                "gravedad": "menor"
              }
            ],
            "loQueSostieneElCierre": "He recorrido los 42 escenarios contra el código y las pruebas, sin ejecutar nada. Los 42 tienen prueba: @s2 y @s15, que no aparecen etiquetados en los ficheros del conector, viven en AesGcmSecretCipherTest.java y ExternalIssueCriterionTest.java, y los verifiqué uno por uno. La mayoría de los oráculos son buenos y discriminan de verdad. Verifiqué a fondo tres, borrando mentalmente la producción: (1) @s16 —ImportGithubIssuesTest.java:129-164— quitar MAX_PAGES hace la fila de 201 pedir página 3 y falla; quitar el break de !listed.full() hace la fila de 99 pedir dos páginas y falla; fijar more a false rompe truncated=true. Discrimina. (2) @s24 —ImportGithubIssuesTest.java:361-383 y GithubConnectorPersistenceTest.java:350-369— borrar la guarda previa o la del callback de newTask rompe una prueba cada una, y la de persistencia comprueba que el callback ve el estado actual del proyecto en la misma transacción. Discrimina. (3) @s37/@s38 —github-connector.test.tsx:220-322— el setToken(\\\"\\\") lo mata la rama 409, el foco al campo y el aria-describedby están afirmados por contenido real, y el resumen mide «Creadas 199 / Omitidas 0 / Fallidas 1», el aviso de truncamiento con su contraprueba (:345) y el foco en el h1. Discrimina. Comprobé además que el escenario de concurrencia @s25 tiene un oráculo real de ocho hilos contra PostgreSQL efímero (GithubConnectorPersistenceTest.java:402-445), que la frontera de @s26 (14:59 frente a 15:01) sí distingue el signo de ABANDONED_AFTER, que @s34 tiene oráculo de exportación con las catorce colecciones (ConnectorExportExposureTest.java) y de almacenes del navegador y auditoría de sesión en e2e, y que el ámbito de Stryker (frontend/stryker.github-connector.config.json) apunta a los tres ficheros correctos. Lo que NO he verificado: no he ejecutado ninguna prueba ni campaña, y los dos mutations.xml del disco están truncados a media escritura, así que no he podido contrastar la cifra 467/502 ni la lista de clases con cero mutantes que exige el punto 7 del juez."
          },
          {
            "feature": "27 — conector GitHub (`features/github_connector.feature`)",
            "lente": "seguridad y datos",
            "rechazaria": true,
            "motivos": [
              {
                "que": "La fila 1 de @s41 («navego a otra pantalla y vuelvo con la misma sesión → repository conserva \"octocat/Hello-World\" y el campo token está vacío») NO la cumple el producto y NO la mide ningún oráculo. `App.tsx:98-99` renderiza `<GithubConnector>` por render condicional, así que salir de /integraciones/github lo DESMONTA; `repository` es `useState(\"\")` local (`github-connector.tsx:61`), y al volver el campo está vacío. La prueba que dice cubrirlo, `frontend/src/github-connector.test.tsx:540` — «forgets the token but keeps the repository when the screen is remounted» — sólo afirma las DOS cosas del token (`:555` token vacío, `:556` el token no está en el HTML) y no afirma nunca el repositorio. Comprobado: `grep toHaveValue` en la unitaria y en el E2E sólo da la fila 409 de @s37 (`github-connector.test.tsx:262`, `e2e/github-connector.spec.mjs:473`), que es otro caso y ocurre sin desmontar. El veredicto vigente da por buenos «los 41 escenarios ya cubiertos» sin volver a mirarlos: esa afirmación es falsa para esta fila.",
                "donde": "frontend/src/github-connector.tsx:61 y frontend/src/App.tsx:98-99; nombre engañoso en frontend/src/github-connector.test.tsx:540-557",
                "porQueBloquea": "Marcar `done` afirma que los 42 escenarios están cubiertos. Aquí hay una fila del contrato que el código contradice y que ningún assert toca, disfrazada por un nombre de test que promete lo que no comprueba: exactamente la clase de «condición declarada cerrada» que esta noche ha estado destapando. El daño de producto es pequeño (comodidad), pero la puerta de cobertura no está pasada.",
                "gravedad": "bloqueante"
              },
              {
                "que": "`ImportIssues.collect()` descifra el token en `:145-148` y lanza `SecretUndecipherableException` cuando ninguna clave del llavero abre el texto guardado. Esa excepción es `RuntimeException` pura (`SecretUndecipherableException.java`) y `run()` sólo captura `IssueSourceException` (`:108`), `ProjectCompletedException` (`:110`) y `StorageUnavailableException` (`:112`). Se escapa por encima de `receipts.begin()` (`:74`), que YA ha insertado un recibo `running`. Resultado: el 503 CONNECTOR_KEY_MISMATCH sale, pero el recibo queda `running` con `finished_at IS NULL` hasta que otro intento lo barra a los 15 minutos. Mientras tanto: cualquier nuevo POST choca con el índice único parcial y responde 409 IMPORT_IN_PROGRESS (mentira: no hay nada importando), y la pantalla entra en el estado «Hay una importación en curso» (`github-connector.tsx: running → canImport false`), que oculta el botón de importar y NO enseña la causa real. La rama no la ejerce ninguna prueba: `grep -rn \"Undecipherable\"` en `backend/src/test/.../application/` no da un solo acierto; el único test es `GithubConnectorApiTest:709`, que la simula con un mock EN la frontera HTTP y por tanto nunca ve el recibo colgado. El disparador es real y operativo: rotar `APP_CONNECTOR_KEY` sin conservar `key-previous`, o restaurar una copia con otra clave.",
                "donde": "backend/src/main/java/com/apptolast/organization/application/ImportIssues.java:74 y :99-113 y :145-148",
                "porQueBloquea": "Es una rama que nadie ejecuta, alrededor del manejo de la clave de cifrado, y deja estado persistente incoherente: un recibo `running` que no corresponde a ninguna importación viva. @s25, @s26 y @s27 construyen sus garantías sobre el significado de `running` («hay una importación en curso» o «murió a mitad»), y este camino lo rompe sin que nada lo mida. El contrato del recibo dice, en todos los fallos que sí nombra, que queda `failed` con su errorCode.",
                "gravedad": "serio"
              },
              {
                "que": "`GithubIssueConnections` NO está en ningún ámbito de mutación de los que se han medido. `githubConnectorClasses` (build.gradle.kts:113-136) enumera `ConnectGithub*`, `ImportIssues*`, `ReadGithubConnection*`, `DisconnectGithub*`, `ReadIssueImport*`, `ConnectorFailures*`… y no incluye `application.GithubIssueConnections*`; `additionalConnectorsClasses` (`:82-110`) sí lista su gemela `GitlabIssueConnections*` pero, por su propio comentario, excluye a propósito lo compartido con la 27. `noche_cinco` es la unión de esos dos conjuntos, así que tampoco. Sólo la aparece el ámbito por defecto (`core` = `application.*`, `:661`), que nadie corre. Y esa clase no es un portador de datos: mapea la fila a `IssueConnection` (repositorio, referencia, texto cifrado, validez) y, sobre todo, implementa `invalidate()`, que es lo que marca la conexión como inválida tras el 401 de @s22. Un mutante VOID_METHOD_CALLS sobre `connections.invalidate(ownerId)` significa «el token rechazado sigue considerándose válido y se reenvía en cada importación» y no se genera en ninguna campaña.",
                "donde": "backend/build.gradle.kts:113-136 (ámbito github_connector) frente a backend/src/main/java/com/apptolast/organization/application/GithubIssueConnections.java",
                "porQueBloquea": "El juez exigió «ninguna clase nombrada en el ámbito con cero mutantes» y desglose por clase; una clase que ni siquiera está en el ámbito no aparece en el desglose, así que el informe puede salir impecable con esta clase sin un solo mutante. Es el patrón que la noche ya cazó cuatro veces. El 93,03 % no cubre esta clase.",
                "gravedad": "serio"
              },
              {
                "que": "El ámbito `github_connector` incluye el glob de paquete `com.apptolast.organization.adapter.connectors.*` (`:130`), que arrastra `GitlabApiBase` y `HttpGitlabIssueSource` — ambas listadas también, una por una, en `additionalConnectorsClasses` (`:106-107`). O sea: las dos campañas se solapan por construcción, justo lo contrario de lo que declara el comentario de `:41-43` («No incluye las clases compartidas con la 27 … un mutante contado dos veces no informa»). El adaptador HTTP de GitLab no es pequeño, y sus mutantes los matan las pruebas de la 29.",
                "donde": "backend/build.gradle.kts:130 frente a :41-43 y :106-107",
                "porQueBloquea": "La cifra que se presenta como la de la feature 27 —93,03 %, 467/502— tiene en el denominador clases que no son de la 27 y en el numerador muertes que no las provocan las pruebas de la 27. El juez pidió explícitamente «no acepto un porcentaje sin el recuento al lado» porque un ámbito mal apuntado da buena puntuación; aquí el ámbito está contaminado en la dirección que infla.",
                "gravedad": "serio"
              },
              {
                "que": "`PersonalAccessToken.hint()` devuelve el token ENTERO cuando mide 4 caracteres o menos (`value.length() <= HINT_LENGTH ? value : substring(...)`), y el dominio no impone longitud mínima distinta de 1 (sólo nulo/vacío, >255 y no-ASCII). `PostgresGitlabConnectionStore` persiste ese `token_hint` en claro (columna `TEXT CHECK char_length BETWEEN 1 AND 4`, V29) y lo devuelve en su DTO. La clase del dominio la posee la 27 —está en su ámbito de mutación y su @s6 fija sus reglas—, y su propia V30 razona por escrito sobre «un token de un solo carácter, que el dominio admite».",
                "donde": "backend/src/main/java/com/apptolast/organization/domain/PersonalAccessToken.java:38-40; consumida por el almacén de GitLab (V29__additional_connectors.sql, columna token_hint)",
                "porQueBloquea": "Un secreto corto acaba en claro en la base de datos y en una respuesta de API, por un camino que la 27 abre al no fijar longitud mínima. No lo ejerce ninguna ruta de la 27, así que no bloquea su cierre, pero la corrección (mínimo de dominio) es de la 27 y debe salir con dueño, no como nota suelta.",
                "gravedad": "menor"
              },
              {
                "que": "`ReadIssueImport.execute` busca el recibo sólo por `(owner_id, id)` — `PostgresIssueImportReceiptStore.read()` no filtra por `source`, a diferencia de `latest(ownerId, source)`, que sí lo hace desde la V29. Así, `GET /api/v1/me/connectors/github/imports/{id}` devuelve 200 con un recibo de GitLab del mismo propietario. No cruza propietarios (@s30 se mantiene) y el DTO declara `source`, y el cliente lo rechaza (`github-connector-client.ts` exige `source === \"github\"`), así que sale como «Confirmación incompatible».",
                "donde": "backend/src/main/java/com/apptolast/organization/application/ReadIssueImport.java:20-22 y adapter/persistence/PostgresIssueImportReceiptStore.java:180-188",
                "porQueBloquea": "No hay fuga entre propietarios, sólo entre conectores del mismo dueño, y el contrato de @s30 no la prohíbe expresamente. Queda como incoherencia entre las dos lecturas del mismo almacén (una filtra por origen y la otra no) que nadie mide.",
                "gravedad": "menor"
              },
              {
                "que": "El `full_name` que devuelve GitHub se guarda tal cual como `repository` (`ConnectGithub:44`, `identity.fullName()`) sin volver a pasarlo por `GithubRepository` —la validación sólo se aplica a lo que escribe la persona— y la única cota es el `CHECK char_length BETWEEN 3 AND 140` de V25. Después se concatena a pelo en la URL saliente (`GithubApiBase.repository()` → `value + \"/repos/\" + repository`) y se pasa a `URI.create` en `HttpGithubIssueSource.get()`, que sólo captura `InterruptedException` e `IOException`. Un `full_name` con espacio, `?` o `#` produce `IllegalArgumentException`, que sube por `collect()` y sale por el mismo agujero del hallazgo 2: recibo `running` colgado y 500.",
                "donde": "backend/src/main/java/com/apptolast/organization/application/ConnectGithub.java:44-49; adapter/connectors/GithubApiBase.java:41-47; adapter/connectors/HttpGithubIssueSource.java:96-118",
                "porQueBloquea": "Requiere que el proveedor al otro lado devuelva un `full_name` no canónico, y la lista blanca de `GithubApiBase` limita ese lado a api.github.com o loopback, así que el riesgo real es bajo. Lo anoto porque el valor es de fuera y viaja sin validar hasta una URL y hasta una columna, y porque su fallo cae en la misma rama sin capturar del hallazgo 2.",
                "gravedad": "menor"
              }
            ],
            "loQueSostieneElCierre": "Leído por mí sobre este árbol, sin ejecutar suite, `harness init` ni mutación. Lo que SÍ aguanta mi lente: (1) El cifrado. `AesGcmSecretCipher` es AES-256-GCM con nonce nuevo por escritura, etiqueta de 128 bits y `ownerId` como AAD (`updateAAD`), formato 12+n+16 sin byte de versión, y el oráculo de frontera de `ciphertext.length <= SHORTEST` es de los buenos: `AesGcmSecretCipherTest:102-111` cifra la cadena vacía para obtener 28 octetos VÁLIDOS y exige que se rechacen, que es el único caso que distingue `<=` de `<`. La deuda del `CHECK` que el juez dejó abierta está saldada de verdad: existe `V30__github_connector_ciphertext_bounds.sql` con `BETWEEN 29 AND 283` y con el comentario de V25 corregido en su cabecera. (2) El token no sale por ningún canal que yo haya encontrado: ningún método de `ConnectorAudit` recibe el token (y `ConnectorAuditTest:88-95` lo afirma además en base64 y por reflexión sobre los nombres de método); `IssueSourceException` lleva sólo la categoría, nunca el cuerpo (`HttpGithubIssueSourceTest:380`); `StoredConnection.toString` y `PersonalAccessToken.toString` redactan; `ConnectorExportExposureTest` fija las catorce colecciones del volcado y prohíbe por nombre las del conector; el E2E comprueba localStorage, sessionStorage, cookies, el HTML y la respuesta de `/api/session`, en claro y en base64. (3) Aislamiento entre propietarios: TODAS las sentencias del conector llevan `owner_id=?` con parámetro enlazado —`find`/`save`/`delete`/`invalidate` de la conexión, `read`/`latest`/`progress`/`finish`/`importing`/`interruptAbandoned` de los recibos, y `alreadyLinked`/`insertLink`/`insertEvent` más el `SELECT … FOR UPDATE` de `projects WHERE owner_id=? AND id=?` en `PostgresImportedTaskCommit`—; no hay una sola concatenación de SQL en producción. Recibo ajeno e inexistente dan el mismo 404 IMPORT_NOT_FOUND, y proyecto ajeno e inexistente el mismo 404 RESOURCE_NOT_FOUND, con el orden de precondiciones fijo de @s29 respetado en `ImportIssues.execute`. (4) Autorización: `ApiCredentialBearerFilter` deniega por defecto —ninguna ruta del conector está en su lista de permisos, así que cualquier Bearer se lleva 403 API_SCOPE_DENIED, también en GET, no sólo en las dos filas que el contrato enumera— y `GithubConnectorApiTest:626-770` cubre las cinco rutas sin sesión, las dos con Bearer, CSRF ausente e inválido y Origin ajeno, con `verifyNoInteractions` sobre los casos de uso. (5) SSRF: `GithubApiBase` valida al construir el bean, sólo api.github.com por https o loopback, sin ruta, consulta, fragmento ni credenciales; `apiBase` como propiedad de petición se rechaza con UNKNOWN_FIELD; el cliente HTTP va con `Redirect.NEVER` y el cuerpo se lee acotado a 5 MiB con plazo por los dos lados (`BoundedResponse`). El servicio falso vive bajo el perfil `e2e` de compose y no lo levanta un despliegue normal. (6) Los códigos de error del recibo → estado HTTP sí tienen oráculo parametrizado (`GithubConnectorApiTest:469-472`: STORAGE_UNAVAILABLE→503, CONNECTION_INVALID→409, GITHUB_REPOSITORY_UNAVAILABLE→409, PROJECT_COMPLETED→409) y el literal `status='invalid'` se ejerce contra PostgreSQL de verdad en `GithubConnectorPersistenceTest:232`, con el CHECK de la tabla como red. Nada de lo anterior lo he dado por bueno por venir escrito en un informe."
          },
          {
            "feature": "27 — conector GitHub (`features/github_connector.feature`)",
            "lente": "Lo que se rompió y lo que se tocó deprisa: historial reciente de los ficheros de la feature, cambios de producción sin prueba, aserciones que no pueden fallar, y arreglos que silencian el síntoma. Contrastado contra los artefactos de mutación reales en disco, no contra las cifras declaradas.",
            "rechazaria": true,
            "motivos": [
              {
                "que": "`Slf4jConnectorAudit` está nombrada en el ámbito PIT de la feature y generó CERO mutantes en la campaña que sostiene el 93,03 %. El juez lo declaró causal de rechazo con estas palabras: «Ninguna clase nombrada en el ámbito con cero mutantes, salvo los cuatro portadores de datos… Cualquier otra clase a cero es un glob muerto y hay que arreglarlo antes de dar la campaña por buena». Los cuatro portadores exentos (IssuePage 7, StoredConnection 8, ConnectionView 6, IssueSourceException 10) SÍ recibieron mutantes; la única clase a cero es el auditor. Verificado: `grep -c Slf4jConnectorAudit backend/build/reports/pitest-github-connector/mutations.xml` = 0, y el informe HTML no tiene paquete `adapter.logging` (27 clases: 1+7+1+3+11+4). Causa raíz confirmada en el propio fichero de build: el bloque pitest fija `excludedMethods` pero NO fija `mutators` ni `avoidCallsTo` (`backend/build.gradle.kts:733`), así que el `avoidCallsTo` por defecto de PIT suprime las llamadas a `org.slf4j`, y esta clase es puro logging. Es el MISMO defecto, en la clase gemela, que `progress/auditoria_condiciones_cierre.md` ya declara bloqueante para la feature 25 (`Slf4jWebhookAudit`, «la puerta no vale aunque la puntuación salga alta»). Aquí ni siquiera está declarado: la bitácora de cierre no lo menciona.",
                "donde": "backend/build.gradle.kts:135 (la entrada del ámbito) y :733 (el bloque sin `avoidCallsTo`); backend/build/reports/pitest-github-connector/index.html (27 clases, ninguna de `adapter.logging`)",
                "porQueBloquea": "Es una condición dura del veredicto vigente, literal y verificable, y está incumplida sin declarar. Además deja sin oráculo de mutación justamente la clase que implementa «bitácora sin token» (@s?, commit 11a0c80b): la redacción de la bitácora del conector no tiene un solo mutante en ninguna campaña del repositorio.",
                "gravedad": "bloqueante"
              },
              {
                "que": "El informe de backend que produce el 93,03 % (467/502) sigue mostrando VIVO el mutante de frontera que el juez exigió KILLED uno por uno. En `AesGcmSecretCipher.java.html`, línea 60, mutante 3: «changed conditional boundary → SURVIVED / Killed by: none». El juez escribió: «`:60` `ciphertext.length <= SHORTEST` (NEGATE_CONDITIONALS y CONDITIONALS_BOUNDARY)… Un superviviente en cualquiera de estos puntos ES RECHAZO, aunque la puntuación global pase». Cronología, medida en disco: el HTML del cifrador se escribió a las 11:49:00 y el `index.html` a las 11:52:09; el commit que añade el oráculo (`e5e0d563`, «matar el mutante de frontera del cifrador que el juez exigia») es de las 11:54:17. El oráculo se escribió DESPUÉS de la campaña y se verificó a mano aplicando el mutante; la campaña no se ha vuelto a lanzar. Segundo problema en el mismo sitio: ese commit afirma «el cifrador genero 26 mutantes»; el informe dice 17 (16 KILLED, 1 SURVIVED, 0 NO_COVERAGE). El suelo de 12 se cumple igual, pero la cifra que se presenta como prueba de que el ámbito apunta bien está mal por nueve, y el suelo era precisamente el disparador que el juez puso contra ámbitos mal apuntados.",
                "donde": "backend/build/reports/pitest-github-connector/com.apptolast.organization.adapter.connectors/AesGcmSecretCipher.java.html (línea 60, mutante 3); backend/src/main/java/com/apptolast/organization/adapter/connectors/AesGcmSecretCipher.java:60; commit e5e0d563",
                "porQueBloquea": "La cifra que se ofrece a la puerta (93,03 %) y el artefacto que la respalda contienen el superviviente que el juez declaró causal de rechazo. El arreglo es creíble y probablemente lo mata, pero eso hay que MEDIRLO, no razonarlo: es exactamente la sustitución de medición por argumento que la noche ya ha castigado cuatro veces. Y la cifra de 26 mutantes no la sostiene ningún artefacto.",
                "gravedad": "bloqueante"
              },
              {
                "que": "`progress/mutation_github_connector.md` no existe. El juez lo exige como condición 8: «Todo superviviente, documentado en `progress/mutation_github_connector.md`: matado con test nuevo o justificado como equivalente, uno por uno (C7)». La campaña de backend deja 35 mutantes no muertos: 25 SURVIVED y **10 NO_COVERAGE** (deducidos del informe: cobertura 467/502 frente a fuerza de prueba 467/492). Ninguno está documentado en ninguna parte. Los sin cobertura se reparten en HttpGithubIssueSource (1), HttpGitlabIssueSource (3), GithubConnectorController (1), PostgresIssueImportReceiptStore (2), IssueImportReceipt (2) y BoundedResponse (1). Los únicos ficheros de mutación de la feature son `mutacion_github_connector_frontend_cierre.md` y `mutacion_github_connector_supervivientes.md`, ambos de frontend.",
                "donde": "progress/ (fichero ausente); backend/build/reports/pitest-github-connector/index.html y los index.html de cada paquete",
                "porQueBloquea": "Diez mutantes SIN COBERTURA en una campaña de cierre significan diez ramas de producto que ninguna prueba ejecuta. Es literalmente el patrón que esta noche ha producido siete defectos reales de producto. Y es una condición explícita del veredicto, no una formalidad: el juez la ató a C7.",
                "gravedad": "bloqueante"
              },
              {
                "que": "El contrato de la feature se enmendó a las 00:19 de hoy SIN contrafirma del propietario. `a1b0d20b` reescribe `features/github_connector.feature:146` («once campos» → «doce campos») y `:165-166` (el recibo pasa de `repository` a `source` + `projectPath`, y se añaden dos aserciones nuevas). El propio fichero lleva el formato correcto en las dos enmiendas anteriores: `:55` y `:397` dicen «Enmienda del 9 de septiembre de 2026, ratificada por el propietario». Las líneas nuevas no llevan ni comentario de enmienda ni contrafirma; `grep ratificad` sobre el fichero sólo devuelve esas dos. Es el mismo rasero que `progress/auditoria_condiciones_cierre.md` aplica como C7 BLOQUEANTE a la feature 29, citando precisamente `github_connector.feature:393` como el precedente que sí la lleva.",
                "donde": "features/github_connector.feature:146 y :165-166 (sin marca), frente al precedente de :55 y :397; commit a1b0d20b",
                "porQueBloquea": "CLAUDE.md, regla dura: «No saltes la puerta de aprobación humana sobre los `features/<name>.feature`». Y el proyecto acaba de bloquear la 29 por esto mismo con una desviación menor (allí al menos hay comentario de enmienda; aquí no hay nada). Marcar `done` la 27 con el contrato enmendado por el propio carril sería incoherente con el dictamen que sigue abierto sobre la 29.",
                "gravedad": "bloqueante"
              },
              {
                "que": "El veredicto vigente se emitió sobre `HEAD = cc76ec5` y su disciplina TDD se sostiene en «el diff NO toca `src/` en absoluto… Cero producción nueva». Después del veredicto sí se tocó producción: `a1b0d20b` (00:19) cambia `frontend/src/github-connector-client.ts` (RECEIPT_FIELDS a doce claves, el tipo `GithubImportReceipt`, y `decodeReceipt` pasa de `typeof value.repository !== \"string\"` a `value.source !== GITHUB || !nonEmpty(value.projectPath)`), y `44011086` (10:45) lo vuelve a tocar exportando dos constantes. La superficie que el juez leyó ya no es la que se va a cerrar, y la afirmación de disciplina que sostiene su apartado TDD ha caducado.",
                "donde": "frontend/src/github-connector-client.ts (commits a1b0d20b y 44011086) frente a progress/judge_github_connector_cierre.md, sección «Disciplina TDD»",
                "porQueBloquea": "No es que el cambio sea malo —tiene rojo acreditado y una prueba de contrato que ata el record de Java al cliente—, es que el veredicto que se invoca para cerrar no lo ha visto. Cerrar con un APPROVED emitido sobre otro árbol es exactamente el «una condición declarada cerrada tampoco es evidencia» del encargo.",
                "gravedad": "serio"
              },
              {
                "que": "El ámbito PIT `github_connector` mide producción de la feature 29. El comodín `com.apptolast.organization.adapter.connectors.*` arrastra `GitlabApiBase` (27 mutantes) y `HttpGitlabIssueSource` (54 mutantes): 81 de los 502 (el 16 %) son código GitLab, y aportan 4 de los 25 supervivientes y 3 de los 10 sin cobertura. Además `ImportGuard*` está en `githubConnectorClasses` (`:121`) aunque sólo lo llaman `ConnectGitlab` y `DisconnectGitlab` —hallazgo ya escrito en `progress/auditoria_condiciones_cierre.md` y todavía sin corregir—. Descontado GitLab, la feature 27 mide 393/421 = 93,3 %: sigue pasando, pero la puntuación declarada no es la de la feature que se cierra.",
                "donde": "backend/build.gradle.kts:130 (`adapter.connectors.*`) y :121 (`ImportGuard*`); backend/build/reports/pitest-github-connector/com.apptolast.organization.adapter.connectors/index.html",
                "porQueBloquea": "El juez pidió el desglose por clase precisamente porque «un ámbito mal apuntado da buena puntuación sin generar un solo mutante de la clase que importa». Aquí el desglose existe pero nadie lo ha leído: nadie ha notado ni las clases de otra feature dentro ni la clase propia a cero. No tumba el umbral, pero invalida la lectura de la puerta.",
                "gravedad": "serio"
              },
              {
                "que": "Las dos pruebas de persistencia que justifican la migración V30 llevan una aserción que no puede fallar y no tocan el cifrador real. `s1_theShortestTokenTheDomainAcceptsAlsoFitsInTheColumn` construye `var sealed = new byte[NONCE_BYTES + shortest.value().length() + TAG_BYTES]` y a continuación afirma `assertThat(sealed).hasSize(29)`: es cierta por construcción, con cualquier valor de las constantes locales del test. Lo mismo en la del token largo con 283. Ninguna de las dos llama a `AesGcmSecretCipher`, así que nada ata la cota 29..283 de la V30 al formato en reposo que el cifrador produce de verdad. Si el formato vuelve a cambiar (por ejemplo si regresa el byte de versión), las dos pruebas siguen verdes y la restricción vuelve a rechazar filas legítimas con un 500 — que es literalmente el defecto que estas pruebas se escribieron para impedir que se repitiera.",
                "donde": "backend/src/test/java/com/apptolast/organization/adapter/persistence/GithubConnectorPersistenceTest.java, métodos `s1_theShortestTokenTheDomainAcceptsAlsoFitsInTheColumn` y `s1_theLongestTokenTheDomainAcceptsAlsoFitsInTheColumn` (commit 02400b8f)",
                "porQueBloquea": "No bloquea por sí solo: la mitad útil (que PostgreSQL acepte la fila) sí muerde, y el juez ya dio la deuda del CHECK por cerrada. Pero es una aserción tautológica presentada como el oráculo que «fija el rango entero para que una cota calculada para otro formato no vuelva a pasar inadvertida», y no lo hace.",
                "gravedad": "menor"
              },
              {
                "que": "La campaña de frontend no dice contra qué commit se midió y hay deriva medible dentro de la ventana. `progress/mutacion_github_connector_frontend_cierre.md:3` dice sólo «sobre `main`». El informe se escribió a las 10:55:31; `44011086` toca `frontend/src/github-connector-client.ts` (dos `export` nuevos) y su test (+66 líneas) a las 10:45:01, diez minutos antes. Una corrida de 589 mutantes con `concurrency: 8` empieza bastante antes de las 10:45, así que lo más probable es que el 85,06 % se midiera sobre el árbol anterior. El juez lo pidió expresamente: «Ambas — 4. Medidas sobre este árbol… El informe debe decir contra qué commit se midió». Las cifras en sí las he verificado y son exactas (501/589 = 85,06 %, 0 sin cobertura, los tres módulos con mutantes).",
                "donde": "progress/mutacion_github_connector_frontend_cierre.md:3; frontend/reports/mutation-github-connector/mutation.json (mtime 2026-09-10 10:55:31) frente al commit 44011086 (10:45:01)",
                "porQueBloquea": "No cambia el veredicto numérico —los dos exports no generan mutantes y el total sigue siendo 589—, pero deja la trazabilidad rota justo en la condición que el juez escribió para impedirlo, y ya no se puede reconstruir a posteriori.",
                "gravedad": "menor"
              }
            ],
            "loQueSostieneElCierre": "Verificado por mí sobre los artefactos, no leído en las bitácoras:\n\n**Frontend, cifras exactas.** Parseando `frontend/reports/mutation-github-connector/mutation.json`: 589 mutantes, 501 Killed, 88 Survived, **0 sin cobertura**. 501/589 = 85,06 %, por encima del umbral 80 de `stryker.github-connector.config.json`. Los tres módulos del `mutate` tienen mutantes (246 / 341 / 2), ninguno a cero, que era la condición 2 del juez. Coincide exactamente con lo declarado.\n\n**Backend, el total y el suelo del cifrador.** El `index.html` de PIT da 502 mutantes en 27 clases y 467 muertos = 93 %, que es la cifra declarada. `AesGcmSecretCipher` genera **17** mutantes con **cero NO_COVERAGE**: cumple el suelo de 12 del juez (aunque no los 26 que dice el commit). `ConnectorKeyRing` genera 13, cumple su suelo de 1. De los cinco puntos que el juez exigió KILLED uno por uno, cuatro lo están y con nombre de prueba asesina: `:51` (MATH, `s2_everyWriteUsesAFreshNonce`), `:52` y `:53` (los dos `System.arraycopy`), `:54`, `:59`, y el NEGATE_CONDITIONALS de `:60` (dos, muertos por `s1_ciphertextIsNonceThenSealedAndHidesTheToken` y por `b5_aTruncatedOrEmptyCiphertextIsRejected…`). El único que falla es el CONDITIONALS_BOUNDARY de `:60`, que es el motivo 2.\n\n**El bloqueante original del juez está de verdad cerrado.** `AesGcmSecretCipher` no tiene byte de versión (`SHORTEST = NONCE_BYTES + TAG_BITS / 8` = 28), y la V30 fija la cota en 29..283 coherente con eso. El comentario de la V30 explica el porqué donde toca.\n\n**El arreglo del recibo es real y ataca la causa, no el síntoma.** La regresión (`decodeReceipt` rechazando el recibo de doce claves) tenía rojo acreditado por ejecución con el mensaje literal, y `44011086` añade la guarda que faltaba: una prueba que lee el `record ImportResponse` del controlador Java y lo compara contra la misma constante `RECEIPT_FIELDS` que usa producción — con su techo (Jackson, `@JsonProperty`) escrito en el javadoc en vez de disimulado, y con el ayudante lanzando excepción si no encuentra el fichero, de modo que no puede pasar en verde por accidente.\n\n**Los 44 oráculos nuevos de frontend no son relleno.** Revisadas sus aserciones una a una: son de conducta (foco, `defaultPrevented`, presencia/ausencia de controles, número de llamadas al transporte), no de implementación, y ninguna prueba anterior se relajó ni se borró — el diff de `ba972804` sólo toca ficheros de prueba, y el de producción está vacío en ese ciclo.\n\n**C5 cumplido.** `progress/history.md:731-741` sí tiene la entrada del carril, contra lo que decía la auditoría anterior.\n\n**No he ejecutado** ninguna suite, ni `bin/harness init`, ni ninguna campaña: la restricción de sesión se ha respetado íntegra. Por eso el punto 5 del juez (`harness init` verde, «si sale rojo, este veredicto decae») sigue sin verificar por nadie, y `progress/current.md:30` lo da como pendiente."
          }
        ]
      },
      {
        "feature": "30-automatizaciones",
        "votos": [
          {
            "feature": "30 — automations",
            "lente": "Cobertura del contrato: recorrido escenario a escenario de features/automations.feature (43 escenarios, ~120 filas de Examples) comprobando que cada cláusula tiene un oráculo que puede fallar",
            "rechazaria": true,
            "motivos": [
              {
                "que": "@s4 fila 2 («C completed → 201 y la regla se guarda») no tiene oráculo en ningún sitio del árbol. La única producción que decide esa fila es el literal SQL del bean automationTargets: \"SELECT 1 FROM projects WHERE id = ? AND owner_id = ?\", cuyo Javadoc dice explícitamente «whatever its status». El helper del único test de integración que ejerce ese bean, AutomationWiringTest.project(String owner, String status), acepta un estado — y jamás se le pasa \"completed\": las tres llamadas usan \"active\". Grep de \"completed\" en AutomationWiringTest: cero resultados; el único UPDATE ... status='completed' del árbol está en AutomationPersistenceTest:382 y ataca AutomationFacts.projectCompleted, que es otro puerto.",
                "donde": "backend/src/main/java/com/apptolast/organization/adapter/config/ApplicationConfiguration.java:668-676 (producción sin oráculo); backend/src/test/java/com/apptolast/organization/adapter/config/AutomationWiringTest.java:44-53 y :123-134 (el helper que podría cerrarlo y no lo hace)",
                "porQueBloquea": "Añadir \" AND status = 'active'\" a esa cadena rompe @s4 fila 2 (la regla dejaría de poder crearse), y con ella la premisa entera de @s21 fila 1 y @s32 fila 1 —PROJECT_COMPLETED en ejecución y en simulación sólo existe si la regla se pudo guardar antes—, y toda la suite sigue verde. Es exactamente el modo de fallo que esta noche ya ha cobrado diez veces: literal de SQL que PIT no muta y ningún test discrimina. Además es la fila que separa «error de guardado» de «fallo determinista de ejecución», que es la distinción sobre la que se apoya la mitad del bloque de ejecución del contrato.",
                "gravedad": "bloqueante"
              },
              {
                "que": "Tres pruebas etiquetadas @s43 sancionan como correcto un estado del que la interfaz no se recupera, y su oráculo no lo puede detectar. save(), simulate() y toggle() comparten el mismo writeRequest.current (automations.tsx:254, :296, :319). Cuando una supera a otra, el perdedor sale por `if (!mounted.current || writeRequest.current !== controller) return;` y su `finally` —`if (mounted.current && writeRequest.current === controller) setSaving(false)`— no se ejecuta nunca. `saving` (o `busyToggle`) queda en true de por vida: `disabled={saving}` en el botón Guardar (:534) y la guarda de entrada `if (!editing || saving) return;` (:253) dejan Guardar inerte para siempre, con el borrador atrapado dentro del editor; igual con `disabled={busyToggle === rule.id}` (:433). Las tres pruebas ejecutan literalmente ese camino (Guardar → Simular; Simular → Guardar; interruptor → Simular) y sólo afirman queryByRole(\"alert\") ausente y que el borrador/el texto siguen ahí. Ninguna comprueba que Guardar o el interruptor vuelvan a ser usables.",
                "donde": "frontend/src/automations.tsx:253-285, :296, :319-336, :433, :534; frontend/src/automations.test.tsx:1018-1042, :1044-1071, :1074-1100",
                "porQueBloquea": "Dos cosas a la vez. Primera: los Examples de @s43 son «navega a /proyectos», «cierra sesión» y «cambia a otra regla» — «otra escritura la supera» no está en el contrato, así que estas tres pruebas inflan la cobertura aparente de un escenario con una situación que no le pertenece, y de paso dan por bueno el defecto. Segunda: la cláusula que sí está en el contrato, @s40 fila 1 («el botón queda deshabilitado HASTA la respuesta»), se incumple: aquí queda deshabilitado para siempre. La prueba de @s40 (:305) sólo mide el caso sin interferencia, y las de @s43 miden el caso con interferencia sin mirar el botón. Entre las dos no hay ninguna aserción que pueda fallar por esto. Está anotado como conocido, pero un juez no acepta que además esté congelado en las pruebas como comportamiento esperado.",
                "gravedad": "bloqueante"
              },
              {
                "que": "@s41 «la fila con createdTaskId contiene un enlace a la tarea creada»: el fixture pone createdTaskId igual al id del proyecto (`createdTaskId: status === \"succeeded\" ? PROJECT : null`, con PROJECT = \"11111111-1111-4111-8111-111111111111\", que es también rule.action.projectId), y la aserción espera `/proyectos/${PROJECT}/tareas/${PROJECT}`. Los dos segmentos del href son el mismo UUID.",
                "donde": "frontend/src/automations.test.tsx:435 y :463-466, contra frontend/src/automations.tsx:577-584",
                "porQueBloquea": "El oráculo no discrimina: si la producción intercambiara los dos identificadores, o usara projectId en los dos segmentos, o createdTaskId en los dos, la prueba pasa igual. La cláusula «enlace a la tarea creada» queda sin comprobar. Y hay una rama de producción adyacente que nadie ejecuta: cuando la regla es NOTIFY_WEBHOOK el segmento de proyecto se resuelve a \"\" y el href sale como /proyectos//tareas/<id>. Basta cambiar el fixture a dos UUID distintos para que el oráculo empiece a medir.",
                "gravedad": "serio"
              },
              {
                "que": "@s31 filas 3 y 4 —la ventana de «los últimos 100 eventos»— no tienen oráculo. El único sitio donde se afirma el 100 es `assertThat(requestedLimit).isEqualTo(SimulateAutomation.WINDOW)`, que compara el valor pedido contra la propia constante de producción; y ningún fixture de SimulateAutomationTest pasa nunca de 5 eventos, así que ni «evaluados 100 de 130» ni «100 coincidencias, sin la más antigua» se recorren jamás. Contrasta con @s34, donde el mismo patrón sí discrimina: ReadAutomationRunsTest:56 afirma `hasSize(5)` sobre un fixture de 25 ejecuciones, y eso clava PAGE_SIZE=20 aunque la primera aserción use la constante.",
                "donde": "backend/src/test/java/com/apptolast/organization/application/SimulateAutomationTest.java:113 y :145-162; backend/src/main/java/com/apptolast/organization/application/SimulateAutomation.java:12 y :39",
                "porQueBloquea": "Es una aserción autorreferencial sobre la única cifra que esas dos filas del Outline afirman. Atenuante que le baja la gravedad: AutomationPersistenceTest:308-326 sí ejerce el ORDER BY ... LIMIT real con límite 2 sobre 4 filas, así que el mecanismo «los más recientes primero, hasta el límite» está probado; lo que no está probado es que el límite que pide el caso de uso sea 100.",
                "gravedad": "menor"
              },
              {
                "que": "@s36 fila 5 («POST /api/v1/me/automations/simulate con Origin ajeno → 403») no tiene prueba propia. El único test de origen ajeno usa PUT /api/v1/me/automations/{id}, y el único test de CSRF ausente usa POST /api/v1/me/automations. La ruta /simulate —la única POST del contrato que no escribe, y por tanto la candidata natural a que alguien la excluya del guardián— nunca se prueba contra Origin ni contra CSRF.",
                "donde": "backend/src/test/java/com/apptolast/organization/adapter/AutomationsApiTest.java:548-570 (los dos únicos tests) frente a features/automations.feature:485-487",
                "porQueBloquea": "Riesgo real bajo, porque SecurityConfiguration:127-130 aplica csrf().withDefaults() y el OriginGuard a toda la cadena sin ignoringRequestMatchers, así que hoy la protección es estructural. Pero la fila del contrato existe precisamente para fijar que /simulate no se exceptúa, y hoy no hay nada que lo impida sin que la suite lo note.",
                "gravedad": "menor"
              },
              {
                "que": "Toda la evidencia de @s42 (matriz responsive de 320/768/1280/1440, texto al 200 %, zoom nativo, objetivos de 44x44 y axe sin serious/critical) vive sólo en Playwright, y `bin/harness test` no lo ejecuta: scripts/project.mjs:478-481 corre `node --test scripts/project.test.mjs`, la tarea de Gradle y `pnpm --dir frontend test` (vitest). Los ficheros e2e sólo aparecen en la tarea `lint`, y ahí únicamente como `node --check` (comprobación de sintaxis). No hay ninguna mención a e2e en init.sh, init.ps1, bin/harness ni harness.config.json.",
                "donde": "scripts/project.mjs:458-481; package.json (test:e2e y test:e2e:browser son comandos aparte); e2e/automations-ux.spec.mjs, e2e/automations.spec.mjs, e2e/automations-native-zoom.spec.mjs",
                "porQueBloquea": "Las pruebas de @s42 están bien escritas —recorrido con Tab real contra el orden del DOM leído, anillo medido en cada parada, 44x44 en los cuatro anchos, axe en claro y oscuro—, pero el escenario no está cubierto por la puerta que cierra la feature: nada de eso corre en `bin/harness test` ni en `bin/harness verify`. Es una convención del repositorio que afecta también a webhooks y external_calendar, no un fallo de esta noche; por eso no lo marco bloqueante. Debe quedar escrito antes de poner `done`, junto a la condición 4 del juez.",
                "gravedad": "serio"
              },
              {
                "que": "@s6 filas 4 y 5 y @s17 tercera cláusula quedan sin oráculo. En @s6, «criterionTemplate de exactamente 2000 puntos de código» y `criterionTemplate \"\"` nunca se envían por la API para comprobar el «se guardan byte a byte como se enviaron»: el único test de @s6 usa la fila de las llaves sueltas, y AutomationDraftTest sólo comprueba que 2000 se acepta en el constructor (el criterio vacío no se prueba en ninguna parte, aunque CreateTaskAction sí lo permita: la guarda de isEmpty() está sólo sobre titleTemplate). En @s17, «las 3 tareas creadas tienen createdAt en ese mismo orden» no tiene nada: AutomationEffect.CreateTask no lleva instante y el unitario sólo afirma hasSize(3).",
                "donde": "features/automations.feature:95-96 y :238; backend/src/test/java/com/apptolast/organization/adapter/AutomationsApiTest.java:331-359; backend/src/test/java/com/apptolast/organization/domain/AutomationDraftTest.java:36; backend/src/test/java/com/apptolast/organization/application/ExecuteAutomationsTest.java:175",
                "porQueBloquea": "Cláusulas del contrato sin ninguna aserción. La de @s17 ya está registrada por el juez como H3; la de @s6 no está en su dictamen porque su revisión sólo entró en los nueve escenarios del ejecutor. Ninguna de las dos justifica por sí sola parar el cierre, pero las dos deben quedar anotadas como deuda con destinatario, no desaparecer.",
                "gravedad": "menor"
              }
            ],
            "loQueSostieneElCierre": "Lo he verificado yo, leyendo el árbol; no he ejecutado nada (respetado el modo solo lectura: cero suites, cero campañas, ni siquiera una clase suelta).\n\nRecorrido completo del .feature: los 43 escenarios y sus tablas de Examples, mapeados uno a uno contra los 15 ficheros de prueba del backend, los 3 de vitest y los 3 de Playwright. La cobertura nominal es buena de verdad: no hay ningún escenario huérfano y casi ninguna fila lo está.\n\nTres escenarios verificados a fondo, borrando mentalmente la producción:\n- @s34 (paginación de veinte en veinte). Parece la tautología típica —`hasSize(ReadAutomationRuns.PAGE_SIZE)`— pero NO lo es: la segunda aserción, `hasSize(5)` sobre un fixture de 25 ejecuciones (ReadAutomationRunsTest.java:47-56), clava PAGE_SIZE en 20 exactamente. Si fuera 10, la segunda página traería 10 y la prueba caería. Discrimina. La versión API (AutomationsApiTest:613) usa `content().json(..., true)` en modo estricto, así que el «cada item contiene exactamente» de las diez claves también está atado. Lo mismo @s30 (:661).\n- @s28 (guarda de bucles). El ejecutor consulta la guarda de verdad: el doble de ExecuteAutomationsTest es un lambda, no una reimplementación, y si ExecuteAutomations dejara de mirar matcher.loopGuarded las dos filas de :439 caerían. Y la guarda real —el SQL de PostgresAutomationEvents:102-109— sí tiene su oráculo contra Postgres, con los tres casos que importan: propia true, ajena false, humana false, y la fila 2 (regla borrada) sostenida por AutomationPersistenceTest:287-298, que comprueba que la ejecución sobrevive al borrado y la guarda sigue diciendo true.\n- @s8 (límites por puntos de código). Es el bloque mejor probado de la feature: AutomationDraftTest:27-45 usa emoji (par suplente) y prueba las dos orillas —80 acepta / 81 rechaza, 160 y 2000 y 1440 aceptan, 161 y 2001 y 0 y 1441 rechazan—, así que sustituir codePointCount por length() muere. Las 14 filas del Outline están, entre esa clase y las dos parametrizadas de AutomationsApiTest:361-411.\n\nOtras comprobaciones que hice y salieron limpias: los 12 disparadores publicados y los 4 rechazados (ValueSource literal, no derivado del enum); los DTO cerrados de trigger y de las dos formas de action (:964, :980); las 7 filas de If-Match de @s13 incluido el «*»; @s9 con su cláusula del PUT a cupo lleno; @s10 concurrente contra el índice único real; @s37 con las tres pruebas de navegación, incluido el «sin desplazar Hoy» medido sobre links[0]; @s42 con 44x44, Tab real y axe en los cuatro anchos.\n\nLo que NO he verificado: no he contrastado los rangos línea:columna de stryker.automations.config.json contra el fichero de producción de hoy (H1 del juez sigue vivo hasta que alguien lo mida), ni he mirado los 26 supervivientes de PIT ni los 2 de Stryker uno por uno. Y las cifras de mutación que me han dado no las he podido corroborar."
          },
          {
            "feature": "30 — automatizaciones",
            "lente": "seguridad y datos",
            "rechazaria": true,
            "motivos": [
              {
                "que": "`queue()` confunde «el endpoint ya no es un endpoint activo de este propietario» con «otro worker se me adelantó», y el ejecutor descarta el evento entero en silencio, sin fila de ejecución. El INSERT de la entrega no tiene ON CONFLICT y el id de entrega es un UUID recién sorteado: `affected == 0` NUNCA puede significar un reclamo perdido. Sólo puede significar que el SELECT no casó, es decir que `e.owner_id`/`e.status='active'` fallaron (o que la fila de outbox no está). Aun así se lanza `AutomationClaimedException`, que `ExecuteAutomations:89` interpreta como «ya lo hizo otro» y responde `return true` SIN llamar a `record()`. La transacción `confirming` ya ha revertido: se pierde la ejecución de la regla de webhook, la de CUALQUIER otra regla que casara el mismo evento (p. ej. un CREATE_TASK ya aplicado en el mismo `apply()`), su tarea, su TaskCreated.v1 y el avance del cursor. Pero el paseo continúa, y el commit del evento siguiente hace UPSERT del cursor a su posición absoluta (`advance`, :212): el evento saltado no se vuelve a leer jamás. El contrato pide `attempt 1, status failed, errorCode ENDPOINT_NOT_FOUND` (features/automations.feature:282 y filas 4-5 de :291-292) y una fila por (regla, evento) (:263); aquí no queda ni una línea de bitácora, porque `log()` sólo se llama tras un commit bueno. No hace falta un atacante: el worker de la feature 25 desactiva endpoints solo (`PostgresWebhookWork.disable`, :108-121) en el mismo proceso, así que la ventana entre `endpoints.isActiveEndpointOf` (ExecuteAutomations:178, fuera de transacción) y el INSERT es una condición de producto normal, no exótica.",
                "donde": "backend/src/main/java/com/apptolast/organization/adapter/persistence/PostgresAutomationWork.java:157-175 (y su lectura en application/ExecuteAutomations.java:81-90)",
                "porQueBloquea": "Es pérdida silenciosa y permanente de ejecuciones, justo la promesa del encabezado del contrato («sin perder el control de lo que se ejecutó y por qué»), y viola dos cláusulas explícitas (@s21 filas de webhook y el invariante de :263). Además NO tiene ninguna prueba: `AutomationClaimedException` no aparece en ningún test del árbol; la única prueba que llega a este SQL es el camino feliz de @s26 (AutomationExecutionTest.java:133-161), con un endpoint activo del propietario. Y por ser una rama guardada por literales SQL, PIT no le generará mutante ni con la campaña que exige la condición 2 del juez.",
                "gravedad": "bloqueante"
              },
              {
                "que": "Los cuatro predicados de aislamiento/estado del `AutomationWork` son literales de SQL sin ningún oráculo: `window()` `WHERE owner_id = ?` (:271-274), `withTheirRuns` `WHERE owner_id = ?` (:252-256), `queue()` `e.owner_id = ? AND e.status = 'active'` (:167) y `claim()` `AND status = 'retry'` (:192). `PostgresAutomationWork` sólo se ejercita a través de `execute.runCycle()` en `AutomationExecutionTest`, y CADA test de esa clase crea un propietario nuevo y único (`owner()`, :250-258): no hay un solo test en el que el ciclo de A corra mientras B tiene eventos, reglas o endpoints que casen. `AutomationPersistenceTest` sí hace ese trabajo, y bien, pero sólo para `PostgresAutomationStore`, `PostgresAutomationRuns` y `PostgresAutomationEvents` (`recent(owner(),100)` vacío, `projectOfTask(owner(),…)` vacío, `createdByAutomation(owner(),…)` falso). El `AutomationWork` se quedó fuera. La rama de reintento de `claim()` (attempt > 1) no se ejecuta NUNCA contra Postgres: @s20/@s22 viven enteros en `ExecuteAutomationsTest` con `FakeWork`.",
                "donde": "backend/src/main/java/com/apptolast/organization/adapter/persistence/PostgresAutomationWork.java:167, :192, :252, :271",
                "porQueBloquea": "Es exactamente el punto ciego que esta noche ha destapado diez casos: PIT no muta cadenas, así que la campaña que la condición 2 exige sobre esta misma clase informará cobertura alta y no generará un solo mutante para ninguno de los cuatro. Borrar `AND e.owner_id = ?` de :167 dejaría la suite verde y entregaría el payload de A a la cola de entregas de B, firmado con el secreto de B y enviado a la URL de B — el `INSERT … SELECT` toma `e.owner_id` del endpoint y `o.payload` del evento de A. Hoy hay cinturón (la comprobación en Java) y tirantes (el SQL), pero los tirantes no los sujeta ninguna prueba, y `AND status = 'retry'` es justo la guarda cuya ausencia el juez señaló como H6 en `upsert()`.",
                "gravedad": "serio"
              },
              {
                "que": "`runCycle()` no aísla el fallo de un propietario del resto. `for (var owner : work.ownersWithRules()) walk(owner);` no envuelve nada, `walk()` deja escapar lo que lancen `work.cursor`, `startCursor` y `work.after`, y `process()` calcula `outcomes` (con `matcher.loopGuarded`, que llama a `AutomationEvent.uuid(\"taskId\")` → `IllegalArgumentException` si el payload no lo trae) FUERA del try; además `rows.forEach(work::record)` (:96) va dentro del catch, sin protección. Cualquier fallo atribuible a un propietario aborta el ciclo para todos los que `SELECT DISTINCT owner_id` devuelva después de él. `AutomationSchedule.tick` lo traga y registra sólo `failure.getClass().getSimpleName()`: ni propietario, ni evento, ni causa.",
                "donde": "backend/src/main/java/com/apptolast/organization/application/ExecuteAutomations.java:53, :81-97 (y adapter/config/AutomationSchedule.java:24-31)",
                "porQueBloquea": "Es aislamiento entre propietarios en su forma de disponibilidad: un dato envenenado de un propietario deja sin automatizaciones a todos los demás, indefinidamente y sin diagnóstico posible (una línea de warn por segundo con el nombre de una clase). Ningún test corre un ciclo con más de un propietario; la cláusula «sin detener las demás reglas» de @s21 se afirma sólo dentro de un evento de un propietario, así que el ámbito mayor no tiene oráculo.",
                "gravedad": "serio"
              },
              {
                "que": "`queue()` sella `next_attempt_at`, `created_at` y `updated_at` de la entrega con `notify.event().occurredAt()`, el instante del EVENTO, no el instante en que se encola. En el propio escenario @s15 (worker apagado, se enciende y camina el atraso) toda entrega de automatización nace con las tres marcas en el pasado. El `created_at` que la feature 25 muestra al propietario deja de ser cuando se creó la entrega, y el índice `webhook_deliveries_due (next_attempt_at, id) WHERE status='pending'` (V23__webhooks.sql:53-54) las coloca por delante de todas las entregas legítimas.",
                "donde": "backend/src/main/java/com/apptolast/organization/adapter/persistence/PostgresAutomationWork.java:159, :163, :170-172",
                "porQueBloquea": "El oráculo de @s26 (AutomationExecutionTest.java:143-150) afirma endpoint_id, event_id, event_type, status y body — ninguna de las tres marcas de tiempo. Nada notaría que están mal, ni en un sentido ni en el otro, así que es una tercera conducta de este adaptador que depende de un literal sin ningún testigo.",
                "gravedad": "menor"
              },
              {
                "que": "El editor no tiene control para `action.projectId` ni para `action.estimatedMinutes`: los únicos controles son `automation-name`, `automation-trigger`, `automation-condition`, `automation-title` y `automation-criterion`. Una regla nueva apunta siempre a `projects[0]?.id ?? \"\"` (`blank`, :91-102). Si la lista de proyectos aún no ha llegado o está vacía, Guardar envía `projectId: \"\"`, el servidor responde 400 sobre `action.projectId`, y `controlIdOf` no tiene entrada para ese campo: cae en el `?? \"automation-name\"` y cuelga el mensaje del servidor — y mueve el foco — al campo Nombre. Lo mismo con `action.estimatedMinutes` y `action.type`.",
                "donde": "frontend/src/automations.tsx:91-102, :461-519 y :615-625",
                "porQueBloquea": "@s38 pide que el editor «asocie cada error del servidor a su campo», y aquí un error de destino se atribuye visiblemente a otro campo, con el foco incluido; la prueba de @s38 sólo ejercita `action.criterionTemplate`, que sí está en el mapa, así que la rama por defecto de `controlIdOf` no tiene oráculo. Es menor porque no filtra nada y sólo desorienta.",
                "gravedad": "menor"
              }
            ],
            "loQueSostieneElCierre": "He leído entero, no en diagonal: `PostgresAutomationWork`, `PostgresAutomationStore`, `PostgresAutomationRuns`, `PostgresAutomationEvents`, `ExecuteAutomations`, `AutomationMatcher`, `AutomationRendering`, `AutomationReferences`, `SimulateAutomation`, `ReadAutomationRuns`, `AutomationController`, `AutomationBody`, `AutomationView`, `AutomationRunCursorCodec`, `AutomationCursor`, `AutomationEvent`, `AutomationSchedule`/`AutomationConfiguration`, `Slf4jAutomationAudit`, `V28__automations.sql`, `V1`/`V2`/`V23`, `SecurityConfiguration`, `ApiCredentialBearerFilter`, `OutboxMessage`, y del lado de pruebas `AutomationExecutionTest`, `AutomationWiringTest` y `AutomationPersistenceTest` completos, más `automations.tsx` y `automations-api.ts` enteros. No he ejecutado nada (ni suite, ni init, ni mutación).\n\nLo que SÍ he verificado que está bien y no debe volver a discutirse:\n\n- **Fugas de secretos: ninguna.** El puerto `AutomationAudit` admite sólo UUID, String de estado, int y código; el título renderizado y el nombre del proyecto no caben por tipo, no por disciplina. `Slf4jAutomationAudit` escribe exactamente esos cinco. `AutomationSchedule` registra sólo `getSimpleName()`. Los DTO de `AutomationView` son records cerrados por variante, con `WebhookAction`/`WebhookPreview` llevando sólo `endpointId`: el `secret_ciphertext` del endpoint no aparece en ninguna consulta de esta feature. Los mensajes de error de `AutomationBody` y de `ReadAutomationRuns` son literales fijos («Revisa el valor de este campo.»), no eco de la entrada.\n- **Aislamiento en las rutas HTTP: correcto.** Todas las consultas de reglas y ejecuciones llevan `owner_id = ?` (`PostgresAutomationStore` en create/list/find/replace/delete/locked, `PostgresAutomationRuns.select()`), el `locked()` hace `FOR UPDATE` con owner e id y devuelve 404 antes de mirar la versión, y `ReadAutomationRuns:26` rechaza un cursor cuyo `ruleId` no sea el pedido (@s35) antes de tocar el almacén. `AutomationReferences` valida condición, proyecto destino y endpoint contra el propietario en create, replace **y** simulate, con el bean real de `webhookEndpointLookup` (`ApplicationConfiguration:682-694`, `id = ? AND owner_id = ? AND status = 'active'`) y con el oráculo de cuatro casos de `AutomationWiringTest:141-156`.\n- **Canal bearer: cerrado por lista blanca.** Comprobé que la cadena de `Order(1)` captura cualquier petición con `Authorization`, pero `ApiCredentialBearerFilter.PERMISSIONS` no incluye ninguna ruta `/api/v1/me/**`: una credencial de integración recibe 403 `API_SCOPE_DENIED` sobre las automatizaciones, no las alcanza. No hay escalada de privilegio por token.\n- **`PostgresAutomationEvents` está bien probado en aislamiento**, y eso es lo que sostiene @s27: `recent(owner(),100)` vacío, `projectOfTask(owner(),…)` vacío, `createdByAutomation(owner(),…)` falso (`AutomationPersistenceTest`). `status <> 'blocked'` es seguro porque `outbox_events.status` es `NOT NULL DEFAULT 'pending'` (V1:18) — no hay trampa de NULL.\n- **`AutomationCursor.precedes` reutiliza `WebhookCursor.compareUnsigned`**, es decir la comparación sin signo que la feature 25 ya pagó; no repite el error de ordenación de UUID.\n- **Frontend, cláusulas de identidad: con oráculo real.** `Automations` remonta por `key={owner}` y `automations.test.tsx:500-547` lo comprueba de verdad (simula con «ana», rerender con «bruno», y afirma que el título resuelto desaparece, que no queda región de simulación ni editor, que hay dos GET y que `localStorage`/`sessionStorage` siguen vacíos). No hay `localStorage`, `sessionStorage` ni `dangerouslySetInnerHTML` en `automations.tsx`; `previewTemplate` produce texto que React escapa.\n\nNinguno de mis cinco motivos toca esas cinco cosas: lo que sostiene el cierre es la mitad HTTP y de dominio de la feature. Lo que lo impide vive entero en el adaptador nuevo de esta madrugada, `PostgresAutomationWork`, que es la única clase de la feature sin una prueba que la mire desde fuera con dos propietarios en la misma base."
          },
          {
            "feature": "30 — automatizaciones",
            "lente": "Lo que se rompió y lo que se tocó deprisa: historial reciente de los ficheros de la feature, cambios de producción sin prueba que los cubra, pruebas que no pueden fallar, aserciones relajadas y arreglos que silencian el síntoma. Contrastado contra los informes de mutación medidos (backend/build/reports/pitest-automations/mutations.xml y frontend/reports/mutation-automations/mutation.json), no contra las bitácoras.",
            "rechazaria": true,
            "motivos": [
              {
                "que": "La condición 2 del juez —BLOQUEANTE— está incumplida: no existe la lista nominal de supervivientes con veredicto escrito. Para el backend no hay ningún progress/mutation_automations_*.md; progress/mutacion_cinco_features.md sólo da conteos por clase. Los 26 mutantes no muertos del informe (14 SURVIVED + 12 NO_COVERAGE) no tienen veredicto en ninguna parte del repositorio. No es burocracia: leer esa lista es exactamente lo que destapa los cuatro hallazgos siguientes, y ninguno estaba visto.",
                "donde": "progress/mutacion_cinco_features.md:26-33 (frente a judge_automations_cierre.md, Condición 2) y backend/build/reports/pitest-automations/mutations.xml",
                "porQueBloquea": "El juez condicionó la aprobación a registrar cada superviviente con un veredicto escrito, precisamente porque el modo de fallo de este proyecto no es la puntuación baja. La puntuación se publicó (96 %) y la lista no se leyó.",
                "gravedad": "bloqueante"
              },
              {
                "que": "Prueba placebo con nombre que promete lo contrario. `s3_acceptsAConditionOverAnOwnProject` monta `when(create.create(eq(\"owner\"), any())).thenReturn(rule(1, conditioned))` y luego afirma `$.condition.projectId`: está comprobando el eco de su propio stub, no lo que el parser produjo. PIT lo acredita: `AutomationBody::condition L48 replaced return value with null` SOBREVIVE. Con ese mutante, una regla que el propietario acota a un proyecto se guarda como «cualquier proyecto» —se dispara en todos sus proyectos— y toda la suite sigue verde. El único test que sí verifica el draft entregado al caso de uso (`verify(create).create(eq(\"owner\"), eq(draft()))`) usa un draft con condition null, así que la rama no nula no la mira nadie.",
                "donde": "backend/src/test/java/com/apptolast/organization/adapter/AutomationsApiTest.java:165-189 y :901; superviviente en backend/src/main/java/com/apptolast/organization/adapter/http/AutomationBody.java:48",
                "porQueBloquea": "@s3 es una cláusula de alcance: una regla acotada que pasa a global es ampliación silenciosa de efectos sobre datos del propietario. La prueba que la cubre no puede fallar, y el informe de mutación lo dice desde las 04:26 sin que nadie lo leyera.",
                "gravedad": "serio"
              },
              {
                "que": "`ExecuteAutomations` L143, `lambda$attemptOf$0 -> true`, SOBREVIVE. Es el filtro `run -> rule.id().equals(run.ruleId())` que escoge la ejecución previa DE ESTA regla entre las del evento. Ningún test del árbol le da a un evento ejecuciones previas de más de una regla: `withRuns(...)` recibe un único run y todos los montajes usan `givenARuleThatCreatesTasks()` con una sola regla. Con el mutante, si R1 quedó `failed` sobre E y R2 sigue en `retry`, R2 se salta para siempre (o hereda el runId y el número de intento de R1). Es justo el cruce de @s21 («sin detener las demás reglas») con @s22 (el reintento), y no tiene oráculo.",
                "donde": "backend/src/main/java/com/apptolast/organization/application/ExecuteAutomations.java:143; montajes en backend/src/test/java/com/apptolast/organization/application/ExecuteAutomationsTest.java:390-435",
                "porQueBloquea": "Es el motor, probado con un doble en memoria y en milisegundos: ahí el juez dijo que un superviviente no tiene coartada. El fallo que deja pasar es pérdida permanente y silenciosa de una automatización del propietario.",
                "gravedad": "serio"
              },
              {
                "que": "Cinco mutantes NO_COVERAGE en `PostgresAutomationWork`: `record L126` (dos: el `executeWithoutResult` y el `upsert` de dentro), `startCursor L85`, `write L341` y `runOf L316`. O sea: el `upsert` con `ON CONFLICT (rule_id,event_id) DO UPDATE` —exactamente el H6 del juez, la escritura que puede pisar el `succeeded` de otro worker—, la creación del cursor inicial de un propietario nuevo (@s16) y el RowMapper que relee ejecuciones no los ejecuta ninguna prueba, ni unitaria ni con contenedor. @s16, @s20 y @s22 se sostienen sólo sobre el doble `FakeWork`.",
                "donde": "backend/src/main/java/com/apptolast/organization/adapter/persistence/PostgresAutomationWork.java:85, :124-130, :204-212, :315, :339-345",
                "porQueBloquea": "El juez pidió por escrito una decisión sobre H6 (arreglar o justificar) y exigió que esta clase recibiera mutantes. Recibe 38 y cinco caen en código que nadie ejecuta, incluida la línea del hallazgo. La mitad de recuperación del adaptador real —reintento, cursor inicial, relectura— está sin probar.",
                "gravedad": "serio"
              },
              {
                "que": "`AutomationView$Rule::enabled -> true` SOBREVIVE y no hay una sola aserción de `enabled:false` en toda `AutomationsApiTest`. La fila 1 de @s12 («enabled false → la lectura devuelve enabled false») no tiene oráculo en la capa HTTP; sólo en la de aplicación (`AutomationWiringTest.java:117`, sobre el draft, no sobre el JSON). Es una familia: los accesores de `AutomationView` sólo se afirman en el caso feliz CREATE_TASK — también sobreviven `Run::deliveryId`, `Match::loopGuarded`, `WebhookPreview::eventId`, `TaskPreview::completionCriterion` y `AutomationRun::deliveryId`.",
                "donde": "backend/src/main/java/com/apptolast/organization/adapter/http/AutomationView.java:39, :52, :68, :77, :79; ausencia en backend/src/test/java/com/apptolast/organization/adapter/AutomationsApiTest.java",
                "porQueBloquea": "El interruptor del frontend se pinta con ese campo del JSON. Una regla pausada mostrada como activa es la clase de fallo que el contrato nombra explícitamente en @s12, y el juez dio @s12 por cerrada salvo la fila 4.",
                "gravedad": "serio"
              },
              {
                "que": "La cifra publicada «96,00 %» no está medida. El `mutations.xml` da 581 muertos de 607 = **95,72 %**; el 96 sale del entero redondeado del index.html de PIT. Dos decimales que nadie calculó, en un documento cuyo encabezado dice «todas las cifras están medidas, no declaradas». Además la campaña es de las 04:26 y el superviviente de `AutomationConfiguration` se mató a las 04:28 (5309e409): la cifra publicada ya no describe el árbol.",
                "donde": "progress/mutacion_cinco_features.md:11 frente a backend/build/reports/pitest-automations/mutations.xml",
                "porQueBloquea": "Es exactamente la infracción de AGENTS.md:51 que el juez persiguió en H5, en pequeño y en el documento que hace de puerta.",
                "gravedad": "menor"
              },
              {
                "que": "`Slf4jAutomationAudit` está ahora en el ámbito de PIT pero recibe **cero** mutantes: no aparece ni una vez en el XML. La condición 2 decía «clases que tienen que recibir mutantes sí o sí». La explicación del documento («ningún adaptador de bitácora los recibe: es una propiedad de los mutadores») acierta en el fondo pero no nombra la causa —el filtro `FLOGCALL` de PIT, activo por defecto, que descarta las mutaciones dentro de llamadas a logging— ni cierra el hueco: a diferencia de la feature 25, que escribió `Slf4jWebhookAuditTest`, aquí no hay test unitario del adaptador. La línea que afirma @s19 sigue medida por una campaña que no le genera mutantes.",
                "donde": "backend/build.gradle.kts:570 (patrón en ámbito), backend/src/main/java/com/apptolast/organization/adapter/logging/Slf4jAutomationAudit.java, sin Slf4jAutomationAuditTest en backend/src/test",
                "porQueBloquea": "Meter el patrón satisface la letra de H7 y no su motivo. La condición se declaró cerrada; la clase sigue sin un solo mutante.",
                "gravedad": "menor"
              },
              {
                "que": "`ExecuteAutomations` L97 sobrevive dos veces (`removed call to List::forEach` y, dentro del lambda, `removed call to ::log`): la bitácora del camino de fallo se puede borrar entera sin que caiga ninguna prueba. El contrato sólo exige el log en @s19 (camino de éxito), así que no es incumplimiento; pero es la misma línea que la feature presenta como su evidencia de auditoría, y la que el juez validó con lupa en la sección 6.",
                "donde": "backend/src/main/java/com/apptolast/organization/application/ExecuteAutomations.java:97",
                "porQueBloquea": "No bloquea por contrato. Pide un veredicto escrito, que es justo lo que falta (motivo 1).",
                "gravedad": "menor"
              },
              {
                "que": "La evidencia del frontend no es reproducible tal como está commiteada. `progress/verificacion_mutantes_automations.json`, que la bitácora declara como «el veredicto de cada uno», contiene **11** de los 198 —el script lo sobrescribe entero en cada pasada, y el resto sólo sobrevive en el historial de git de ese fichero—; y uno de esos 11 está grabado como SOBREVIVE. El propio `scripts/verificar-mutantes-automations.mjs` conserva hoy 178 anclas: las 23 del racimo 3 ya no están en ninguna parte. Detalle operativo: el script escribe sobre `frontend/src/automations.tsx` y `automations-api.ts` reales y, si se interrumpe, los deja mutados.",
                "donde": "progress/verificacion_mutantes_automations.json, scripts/verificar-mutantes-automations.mjs, progress/mutacion_automations_frontend.md:19-22",
                "porQueBloquea": "El método de acreditación es bueno; el rastro que deja no permite comprobarlo sin reconstruirlo desde git. Y la medida real (91,18 %) ya cubre el umbral, así que esto es higiene, no puerta.",
                "gravedad": "menor"
              },
              {
                "que": "El arreglo de `actionOf` es correcto y mínimo, pero dejó sin oráculo la rama que hizo inofensiva: los ternarios de `editingOf` que vacían los campos de tarea para una regla de webhook acumulan **diez supervivientes** medidos (L110:16, L110:77, L112:72, L114:7, L116:11, L118:7 ×2 + LogicalOperator, L119:7, L121:11). Al devolver intacta la acción, esos valores dejaron de viajar en el cuerpo del PUT y sólo se ven en la pantalla; y ninguna prueba afirma qué muestra el editor al abrir una regla de webhook. El defecto anotado nº 3 no tiene oráculo alguno.",
                "donde": "frontend/src/automations.tsx:104-138 (editingOf/actionOf); supervivientes en frontend/reports/mutation-automations/mutation.json",
                "porQueBloquea": "No bloquea —el fichero mide 84,60 % y el ámbito 91,18 %—, pero es el patrón que la lente busca: el arreglo movió el síntoma fuera del alcance de las pruebas en vez de cerrar la rama.",
                "gravedad": "menor"
              }
            ],
            "loQueSostieneElCierre": "Verificado por mí, no leído: (1) La condición 1 del juez, la única bloqueante que quedaba, está VERDE — ejecuté `node --test --test-name-pattern \"automations Stryker configuration\" scripts/project.test.mjs` → 1 pass, 0 fail. Además la guarda ya no repite la constante: valida los cuatro rangos por contenido (scripts/project.test.mjs:2212-2249) y comprobé a mano que `App.tsx:49` cae sobre `automations = route === \"/automatizaciones\"`, `61:12-62:32` sobre la etiqueta de sección, `94:10-95:40` sobre `<Automations owner={username} />` y `workspace.tsx:138` sobre el `<RouteLink>`. (2) Las cifras de mutación son reales y las reproduje parseando los informes: backend 607 mutantes, 581 muertos, 14 supervivientes, 12 sin cobertura; frontend 884 / 806 muertos = 91,18 %, con automations-api.ts en 99,73 % y automations.tsx en 84,60 %, y el informe de las 12:26 es posterior al último commit de la feature (12:09). (3) Los umbrales duros de la condición 2 se cumplen: ExecuteAutomations 48/51 = 94,1 % (≥ 0,90 exigido), global backend 95,72 % y frontend 91,18 % (≥ 80). (4) Las seis clases que el juez exigió están todas en `automationsClasses` (backend/build.gradle.kts:545-578) y las cinco primeras reciben mutantes de verdad; sólo `Slf4jAutomationAudit` recibe cero, por el filtro FLOGCALL de PIT. (5) El único cambio de producción de la noche —`actionOf` en automations.tsx— es real, mínimo, tiene rojo acreditado y tres pruebas nuevas que lo cubren; revisé su diff línea a línea. (6) El commit `c414a41b style:` es formato puro: comprobé el diff completo, no relaja ni una aserción. (7) Las pruebas @s43 de aborto (automations.test.tsx:1149-1180) miran el `AbortSignal` capturado, no la pantalla: discriminan de verdad. (8) `AutomationCursor::precedes L20` figura como superviviente pero es equivalente: el mutante de frontera `> 0` → `>= 0` está bajo la guarda `byInstant != 0`. No lo cuento como hallazgo. NO he ejecutado la suite, ni `harness init`, ni ninguna campaña."
          }
        ]
      }
    ],
    "sintesis": "**VEREDICTO: ninguna de las dos se cierra.**\n\nVerificado por mí sobre el árbol (sin ejecutar suite, init ni campañas; sólo lectura de código y parseo de los informes ya escritos en disco).\n\n---\n\n## 1. Feature 27 — github: NO CERRAR\n\n### Bloqueantes que sobreviven\n\n**B1. @s41 fila 1: cláusula del contrato que el producto incumple y ningún expect() mide.** (dos jueces coinciden; verificado)\n- `features/github_connector.feature:525` exige «repository conserva \"octocat/Hello-World\"».\n- `frontend/src/App.tsx:90-99`: `<GithubConnector>` se renderiza dentro de la cadena de ternarios por ruta → salir de /integraciones/github lo desmonta. `frontend/src/github-connector.tsx:61`: `useState(\"\")`.\n- `frontend/src/github-connector.test.tsx:540-557`: el test se llama «keeps the repository» y tras `view.unmount()+render()` sólo afirma `toHaveValue(\"\")` del token y el innerHTML. Cero aserciones sobre repository.\n- **Para cerrar:** o se implementa (subir el estado / releerlo) y se añade `expect(getByLabelText(/repositorio/i)).toHaveValue(\"octocat/Hello-World\")`, o se enmienda la fila con contrafirma del propietario. Hoy esa aserción sale roja.\n\n**B2. `Slf4jConnectorAudit`: cero mutantes, y está nombrada en el ámbito.** (verificado en la campaña NUEVA)\n- `backend/build.gradle.kts:135` la declara; `grep -c Slf4jConnectorAudit backend/build/reports/pitest-github-connector/mutations.xml` = **0**.\n- Causa: `backend/build.gradle.kts:725-735` fija `excludedMethods` pero no `avoidCallsTo`, así que el `avoidCallsTo` por defecto de PIT suprime las llamadas a slf4j y la clase es puro logging.\n- Es la condición dura del juez y el mismo defecto ya declarado bloqueante para `Slf4jWebhookAudit` (feature 25).\n- **Para cerrar:** `avoidCallsTo.set(emptySet())` en el ámbito, o un `Slf4jConnectorAuditTest` como el de la 25, y re-medir.\n\n**B3. `GithubIssueConnections` no está en NINGÚN ámbito: cero mutantes.** (verificado)\n- `backend/build.gradle.kts:111-136` no la nombra (su gemela `GitlabIssueConnections*` sí está, en `:89`). Recuento en el XML: **0 mutantes**.\n- Es la clase que fija `source()=\"github\"` y la que implementa `invalidate()` (el 401 de @s22): un `VOID_METHOD_CALLS` sobre `invalidate` significaría «el token rechazado se sigue reenviando» y no se genera en ninguna campaña.\n- **Para cerrar:** añadirla a `githubConnectorClasses` y re-medir.\n\n**B4. Falta `progress/mutation_github_connector.md` (condición 8 del juez).**\n- La campaña **completa** (mutations.xml + index.html, mtime 12:35) da **468/502 = 93,23 %**, con **24 SURVIVED + 10 NO_COVERAGE**. Ninguno tiene veredicto escrito en ningún fichero del repo (sólo hay los dos de frontend).\n- Los 10 sin cobertura, por clase: `BoundedResponse` 1, `GithubConnectorController` 1, `HttpGithubIssueSource` 1, `HttpGitlabIssueSource` 3, `IssueImportReceipt` 2, `PostgresIssueImportReceiptStore` 2.\n\n**B5. El contrato se enmendó sin contrafirma.** (verificado en `git show a1b0d20b`)\n- Cambia `features/github_connector.feature:146` («once campos» → «doce campos») y `:165-166` (recibo `repository` → `source` + `projectPath`, más dos aserciones nuevas), sin comentario de enmienda. Las dos enmiendas anteriores del mismo fichero sí lo llevan (`:55` y `:397`, «ratificada por el propietario»); `grep ratificad` sólo devuelve esas dos.\n- CLAUDE.md, regla dura. Y es el mismo rasero con el que está bloqueada la 29.\n- **Para cerrar:** comentario de enmienda + contrafirma del propietario sobre esas dos líneas.\n\n### Desmentidos (NO actuar sobre ellos)\n\n- **El superviviente de frontera de `AesGcmSecretCipher:60` ya NO existe.** El juez 3 leyó un informe de las 11:52 y el juez 1 leyó ficheros truncados a media escritura. El artefacto vigente (12:35, XML cerrado, 502 mutaciones) da `AesGcmSecretCipher` línea 60 `ConditionalsBoundaryMutator` = **KILLED**, y los dos `NegateConditionals` de :60 y el de :59 también. La cifra a publicar es **468/502 = 93,23 %**, no 467 ni «93,03 %». (Ojo: si hay una campaña reescribiendo ese directorio ahora mismo, re-leer antes de citar.)\n- **El hallazgo del U+00A0 en `ExternalIssue` es FALSO.** Los ficheros SÍ llevan los caracteres literales, comprobado byte a byte: `ExternalIssueTest.java:20` es `\" \\u2003Arreglar login\\u00a0\"` y `:21` es `\" \\u00a0 \"`; `ExternalIssueCriterionTest.java:23` es `{\"\", \"   \", \"\\u00a0\\u2003\\n\\r\\n\\t\"}`. Sustituir el regex por `String.strip()` rompe la fila `:21`. El oráculo discrimina. Tiene razón el juez que no lo cita.\n\n### Deuda seria a dejar escrita (no bloquea)\n\n1. **Recibo `running` colgado por clave rotada.** `ImportIssues.java:74` inserta el recibo y `:99-113` sólo captura `IssueSourceException`, `ProjectCompletedException` y `StorageUnavailableException`; `:145-148` lanza `SecretUndecipherableException` (RuntimeException pura), que se escapa. Resultado: 503 CONNECTOR_KEY_MISMATCH y recibo `running` con `finished_at NULL` durante 15 min, que da 409 IMPORT_IN_PROGRESS falso. Único test: `GithubConnectorApiTest:708-709`, con mock en la frontera HTTP, que no ve el recibo. El contrato no nombra ese código, por eso es deuda y no incumplimiento.\n2. **Ámbito contaminado:** `build.gradle.kts:130` (`adapter.connectors.*`) arrastra `GitlabApiBase` y `HttpGitlabIssueSource`, que además están una por una en `:106-107` — contradice el comentario de `:81-83`. En la campaña vigente `HttpGitlabIssueSource` aporta 3 NO_COVERAGE y 4 SURVIVED. Y `ImportGuard*` (`:121`) sólo lo usan `ConnectGitlab`/`DisconnectGitlab`.\n3. **Cláusulas del Then sin oráculo:** @s3 (`:38`), @s29 «cero recibos nuevos y cero peticiones» — `ImportGithubIssuesTest.java:432-462` sólo afirma la excepción —, @s27 (`:434-436`), @s41 fila 4, y el `GREATEST(?, started_at)` de `PostgresIssueImportReceiptStore.java:135` (@s12, `:157`).\n4. `PersonalAccessToken.hint()` devuelve el token entero con ≤4 caracteres y el dominio no impone mínimo; se persiste en claro como `token_hint`.\n5. `ReadIssueImport` / `PostgresIssueImportReceiptStore.read()` no filtran por `source`, a diferencia de `latest()`.\n\n---\n\n## 2. Feature 30 — automatizaciones: NO CERRAR\n\n### Bloqueantes que sobreviven\n\n**B1. Pérdida silenciosa y permanente de ejecuciones en `queue()`.** (verificado línea a línea)\n- `backend/.../persistence/PostgresAutomationWork.java:157-175`: el id de entrega es un `UUID.randomUUID()` recién sorteado y el INSERT no lleva `ON CONFLICT`, así que **`affected == 0` no puede significar «otro worker se adelantó»**: sólo puede significar que `e.owner_id = ? AND e.status = 'active'` dejó de casar. Aun así lanza `AutomationClaimedException`.\n- `application/ExecuteAutomations.java:87-90` la interpreta como «otro lo hizo» y hace `return true` **sin llamar a `record()`**: la transacción ya revirtió, se pierde la ejecución de esa regla y la de cualquier otra del mismo evento, y el commit del evento siguiente adelanta el cursor (`advance`), así que el evento no se vuelve a leer nunca.\n- El contrato pide `attempt 1, status failed, ENDPOINT_NOT_FOUND` (@s21). Ningún test del árbol menciona `AutomationClaimedException` (`grep` en `backend/src/test`: cero aciertos). Disparador normal: el worker de la 25 desactiva endpoints solo, entre el `isActiveEndpointOf` previo y el INSERT.\n- **Para cerrar:** que `queue()` no clasifique el 0 como reclamo (distinguir causa y registrar `failed`/`ENDPOINT_NOT_FOUND`), más un test de persistencia que desactive el endpoint entre la comprobación y la inserción.\n\n**B2. La interfaz se queda bloqueada para siempre cuando dos escrituras se cruzan.** (verificado)\n- `frontend/src/automations.tsx`: `save()` (:252-289), `simulate()` (:291-313) y `toggle()` (:315-337) comparten `writeRequest.current`. El `finally` del perdedor —`if (mounted.current && writeRequest.current === controller) setSaving(false)`— no se ejecuta nunca, así que `saving` (o `busyToggle`) queda en `true` de por vida: `if (!editing || saving) return` y `disabled={saving}` dejan Guardar inerte con el borrador dentro.\n- Incumple @s40 fila 1 (`features/automations.feature:533`, «el botón queda deshabilitado **hasta la respuesta**»).\n- Agravante: `automations.test.tsx:1018-1100` congela ese estado como correcto, y encima con una situación que **no está en los Examples de @s43** (`:568-571`: sólo «navega a /proyectos», «cierra sesión», «cambia a otra regla»).\n- **Para cerrar:** liberar el flag en el `finally` cuando el componente sigue montado, independientemente de la identidad del controlador (o un controlador por operación), y afirmar en el test que Guardar y el interruptor vuelven a ser usables.\n\n**B3. Falta la lista nominal de supervivientes de backend (condición 2 del juez).**\n- Parseado por mí: `pitest-automations/mutations.xml` = **607 mutantes, 581 KILLED, 14 SURVIVED, 12 NO_COVERAGE** → **95,72 %**, no el «96,00 %» publicado en `progress/mutacion_cinco_features.md:11`. No existe ningún `progress/mutation_automations_*.md`; los 26 no muertos no tienen veredicto escrito.\n\n**B4. @s4 fila «C completed → 201» sin ningún oráculo (barato de cerrar, pero es el patrón de la noche).**\n- `features/automations.feature:69` la exige. La única producción que la decide es el literal de `ApplicationConfiguration.java:668-676` («whatever its status» en el Javadoc). `AutomationWiringTest.java:45` tiene el helper `project(owner, status)` y las tres llamadas (`:82`, `:100`, `:126-127`) pasan siempre `\"active\"`. Añadir `AND status = 'active'` a esa cadena deja verde toda la suite y tumba la premisa de @s21 fila 1 y @s32 fila 1.\n- **Para cerrar:** un test que cree la regla contra un proyecto `completed` y espere 201.\n\n### Deuda seria a dejar escrita (no bloquea)\n\n1. `AutomationBody:48` `NullReturnVals` **SURVIVED** (verificado en el XML): una regla acotada a un proyecto se guardaría como global. La prueba de @s3 (`AutomationsApiTest.java:165-189`) afirma el eco de su propio stub.\n2. `ExecuteAutomations` tiene 3 supervivientes, entre ellos `:143` (`lambda$attemptOf$0 -> true`): ningún test da a un evento ejecuciones previas de más de una regla.\n3. `PostgresAutomationWork`: **5 NO_COVERAGE** (verificado), incluidos `record`/`upsert` L126 —el H6 del juez— y `startCursor` L85. @s16, @s20 y @s22 viven sólo sobre el doble `FakeWork`.\n4. `AutomationView$Rule::enabled -> true` sobrevive; no hay ninguna aserción de `enabled:false` en la capa HTTP (@s12 fila 1). Misma familia: `Run::deliveryId`, `Match`, `WebhookPreview`, `TaskPreview`.\n5. `Slf4jAutomationAudit`: **0 mutantes** (verificado), igual que en la 27 y por la misma causa (filtro de llamadas a logging). A diferencia de la 25, aquí no hay test unitario del adaptador.\n6. `ExecuteAutomations.java:53` y `:81-97`: `runCycle` no aísla el fallo de un propietario; un dato envenenado deja sin automatizaciones a todos los demás, y `AutomationSchedule` sólo registra `getSimpleName()`.\n7. Los cuatro predicados SQL de aislamiento de `PostgresAutomationWork` (`:167`, `:192`, `:252`, `:271`) sin ningún test con dos propietarios en la misma base.\n8. @s42 sólo tiene evidencia en Playwright, y `bin/harness test` no lo ejecuta (`scripts/project.mjs:458-481`).\n9. `automations.test.tsx:435` y `:463-466`: el fixture de @s41 usa el mismo UUID para proyecto y tarea, así que el enlace no discrimina.\n10. `automations.tsx:91-102` y `:615-625`: sin control para `action.projectId`/`estimatedMinutes`, el error del servidor y el foco caen en el campo Nombre (`?? \"automation-name\"`).\n\n---\n\n## 3. Contradicciones resueltas\n\n| Disputa | Quién acierta |\n|---|---|\n| 27: ¿superviviente de frontera en `AesGcmSecretCipher:60`? | **Nadie de los tres, ya está muerto.** La campaña vigente (12:35) lo da KILLED. Publicar 468/502 = 93,23 %. |\n| 27: ¿los `mutations.xml` están truncados? | Lo estaban cuando miró el juez 1; ahora el de github_connector está completo y cerrado. |\n| 27: ¿falta oráculo de U+00A0 en `ExternalIssue`? | **No falta.** Los tests llevan los caracteres literales. Descartar el hallazgo. |\n| 27: ámbito con `GithubIssueConnections` fuera y GitLab dentro | **Los dos jueces que lo dicen tienen razón**, confirmado en el XML (0 mutantes / `HttpGitlabIssueSource` con 7 no muertos dentro de la campaña de la 27). |\n| 30: ¿96,00 % o 95,72 %? | **95,72 %** (581/607, contado por mí). |\n\n## 4. Nota operativa\n\nNadie ha verificado el punto 5 del juez de la 27 (`harness init` verde) y `progress/current.md:30` lo da pendiente. No lo he ejecutado por la restricción de sesión: debe correrse antes de marcar `done`, cuando las dos campañas liberen la máquina."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Panel"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Sintesis"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "juez:27-github:contrato",
      "phaseIndex": 1,
      "phaseTitle": "Panel",
      "agentId": "ab66d0b93d89bd239",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789035814421,
      "queuedAt": 1789035814365,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Feature 27 — conector de GitHub (features/github_connector.…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites nada. NO ejecutes la suite completa, ni\nbin/harness init, ni campanas de mutacion: hay dos campanas corriendo y cuatro\ncarriles trabajando en la misma maquina. Lee todo lo que quieras; como mucho UNA\nclase de test o UN fichero de vitest.\n\nQUE SE JUEGA. El orquestador esta a punto d…",
      "promptFramed": false,
      "lastProgressAt": 1789036649515,
      "tokens": 247815,
      "toolCalls": 89,
      "durationMs": 835088,
      "resultPreview": "{\"feature\":\"Feature 27 — conector de GitHub (features/github_connector.feature, 42 escenarios)\",\"lente\":\"Cobertura del contrato: recorrido escenario a escenario del .feature buscando cláusulas del Then sin oráculo que pueda fallar, más verificación profunda de tres escenarios elegidos (s16 paginación, s24 proyecto terminado, s37/s38 formulario y resumen).\",\"rechazaria\":true,\"motivos\":[{\"que\":\"@s41…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "juez:27-github:seguridad",
      "phaseIndex": 1,
      "phaseTitle": "Panel",
      "agentId": "a24735957226fde72",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789035814423,
      "queuedAt": 1789035814366,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "27 — conector GitHub (`features/github_connector.feature`)",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites nada. NO ejecutes la suite completa, ni\nbin/harness init, ni campanas de mutacion: hay dos campanas corriendo y cuatro\ncarriles trabajando en la misma maquina. Lee todo lo que quieras; como mucho UNA\nclase de test o UN fichero de vitest.\n\nQUE SE JUEGA. El orquestador esta a punto d…",
      "promptFramed": false,
      "lastProgressAt": 1789036424374,
      "tokens": 193952,
      "toolCalls": 58,
      "durationMs": 608555,
      "resultPreview": "{\"feature\":\"27 — conector GitHub (`features/github_connector.feature`)\",\"lente\":\"seguridad y datos\",\"rechazaria\":true,\"motivos\":[{\"que\":\"La fila 1 de @s41 («navego a otra pantalla y vuelvo con la misma sesión → repository conserva \\\"octocat/Hello-World\\\" y el campo token está vacío») NO la cumple el producto y NO la mide ningún oráculo. `App.tsx:98-99` renderiza `<GithubConnector>` por render cond…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "juez:27-github:regresion",
      "phaseIndex": 1,
      "phaseTitle": "Panel",
      "agentId": "a4359f60ac005e6be",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789035814426,
      "queuedAt": 1789035814366,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "27 — conector GitHub (`features/github_connector.feature`)",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites nada. NO ejecutes la suite completa, ni\nbin/harness init, ni campanas de mutacion: hay dos campanas corriendo y cuatro\ncarriles trabajando en la misma maquina. Lee todo lo que quieras; como mucho UNA\nclase de test o UN fichero de vitest.\n\nQUE SE JUEGA. El orquestador esta a punto d…",
      "promptFramed": false,
      "lastProgressAt": 1789036277554,
      "tokens": 137165,
      "toolCalls": 38,
      "durationMs": 461733,
      "resultPreview": "{\"feature\":\"27 — conector GitHub (`features/github_connector.feature`)\",\"lente\":\"Lo que se rompió y lo que se tocó deprisa: historial reciente de los ficheros de la feature, cambios de producción sin prueba, aserciones que no pueden fallar, y arreglos que silencian el síntoma. Contrastado contra los artefactos de mutación reales en disco, no contra las cifras declaradas.\",\"rechazaria\":true,\"motivo…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "juez:30-automatizaciones:contrato",
      "phaseIndex": 1,
      "phaseTitle": "Panel",
      "agentId": "a80da5f5d05214e87",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789035814469,
      "queuedAt": 1789035814464,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "30 — automations",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites nada. NO ejecutes la suite completa, ni\nbin/harness init, ni campanas de mutacion: hay dos campanas corriendo y cuatro\ncarriles trabajando en la misma maquina. Lee todo lo que quieras; como mucho UNA\nclase de test o UN fichero de vitest.\n\nQUE SE JUEGA. El orquestador esta a punto d…",
      "promptFramed": false,
      "lastProgressAt": 1789036338518,
      "tokens": 177377,
      "toolCalls": 47,
      "durationMs": 522697,
      "resultPreview": "{\"feature\":\"30 — automations\",\"lente\":\"Cobertura del contrato: recorrido escenario a escenario de features/automations.feature (43 escenarios, ~120 filas de Examples) comprobando que cada cláusula tiene un oráculo que puede fallar\",\"rechazaria\":true,\"motivos\":[{\"que\":\"@s4 fila 2 («C completed → 201 y la regla se guarda») no tiene oráculo en ningún sitio del árbol. La única producción que decide es…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "juez:30-automatizaciones:seguridad",
      "phaseIndex": 1,
      "phaseTitle": "Panel",
      "agentId": "aef416fd7f67ec775",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789035814473,
      "queuedAt": 1789035814466,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "30 — automatizaciones",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites nada. NO ejecutes la suite completa, ni\nbin/harness init, ni campanas de mutacion: hay dos campanas corriendo y cuatro\ncarriles trabajando en la misma maquina. Lee todo lo que quieras; como mucho UNA\nclase de test o UN fichero de vitest.\n\nQUE SE JUEGA. El orquestador esta a punto d…",
      "promptFramed": false,
      "lastProgressAt": 1789036422927,
      "tokens": 202119,
      "toolCalls": 50,
      "durationMs": 607105,
      "resultPreview": "{\"feature\":\"30 — automatizaciones\",\"lente\":\"seguridad y datos\",\"rechazaria\":true,\"motivos\":[{\"que\":\"`queue()` confunde «el endpoint ya no es un endpoint activo de este propietario» con «otro worker se me adelantó», y el ejecutor descarta el evento entero en silencio, sin fila de ejecución. El INSERT de la entrega no tiene ON CONFLICT y el id de entrega es un UUID recién sorteado: `affected == 0` N…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "juez:30-automatizaciones:regresion",
      "phaseIndex": 1,
      "phaseTitle": "Panel",
      "agentId": "aea089c08d00183de",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789035814475,
      "queuedAt": 1789035814466,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "30 — automatizaciones",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites nada. NO ejecutes la suite completa, ni\nbin/harness init, ni campanas de mutacion: hay dos campanas corriendo y cuatro\ncarriles trabajando en la misma maquina. Lee todo lo que quieras; como mucho UNA\nclase de test o UN fichero de vitest.\n\nQUE SE JUEGA. El orquestador esta a punto d…",
      "promptFramed": false,
      "lastProgressAt": 1789036607997,
      "tokens": 182442,
      "toolCalls": 61,
      "durationMs": 792175,
      "resultPreview": "{\"feature\":\"30 — automatizaciones\",\"lente\":\"Lo que se rompió y lo que se tocó deprisa: historial reciente de los ficheros de la feature, cambios de producción sin prueba que los cubra, pruebas que no pueden fallar, aserciones relajadas y arreglos que silencian el síntoma. Contrastado contra los informes de mutación medidos (backend/build/reports/pitest-automations/mutations.xml y frontend/reports/…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "sintesis",
      "phaseIndex": 2,
      "phaseTitle": "Sintesis",
      "agentId": "aadc21d78f296fa4f",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1789036649519,
      "queuedAt": 1789036649517,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd C:/Users/vhurt/OneDrive/Escritorio/Proyectos/Organizacio…",
      "promptPreview": "Trabajas en C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb, rama main.\n\nERES DE SOLO LECTURA. No edites nada. NO ejecutes la suite completa, ni\nbin/harness init, ni campanas de mutacion: hay dos campanas corriendo y cuatro\ncarriles trabajando en la misma maquina. Lee todo lo que quieras; como mucho UNA\nclase de test o UN fichero de vitest.\n\nQUE SE JUEGA. El orquestador esta a punto d…",
      "promptFramed": false,
      "lastProgressAt": 1789036910016,
      "tokens": 111820,
      "toolCalls": 16,
      "durationMs": 260495,
      "resultPreview": "**VEREDICTO: ninguna de las dos se cierra.**\n\nVerificado por mí sobre el árbol (sin ejecutar suite, init ni campañas; sólo lectura de código y parseo de los informes ya escritos en disco).\n\n---\n\n## 1. Feature 27 — github: NO CERRAR\n\n### Bloqueantes que sobreviven\n\n**B1. @s41 fila 1: cláusula del contrato que el producto incumple y ningún expect() mide.** (dos jueces coinciden; verificado)\n- `featu…"
    }
  ],
  "totalTokens": 1252690,
  "totalToolCalls": 359
}