# Revisión independiente del montaje de tarea17

**APPROVED parcial en lectura** para los dos hashes observados y revalidados al cerrar. Sólo `frontend/src/work-session.tsx` y `frontend/src/work-session.test.tsx`; no se ejecutaron suites ni se editaron fuentes. Lecturas24898a/1f87c3/86511d; hashes2ac033: fuente113D20EADD6313AEB590235EA86D1E13B79F17A518E7C5841C40BDD42AEE3E7D, testC80AB50632093BF88F253638CF34847AF247AEA609D7100DF87E3FB23589A50A. B acredita47/47 GREENc5ecdf, con ciclos de montaje/coordinación en docs/tdd_end_time_frontend.md; no es una ejecución del juez.

El contenedor ActiveSession queda ligado a active.id y comparte una instancia local del coordinador entre State y End. Se acepta primero la adquisición síncrona y después se aborta GETactive padre, todavía antes del POST. Una adquisición rechazada no invalida esa consulta. Al confirmar, settle invalida las lecturas hijas, incrementa generación y la notificación padre aborta GETactive que pudiera haberse iniciado durante el envío antes de solicitar una nueva lectura. El registro padre permanece en Session, sin duplicarlo en un almacén global ni cambiar14.

Los oráculos nuevos verifican conducta observable: PAUSE bloquea EXTEND en la composición real; GETactive pendiente antes de PAUSE no entrega401 al observer ni al callback; una GETactive iniciada durante PAUSE queda invalidada al confirmar, se observa tercera lectura fresca y permanece el estado paused. La respuesta401 se difiere antes de entregar ResponseHTTP, coherente con el observer síncrono del cliente. La corrección de generaciones del panel, revisada separadamente en review_end_time_frontend_panel.md, protege el uso de S/E posteriores; este montaje no reemplaza esa guarda.

La identidad gestionada es la sesión global devuelta por GETactive. Se pasa `session={active}` a ambos paneles, sin sustituir projectId/taskId por los de la tarea anfitriona. Su key activa desmonta la coordinación al cambiar de sesión. El test heredado «shows an active session from another task with a fixed end and task link» comprueba el enlace y hechos de esa otra tarea y la ausencia de nuevo inicio. Ese fixture devuelve envelope A también a lecturas hijas, por lo que **no acredita una nueva lectura E válida de otra tarea**. La conexión de identidad de17 se justifica en esta revisión por el paso íntegro de active a los clientes ya validados; no se inventa un test ejecutado ni se exige una matriz nueva sin defecto diferenciable.

El delta de fixtures conserva las aserciones históricas. Se añaden respuestas S/E por ruta y se incrementa en una el conteo de las peticiones correspondientes, verificando explícitamente E. El fixture de401 vigente pasa de consumo por orden a distinguir GETactive, evitando que el nuevo E consuma el error destinado al padre. No se silencian alertas ni se filtran fallos mediante selectores arbitrarios.

Sin bloqueante funcional nuevo localizado en este alcance. Quedan fuera Reader, coordinación CLOSE, retirada inmediata de controles tras cierre, UX de navegador y gates finales17. Los problemas del primer freeze de panel se consideran según su dictamen corregido, no como fallos de este montaje. No se revisa toda la feature14 nuevamente.

La coordinación se solicitó a B antes de leer y se comunicaron ambos hashes. Relectura final sin cambios; se liberan los dos archivos para trabajo posterior. Cualquier delta posterior queda fuera de este dictamen, sin exigir que Reader espere.
