# Auditoria de colisiones antes de fusionar 25, 28 y 30 — 9 de septiembre de 2026

Barrido de ocho dimensiones sobre las tres ramas y main, con verificacion adversarial
de cada hallazgo (se refuta por defecto). 55 agentes, 47 colisiones candidatas,
**25 confirmadas** (8 bloqueantes) y 22 descartadas.

Ramas auditadas: claude/webhooks (25, 8389040), claude/external-calendar (28, b376c8c),
claude/automations (30, 4db60a0). main en 9d81c17 con las features 24, 26 y 27 dentro.

## Confirmadas

### 1. [BLOQUEANTE] add/add sobre application.AddressPolicy: mismo nombre cualificado, una clase estatica frente a una interfaz, y la logica invertida

- **Dimension:** nombres-java
- **Ramas:** claude/webhooks y claude/external-calendar (entre si; main no tiene la clase)
- **Rutas:** backend/src/main/java/com/apptolast/organization/application/AddressPolicy.java (webhooks, lineas 12-21), backend/src/main/java/com/apptolast/organization/application/AddressPolicy.java (external-calendar, lineas 10-23), backend/src/main/java/com/apptolast/organization/adapter/config/ApplicationConfiguration.java:458-461 y :493-496 (webhooks), backend/src/main/java/com/apptolast/organization/adapter/config/ApplicationConfiguration.java:436-442 y :453 (external-calendar), backend/src/test/java/com/apptolast/organization/application/AddressPolicyTest.java (ambas ramas)

**Evidencia.** Es la unica interseccion exacta de ficheros anadidos entre las tres ramas (comm -12 sobre los estados A de git diff --name-status). En webhooks: 'public final class AddressPolicy' con constructor privado (linea 17) y API estatica 'public static boolean isBlocked(InetAddress address)' (linea 19), implementada con comparaciones byte a byte. En external-calendar: 'public interface AddressPolicy' con 'boolean allows(InetAddress address)' (linea 11) y dos fabricas estaticas, 'blockingPrivateAddresses()' (linea 13, devuelve PublicAddressPolicy) y 'allowingPrivateAddresses()' (linea 21, 'address -> true'). Los dos javadoc declaran el MISMO concepto: webhooks dice 'Egress policy shared by every outbound connector (features 25, 27 and 28) ... Amendment B2 of the security review'; external-calendar dice 'La comparten los conectores (features 25, 27 y 28); al integrar se unifica con la implementacion de la feature 25. Enmienda B2 de la revision de seguridad'. Las listas de rangos coinciden practicamente una a una (0.0.0.0/8, 10/8, 100.64/10, 127/8, 169.254/16, 172.16/12, 192.0.0.0/24, 192.88.99/24, 192.168/16, 198.18/15, 224/4, 240/4 y fc00::/7, fe80::/10, ff00::/8, 6to4 2002::/16, NAT64 64:ff9b::/96). Ademas ambas ramas anaden el mismo fichero de prueba application/AddressPolicyTest.java, tambien add/add.

**Consecuencia.** git marca el conflicto en los dos ficheros (main y test) y la fusion se para. Es el MISMO concepto duplicado, no dos conceptos con el mismo nombre: hay que unificar, no renombrar. El riesgo real esta en como se unifica. (1) La polaridad esta invertida: 'isBlocked'==true significa prohibido y 'allows'==true significa permitido; una resolucion apresurada que sustituya 'AddressPolicy::isBlocked' por 'policy::allows' en ApplicationConfiguration.java:461 y :496 de webhooks deja la guardia SSRF al reves, permitiendo solo direcciones privadas y bloqueando internet, y ningun test de webhooks lo detecta porque sus casos se escribieron contra la forma estatica. (2) Si gana la version de webhooks se pierde el interruptor 'allowingPrivateAddresses()' que external-calendar cablea desde app.connectors.allow-private-addresses (ApplicationConfiguration.java:436-442): sin el, la pila E2E no puede apuntar a un receptor local. (3) Si gana la version de external-calendar, webhooks deja de compilar en los dos usos de referencia a metodo estatico.

**Arreglo.** Unificar sobre la forma de external-calendar (interfaz AddressPolicy con 'allows' + PublicAddressPolicy + las dos fabricas), porque es la unica que admite inyeccion y la unica que trae la valvula de E2E. Borrar la clase estatica de webhooks, convertir WebhookDestinationGuard y JdkWebhookSender para que reciban un AddressPolicy en vez de un Predicate<InetAddress>, y en ApplicationConfiguration de webhooks sustituir 'AddressPolicy::isBlocked' por el bean connectorAddressPolicy negando el sentido explicitamente (no 'policy::allows' donde se esperaba 'isBlocked'). Fusionar los dos AddressPolicyTest en uno solo conservando los casos de ambas ramas (los de webhooks cubren la forma compatible ::a.b.c.d y NAT64, que en la version de external-calendar se bloquean por rango entero, no por normalizacion).

**Correccion del verificador.** La colision es real y esta bien calibrada como bloqueante, pero la descripcion se queda corta en dos puntos que cambian el plan de resolucion.

(A) NO es solo una inversion de polaridad: las dos implementaciones DISCREPAN EN COMPORTAMIENTO sobre un conjunto concreto de direcciones. webhooks normaliza las formas IPv6 con IPv4 embebida (mapeada, compatible, 6to4 2002::/16 y NAT64 64:ff9b::/96) a la IPv4 embebida y despues aplica las reglas IPv4; external-calendar solo normaliza la forma mapeada y bloquea 2002::/16, 64:ff9b::/96 y ::/96 como rangos ENTEROS (asi lo documenta PublicAddressPolicy). Resultado: para una IPv4 publica embebida los dos discrepan, y cada suite de pruebas fija su respuesta contraria. webhooks (s5_b2_publicLiteralsAreAllowed) afirma que '2002:cb00:7105::1' y '64:ff9b::203.0.113.5' se PERMITEN; external-calendar afirma que '2002:5db8:d822::1' y '64:ff9b::5db8:d822' se BLOQUEAN. Ninguna union de los dos ficheros de prueba pasa en verde: la unificacion exige una decision de seguridad explicita (recomendable la de external-calendar, mas restrictiva) y reescribir los casos de la rama que pierda, no un simple renombrado de metodo.

(B) El radio de la consecuencia (2) es mayor que "la pila E2E no puede apuntar a un receptor local". El interruptor app.connectors.allow-private-addresses de external-calendar esta cableado en application.properties:21, .env.example:31, docker-compose.yml:31, scripts/e2e.mjs:47, y ademas lo VERIFICAN pruebas: ConnectorSettingsTest (backend, lineas 21/35/52/61/62) y scripts/project.test.mjs:2115 y :2124. Si gana la version estatica de webhooks, no solo se pierde el interruptor: caen esas pruebas de wiring en rojo.

(C) Matiz de inventario: external-calendar anade ademas application/PublicAddressPolicy.java (con el record interno Cidr), fichero que webhooks no tiene. No genera conflicto por si mismo, pero forma parte de la superficie a unificar y es la implementacion que sobrevive si se adopta la interfaz.

Forma correcta de resolver: adoptar la interfaz de external-calendar (conserva el puerto, el interruptor y la sustituibilidad en pruebas), portar la logica ganadora a PublicAddressPolicy, y en ApplicationConfiguration de webhooks cambiar las lineas 461 y 496 a inyectar el bean AddressPolicy y pasar 'address -> !policy.allows(address)' (NO 'policy::allows'), porque los dos consumidores esperan un Predicate cuyo true significa BLOQUEADO. Anadir un test de la union que fije la respuesta 6to4/NAT64 elegida.

### 2. [BLOQUEANTE] AddressPolicy: la 25 la escribe como clase estatica y la 28 como interfaz con bean; los dos ApplicationConfiguration se excluyen

- **Dimension:** spring-wiring
- **Ramas:** claude/webhooks vs claude/external-calendar (ambas contra main, que no tiene la clase)
- **Rutas:** backend/src/main/java/com/apptolast/organization/application/AddressPolicy.java (nueva en las dos ramas), backend/src/main/java/com/apptolast/organization/adapter/config/ApplicationConfiguration.java:461 y :496 (webhooks), backend/src/main/java/com/apptolast/organization/adapter/config/ApplicationConfiguration.java:435-455 (external-calendar), backend/src/test/java/com/apptolast/organization/application/AddressPolicyTest.java (nueva en las dos ramas)

**Evidencia.** `git diff --name-status main...` marca `A backend/src/main/java/com/apptolast/organization/application/AddressPolicy.java` en las DOS ramas: main no la tiene, asi que no hay base comun y git no puede fusionar el contenido. En claude/webhooks, AddressPolicy.java:12 es `public final class AddressPolicy`, :17 `private AddressPolicy() {}` y :19 `public static boolean isBlocked(InetAddress address)`; su ApplicationConfiguration la consume como referencia a metodo estatico en dos beans: linea 461 `com.apptolast.organization.application.AddressPolicy::isBlocked` (bean `webhookDestinationGuard`) y linea 496 la misma referencia dentro de `webhookSender`. En claude/external-calendar, AddressPolicy.java:10 es `public interface AddressPolicy` con :11 `boolean allows(InetAddress address)`, :13 `static AddressPolicy blockingPrivateAddresses()` y :21 `static AddressPolicy allowingPrivateAddresses()`; y su ApplicationConfiguration:435-443 declara `@Bean com.apptolast.organization.application.AddressPolicy connectorAddressPolicy(@Value("${app.connectors.allow-private-addresses:false}") boolean allowPrivateAddresses)` que devuelve una de las dos factorias, consumido en :453 por `OutboundGuard outboundGuard(HostResolver, AddressPolicy policy)`. Una interfaz sin `isBlocked` y una clase final con constructor privado y sin `allows` no son la misma cosa: no existe union que satisfaga ambos usos tal cual. Ademas las dos ramas anaden AddressPolicyTest.java en la misma ruta, con contenidos distintos. Es exactamente el patron ya visto con SecretCipher entre 27 y 28, pero aqui NO esta resuelto: la 25 nunca vio la unificacion que hizo la 28 (b376c8c).

**Consecuencia.** El fichero AddressPolicy.java sale en conflicto add/add en la segunda fusion. Resuelto de cualquiera de las dos maneras, la otra rama deja de compilar y no hay contexto de Spring: si gana la interfaz de la 28, ApplicationConfiguration:461 y :496 fallan con «cannot find symbol: method isBlocked» y no compila ni el modulo backend; si gana la clase final de la 25, fallan ApplicationConfiguration:441-442 («cannot find symbol: allowingPrivateAddresses/blockingPrivateAddresses») y el bean `connectorAddressPolicy` no puede instanciarse. En cualquiera de los dos casos no arranca nada: ni el arnes, ni un solo @SpringBootTest, ni la pila E2E. Hay un segundo efecto silencioso si se unifica hacia la interfaz sin revisar la 25: la guardia de webhooks pasaria a depender del bean `connectorAddressPolicy`, es decir de `app.connectors.allow-private-addresses`, un interruptor que la 25 nunca conocio y que desactiva su proteccion SSRF entera (external-calendar/.env.example lo documenta como «DANGER ... DISABLES the SSRF guard»).

**Arreglo.** Unificar AddressPolicy antes de fusionar, en la forma de la 28 (interfaz con `allows` y las dos factorias), porque es la unica de las dos que admite el interruptor de configuracion y ya esta cableada como bean. Portar la tabla de rangos de la 25 (AddressPolicy.java:29-84 de claude/webhooks: mapped, compatible, NAT64, 6to4, 100.64/10, 192.0.0/24, 192.88.99/24, 198.18/15, multicast) al cuerpo de `PublicAddressPolicy` de la 28, que es la implementacion mas completa de las dos. Cambiar despues ApplicationConfiguration:458-462 y :493-497 de la 25 para que `webhookDestinationGuard` y `webhookSender` reciban el bean `AddressPolicy` por parametro y llamen `policy.allows(addr)` negado, en vez de la referencia estatica. Fusionar tambien los dos AddressPolicyTest en un unico fichero que cubra los rangos de las dos ramas.

**Correccion del verificador.** La colision es real y bloqueante, con estas precisiones y ampliaciones de alcance:

NUMEROS DE LINEA
- webhooks AddressPolicy.java: la clase es :12 y el constructor privado :17 (correcto), pero 'isBlocked(InetAddress)' esta en :21, no en :19.
- external-calendar ApplicationConfiguration: el bean 'connectorAddressPolicy' abarca :435-443 con la firma en :436 y las factorias en :441-442; 'outboundGuard' esta en :451-455 con el parametro AddressPolicy en :453 (la colision dice ':435-455' y ':453', aproximado pero correcto).
- webhooks ApplicationConfiguration :461 y :496: exactos.

ALCANCE MAYOR DEL QUE SE DESCRIBE (esto importa para estimar el arreglo)
1. El conflicto no se limita a AddressPolicy.java: ApplicationConfiguration.java tiene 432 lineas en main y AMBAS ramas anaden sus bloques de beans al final del fichero, en zonas adyacentes. Ese fichero saldra tambien en conflicto por si mismo en la segunda fusion, ademas del add/add de AddressPolicy.
2. Hay un tercer consumidor en webhooks que la colision no menciona: backend/src/test/java/com/apptolast/organization/application/CreateWebhookTest.java:19 usa 'new WebhookDestinationGuard(CreateWebhookTest::resolve, AddressPolicy::isBlocked)'. Si gana la interfaz, tambien rompe la compilacion del sourceSet de test.
3. external-calendar arrastra mas dependencias de la interfaz de las citadas: OutboundHostGuard.java:14,16 (campo y constructor), OutboundHostGuardTest.java:32,88,96, ExternalCalendarWiringTest.java:10,71,79,87 (llama a CONFIGURATION.connectorAddressPolicy(false/true) y a .allows(...)) y ExternalCalendarIsolationTest.java:69, que nombra 'AddressPolicy' dentro de un patron de regex de aislamiento arquitectonico. Si gana la clase final de la 25, todos estos rompen.
4. external-calendar anade ademas backend/src/main/java/com/apptolast/organization/application/PublicAddressPolicy.java (la implementacion CIDR real). Es fichero nuevo solo en esa rama, no colisiona por si mismo, pero es la pieza que hay que conservar en cualquier unificacion: contiene las listas CIDR explicitas (BLOCKED_IPV4 / BLOCKED_IPV6) equivalentes a la logica por bytes de la 25.

FORMA DEL ARREGLO (no es simplemente 'elegir uno')
La union correcta es la interfaz de la 28 (AddressPolicy.allows) + PublicAddressPolicy, y adaptar los dos puntos de la 25: WebhookDestinationGuard y JdkWebhookSender reciben un predicado 'esta bloqueada', asi que basta inyectar el bean y pasar 'address -> !policy.allows(address)' en ApplicationConfiguration:461 y :496, y lo mismo en CreateWebhookTest:19. Pero eso ata la guardia SSRF de la 25 al bean connectorAddressPolicy y por tanto al interruptor app.connectors.allow-private-addresses, que existe de verdad en la 28 (.env.example, application.properties, docker-compose.yml, scripts/e2e.mjs, docs/external-calendar.md, ConnectorSettingsTest). Hay que decidir conscientemente si la 25 acepta quedar bajo ese interruptor y documentarlo; si no, la 25 debe consumir directamente 'AddressPolicy.blockingPrivateAddresses()' en vez del bean. Y hay que fusionar a mano los dos AddressPolicyTest.java: el de la 25 aporta casos que el de la 28 no cubre (formas IPv6 6to4 y NAT64 enumeradas por bytes), asi que descartarlo sin mas pierde cobertura de seguridad y, casi seguro, puntuacion de mutacion.

### 3. [BLOQUEANTE] Webhooks y Automatizaciones se pelean por la misma plaza: la ultima del menu, justo detras de «API para integraciones»

- **Dimension:** frontend
- **Ramas:** claude/webhooks contra claude/automations (claude/external-calendar no compite: entra en otro sitio)
- **Rutas:** frontend/src/workspace.tsx:20-21 y :114-119 en ambas ramas, frontend/src/workspace.tsx:79-84 en external-calendar, features/webhooks.feature:441-446, frontend/src/webhooks-route.test.tsx:44-65, features/automations.feature:496-500

**Evidencia.** Las dos ramas insertan su RouteLink en el MISMO punto: tras el bloque de «API para integraciones» que en main acaba en workspace.tsx:112, ambas escriben lineas 114-119 (webhooks: href="/webhooks"; automations: href="/automatizaciones"), y ambas anaden su miembro al union type justo tras `| "API para integraciones"` (linea 20). `git merge-tree main claude/webhooks claude/automations` devuelve conflicto add/add literal en los dos sitios: `<<<<<<< .our | "Webhooks" ======= | "Automatizaciones" >>>>>>> .their` y el mismo choque dentro de un unico `<RouteLink ...>`. Contratos: features/webhooks.feature:444 exige «abre Webhooks desde la entrada de navegacion situada tras API para integraciones» y webhooks-route.test.tsx:61-64 lo fija con `names[api+1] === "Webhooks"`; features/automations.feature:500 solo pide «la navegacion contiene la entrada «Automatizaciones» sin desplazar «Hoy»», pero los tests de la rama van mucho mas alla y la clavan en la ultima posicion (appearance.test.tsx at(-1), export-data.test.tsx at(-1), App.test.tsx links[length-1]). external-calendar entra entre «Revision semanal» y «Apariencia» (workspace.tsx:79-84), no toca la cola y no colisiona con nadie.

**Consecuencia.** El merge se detiene con conflicto en workspace.tsx. Resuelto de cualquiera de las dos formas posibles, alguien queda rojo: con orden ... API, Webhooks, Automatizaciones se rompen appearance.test.tsx at(-2)/at(-3), export-data.test.tsx at(-2)..at(-5) y App.test.tsx length-2/length-3 de la 30; con orden ... API, Automatizaciones, Webhooks se rompe webhooks-route.test.tsx:63 (`names[api+1]`) y con el su contrato @s36. Respuesta directa a la pregunta: a nivel de CONTRATO las tres posiciones si son satisfacibles a la vez (webhooks exige inmediatamente-tras-API, automations solo exige no desplazar «Hoy», external-calendar no fija posicion): el orden valido es Hoy ... Revision semanal, Calendario externo, Apariencia, Exportacion, Calendario, Importacion, API para integraciones, Webhooks, Automatizaciones. A nivel de TESTS tal y como estan escritos hoy NO son satisfacibles.

**Arreglo.** Resolver workspace.tsx dejando Webhooks inmediatamente despues de «API para integraciones» y Automatizaciones detras de Webhooks, con los dos miembros del union type. Y reescribir las aserciones de la 30 (App.test.tsx, appearance.test.tsx, export-data.test.tsx) al estilo por-nombre que ya usa la 25 (indexOf sobre los textos, orden relativo), en vez de reindexar at(-N) a mano.

**Correccion del verificador.** TITULO: Conflicto add/add mecanico en workspace.tsx entre Webhooks y Automatizaciones, mas cuatro ficheros de test que se resuelven a favor de la reescritura de webhooks

GRAVEDAD CORREGIDA: de «bloqueante» a «medio / conflicto mecanico». git detiene la fusion y hay que intervenir a mano, pero la resolucion es unica y no rompe ningun contrato ni ningun test.

FICHEROS EN CONFLICTO (verificados con `git merge-tree main claude/webhooks claude/automations`):
  - frontend/src/workspace.tsx  — dos conflictos literales: union type tras `| "API para integraciones"` y el bloque `<RouteLink>` nuevo tras la entrada de API.
  - frontend/src/App.test.tsx, frontend/src/appearance.test.tsx, frontend/src/export-data.test.tsx — «changed in both» sobre las mismas lineas de asercion de navegacion.
  (Fuera de la dimension frontend, la misma fusion tambien marca ApplicationConfiguration.java, WeeklyReviewPersistenceTest.java y progress/current.md; se tratan aparte.)

RESOLUCION CORRECTA:
  1. workspace.tsx: conservar las DOS entradas en este orden: ... Importacion, API para integraciones, Webhooks, Automatizaciones. Y las dos ramas del union type: `| "API para integraciones" | "Webhooks" | "Automatizaciones"`.
  2. App.test.tsx, appearance.test.tsx, export-data.test.tsx: tomar la version de claude/webhooks (orden relativo por nombre con `names.indexOf(...)`), NO la de claude/automations (indices negativos desplazados). La version de webhooks es estable frente a cualquier entrada anadida despues; la de automations volveria a romperse con la siguiente feature.
  3. Con esa resolucion quedan verdes: webhooks-route.test.tsx:63 (`names[api+1] === "Webhooks"`), automations-route.test.tsx (solo exige `links[0]` = «Hoy» y presencia), y los tres tests compartidos desanclados. Ningun contrato Gherkin queda incumplido: webhooks.feature:444 exige inmediatamente-tras-API y se cumple; automations.feature:500 solo exige no desplazar «Hoy» y se cumple.

RIESGO RESIDUAL REAL QUE LA AFIRMACION NO MENCIONA (patron 4 conocido): tras fusionar las tres ramas, workspace.tsx gana 3 lineas en el union type y 18 en el <nav>, asi que TODOS los rangos linea:columna de workspace.tsx en las configuraciones de Stryker quedan invalidos. Estado actual verificado:
  - main:               appearance 78:10-83:22 | export-data 81:10-86:22 | integration-api 98:10-105:22
  - claude/webhooks:    identicos a main (NO recalculados pese a que la rama desplaza una linea)
  - claude/automations: 79:10-84:22 | 85:10-90:22 | 106:10-113:22
  - claude/external-cal: 85:10-90:22 | 81:10-86:22 | 98:10-105:22
  Ningun juego de valores sera correcto tras la fusion triple. Hay que recalcular a mano los tres rangos (y cualquier rango de workspace.tsx en las configuraciones nuevas de webhooks y automations) DESPUES de resolver workspace.tsx, o las campañas de mutacion de appearance, export-data e integration-api apuntaran a codigo equivocado. Este es el trabajo real que queda, no la supuesta insatisfacibilidad de los tests.

### 4. [BLOQUEANTE] Las tres ramas reescriben el mismo ternario `section={...}` y la misma cabecera de la cadena de hijos de App.tsx

- **Dimension:** frontend
- **Ramas:** claude/webhooks, claude/external-calendar y claude/automations entre si (conflicto a tres bandas sobre el mismo ancla)
- **Rutas:** frontend/src/App.tsx:48-70 (ternario de `section`), frontend/src/App.tsx:72 (`{githubConnector && username ? (`), frontend/src/App.tsx:18-20 (imports) y :39-41 (constantes de ruta)

**Evidencia.** Las tres ramas anteponen su propia rama al ternario, lo que reindenta las 22 lineas siguientes: webhooks `webhooks ? "Webhooks"`, external-calendar `externalCalendar ? "Calendario externo"`, automations `automations ? "Automatizaciones"`. `git merge-tree main claude/webhooks claude/automations` da `<<<<<<< .our webhooks ? "Webhooks" ======= automations ? "Automatizaciones" >>>>>>> .their` y un segundo conflicto en `{webhooks && username ? (<Webhooks .../>` contra `{automations && username ? (<Automations .../>`; `git merge-tree main claude/webhooks claude/external-calendar` da el mismo conflicto en el ternario, y `git merge-tree main claude/external-calendar claude/automations` lo repite ademas en la linea de import y en la de la constante de ruta.

**Consecuencia.** App.tsx no se fusiona sola en ninguna de las tres combinaciones. Al resolver a mano es facil perder una rama del ternario (la seccion queda `null` y la miga de pan dice «Pagina no encontrada» aunque la vista se pinte) o perder una rama de la cadena de hijos (la ruta cae al 404). Ojo tambien a que external-calendar coloca `externalCalendar && username` DESPUES de `integrationsIndex` en la cadena de hijos pero PRIMERO en el ternario de `section`: al reunificar hay que mantener las dos listas coherentes.

**Arreglo.** Resolver App.tsx anadiendo las tres constantes de ruta, los tres imports y las tres ramas en el mismo orden en el ternario de `section` y en la cadena de hijos. Ninguna de las tres rutas nuevas (/webhooks, /calendario-externo, /automatizaciones) solapa con las expresiones regulares existentes ni entre si: las tres son comparaciones exactas y /calendario-externo no colisiona con `route === "/calendario"`.

**Correccion del verificador.** Version correcta:

Titulo: App.tsx no se fusiona sola en ninguna de las tres combinaciones: las tres ramas anteponen su propia rama a la cabeza del ternario `section={...}`.

Ancla real (main:frontend/src/App.tsx, confirmada linea a linea):
  - 18-20 imports, 39-41 constantes de ruta, 48-70 ternario de `section`, 72 `{githubConnector && username ? (`.

Conflictos reales por par (git merge-tree, verificados):
  - claude/webhooks x claude/automations: DOS conflictos. (a) cabeza del ternario, `webhooks ? "Webhooks"` contra `automations ? "Automatizaciones"`; (b) cabeza de la cadena de hijos, `{webhooks && username ? (<Webhooks owner={username} />` contra `{automations && username ? (<Automations owner={username} />`, porque ambas sustituyen la misma linea 72.
  - claude/webhooks x claude/external-calendar: UN solo conflicto, el de la cabeza del ternario. La cadena de hijos NO choca: external-calendar inserta su rama DESPUES de `IntegrationsIndex` (linea 79-81 de su version) y webhooks la inserta ANTES de `GithubConnector`, asi que son hunks disjuntos y git los fusiona solo. La afirmacion original de "el mismo conflicto" en este par es correcta solo para el ternario.
  - claude/external-calendar x claude/automations: CUATRO conflictos: linea de import, linea de la constante de ruta, cabeza del ternario y cabeza de la cadena de hijos.

Consecuencia correcta: la fusion se detiene y hay que resolver a mano. El riesgo real es la resolucion manual descuidada, que puede perder una rama del ternario (la vista se pinta pero la miga de pan dice «Pagina no encontrada» porque `section` cae a null) o una rama de la cadena de hijos (la ruta cae al 404). La resolucion correcta es acumulativa: conservar las tres cabeceras del ternario y las tres entradas de la cadena.

CORRECCION al submatiz de coherencia entre listas: la advertencia de que hay que mantener coherentes el orden del ternario y el de la cadena de hijos esta sobrevalorada. Todos los predicados implicados (`externalCalendar`, `webhooks`, `automations`, `integrationsIndex`, `integrationApi`, `calendar`, `importData`, `exportData`, `appearance`) son comparaciones de igualdad exacta sobre literales de ruta disjuntos (`/calendario-externo`, `/webhooks`, `/automatizaciones`, `/integraciones`, `/integraciones/api`, `/calendario`, ...), y el unico predicado de prefijo, `route.startsWith("/proyectos")`, va el ultimo en ambas listas. Por tanto el orden relativo entre esas ramas es semanticamente irrelevante: la asimetria de external-calendar es una cuestion de legibilidad y de facilidad de revision, no un fallo de correccion. No conviene gastar tiempo de resolucion en reordenarlas.

Colisiones adyacentes que aparecen en el mismo merge y conviene resolver a la vez:
  - frontend/src/appearance.test.tsx choca en el par webhooks x automations sobre las aserciones de orden de navegacion: webhooks las reescribio a orden-por-nombre en la cola («Later features append entries...») y automations mantuvo las posicionales `.at(-6)`...`.at(-1)` anadiendo "Automatizaciones". La resolucion buena es quedarse con la version por nombre de webhooks y anadirle las entradas de las otras dos ramas. En el par external-calendar x automations este fichero fusiona solo.
  - Los rangos `src/App.tsx:<linea>:<columna>` de las configuraciones de Stryker se desplazan en las tres ramas (p. ej. `src/App.tsx:57:18-69:34` pasa a `61:20-73:36`), que es el cuarto patron ya conocido: hay que recalcular esos rangos una sola vez despues de integrar las tres, no rama a rama.

Gravedad: bloqueante para la fusion (git se para y no se puede automatizar), pero es una colision ANUNCIADA, no silenciosa: git la marca siempre, asi que no puede colarse sin que nadie la vea. El coste es de minutos si se resuelve de forma acumulativa y con las tres ramas delante a la vez, en vez de fusionar por pares en secuencia.

### 5. [BLOQUEANTE] Dos ramas recalculan los MISMOS rangos linea:columna de Stryker a valores distintos, y tras fusionar las tres ninguno de los dos es correcto

- **Dimension:** mutacion
- **Ramas:** claude/external-calendar vs claude/automations vs claude/webhooks (y contra main)
- **Rutas:** frontend/stryker.ics-calendar.config.json:7-10, frontend/stryker.appearance.config.json:10-13, frontend/stryker.export-data.config.json:7-10, frontend/stryker.history.config.json:9-12, frontend/stryker.import-data.config.json:10-14, frontend/stryker.integration-api.config.json:10-13, scripts/project.test.mjs:229-232, scripts/project.test.mjs:1642-1645, scripts/project.test.mjs:1674-1676, scripts/project.test.mjs:1907-1910, frontend/src/App.tsx:35-95, frontend/src/workspace.tsx:78-112

**Evidencia.** main fija en stryker.ics-calendar.config.json 'src/App.tsx:37:8-37:42', 'src/App.tsx:53:14-54:29', 'src/App.tsx:88:10-89:37', 'src/workspace.tsx:90:10-95:22'. La rama external-calendar los reescribe a 38:8 / 57:16-58:30 / 94:10-95:37 / workspace 97:10-102:22; la rama automations los reescribe a 38:8 / 57:16-58:30 / 94:10-95:37 / workspace 91:10-96:22. En stryker.appearance.config.json pasa lo mismo: external-calendar pone 'src/workspace.tsx:85:10-90:22' y automations 'src/workspace.tsx:79:10-84:22' sobre el mismo nodo (el enlace Apariencia, que en main ocupa workspace.tsx:78-83). He verificado por que difieren: cada rama anade +1 linea al union de tipos de Workspace (workspace.tsx:14-21) y +6 lineas de RouteLink, pero external-calendar inserta su enlace tras 'Revision semanal' (workspace.tsx:77) mientras automations y webhooks lo insertan al final del nav (tras workspace.tsx:112). En App.tsx cada rama anade +1 import, +1 const de ruta, +4 lineas al ternario de section (con +2 columnas de sangria a todas las ramas previas) y +2 lineas al bloque de render: total +8 lineas por rama. Fusionadas las tres el desplazamiento es +3/+6/+12/+18 segun la zona, no el +1/+4/+6 que asumio cada rama. La rama webhooks toca frontend/src/App.tsx y frontend/src/workspace.tsx (mismos nodos) y no actualiza NINGUNA configuracion de Stryker ni ningun guarda.

**Consecuencia.** stryker.appearance.config.json y stryker.ics-calendar.config.json daran conflicto textual (las dos ramas editan las mismas lineas) y quien resuelva escogera un lado: en ambos casos el valor escogido sera erroneo. Peor: stryker.export-data, stryker.history, stryker.import-data y stryker.integration-api solo las toca automations, asi que fusionan LIMPIO y quedan silenciosamente apuntando al codigo equivocado. Y como los guardas de scripts/project.test.mjs que fijan esos numeros (lineas 229-232, 1642-1645, 1674-1676, 1907-1910) vienen de la misma rama que la configuracion, el par sigue coherente y la suite pasa en verde. Resultado: campanas enteras de mutacion (ics_calendar, appearance, export_data, history, import_data, integration_api) mutando nodos que no son los suyos, con el umbral break=80 evaluado sobre codigo ajeno. Concretamente, tras fusionar las tres, los valores correctos serian: ics-calendar App.tsx 40:8-40:42, 65:20-66:35, 106:10-107:37 y workspace.tsx 99:10-104:22; appearance App.tsx 38:8-38:44, 69:24-81:40 y workspace.tsx 87:10-92:22; export-data workspace.tsx 93:10-98:22; import-data workspace.tsx 105:10-113:22; integration-api workspace.tsx 114:10-121:22; history workspace.tsx 69:10-74:22.

**Arreglo.** Fusionar primero App.tsx y workspace.tsx, y solo despues recalcular TODOS los rangos contra el fichero fusionado, no aceptar los numeros de ninguna rama. El fichero progress/export_frontend_scope_nodes.json (que automations ya actualiza) demuestra que los rangos son nodos de AST con su texto: usar ese mismo procedimiento (extraer el nodo por su texto, no por su linea) para regenerar las seis configuraciones y sus guardas gemelos en scripts/project.test.mjs en el mismo commit. Idealmente, sustituir el anclaje por linea:columna por un anclaje estable antes de la siguiente tanda de carriles paralelos.

**Correccion del verificador.** La colision es real y sigue siendo bloqueante, pero el "queda todo en verde" es falso para dos de las seis campanas y hay dos hechos adicionales que cambian el plan de arreglo.

1) NO todo queda silenciosamente verde. scripts/project.test.mjs tiene DOS guardas SEMANTICOS que leen el fuente en el rango y comprueban contenido, no solo literales:
   - main scripts/project.test.mjs:242-260 (test "ics calendar Stryker selects its own nodes of the shared files"): expected = [["calendar = route","/calendario"],["calendar","Calendario"],["calendar && username","<Calendar owner={username} />"],["<RouteLink","/calendario"]], y para config.mutate.slice(0,4) hace readFileSync del fichero, recorta lineas/columnas y assert.ok(selected.startsWith(...)) e includes(...).
   - main scripts/project.test.mjs:1933-1968 (test "appearance Stryker preserves all candidates..."): mismo patron con expectedStarts = ["appearance =","appearance","appearance","<RouteLink","if (session?.authenticated","function isPrivateRoute"] sobre config.mutate.slice(3).
   Tras fusionar las tres, cualquiera de los dos lados del conflicto (97:10-102:22 o 91:10-96:22 para ics; 85:10-90:22 o 79:10-84:22 para appearance) cae sobre "Exportación"/"Apariencia"/"Revisión semanal"/"Calendario externo" en vez de "<RouteLink" y esos dos tests se PONEN EN ROJO. Es decir: ics_calendar y appearance son AUTODETECTABLES; te obligaran a arreglarlas.
   El verde silencioso real se limita a CUATRO campanas: export_data, import_data, integration_api e history. Sus guardas (project.test.mjs:1639-1648 para history, 1671-1678 para el Stryker por defecto, y los equivalentes de export/import/integration) son solo assert.deepEqual contra literales, sin lectura del fuente, y los literales vienen de la misma rama automations que la configuracion: par coherente, suite verde, mutantes sobre nodos ajenos con break=80.

2) claude/webhooks YA ESTA ROTA POR SI SOLA, antes de cualquier fusion. En su arbol App.tsx:37 es `const exportData = route === "/exportacion";` (el `const calendar` se ha ido a la 38) y workspace.tsx:90-95 cruza el limite Exportación/Calendario, pero su stryker.ics-calendar.config.json sigue con 37:8-37:42 / 90:10-95:22 y no toca project.test.mjs. Los dos guardas semanticos de arriba deberian estar fallando ya en la rama. Compruebalo antes de fusionar: si esa rama se declaro verde, es que la suite de project.test.mjs no se ejecuto en ese carril.

3) Deriva PREEXISTENTE en main: main/frontend/stryker.export-data.config.json (workspace.tsx:81:10-86:22), stryker.import-data.config.json (89:10-97:22), stryker.integration-api.config.json (98:10-105:22) y stryker.history.config.json (55:10-60:22) ya apuntan a lineas equivocadas respecto al propio workspace.tsx de main (los nodos reales estan en 84-89, 96-104, 105-112 y 66-71). automations las ARREGLO para su arbol. Por tanto la fusion no las "rompe": las vuelve a romper con otro desfase. No las trates como regresion de automations.

4) La lista de valores correctos de la afirmacion es correcta pero INCOMPLETA: solo cubre las entradas de workspace.tsx de las cuatro campanas silenciosas y omite sus entradas de App.tsx. La regla de desfase de App.tsx tras fusionar las tres es, respecto a main: +0 para lineas 1-18, +3 para 21-39, +4 para 40-41, +6 para 42-48, +12 para 49-71 (con +6 columnas de sangria dentro del ternario de section) y +18 desde la 72 en adelante. Aplicandola faltan al menos: appearance App.tsx tercera entrada 92:10-133:7 -> 110:10-151:7; history App.tsx 36:8-36:57, 75:30-81:40, 118:10-151:7; y las tres entradas de App.tsx de export-data, import-data e integration-api.

Plan de cierre: no resolver los conflictos de stryker.ics-calendar.config.json, stryker.appearance.config.json ni de project.test.mjs eligiendo un lado (ambos son erroneos); recalcular los seis ficheros de configuracion MAS los literales de project.test.mjs (229-232, 1642-1645, 1674-1676, 1907-1910) sobre el App.tsx y el workspace.tsx ya fusionados, y aprovechar para anadir guarda semantico a export_data, import_data, integration_api e history, que hoy es lo unico que impide que esto vuelva a pasar en silencio.

### 6. [BLOQUEANTE] Las dos ramas editan la MISMA linea del perfil PIT por defecto: una resolucion ingenua deja un alcance completo fuera de la union

- **Dimension:** mutacion
- **Ramas:** claude/external-calendar vs claude/automations
- **Rutas:** backend/build.gradle.kts:489, backend/build.gradle.kts:463, backend/build.gradle.kts:491, scripts/project.test.mjs (guarda anadido por external-calendar)

**Evidencia.** En main, backend/build.gradle.kts:489 es una unica linea: 'else -> core + authenticationClasses + ... + icsCalendarClasses + githubConnectorClasses'. external-calendar la reescribe anadiendo '+ externalCalendarClasses' y automations la reescribe anadiendo '+ automationsClasses'. Ambas tocan tambien la misma expresion 'targetClasses.set(when {' (build.gradle.kts:463) y 'targetTests.set(when {' (build.gradle.kts:491): automations inserta su rama como PRIMER caso ('automationsOnly -> automationsClasses' justo tras el when) y external-calendar la inserta tras 'exportPersistenceOnly'. El guarda que external-calendar anade a scripts/project.test.mjs solo comprueba lo suyo: assert.match(build, /else -> core \+ [^\n]*externalCalendarClasses/); no existe guarda equivalente para automationsClasses.

**Consecuencia.** Git marcara conflicto en la linea 489 y quien resuelva puede quedarse con un solo sumando. Si se pierde '+ automationsClasses', ninguna prueba falla (automations no anadio guarda del perfil por defecto) y las 17 familias de clases de la feature 30 que viven en adapter.* dejan de mutarse en la campana completa. Si se pierde '+ externalCalendarClasses', falla el guarda de external-calendar, que al menos es ruidoso. Ademas, si el conflicto se resuelve tomando un lado entero del bloque when, se pierde tambien la declaracion val correspondiente y la compilacion de Gradle rompe (referencia no resuelta), lo cual es preferible al fallo silencioso.

**Arreglo.** Resolver la linea 489 a mano concatenando los DOS sumandos ('+ icsCalendarClasses + githubConnectorClasses + externalCalendarClasses + automationsClasses'), conservar las dos declaraciones val y las cuatro entradas nuevas del when (targetClasses, targetTests, reportDir y el val <x>Only), y anadir en scripts/project.test.mjs el guarda simetrico para automationsClasses que hoy no existe.

**Correccion del verificador.** Titulo: external-calendar y automations reescriben la misma linea del perfil PIT por defecto (backend/build.gradle.kts:489); una resolucion que tome un solo lado pierde un alcance de mutacion completo y solo una de las dos perdidas es detectable.

Ramas: claude/external-calendar vs claude/automations.
Rutas: backend/build.gradle.kts:489 (unico punto de conflicto real), scripts/project.test.mjs:2091 en claude/external-calendar.

Hechos verificados:
- En main, la linea 489 es una unica linea larga: 'else -> core + authenticationClasses + ... + icsCalendarClasses + githubConnectorClasses'. external-calendar la reescribe con '+ externalCalendarClasses' al final; automations la reescribe con '+ automationsClasses' al final. Git marcara conflicto en esa linea al fusionar la segunda rama.
- Todo lo demas de ambas ramas en este fichero se fusiona LIMPIAMENTE, en contra de lo que afirmaba la version original: la 'val externalCalendarClasses' (~377) y la 'val automationsClasses' (~461) estan en hunks disjuntos; el caso 'externalCalendarOnly -> externalCalendarClasses' (~497) y el caso 'automationsOnly -> automationsClasses' (insertado como primer caso justo tras 'targetClasses.set(when {') tampoco se solapan; lo mismo en el bloque targetTests. No hay riesgo de 'val' huerfano ni de fallo de compilacion de Gradle. (Y aunque lo hubiera, un val local sin usar en Kotlin DSL es un aviso, no un error.)
- Guarda asimetrica confirmada: claude/external-calendar anade en scripts/project.test.mjs:2091 'assert.match(build, /else -> core \+ [^\n]*externalCalendarClasses/)'. claude/automations no anade ninguna asercion sobre el contenido de build.gradle.kts: su unica prueba de mutacion comprueba la invocacion '-PmutationScope=automations'.

Consecuencia real: si la resolucion se queda con el lado de external-calendar, se pierde '+ automationsClasses' y ninguna prueba falla; las familias de clases de la feature 30 (domain.Automation*, application.*Automation*, adapter.http.Automation*, adapter.persistence.PostgresAutomation*, AutomationActionJson*) dejan de mutarse en la campana completa, en silencio. Si se queda con el lado de automations, se pierde '+ externalCalendarClasses' y falla la guarda de external-calendar (unico aviso ruidoso de los dos). No hay ningun escenario en el que el fallo se manifieste como error de compilacion.

Resolucion: conservar ambos sumandos en la linea 489: '... + icsCalendarClasses + githubConnectorClasses + externalCalendarClasses + automationsClasses'. Recomendable ademas anadir en scripts/project.test.mjs una guarda simetrica para automationsClasses en el perfil por defecto, para cerrar el punto ciego.

Gravedad correcta: MEDIA / aviso, no bloqueante. Git marca el conflicto obligatoriamente (nadie puede fusionar sin verlo) y la resolucion correcta es de una sola linea; el riesgo no es que se cuele sin verse, sino que se resuelva mal por descuido.

### 7. [BLOQUEANTE] scripts/project.test.mjs: cuatro conflictos textuales y, peor, rangos que git fusiona en silencio dejando el test del arnes en rojo

- **Dimension:** entorno-e2e
- **Ramas:** claude/external-calendar vs claude/automations (y claude/webhooks como tercer desplazamiento no contabilizado)
- **Rutas:** scripts/project.test.mjs:229-238, scripts/project.test.mjs:1931-1940, scripts/project.test.mjs:1996+ (bloques nuevos de cada rama), frontend/src/App.tsx:16-51 (rama webhooks), frontend/src/workspace.tsx

**Evidencia.** git merge-tree main claude/external-calendar claude/automations marca cuatro regiones '<<<<<<< .our / >>>>>>> .their' en scripts/project.test.mjs: (a) ics-calendar, '"src/workspace.tsx:97:10-102:22"' (28) contra '"src/workspace.tsx:91:10-96:22"' (30); (b) appearance, '"src/workspace.tsx:85:10-90:22"' (28) contra '"src/workspace.tsx:79:10-84:22"' (30); (c) y (d) los bloques de tests nuevos ('external calendar targets reject...' contra 'automations backend invokes only its fixed PIT scope') que se solapan al final del fichero. Ademas, las dos ramas escriben EXACTAMENTE los mismos valores para App.tsx ('src/App.tsx:38:8-38:42', '57:16-58:30', '94:10-95:37'), cada una calculada suponiendo UNA sola entrada de navegacion nueva: git los auto-fusiona sin conflicto. Y claude/webhooks anade una tercera entrada (frontend/src/App.tsx:41 '+ const webhooks = route === "/webhooks";' y la reindentacion completa del ternario en 48-78) sin tocar scripts/project.test.mjs ni ninguna configuracion de Stryker.

**Consecuencia.** Al fusionar hay que resolver a mano cuatro conflictos; y aunque se resuelvan, los rangos de App.tsx quedan mal en el arbol fusionado (dos, y con la 25 tres, entradas de navegacion desplazan las lineas) sin que git avise. 'bin/harness test' (que ejecuta 'node --test scripts/project.test.mjs', scripts/project.mjs:399) falla en al menos los tests de ics-calendar, history, appearance y export, y la puerta de cierre de sesion queda bloqueada por un fallo que parece del arnes y no de las features.

**Arreglo.** Fusionar las tres ramas primero en frontend/src/App.tsx y workspace.tsx, recalcular DESDE EL ARBOL FUSIONADO todos los rangos linea:columna, y actualizar en el mismo commit las configuraciones de Stryker afectadas y las expectativas de scripts/project.test.mjs; no aceptar ningun valor auto-fusionado de App.tsx sin volver a medirlo.

**Correccion del verificador.** Titulo: scripts/project.test.mjs y los rangos linea:columna de Stryker sobre App.tsx: cuatro-seis conflictos textuales mas un desplazamiento que git funde en silencio y deja dos tests del arnes en rojo.

Correcciones a la descripcion original:

1) Son SEIS regiones en conflicto en scripts/project.test.mjs, no cuatro: (a) ics-calendar, "src/workspace.tsx:97:10-102:22" (28) vs "91:10-96:22" (30); (b) appearance, "85:10-90:22" vs "79:10-84:22"; (c) test "external calendar targets reject..." vs "automations backend invokes only its fixed PIT scope"; (d) los dos tests de mutacion frontend; (e) la cadena "stryker.external-calendar.config.json" vs "stryker.automations.config.json"; (f) el bloque final "external calendar PIT scope..." vs "automations Stryker configuration mutates only the feature files". Ademas git marca conflicto en frontend/stryker.ics-calendar.config.json, frontend/stryker.appearance.config.json, frontend/src/App.tsx, frontend/src/workspace.tsx, frontend/src/styles.scss, scripts/project.mjs, ApplicationConfiguration.java, backend/build.gradle.kts y progress/current.md.

2) Los tests que REALMENTE se ponen rojos por el desplazamiento son solo DOS, no cuatro: "ics calendar Stryker selects its own nodes of the shared files" y el de la configuracion Stryker de appearance. Son los unicos que recortan el fichero fuente por rango y verifican el texto (scripts/project.test.mjs:249 y :1942, patron lines.slice(Number(startLine)-1, ...) + assert.ok(...startsWith(...))). Los tests de history y de export NO fallan por esto: el de history compara stryker.history.config.json contra literales del propio test (ambos provienen de la rama 30, coherentes entre si) y el de export compara la config contra progress/export_frontend_scope_nodes.json (idem); ninguno cruza contra App.tsx. Listar history y export infla el hallazgo.

3) claude/webhooks no es solo "un tercer desplazamiento no contabilizado": es un fallo ya presente por si solo. `git diff --name-status main...claude/webhooks -- scripts/ frontend/stryker.ics-calendar.config.json frontend/stryker.appearance.config.json` sale vacio, y en esa rama `const calendar` ya esta en App.tsx:38 y `const appearance` en 36, mientras main y sus configs siguen diciendo 37:8-37:42 y 35:8-35:44. Es decir, fusionar SOLO la 25 en main ya deja rojos esos dos tests, y sin ningun marcador de conflicto que lo avise.

Accion al integrar (una sola vez, tras fusionar las tres ramas): recalcular a mano, sobre el App.tsx y el workspace.tsx ya fusionados, los rangos de frontend/stryker.ics-calendar.config.json y frontend/stryker.appearance.config.json, y espejar esos mismos valores en los literales de scripts/project.test.mjs; resolver los seis bloques de conflicto conservando los tests de AMBAS features (no son alternativas, son bloques nuevos independientes). Gravedad: bloqueante, correcta.

### 8. [BLOQUEANTE] Los tests @s33, @s20 y @s22 (features 23, 20 y 22) son reescritos por 25 y por 30 en el mismo hunk con estrategias opuestas

- **Dimension:** contratos
- **Ramas:** claude/webhooks vs claude/automations (y, de rebote, claude/external-calendar)
- **Rutas:** frontend/src/App.test.tsx:16-35, frontend/src/appearance.test.tsx:785-805, frontend/src/export-data.test.tsx:397-407, frontend/src/integration-api.test.tsx:812-826, progress/tdd_external_calendar.md:294-297 y :383-388

**Evidencia.** Los tres ficheros de test son 'M' en las dos ramas y el diff toca exactamente las mismas líneas. En `claude/webhooks` (commit fb83b11 «test: afirmar el orden de navegación por nombre y no por indice») `App.test.tsx` pasa a `names.indexOf("Importación") < names.indexOf("API para integraciones")` con el comentario «Later features append their own entries, so assert order by name rather than by position»; `appearance.test.tsx` y `export-data.test.tsx` reciben la misma conversión a orden relativo por nombre. En `claude/automations` los MISMOS tres tests se dejan por índice y se corren una posición: `App.test.tsx` pasa a `links[links.length - 3] = "Importación"`, `-2 = "API para integraciones"`, `-1 = "Automatizaciones"`; `appearance.test.tsx` pasa de `at(-5..-1)` a `at(-6..-1)` con `at(-1) = "Automatizaciones"`; `export-data.test.tsx` de `at(-4..-1)` a `at(-5..-1)` con `at(-1) = "Automatizaciones"`. La 28 no toca ninguno de los tres a propósito: `progress/tdd_external_calendar.md:294-297` dice «En vez de tocar el test de otro carril, la entrada se coloca antes de "Apariencia": así at(-4), at(-2) y at(-1) siguen valiendo», premisa que documenta de nuevo en :383-388.

**Consecuencia.** Conflicto textual garantizado en los tres ficheros al fusionar 25 y 30 (mismas líneas, dos versiones). Y si se resuelve conservando la versión de la 30, la suite Vitest queda roja aunque el conflicto se cierre «limpio»: la 25 inserta «Webhooks» justo detrás de «API para integraciones» (App.tsx/workspace.tsx), de modo que `links.at(-2)` deja de ser «API para integraciones» y `at(-1)` deja de ser «Automatizaciones» en los tres tests; además esos tests pertenecen a las features 20, 22 y 23, ya cerradas como done, así que el fallo aparece como regresión de features ajenas. Si se conserva la versión de la 25, sobrevive a las tres entradas nuevas pero pierde la afirmación de que «Automatizaciones» existe en la navegación.

**Arreglo.** Resolver los tres conflictos quedándose con la variante por nombre de la 25 y añadirle, en lugar de índices, las comprobaciones de existencia y orden relativo de «Calendario externo», «Webhooks» y «Automatizaciones». Fijar de una vez el orden final de la navegación y reflejarlo en un solo test (el @s33 de import23), dejando los de 20 y 22 comprobando solo su propia entrada.

**Correccion del verificador.** La colisión es real y bloqueante, pero la descripción tiene dos imprecisiones y le falta la trampa silenciosa, que es la parte que de verdad importa.

CORRECCIÓN DE RUTAS:
- `frontend/src/integration-api.test.tsx:812-826` NO es una colisión: solo la 25 lo modifica (`git diff main...claude/automations -- frontend/src/integration-api.test.tsx` sale vacío). Es un cambio de cortesía de la 25 (deja de exigir que «API para integraciones» sea el último enlace y solo comprueba `aria-current`). Merge limpio; sácalo de la lista.
- `progress/tdd_external_calendar.md:294-297` y `:383-388` son evidencia documental, no rutas en colisión. Merge limpio.
- FALTAN dos rutas que sí conflictúan y que la afirmación no menciona:
  * `frontend/src/workspace.tsx` — la 25 y la 30 añaden su entrada en el mismo anclaje (`@@ -110,6`, detrás de «API para integraciones») y su literal de unión en el mismo `@@ -18,6`. Doble conflicto. La 28 no conflictúa aquí (inserta en `@@ -75,6`).
  * `frontend/src/App.tsx` — las TRES ramas reescriben la misma cadena ternaria de `section` en `@@ -46,30`. Conflicto a tres bandas.

LA TRAMPA SILENCIOSA (lo más valioso, y no está en la afirmación):
El orden de las dos entradas nuevas en `workspace.tsx` NO es libre. `frontend/src/webhooks-route.test.tsx:63` (@s36) afirma adyacencia estricta: `expect(names[api + 1]).toBe("Webhooks")`. El test propio de la 30 (`automations-route.test.tsx:42-52`, @s37) solo usa `toContain` y «Hoy sigue primero», sin adyacencia. Por tanto el único orden que satisface ambas suites es:
    ... → «API para integraciones» → «Webhooks» → «Automatizaciones»
Si al resolver se coloca «Automatizaciones» antes que «Webhooks», el conflicto se cierra limpio, no hay señal de git, y @s36 de la propia feature 25 queda rojo.

RESOLUCIÓN CORRECTA (una sola forma que deja todo verde):
1. En `workspace.tsx`, conservar las dos entradas en ese orden exacto, y la de la 28 antes de «Apariencia».
2. En los tres tests compartidos, conservar SIEMPRE la versión de la 25 (orden relativo por nombre): es la única que sobrevive a las tres entradas nuevas. La versión por índice de la 30 es insalvable con tres inserciones.
3. Compensar la pérdida que sí denuncia la afirmación —que la versión de la 25 ya no afirma que «Automatizaciones» exista— NO tocando los tests ajenos, sino apoyándose en `automations-route.test.tsx` @s37, que ya cubre presencia, `href` y `aria-current` de «Automatizaciones». La cobertura no se pierde: cambia de fichero. Esto abarata la resolución: no hay que reescribir nada, basta con descartar el lado de la 30 en los tres hunks.

GRAVEDAD: bloqueante, confirmada, pero por el motivo correcto. Los conflictos de los tres tests + `workspace.tsx` + `App.tsx` los marca git de todos modos, así que no se van a colar. Lo que sí se cuela es (a) resolver a favor de la 30 y dejar 3 tests de features ya `done` en rojo, y (b) el orden Webhooks/Automatizaciones, que git da por bueno en cualquier permutación y rompe @s36 sin previo aviso.

### 9. [ALTA] Dos guardias SSRF y dos puertos llamados HostResolver conviviendo en el mismo paquete application

- **Dimension:** nombres-java
- **Ramas:** claude/webhooks y claude/external-calendar
- **Rutas:** backend/src/main/java/com/apptolast/organization/application/WebhookDestinationGuard.java:9-36 (webhooks, tipo anidado HostResolver en :10-12), backend/src/main/java/com/apptolast/organization/adapter/webhook/JdkWebhookSender.java:38-48 y :92-97 (webhooks), backend/src/main/java/com/apptolast/organization/application/HostResolver.java:10-12 (external-calendar), backend/src/main/java/com/apptolast/organization/application/OutboundGuard.java:5-13 (external-calendar), backend/src/main/java/com/apptolast/organization/application/OutboundHostGuard.java:12-31 (external-calendar), backend/src/main/java/com/apptolast/organization/adapter/net/SystemHostResolver.java:9-17 (external-calendar)

**Evidencia.** webhooks declara 'public final class WebhookDestinationGuard' con un tipo anidado 'public interface HostResolver { InetAddress[] resolve(String host) throws UnknownHostException; }' (lineas 10-12) en el paquete com.apptolast.organization.application. external-calendar anade en ESE MISMO paquete un 'public interface HostResolver { List<InetAddress> resolve(String host); }' de primer nivel (HostResolver.java:10-12), con tipo de retorno distinto (List frente a array) y sin excepcion comprobada; SystemHostResolver.java:12-17 traduce el fallo a lista vacia, mientras el de webhooks propaga UnknownHostException. Los dos guardias hacen lo mismo con vocabularios distintos: WebhookDestinationGuard.check(String url) resuelve, recorre las direcciones y lanza WebhookOperationException con codigos URL_UNRESOLVABLE (linea 29 y 32) o URL_BLOCKED (linea 35); OutboundHostGuard.check(String host) devuelve el enum OutboundGuard.Verdict {ALLOWED, UNRESOLVABLE, BLOCKED} (lineas 22-30). Y hay una tercera copia de la misma comprobacion inline en JdkWebhookSender.guardDestination (lineas 92-97), que repite resolver+recorrer+bloquear con su propia excepcion privada BlockedDestination (linea 121).

**Consecuencia.** git no marca nada porque los nombres cualificados difieren, y Java tampoco protesta: dentro del cuerpo de WebhookDestinationGuard el nombre simple 'HostResolver' resuelve al tipo anidado y fuera resuelve al de primer nivel, asi que compila y queda un paquete con dos puertos homonimos de contrato incompatible. En la practica quedan tres implementaciones de la politica de salida (WebhookDestinationGuard, JdkWebhookSender.guardDestination y OutboundHostGuard) sobre lo que la propia enmienda B2 declara como una sola politica compartida, con tres vocabularios de fallo distintos. Cualquier endurecimiento futuro (nuevo rango prohibido, control de rebinding, cambio de timeouts) hay que aplicarlo tres veces, y olvidar una deja un agujero SSRF silencioso justo en la ruta que no se toco.

**Arreglo.** Quedarse con el par HostResolver (primer nivel, List<InetAddress>, lista vacia = no resuelve) + OutboundGuard/OutboundHostGuard de external-calendar como unica guardia de salida. Borrar el tipo anidado WebhookDestinationGuard.HostResolver, reescribir WebhookDestinationGuard como un traductor delgado que llame a OutboundGuard y convierta Verdict.UNRESOLVABLE/BLOCKED en WebhookOperationException.Code.URL_UNRESOLVABLE/URL_BLOCKED, y hacer que JdkWebhookSender reciba ese mismo OutboundGuard en vez de repetir la comprobacion en guardDestination. Asi la enmienda B3 (resolver una vez y comprobar todas las direcciones) queda en un unico sitio.

**Correccion del verificador.** Titulo correcto: "Dos pilas SSRF completas en application: AddressPolicy anadido por ambas ramas con polaridad booleana invertida (add/add), mas dos puertos HostResolver homonimos que compilan en silencio".

Lo que git SI marcara (bloqueante, resolucion no trivial):
- backend/src/main/java/com/apptolast/organization/application/AddressPolicy.java — anadido (A) por claude/webhooks y por claude/external-calendar. Conflicto add/add. webhooks: 'public final class AddressPolicy' con constructor privado y 'public static boolean isBlocked(InetAddress)' implementado con mascaras de bits. external-calendar: 'public interface AddressPolicy' con 'boolean allows(InetAddress)' mas factorias estaticas blockingPrivateAddresses() (delega en PublicAddressPolicy, lista CIDR explicita) y allowingPrivateAddresses().
- backend/src/test/java/com/apptolast/organization/application/AddressPolicyTest.java — anadido por ambas ramas. Segundo add/add.
- backend/src/main/java/com/apptolast/organization/adapter/config/ApplicationConfiguration.java — modificado (M) por ambas, cada una anadiendo sus @Bean.

Peligro real de la resolucion, que es lo que eleva la gravedad: la polaridad esta INVERTIDA. isBlocked(a)==true significa lo contrario que allows(a)==true. Una resolucion apresurada que sustituya un nombre por otro sin invertir la condicion abre el SSRF de par en par sin que compile mal ni falle ningun test de la otra rama. Ademas la interfaz de la 28 tiene una puerta trasera que la clase de la 25 no tiene: allowingPrivateAddresses(), activada por APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES para el perfil E2E. Resolver hacia la clase estatica de la 25 rompe el E2E de la 28; resolver hacia la interfaz de la 28 obliga a reescribir los dos @Bean de webhooks (AddressPolicy::isBlocked pasa a ser un Predicate derivado de allows negado), WebhookDestinationGuard, JdkWebhookSender, CreateWebhookTest y el AddressPolicyTest de la 25.

Lo que git NO marcara (secundario, pero queda sucio en el arbol fusionado):
- application.HostResolver (primer nivel, List<InetAddress> resolve, traga el fallo) y application.WebhookDestinationGuard.HostResolver (anidado, InetAddress[] resolve throws UnknownHostException) conviven sin error: nombres cualificados distintos, ficheros distintos, Java resuelve el nombre simple al anidado dentro del cuerpo de WebhookDestinationGuard y al de primer nivel fuera. Compila.
- Tres plumbings de la misma decision con tres vocabularios: excepcion WebhookOperationException(URL_UNRESOLVABLE|URL_BLOCKED), excepcion privada BlockedDestination, y enum OutboundGuard.Verdict.

Gravedad: alta, confirmada, pero por el add/add con polaridad invertida de AddressPolicy, no por invisibilidad. Coste real: no es una hora de persecucion, es una decision de diseno (que forma de AddressPolicy sobrevive) mas la reescritura de los llamantes de la rama perdedora, y hay que hacerla con la enmienda B2 delante. El HostResolver duplicado se arregla en el mismo movimiento: elegir el puerto de primer nivel de la 28 y borrar el anidado de WebhookDestinationGuard, decidiendo si el fallo DNS se propaga o se convierte en lista vacia.

### 10. [ALTA] El bean webhookEndpointLookup de la 30 esta clavado en «false» a la espera de la 25, que se fusiona el mismo dia

- **Dimension:** spring-wiring
- **Ramas:** claude/automations con claude/webhooks
- **Rutas:** backend/src/main/java/com/apptolast/organization/adapter/config/ApplicationConfiguration.java:475-483 (automations), backend/src/main/java/com/apptolast/organization/application/WebhookEndpointLookup.java:7 (automations), backend/src/test/java/com/apptolast/organization/adapter/config/AutomationWiringTest.java:94-112 (automations), backend/src/main/java/com/apptolast/organization/application/WebhookEndpoints.java:15 (webhooks)

**Evidencia.** ApplicationConfiguration:475-478 de claude/automations dice literalmente «Extension point for phase two. Feature 25 has not shipped webhook endpoints, so this answers "not mine" for every endpoint and no NOTIFY_WEBHOOK rule can be stored yet. Replacing this single bean with the real adapter of 25 is the whole change.», y :481-483 es `@Bean WebhookEndpointLookup webhookEndpointLookup() { return (owner, endpointId) -> false; }`. Ese mismo bean se inyecta en tres casos de uso reales: `createAutomation` (:496), `replaceAutomation` (:512) y `simulateAutomation` (:530). Y AutomationWiringTest:99-111 fija la conducta en verde: `s5_theWebhookExtensionPointRejectsEveryEndpointUntilFeature25Exists` afirma `assertThat(endpoints.isActiveEndpointOf(owner, UUID.randomUUID())).isFalse()` y que crear una regla con `NotifyWebhookAction` lanza `WebhookEndpointNotFoundException`. La 25 SI trae ya el adaptador que falta: WebhookEndpoints.java:15 expone `Optional<WebhookEndpoint> find(String owner, UUID id)` sobre el bean `PostgresWebhookStore` (ApplicationConfiguration:433-439 de claude/webhooks), y WebhookEndpoint lleva `status` ("active"), como comprueba WebhookWiringTest:46.

**Consecuencia.** Tras fusionar las tres ramas el producto queda con la mitad de la feature 30 muerta y sin ningun sintoma en rojo: la accion NOTIFY_WEBHOOK existe en el dominio, en la API y en la pantalla, pero `createAutomation` y `replaceAutomation` devuelven siempre 422 ENDPOINT_NOT_FOUND («Elige un endpoint propio y activo», ApiErrors de automations) incluso para un endpoint que el usuario acaba de dar de alta en /webhooks y que esta activo en la base de datos. Peor que el fallo: AutomationWiringTest:99 lo bloquea, asi que la suite pasa en verde justo cuando el cableado ya es incorrecto, y quien lo arregle vera ese test romperse y podria «arreglar» el test en vez del bean.

**Arreglo.** En la fusion, sustituir el lambda de ApplicationConfiguration:481-483 por el adaptador real sobre el bean de la 25: `@Bean WebhookEndpointLookup webhookEndpointLookup(WebhookEndpoints endpoints) { return (owner, id) -> endpoints.find(owner, id).filter(e -> "active".equals(e.status())).isPresent(); }`. Reescribir AutomationWiringTest:98-112 para que, en vez de afirmar el rechazo universal, de de alta un endpoint activo con los casos de uso de la 25 y compruebe que la regla NOTIFY_WEBHOOK se guarda, y que un UUID inventado o un endpoint pausado siguen dando WebhookEndpointNotFoundException. Si se decide dejar el stub, hay que ocultar NOTIFY_WEBHOOK en la API y en la pantalla, no dejar que el usuario lo elija para recibir un 422.

**Correccion del verificador.** Real, pero con dos matices sobre la descripcion original.

(1) La consecuencia dice «existe en el dominio, en la API y en la pantalla». En la pantalla NO: el editor de automations.tsx construye siempre `type: "CREATE_TASK"` (linea 134) y no ofrece selector de accion ni de endpoint; la UI solo *renderiza* reglas de webhook ya existentes (etiqueta «Webhook» en :419, «Aviso al webhook» en :547). Por tanto el 422 permanente solo lo sufren los clientes de la API (POST/PUT /automations con `{type: NOTIFY_WEBHOOK, endpointId}`), no el usuario de la web. Eso reduce la superficie visible pero no la gravedad de fondo.

(2) La frase «sin ningun sintoma en rojo» hay que matizarla: las dos ramas anaden sus beans en el mismo hunk final de ApplicationConfiguration.java (@@ -429,4 +429,113 @@ en ambas), asi que git dara conflicto en ese fichero y quien lo resuelva tendra delante el comentario «Feature 25 has not shipped webhook endpoints» pegado a los beans de webhooks de la 25. El riesgo real es la resolucion mecanica de «conservar ambos bloques»: el stub sobrevive y, a partir de ahi, si no hay ningun test rojo.

Version correcta del hallazgo: al fusionar 25 y 30, el puerto `WebhookEndpointLookup` de la 30 se queda con su doble de fase uno (`-> false`) aunque la 25 ya aporta el adaptador real (`WebhookEndpoints.find` + `WebhookEndpoint.isActive()` sobre el bean `PostgresWebhookStore`). Consecuencia: la accion NOTIFY_WEBHOOK de la feature 30 queda muerta por API (422 ENDPOINT_NOT_FOUND siempre, incluso con endpoint propio y activo en la base), incumpliendo el escenario @s5 de features/automations.feature, que exige 201 para un endpoint propio y activo. Ademas AutomationWiringTest:99-111 blinda en verde la conducta equivocada, con nombre y javadoc que invitan a «arreglar el test» en vez del bean.

Accion al integrar: sustituir el bean stub por un adaptador que delegue en `WebhookEndpoints` (`find(owner, endpointId).filter(WebhookEndpoint::isActive).isPresent()`), y reescribir/borrar `s5_theWebhookExtensionPointRejectsEveryEndpointUntilFeature25Exists` para que verifique la conducta real (activo -> se guarda; desactivado, ajeno e inexistente -> 422), que es lo que pide @s5.

Gravedad: la mantengo en alta. Aunque la UI no lo exponga, se fusiona una feature incumpliendo su propio Gherkin aprobado, la suite lo tapa en verde y el unico rastro es un comentario dentro de un conflicto de merge.

### 11. [ALTA] La rama de automatizaciones deja rojo un test de la feature 24 que ya esta en main: el ultimo enlace del menu debe ser «API para integraciones»

- **Dimension:** frontend
- **Ramas:** claude/automations contra main (feature 24); claude/webhooks si arregla ese fichero
- **Rutas:** frontend/src/integration-api.test.tsx:816-822

**Evidencia.** En main el test @s41 hace `const links = ...querySelectorAll("a")` y afirma `expect(links[links.length - 1]).toHaveAccessibleName("API para integraciones")` y `expect(links[links.length - 1]).toHaveAttribute("aria-current", "page")`. `git diff --name-status main...claude/automations -- frontend/` NO lista integration-api.test.tsx, y `git show claude/automations:frontend/src/integration-api.test.tsx` conserva esas mismas lineas intactas, mientras la rama anade «Automatizaciones» como ultima entrada (workspace.tsx:114-119). La rama 25 si lo reparo: en claude/webhooks el test pasa a buscar el enlace por texto y comprobar solo su aria-current, con el comentario «Feature 25 adds Webhooks after this entry».

**Consecuencia.** Con automations en main, `links[links.length-1]` es «Automatizaciones»: el @s41 de la feature 24 falla en las dos aserciones. Solo se salva de rebote si se fusiona tambien la version del fichero que trae webhooks (ninguna otra rama lo toca, asi que git se quedaria con ella sin conflicto); si por cualquier motivo se resuelve conservando la version de main, queda rojo.

**Arreglo.** Tomar explicitamente la version de integration-api.test.tsx de claude/webhooks (busqueda por nombre + aria-current) y verificar tras el merge que ese fichero no volvio a la variante posicional.

**Correccion del verificador.** Gravedad correcta: MEDIA (condicional al orden de integracion), no alta.

Version correcta del hallazgo: claude/automations rompe semanticamente el test @s41 de la feature 24 que ya esta en main (frontend/src/integration-api.test.tsx:816-822) sin tocar ese fichero: anade «Automatizaciones» como ultima entrada del nav (frontend/src/workspace.tsx, RouteLink `/automatizaciones` inmediatamente despues del de «API para integraciones»), y el test afirma sobre `links[links.length - 1]` tanto el nombre accesible «API para integraciones» como `aria-current="page"`. Ambas aserciones caen.

Matiz que cambia la calibracion: integration-api.test.tsx lo modifica UNA SOLA rama (claude/webhooks), que ya lo arreglo pasando a buscar el enlace por texto y comprobar solo aria-current. Git no vera conflicto y se quedara con la version de webhooks, que aguanta tanto «Webhooks» como «Automatizaciones» al final. Por tanto:
  - Si las tres ramas se integran en la misma tanda: el fichero acaba verde solo, sin intervencion.
  - Solo queda rojo si automations aterriza en main SIN webhooks (o si webhooks se aparca), o si alguien resuelve conservando la version de main.

Accion recomendada (barata): fusionar claude/webhooks antes o junto con claude/automations, y verificar despues de la fusion que integration-api.test.tsx en main es la version «find por texto» y no la de last-index.

Hallazgo hermano que SI marcara git y conviene registrar aparte: frontend/src/App.test.tsx lo modifican automations (linea 27: `expect(links[links.length - 1]).toHaveAccessibleName("Automatizaciones")`) y webhooks (elimina la asercion de ultimo enlace y la sustituye por comprobacion de orden por nombre con `names.indexOf("Importación") < names.indexOf("API para integraciones")`). Ahi habra conflicto real de git, y la resolucion debe conservar el enfoque por-nombre de webhooks, no el last-index de automations, o el proximo carril que anada nav lo volvera a romper.

### 12. [ALTA] El E2E de exportacion exige que el ultimo enlace del menu sea «API para integraciones» y ninguna de las tres ramas lo toca

- **Dimension:** frontend
- **Ramas:** claude/webhooks y claude/automations contra main (E2E de la feature 22/24)
- **Rutas:** e2e/export-data.spec.mjs:50-63

**Evidencia.** `await expect(nav.getByRole("link").last()).toHaveAccessibleName("API para integraciones")`, precedido de `nth(-3)` = «Exportacion» y `nth(-2)` = «Importacion». `git diff --name-status main...claude/<rama> -- e2e/` solo lista ficheros NUEVOS en las tres ramas (webhooks-ux.spec.mjs; external-calendar.spec.mjs y external-calendar-ux-audit.spec.mjs; automations-ux.spec.mjs y automations.spec.mjs): ninguna modifica export-data.spec.mjs. Los specs nuevos de las tres ramas no contienen ninguna asercion de posicion de menu (grep de nth(/last()/first() sobre ellos: sin coincidencias, salvo un `getByRole("alert").first()` en webhooks-ux.spec.mjs:325).

**Consecuencia.** Tanto Webhooks como Automatizaciones anaden entradas detras de «API para integraciones», asi que `.last()` deja de serlo y el test @s22/@s24/@s26/@s29 de exportacion falla en la pila E2E. Aviso adicional leido en el mismo bloque: `nth(-3)` espera «Exportacion» pero la cola de main es Exportacion, Calendario, Importacion, API (workspace.tsx:84-112 anadio «Calendario» con la feature 26), de modo que ese trozo del spec ya estaba desalineado con main antes de estas tres ramas.

**Arreglo.** Reescribir el bloque de e2e/export-data.spec.mjs:54-63 por nombre y orden relativo (localizar «Exportacion», «Importacion» y «API para integraciones» y comprobar su orden), como ya hacen frontend/src/calendar.test.tsx:455-459 y github-connector-routing.test.tsx:40-54.

**Correccion del verificador.** Título corregido: «Las entradas de menú que Webhooks y Automatizaciones añaden tras "API para integraciones" rompen las aserciones .last() de DOS specs E2E de main, y ninguna rama las toca».

Alcance real (la descripción original se quedaba corta en un fichero):
- e2e/export-data.spec.mjs:54-63 — first()="Hoy", nth(-3)="Exportación", nth(-2)="Importación", last()="API para integraciones" (test @s22 @s24 @s26 @s29).
- e2e/import-data.spec.mjs:61-67 — first()="Hoy", nth(-2)="Importación", last()="API para integraciones". ESTE NO SE MENCIONABA y se rompe igual.

Mecanismo: frontend/src/workspace.tsx en main cierra la nav con Exportación, Calendario, Importación, API para integraciones. claude/webhooks añade <RouteLink href="/webhooks">Webhooks</RouteLink> y claude/automations añade <RouteLink href="/automatizaciones">Automatizaciones</RouteLink>, ambas insertadas después del bloque de "API para integraciones" y antes de </nav>. Con cualquiera de las dos fusionada, .last() deja de ser "API para integraciones" en ambos specs; con las dos, además nth(-2) queda descolocado en export-data. claude/external-calendar NO está implicada (inserta "Calendario externo" antes de "Apariencia", la cola no cambia).

Por qué git no ayuda: ninguna de las tres ramas modifica ficheros preexistentes de e2e/ (solo añaden webhooks-ux.spec.mjs, external-calendar*.spec.mjs, automations*.spec.mjs). El fallo sale en verde-a-rojo solo al ejecutar la pila E2E.

Nota derivada (esta sí la marcará git, no la confundas con lo anterior): ambas ramas editan las MISMAS líneas de frontend/src/export-data.test.tsx (bloque links.at(-4)..at(-1)) y de workspace.tsx, con estrategias incompatibles — webhooks lo vuelve robusto por nombre, automations lo fija a at(-1)="Automatizaciones"/at(-2)="API para integraciones", que queda falso en cuanto entra webhooks. Al resolver ese conflicto quédate con la variante por nombre de webhooks.

Corrección del "aviso adicional": el desajuste de nth(-3) (espera "Exportación", en main hay "Calendario") es un fallo PREEXISTENTE en main introducido por a3d07b9 (feature 26 añadió el enlace /calendario a workspace.tsx sin actualizar export-data.spec.mjs); no lo cuentes como daño de estas tres ramas, pero arréglalo en el mismo toque.

Arreglo (3 líneas, en main tras la fusión): sustituir en ambos specs las aserciones posicionales por aserciones por nombre/relativas, p. ej. comprobar el orden relativo de Exportación → Calendario → Importación → API para integraciones sobre accessible names, como ya hace webhooks en export-data.test.tsx, en vez de first()/nth(-n)/last().

Gravedad: alta se mantiene por el riesgo de rojo silencioso en la puerta E2E de dos features ya cerradas (22/24 y 27-importación) y porque ningún mecanismo automático lo avisa; el coste de reparación, en cambio, es trivial.

### 13. [ALTA] La feature 25 (webhooks) llega a main sin NINGUN destino de mutacion: ni alcance PIT, ni configuracion de Stryker, ni entrada en project.mjs

- **Dimension:** mutacion
- **Ramas:** claude/webhooks (contra main y contra las otras dos)
- **Rutas:** scripts/project.mjs:40-77, backend/build.gradle.kts:35-62, backend/build.gradle.kts:489, frontend/stryker.config.json:6-59, backend/src/main/java/com/apptolast/organization/adapter/webhook/AesGcmWebhookSecrets.java, backend/src/main/java/com/apptolast/organization/adapter/persistence/PostgresWebhookStore.java, frontend/src/webhooks.tsx, frontend/src/webhooks-client.ts

**Evidencia.** git diff --name-status main...claude/webhooks no toca backend/build.gradle.kts, ni scripts/project.mjs, ni scripts/project.test.mjs, ni ningun frontend/stryker.*.config.json. No existe por tanto 'webhooks-backend'/'webhooks-frontend' en la lista blanca de scripts/project.mjs:40-77 (que termina en 'export_data-persistence-backend' en la linea 75), ni 'val webhooksOnly = scope == "webhooks"' en el bloque de scopes de build.gradle.kts:37-62. El perfil por defecto de PIT solo comodinea 'com.apptolast.organization.domain.*' y 'com.apptolast.organization.application.*' (val core, build.gradle.kts:62), asi que las 13 clases nuevas de adaptador de la feature 25 (adapter.config.WebhookConfiguration, WebhookConnectorStartup, WebhookSchedule; adapter.http.WebhookController, WebhookDeliveryView, WebhookEndpointView; adapter.logging.Slf4jWebhookAudit; adapter.persistence.PostgresWebhookOutbox, PostgresWebhookStore, PostgresWebhookWork; adapter.webhook.AesGcmWebhookSecrets, JdkWebhookSender, WebhookSignature) no entran en ninguna union. En el frontend, frontend/stryker.config.json (perfil por defecto) enumera 55 entradas y ninguna es src/webhooks.tsx ni src/webhooks-client.ts. progress/tdd_webhooks.md lo confirma en su linea 373: 'Nunca se lanzo la suite completa, ni pitest, ni E2E'.

**Consecuencia.** Al fusionar, la feature 25 entra en main sin puerta de mutacion: 'bin/harness mutate webhooks-backend' lanza 'Invalid target', el perfil completo no cubre su firma HMAC (adapter.webhook.WebhookSignature), su cifrado de secretos (AesGcmWebhookSecrets) ni su cliente HTTP saliente (JdkWebhookSender), y ninguna campana de Stryker toca su UI. Es exactamente la clase de codigo (firma, cripto, egreso HTTP) que el umbral de mutacion existe para proteger, y queda con cero mutantes sin que nada falle. Ademas, sus clases de domain/application SI entran de golpe en el perfil por defecto por los comodines de core, lo que engorda la campana completa con codigo cuyo alcance nadie ha dimensionado.

**Arreglo.** Antes de marcar la 25 como cerrada: anadir 'val webhooksOnly = scope == "webhooks"', el val webhooksClasses con las 13 clases de adapter y las de domain/webhook, las entradas en targetClasses/targetTests/reportDir y el sumando '+ webhooksClasses' en la linea del else; anadir 'webhooks-backend' y 'webhooks-frontend' a la lista blanca de scripts/project.mjs:40-77 con sus dos ramas de despacho; crear frontend/stryker.webhooks.config.json con mutate ['src/webhooks-client.ts','src/webhooks.tsx'] y break 80; y anadir los guardas correspondientes en scripts/project.test.mjs siguiendo el patron de los que anaden external-calendar y automations.

**Correccion del verificador.** TITULO: La feature 25 (webhooks) se salta la convencion de alcance de mutacion del repo: no declara scope PIT, ni target en scripts/project.mjs, ni configuracion de Stryker, y arreglarlo aterriza en las mismas zonas que 28 y 30 ya editan.

QUE PASA DE VERDAD:
1. claude/webhooks anade 44 clases de produccion (27 en domain/application, 13 adaptadores) y no toca ni un fichero de arnes.
2. Correccion factual sobre PIT: el perfil por defecto NO es 'solo los comodines de core'. La rama else de build.gradle.kts:489 es 'core + authenticationClasses + ... + integrationApiClasses + icsCalendarClasses + githubConnectorClasses': la convencion del repo es que cada feature declara su val <feature>Only en 37-62, su set <feature>Classes, y lo SUMA a esa union. La 25 no aporta nada de eso, asi que sus adaptadores (adapter.webhook.WebhookSignature, AesGcmWebhookSecrets, JdkWebhookSender; adapter.persistence.PostgresWebhook*; adapter.http.WebhookController; adapter.config.Webhook*; adapter.logging.Slf4jWebhookAudit) quedan fuera tambien del perfil completo. La conclusion se mantiene; el motivo es haberse saltado la convencion, no un comodin pobre.
3. Correccion factual sobre la consecuencia: la rama NO toca feature_list.json y en main la 25 sigue en 'in_progress'. La fusion no la cuela como 'done' ni elude la puerta en silencio: simplemente no hay nada que ejecutar, y el fallo es RUIDOSO ('bin/harness mutate webhooks-backend' -> Invalid target). Es deuda de puerta bloqueante para cerrar la 25, no una regresion silenciosa que entre en main sin que nadie se entere.
4. Lo que si es efecto de fusion, y la afirmacion original no menciona: registrar el alcance obliga a editar exactamente scripts/project.mjs:40-77 (cola de la lista blanca), el bloque de vals de build.gradle.kts:37-62, un set nuevo de clases y la union else de la linea 489, mas scripts/project.test.mjs. claude/external-calendar y claude/automations modifican AMBAS los tres ficheros (project.mjs, project.test.mjs, build.gradle.kts). Por tanto el registro del alcance de la 25 debe hacerse DESPUES de integrar las tres ramas, sobre el resultado ya fusionado, o se convierte en un tercer conflicto en la misma cola de lista y en la misma linea 489.
5. Efecto secundario real y menor: al fusionar, los 27 tipos de domain/application de la 25 entran de golpe en el perfil completo por los comodines de core, engordando la campana completa con codigo cuyo umbral nadie ha medido. Ademas ojo con automations, que declara el patron 'com.apptolast.organization.application.WebhookEndpoint*' en su propio set: tras fusionar la 25 ese glob tambien capturara application.WebhookEndpoints de la feature 25, contaminando el alcance 'automations'.
6. En frontend no hay campana que cubra src/webhooks.tsx ni src/webhooks-client.ts, pero si hay impacto colateral: la rama modifica App.tsx y workspace.tsx, que SI estan en la lista mutate del perfil por defecto (entradas de fichero completo, sin rango, asi que no invalidan rangos; solo crecen).

GRAVEDAD CORREGIDA: media (no alta). Es codigo sensible (HMAC, AES-GCM, egreso HTTP) sin un solo mutante, lo que justifica bloquear el cierre de la 25; pero no rompe la fusion, no deja tests rojos, no marca nada como done y falla de forma visible en cuanto alguien invoque el target. Accion: no cerrar la 25 hasta anadir su scope, y anadirlo sobre main YA fusionado con 28 y 30.

### 14. [ALTA] Las dos ramas insertan en el mismo ancla de la lista blanca de destinos y al final del mismo fichero de guardas

- **Dimension:** mutacion
- **Ramas:** claude/external-calendar vs claude/automations
- **Rutas:** scripts/project.mjs:75-76, scripts/project.test.mjs:1969-1971

**Evidencia.** Ambas ramas anaden su par de destinos inmediatamente despues de la linea scripts/project.mjs:75 ('"export_data-persistence-backend",'): external-calendar mete 'external_calendar-backend' y 'external_calendar-frontend'; automations mete 'automations-backend' y 'automations-frontend'. Es el mismo hunk (@@ -73,6 +75,8 @@ en las dos). En scripts/project.test.mjs las dos ramas anaden sus bloques de pruebas al final del fichero desde la misma linea de contexto: external-calendar con @@ -1969,3 +1996,131 @@ (5 pruebas nuevas) y automations con @@ -1969,3 +1969,44 @@ (3 pruebas nuevas). Ademas external-calendar reformatea con Prettier lineas que automations deja compactas (por ejemplo scripts/project.test.mjs:108, threads.set(...) partido en tres lineas) y reformatea por completo frontend/stryker.appearance.config.json y frontend/stryker.ics-calendar.config.json (arrays expandidos), mientras automations edita esos mismos ficheros en formato compacto.

**Consecuencia.** Conflicto textual en tres ficheros a la vez (project.mjs, project.test.mjs y las dos configuraciones de Stryker reformateadas). Si el conflicto de la lista blanca se resuelve quedandose con un lado, el destino de la otra rama pasa a lanzar 'Invalid target' y sus guardas ('external calendar backend mutation runs only its PIT scope' / 'automations backend invokes only its fixed PIT scope') fallan con Invalid target en vez de con un mensaje util. Si el conflicto del final de project.test.mjs se resuelve quedandose con un lado, se pierden en silencio 3 o 5 guardas de mutacion, incluido el que fija el contenido del alcance PIT de external_calendar.

**Arreglo.** Resolver los dos ficheros conservando ambos lados: cuatro entradas en la lista blanca de scripts/project.mjs y los ocho bloques de prueba al final de scripts/project.test.mjs. Pasar Prettier una sola vez sobre scripts/project.test.mjs y sobre las configuraciones de Stryker despues de resolver, para que el formato deje de ser fuente de conflicto en el siguiente carril.

**Correccion del verificador.** Titulo correcto: "Las coordenadas de mutacion de Stryker quedan mal en las dos ramas tras fusionar, ademas de conflicto textual en cuatro ficheros".

Alcance real (external-calendar vs automations):
- scripts/project.mjs:75-76 — inserciones distintas en el mismo ancla. Conflicto.
- scripts/project.test.mjs — tres conflictos: mismo append al final (extcal +5 guardas, automations +3), y edicion de LAS MISMAS lineas 229-232 y 1907-1910 con valores distintos.
- frontend/stryker.ics-calendar.config.json y frontend/stryker.appearance.config.json — mismas lineas 'mutate' con valores distintos, agravado porque extcal reformatea el JSON completo con Prettier y automations lo deja compacto.
- (colateral) frontend/src/App.tsx y frontend/src/workspace.tsx tambien conflictan: ambas anaden import + rama del ternario de 'section' reescribiendo el mismo bloque, y ambas anaden entrada de navegacion.

La consecuencia grave no es la que alega el informe. Lo del 'Invalid target' es correcto pero menor y evidente al primer test. Lo serio es que NINGUN lado tiene los numeros correctos tras la fusion, asi que el conflicto no se puede resolver eligiendo un lado ni tomando los dos:
- workspace.tsx: main appearance 78:10-83:22 -> extcal 85 (anade 1 linea al tipo union + 6 del bloque nav, todo antes de 78), automations 79 (solo la linea del tipo union; su nav va en 111, despues). Fusionadas ambas el valor correcto es 86. Igual con ics-calendar: main 90 -> extcal 97, automations 91, valor correcto fusionado 98.
- App.tsx: cada rama calculo su desplazamiento con UN import y UNA rama del ternario; con las dos, todos los rangos (36:8, 61:20, 98:10, 38:8, 57:16, 94:10) se desplazan mas de lo que ninguna preve.
Resultado: Stryker apuntara a nodos equivocados (campanas de appearance e ics-calendar invalidadas en silencio, exactamente el patron que ya se materializo hoy) y las guardas de project.test.mjs que fijan esas coordenadas quedaran rojas hasta recalcularlas a mano sobre el App.tsx/workspace.tsx ya fusionados.

Gravedad: alta (confirmada, no rebajable). Resolucion obligatoria: fusionar primero App.tsx y workspace.tsx, y solo despues recalcular desde el fichero fusionado los rangos de los dos stryker.*.config.json y de project.test.mjs; ademas conservar los cuatro destinos de la lista blanca y los dos bloques de guardas (5 de extcal + 3 de automations) al final de project.test.mjs. Decidir tambien si se acepta el reformateo Prettier de extcal en los dos JSON como formato unico.

### 15. [ALTA] scripts/project.mjs: la lista de targets permitidos de mutacion choca en el mismo punto de insercion

- **Dimension:** entorno-e2e
- **Ramas:** claude/external-calendar vs claude/automations
- **Rutas:** scripts/project.mjs:75-77 (main), scripts/project.mjs:76-77 en cada rama, scripts/project.mjs:88-104 (bloque 'mutate' de automations)

**Evidencia.** git merge-tree main claude/external-calendar claude/automations produce '@@ -73,8 +73,13 @@' con '<<<<<<< .our  "external_calendar-backend", "external_calendar-frontend" ======= "automations-backend", "automations-frontend" >>>>>>> .their'. Ambas ramas insertan justo detras de '"export_data-persistence-backend",' (scripts/project.mjs:75 en main).

**Consecuencia.** Conflicto de fusion en el motor del arnes. Si se resuelve quedandose con un solo lado, 'bin/harness mutate <target>' de la otra feature lanza 'Invalid target: ...' y su campana de mutacion no se puede ejecutar, es decir, no se puede cerrar la feature (umbral de mutacion).

**Arreglo.** Resolver la union de los cuatro targets en la lista y conservar los cuatro bloques 'if (task === "mutate" && target === ...)' de ambas ramas; comprobar despues que 'node --test scripts/project.test.mjs' valida los cuatro.

**Correccion del verificador.** Titulo: scripts/project.mjs: ambas ramas anaden sus targets de mutacion al final de la misma lista (conflicto textual trivial)

Ramas: claude/external-calendar vs claude/automations (claude/webhooks no toca el fichero)

Rutas: solo scripts/project.mjs:75-77 (lista de targets validos). El bloque 'mutate' de automations en scripts/project.mjs:88-104 NO forma parte del conflicto: se inserta tras la definicion de backend() (~linea 85) mientras external-calendar inserta el suyo en ~linea 202, y git los fusiona sin intervencion.

Evidencia: git merge-tree main claude/external-calendar claude/automations produce '@@ -73,8 +73,13 @@' con '<<<<<<< .our "external_calendar-backend", "external_calendar-frontend" ======= "automations-backend", "automations-frontend" >>>>>>> .their'. Ambas ramas insertan justo detras de '"export_data-persistence-backend",'.

Consecuencia: git marca el conflicto al fusionar la segunda rama. La resolucion correcta es conservar las cuatro entradas (unir ambas listas); no hay semantica que arbitrar. Si alguien resolviese descartando un lado, 'bin/harness mutate <target>' de esa feature lanzaria 'Invalid target', pero eso lo detectan de inmediato los tests ya existentes: scripts/project.test.mjs:1975/1987 (automations-backend/-frontend) y :2003-2004 (external_calendar-backend/-frontend).

Gravedad: BAJA (conflicto mecanico, autoevidente, cubierto por tests). No merece tiempo de analisis: se resuelve en la fusion en 30 segundos.

Nota: en el mismo merge-tree si aparecen conflictos con contenido real que arbitrar en backend/build.gradle.kts (rama 'else ->' de targetClasses, cada rama anade su propio *Classes) y scripts/project.test.mjs:232-236 (rangos de Stryker src/workspace.tsx 97:10-102:22 vs 91:10-96:22). Esos son los que merecen atencion, no este.

### 16. [ALTA] APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES solo gobierna el egreso de la feature 28; la 25 cablea su politica fija, y ambas comparten el tipo AddressPolicy

- **Dimension:** entorno-e2e
- **Ramas:** claude/external-calendar vs claude/webhooks
- **Rutas:** docker-compose.yml:31 (rama 28), .env.example:26-31 (rama 28), backend/.../adapter/config/ApplicationConfiguration.java:435-443 (rama 28), backend/.../adapter/config/ApplicationConfiguration.java:458-461 y 493-496 (rama 25)

**Evidencia.** La 28 anade al compose 'APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES: ${APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES:-false}' (docker-compose.yml:31) y un bean 'connectorAddressPolicy' que lee '${app.connectors.allow-private-addresses:false}' y devuelve 'AddressPolicy.allowingPrivateAddresses()' o 'blockingPrivateAddresses()'. La 25 no lee ninguna propiedad: pasa la referencia estatica 'com.apptolast.organization.application.AddressPolicy::isBlocked' a webhookDestinationGuard (linea 461) y a webhookSender (linea 496). Las dos ramas crean el MISMO fichero application/AddressPolicy.java con formas incompatibles (clase final con 'public static boolean isBlocked(InetAddress)' en la 25; interfaz con 'boolean allows(InetAddress)' y factorias estaticas en la 28), y los javadoc de ambas dicen textualmente que se comparte entre las features 25, 27 y 28.

**Consecuencia.** Al unificar el tipo (obligatorio, mismo nombre cualificado) la variable de entorno queda con semantica ambigua: o el interruptor documentado como 'solo para E2E' pasa a desactivar tambien la guardia SSRF de los envios de webhooks (una variable de compose apaga el control de destino de un componente que hace peticiones salientes con secreto firmado), o los webhooks se quedan fuera del interruptor y el entorno E2E no puede simular un receptor local. Ninguna de las dos opciones esta decidida en las ramas.

**Arreglo.** Decidir el alcance antes de fusionar: si la variable ha de gobernar todo el egreso, inyectar el bean AddressPolicy unificado tambien en webhookDestinationGuard y webhookSender y actualizar el comentario de .env.example y del compose para nombrar las features 25, 27 y 28; si no, renombrar la variable a algo acotado al calendario externo (p. ej. APP_EXTERNAL_CALENDAR_ALLOW_PRIVATE_ADDRESSES) y dejar explicito que los webhooks nunca la leen.

**Correccion del verificador.** COLISION REAL, pero reencuadrada. La dimension primaria no es 'entorno-e2e' sino DUPLICACION DE TIPO CON RUPTURA DE COMPILACION (mismo patron que el SecretCipher ya resuelto entre 27 y 28), con una decision de seguridad colgando.

Lo que pasa de verdad al fusionar:
1. Conflicto add/add en backend/src/main/java/com/apptolast/organization/application/AddressPolicy.java (no esta en main; lo crean las dos ramas). Git lo marca; imposible pasarlo por alto.
2. Conflicto add/add tambien en backend/src/test/java/com/apptolast/organization/application/AddressPolicyTest.java, con asserts incompatibles: la 25 PERMITE 6to4/NAT64 publicos (2002:cb00:7105::1, 64:ff9b::203.0.113.5) tras normalizar el IPv4 embebido; la 28 BLOQUEA 2002::/16 y 64:ff9b::/96 completos. Elijas la implementacion que elijas, una de las dos baterias se cae y hay que reescribirla, no solo reconciliar el fichero de produccion.
3. Conflicto en ApplicationConfiguration.java: las dos ramas anaden bloques de beans al final, desde el mismo ancla (linea 429). Trivial de resolver (concatenar), pero conflicto al fin.
4. Al unificar al tipo interfaz de la 28, las dos referencias estaticas AddressPolicy::isBlocked de la 25 (webhookDestinationGuard ~461 y webhookSender ~496) dejan de compilar. Hay que decidir explicitamente: (a) inyectar el bean connectorAddressPolicy en ambos puntos, con lo cual APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES=true pasa a desactivar TAMBIEN la guardia SSRF del envio de webhooks firmados; o (b) dejar los webhooks con una politica fija al margen del interruptor. Ninguna rama lo decide.

Correccion del segundo cuerno alegado: es FALSO que la opcion (b) impida al E2E simular un receptor local para webhooks. e2e/webhooks-ux.spec.mjs intercepta todo con page.route("**/api/**") y no entrega ningun webhook real, asi que el E2E de la 25 no necesita el interruptor. El unico riesgo vivo es el de la opcion (a): ampliar en silencio el alcance de una variable de compose documentada en .env.example como 'solo para el perfil e2e' hasta apagar el control de destino de un componente que hace peticiones salientes autenticadas.

Gravedad: se mantiene ALTA, pero por el riesgo de que la resolucion del conflicto se haga a la ligera (git obliga a tocarlo, y la salida facil es cablear el bean unico en los cuatro puntos), no por riesgo de que pase inadvertido. Accion en la integracion: resolver a la forma interfaz de la 28, reescribir AddressPolicyTest unificando la semantica IPv6 (decidir si 6to4/NAT64 publicos se permiten o se bloquean por rango), y dejar por escrito si los webhooks quedan dentro o fuera de APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES, actualizando el comentario de .env.example en consecuencia.

### 17. [MEDIA] El parche de CASCADE de la feature 25 y la conversion pendiente a TestDatabase chocan en la misma linea de WeeklyReviewPersistenceTest

- **Dimension:** migraciones
- **Ramas:** claude/webhooks contra main (contra el commit de limpieza de fixtures que aun falta por hacer)
- **Rutas:** backend/src/test/java/com/apptolast/organization/adapter/persistence/WeeklyReviewPersistenceTest.java:707-711, backend/src/test/java/com/apptolast/organization/support/TestDatabase.java

**Evidencia.** Las tres ramas parten del mismo merge-base, 9d81c17, y van cuatro commits por detras de la punta de main (9c2dbe4): 'git merge-base <rama> main' devuelve 9d81c17 para las tres y 'git rev-list --count <rama>..main' devuelve 4 en las tres. Entre esos cuatro commits esta 1d03160, el que introduce TestDatabase. Por eso 'git ls-tree -r <rama> -- backend/src/test/java/com/apptolast/organization/support/' sale vacio en claude/webhooks, claude/external-calendar y claude/automations: ninguna de las tres ha visto siquiera el helper. Y claude/webhooks modifica precisamente la linea 710 de WeeklyReviewPersistenceTest.java para anadirle ' CASCADE', con el razonamiento escrito en su commit de reasentamiento 37b2da5 ('REGRESION AJENA, YA ROJA EN origin/main ... Uso TRUNCATE CASCADE en vez de anadir tabla por tabla').

**Consecuencia.** Si la conversion de fixtures a TestDatabase.empty se hace en main antes de fusionar webhooks -que es el orden correcto segun el hallazgo anterior-, esa misma linea 710 queda cambiada por los dos lados y el merge de claude/webhooks da conflicto justo ahi. El riesgo real no es el conflicto, es resolverlo mal: quedarse con la version de la rama devuelve esa clase al TRUNCATE enumerado a mano y reabre el agujero para la siguiente migracion con clave ajena. Si en cambio se fusiona webhooks primero, el arreglo de CASCADE sobrevive pero convive con 23 clases enumeradas a mano, es decir dos disciplinas distintas para el mismo problema en el mismo directorio.

**Arreglo.** Decidir el orden explicitamente y dejarlo escrito: convertir las 24 fixtures a TestDatabase.empty(jdbc) en main en un commit propio, y al fusionar claude/webhooks resolver el conflicto de WeeklyReviewPersistenceTest.java:707-711 quedandose con la version de main (la llamada al helper), no con el CASCADE de la rama. Comprobar despues que 'git grep -n "TRUNCATE project_custom" backend/src/test' no devuelve nada.

**Correccion del verificador.** Gravedad correcta: BAJA (trivial de resolver), no media.

Descripcion correcta: main ya esta en e3c08a3, que convirtio 29 fixtures a TestDatabase.empty(jdbc), WeeklyReviewPersistenceTest entre ellas. claude/webhooks parte de 9d81c17 y modifica esa misma linea 710 anadiendole ' CASCADE'. Al fusionar webhooks con main habra exactamente UN conflicto en todo el merge, en backend/src/test/java/com/apptolast/organization/adapter/persistence/WeeklyReviewPersistenceTest.java, en el @BeforeEach clean().

Resolucion, sin margen de duda: quedarse con el lado de main (TestDatabase.empty(jdbc);) y descartar la version con CASCADE de la rama, conservando el import de com.apptolast.organization.support.TestDatabase. TestDatabase.empty ya descubre las tablas en information_schema y emite "TRUNCATE ... RESTART IDENTITY CASCADE", asi que absorbe integramente la intencion del parche de la rama y ademas cubre las tablas futuras con clave ajena. No hay decision de orden de fusion que tomar ni disciplina duplicada que arrastrar.

Contexto util para el integrador (verificado con merge-tree en seco): claude/external-calendar fusiona con main sin ningun conflicto; claude/automations tiene un unico conflicto y es en progress/current.md, no en codigo.

### 18. [MEDIA] Dos puertos distintos para lo mismo (comprobar que un endpoint de webhook es del propietario y esta activo), uno de ellos cableado como stub que devuelve siempre false

- **Dimension:** nombres-java
- **Ramas:** claude/automations frente a claude/webhooks
- **Rutas:** backend/src/main/java/com/apptolast/organization/application/WebhookEndpointLookup.java:9-12 (automations), backend/src/main/java/com/apptolast/organization/adapter/config/ApplicationConfiguration.java (automations, bean webhookEndpointLookup: '(owner, endpointId) -> false'), backend/src/main/java/com/apptolast/organization/application/WebhookEndpoints.java:9-20 (webhooks), backend/src/main/java/com/apptolast/organization/domain/WebhookEndpoint.java:7-20 (webhooks)

**Evidencia.** automations define 'public interface WebhookEndpointLookup { boolean isActiveEndpointOf(String owner, UUID endpointId); }' con el javadoc 'Port over the webhook endpoints of feature 25: true only for an active endpoint of this owner. Phase one ships a stub that always answers false; phase two plugs the real adapter', y en ApplicationConfiguration lo cablea con el comentario 'Extension point for phase two. Feature 25 has not shipped webhook endpoints, so this answers <<not mine>> for every endpoint and no NOTIFY_WEBHOOK rule can be stored yet. Replacing this single bean with the real adapter of 25 is the whole change', devolviendo el lambda '(owner, endpointId) -> false'. La premisa ya no se sostiene: webhooks aterriza el mismo dia con 'WebhookEndpoints.find(String owner, UUID id)' devolviendo Optional<WebhookEndpoint> (linea 15) y WebhookEndpoint expone 'String status' (linea 12) con la constante ACTIVE = "active" (linea 17), que es literalmente lo que WebhookEndpointLookup necesita. El uso final esta en CreateAutomation, que pasa el lookup a AutomationReferences (lineas 17 y 20), y su rechazo se traduce a 422 ENDPOINT_NOT_FOUND en ApiErrors mediante WebhookEndpointNotFoundException.

**Consecuencia.** git no ve conflicto y todo compila y pasa en verde, que es lo peligroso: al fusionar quedan dos puertos sobre el mismo concepto y el bean stub sigue devolviendo false para cualquier endpoint, asi que NINGUNA regla de automatizacion con accion NOTIFY_WEBHOOK se puede guardar aunque el endpoint exista y este activo, y el usuario recibe siempre un 422 ENDPOINT_NOT_FOUND con el mensaje 'Elige un endpoint propio y activo'. La feature 30 llega muerta en su integracion con la 25 sin que ningun test lo denuncie, porque los tests de automations se escribieron contra el stub.

**Arreglo.** En la integracion, sustituir el bean stub por un adaptador real de una linea sobre el puerto de webhooks: '(owner, endpointId) -> endpoints.find(owner, endpointId).filter(e -> WebhookEndpoint.ACTIVE.equals(e.status())).isPresent()'. Mejor aun, borrar WebhookEndpointLookup y hacer que AutomationReferences dependa directamente de WebhookEndpoints, para que no queden dos puertos sobre la misma tabla. Y anadir una prueba de integracion que cree un endpoint activo y guarde una regla NOTIFY_WEBHOOK contra el, que es el caso que hoy no cubre nadie.

**Correccion del verificador.** Titulo correcto: "El bean stub WebhookEndpointLookup de la 30 queda cableado a 'false' tras aterrizar la 25, y un test de contrato lo fija en verde". NO es una duplicacion de puertos: WebhookEndpoints (claude/webhooks) es el puerto CRUD del repositorio de endpoints, propiedad de la feature 25, y WebhookEndpointLookup (claude/automations) es un puerto de consulta estrecho al estilo ISP; convivir es diseno legitimo. El defecto real es una premisa caducada: el bean de ApplicationConfiguration.java:480-483 devuelve '(owner, endpointId) -> false' porque "Feature 25 has not shipped", y la 25 aterriza hoy. Alcance mayor que el alegado: ese unico bean se inyecta en createAutomation (linea 496), replaceAutomation (512) Y simulateAutomation (530), asi que crear, sustituir y previsualizar una regla NOTIFY_WEBHOOK devuelven siempre 422 ENDPOINT_NOT_FOUND, incumpliendo el escenario @s5 del Gherkin de la propia 30 (features/automations.feature:74-80: endpoint E propio y activo debe dar 201). Trampa anadida: backend/src/test/java/com/apptolast/organization/adapter/config/AutomationWiringTest.java:99-111 asserta que el lookup responde false, luego la suite queda verde tras la fusion y ademas rompera a quien arregle el bean, en vez de avisar antes. Accion de integracion (no es conflicto de git, hay que hacerla a mano): sustituir el bean por un adaptador que delegue en WebhookEndpoints.find(owner, id).map(WebhookEndpoint::isActive).orElse(false), borrar el comentario "phase two" y el javadoc de WebhookEndpointLookup, y reescribir ese test de cableado para cubrir los cuatro casos de @s5 (propio activo -> true; propio desactivado, de otro dueno e inexistente -> false). Gravedad: baja-media, no media: la consecuencia es real y silenciosa, pero es un pendiente ya documentado en progress/current.md de automations ("No demostrado: la fase 2 entera... El punto de extension WebhookEndpointLookup responde false y tiene test de contrato") y el plan de carriles ya ordenaba 25 antes de la fase 2 de la 30; el riesgo es que se olvide al fusionar las tres el mismo dia.

### 19. [MEDIA] Webhooks y Automatizaciones reescriben a la vez los mismos tres tests de posicion, con estrategias opuestas

- **Dimension:** frontend
- **Ramas:** claude/webhooks contra claude/automations
- **Rutas:** frontend/src/App.test.tsx:19-26, frontend/src/appearance.test.tsx:787-802, frontend/src/export-data.test.tsx:396-403

**Evidencia.** En los tres ficheros `git merge-tree main claude/webhooks claude/automations` marca «changed in both» con bases y blobs distintos. webhooks los vuelve independientes de la posicion (mapea los textos y comprueba `indexOf` creciente, con el comentario «Later features append their own entries, so assert order by name rather than by position»); automations reindexa a mano: appearance.test.tsx pasa de at(-5)..at(-1) a at(-6)..at(-1) anadiendo `at(-1) === "Automatizaciones"`, export-data.test.tsx pasa de at(-4)..at(-1) a at(-5)..at(-1) con `at(-1) === "Automatizaciones"`, y App.test.tsx pasa de length-2/length-1 a length-3/length-2/length-1.

**Consecuencia.** Conflicto textual en los tres ficheros. Si se resuelve quedandose con la version de automations, las aserciones vuelven a ser posicionales y se rompen en cuanto Webhooks se cuela en la cola (y volveran a romperse con la feature siguiente); si se resuelve quedandose con la de webhooks, se pierde la unica comprobacion de que «Automatizaciones» aparece en el menu.

**Arreglo.** Quedarse con la version por-nombre de webhooks en los tres ficheros y anadirle una linea que compruebe la presencia y el orden relativo de «Automatizaciones» (por ejemplo, que su indice sea mayor que el de «API para integraciones»), en vez de fijar at(-1).

**Correccion del verificador.** Titulo: Conflicto textual en tres tests de navegacion entre Webhooks y Automatizaciones, con resolucion unica evidente

Ramas: claude/webhooks contra claude/automations
Rutas: frontend/src/App.test.tsx (hunk @@ -16,13), frontend/src/appearance.test.tsx (hunk @@ -785,21), frontend/src/export-data.test.tsx (hunk @@ -397,10)

Hechos verificados:
- `git merge-tree 9d81c17 claude/webhooks claude/automations` marca los tres ficheros como «changed in both» con base/our/their distintos, y los hunks solapan las mismas lineas. Git conflictara; no hay resolucion automatica.
- claude/webhooks reescribe las tres aserciones para que sean independientes de la posicion (mapea textContent, normaliza con /[^\p{L}\s]/gu, y comprueba orden relativo via indexOf), con el comentario «Later features append their own entries, so assert order by name rather than by position».
- claude/automations reindexa a mano: appearance at(-5)..at(-1) -> at(-6)..at(-1); export-data at(-4)..at(-1) -> at(-5)..at(-1); App.test length-2/length-1 -> length-3/length-2/length-1; en los tres anade `at(-1) === "Automatizaciones"`.
- Ambas ramas insertan su RouteLink en frontend/src/workspace.tsx en el mismo punto, inmediatamente detras de «API para integraciones». Tras la fusion la cola del menu tiene DOS entradas nuevas, asi que las aserciones posicionales de automations fallan sea cual sea el orden en que queden.

Consecuencia correcta:
- Conflicto textual seguro en los tres ficheros, que hay que resolver a mano.
- Resolver «quedandose con automations» reintroduce aserciones posicionales que quedan rojas de inmediato (y volverian a romperse con la feature siguiente).
- Resolver «quedandose con webhooks» NO pierde cobertura: automations aporta frontend/src/automations-route.test.tsx, fichero nuevo que fusiona limpio y que ya verifica la entrada de menu de forma posicional-independiente en tres tests (href /automatizaciones, aria-current=page en su ruta, presencia en la lista desde «/», y ausencia de aria-current desde /exportacion). No existe el dilema alegado.

Resolucion recomendada (mecanica): tomar en los tres ficheros la version de claude/webhooks y, si se quiere reforzar, anadir una sola linea por fichero del tipo `expect(names).toContain("Automatizaciones")`, sin volver a indices.

Aviso adicional descubierto al verificar (fuera del alcance original pero de la misma familia): claude/automations dejo intacto frontend/src/integration-api.test.tsx:819-822, que sigue afirmando que el ultimo enlace del menu es «API para integraciones»; con su propia entrada anadida detras ese test ya deberia estar rojo en su propia rama. claude/webhooks si reescribe ese mismo test a busqueda por nombre y ese fichero fusiona sin conflicto, de modo que la fusion lo repara sola. No revertirlo al resolver.

Gravedad corregida: baja (conflicto que git senala de todos modos, con una resolucion unica y evidente y cero perdida de cobertura; el unico riesgo es que alguien resuelva «take theirs» por inercia, y eso se detecta en la primera ejecucion de la suite frontend).

### 20. [MEDIA] Los rangos linea:columna de Stryker sobre App.tsx y workspace.tsx solo estan recalculados para UNA entrada de navegacion nueva, no para tres

- **Dimension:** frontend
- **Ramas:** claude/external-calendar y claude/automations entre si y contra claude/webhooks (que no toca ninguna configuracion de Stryker)
- **Rutas:** frontend/stryker.appearance.config.json:10-13 (main) frente a :14-17 (external-calendar) y :10-13 (automations); frontend/stryker.ics-calendar.config.json:7-10 (main), :11-14 (external-calendar), :7-10 (automations)

**Evidencia.** main fija `src/App.tsx:35:8-35:44`, `src/App.tsx:57:18-69:34`, `src/App.tsx:92:10-133:7`, `src/workspace.tsx:78:10-83:22`. external-calendar y automations los mueven a los MISMOS valores de App.tsx (`36:8-36:44`, `61:20-73:36`, `98:10-139:7`, es decir +1 linea, la de su unico import) pero a valores DISTINTOS de workspace.tsx: 85:10-90:22 en external-calendar (porque inserta su enlace en workspace.tsx:79-84, antes de «Apariencia») frente a 79:10-84:22 en automations (que inserta al final y no desplaza nada). claude/webhooks no modifica ningun stryker.*.config.json pese a anadir tambien un import y una constante a App.tsx. Lo mismo ocurre en stryker.ics-calendar.config.json (workspace.tsx:97:10-102:22 en external-calendar frente a 91:10-96:22 en automations) y automations toca ademas export-data, history, import-data e integration-api.

**Consecuencia.** Conflicto textual en stryker.appearance.config.json y stryker.ics-calendar.config.json, y sobre todo: con las TRES ramas dentro, App.tsx crece +3 lineas de import (no +1) y el bloque «Apariencia» de workspace.tsx queda en 85, asi que cualquiera de los dos juegos de numeros apunta a codigo equivocado. Los rangos de Stryker se acotan a mano: si senalan lineas que ya no son las del ternario o del RouteLink, la campana muta codigo ajeno o ninguno y el umbral de mutacion pasa a ser vacio.

**Arreglo.** Despues de resolver App.tsx y workspace.tsx, recalcular de una sola vez los rangos de los seis stryker.*.config.json sobre el fichero YA fusionado, en lugar de aceptar los de cualquier rama.

**Correccion del verificador.** TITULO CORREGIDO: Los rangos linea:columna de Stryker quedan desfasados tras la fusion a tres bandas en SEIS ficheros de configuracion, y solo dos de ellos daran conflicto visible.

ARITMETICA CORREGIDA. Cada rama anade DOS lineas que desplazan, no una, en workspace.tsx: un miembro de la union de tipos ademas del bloque de navegacion. webhooks anade `| "Webhooks"` y automations `| "Automatizaciones"`, ambos tras `"API para integraciones"` (main:20); external-calendar anade `| "Calendario externo"` tras `"Calendario"` (main:18). Con las tres dentro el desplazamiento previo a la navegacion es +3, no +1. Por tanto:
  - «Apariencia» queda en 87:10-92:22 (no en 85 como dice la afirmacion, ni en 79 como dice automations, ni en 85 como dice external-calendar).
  - «Calendario» (ics-calendar) queda en 99:10-104:22 (no 97 ni 91).
  - En App.tsx los tres imports si suman +3 (webhooks tras IntegrationApi en main:19; external-calendar y automations tras IntegrationsIndex en main:21) y ademas hay +3 consts y tres ramas nuevas del ternario (+2 lineas cada una) con reindentacion completa, de modo que 36:8 / 61:20 / 98:10 tampoco valen: `const appearance` cae en 38 y el ternario y el bloque de render se desplazan y crecen aun mas.
Conclusion: ninguno de los dos juegos de numeros es correcto, y tampoco lo es el numero propuesto por la afirmacion. Hay que recalcular los seis ficheros DESPUES de fusionar, no elegir un lado.

ALCANCE QUE FALTA (la parte mas peligrosa, omitida en la afirmacion). automations reescribe ademas stryker.export-data.config.json, stryker.history.config.json, stryker.import-data.config.json y stryker.integration-api.config.json. Ni external-calendar ni webhooks tocan esos cuatro ficheros, asi que GIT LOS FUSIONA LIMPIAMENTE, SIN MARCA DE CONFLICTO, arrastrando valores calculados para un mundo en el que solo existe automations. Ejemplo verificado: automations fija import-data -> workspace.tsx:97:10-105:22, pero tras la fusion a tres bandas «Importacion» esta en 105:10-113:22. Deriva silenciosa que sobrevive intacta a la resolucion de conflictos, porque nadie mirara esos ficheros. Los dos ficheros que si dan conflicto (appearance, ics-calendar) son en realidad los benignos: gritan.

MATIZ DE CALIBRACION. main ya venia desfasado en esos cuatro ficheros antes de la fusion (main declara export-data -> workspace.tsx:81:10-86:22 cuando «Exportacion» esta en 84-89; integration-api -> 98:10-105:22 cuando el RouteLink esta en 105-113). La reescritura de automations era una reparacion legitima de deriva preexistente, solo que calibrada para el mundo equivocado. No hay que revertirla, hay que recalcularla.

GRAVEDAD: media se queda corta para la mitad silenciosa. Los dos ficheros en conflicto son media (git los marca, se resuelven a mano recalculando). Los cuatro que se fusionan limpios son media-alta: con `break: 80` en los umbrales, una campana cuyos rangos apuntan a codigo ajeno o inexistente muta lo que no toca o no genera mutantes, y un umbral sobre un conjunto vacio pasa de forma vacua. Eso es precisamente la puerta de cierre de sesion del arnes (`bin/harness verify`), asi que el fallo es de la puerta que deberia detectarlo.

ACCION RECOMENDADA: tras integrar las tres ramas, recalcular a mano los rangos de los seis ficheros contra el App.tsx y workspace.tsx ya fusionados, y de paso revisar los dos ficheros nuevos (stryker.external-calendar.config.json y stryker.automations.config.json), que no contienen rangos linea:columna sobre App.tsx ni workspace.tsx y por tanto no se ven afectados.

### 21. [MEDIA] El alcance PIT de external_calendar apunta a un paquete que no existe: el cifrado AES-GCM no recibe ni un mutante y el guarda lo bendice

- **Dimension:** mutacion
- **Ramas:** claude/external-calendar (arrastre de la unificacion ya resuelta de SecretCipher entre las features 27 y 28)
- **Rutas:** backend/build.gradle.kts (val externalCalendarClasses, entradas adapter.crypto.AesGcmSecretCipher* y adapter.crypto.ConnectorCipher*), scripts/project.test.mjs (prueba 'external calendar PIT scope covers the whole slice and extends the default'), backend/src/main/java/com/apptolast/organization/adapter/connectors/AesGcmSecretCipher.java:1

**Evidencia.** El val externalCalendarClasses que anade la rama incluye 'com.apptolast.organization.adapter.crypto.AesGcmSecretCipher*' y 'com.apptolast.organization.adapter.crypto.ConnectorCipher*'. Pero la clase real declara 'package com.apptolast.organization.adapter.connectors;' (primera linea de backend/src/main/java/com/apptolast/organization/adapter/connectors/AesGcmSecretCipher.java) y git ls-tree sobre la rama muestra que bajo adapter/ solo existen connectors/{AesGcmSecretCipher,ConnectorKeyRing,GithubApiBase,HttpGithubIssueSource} y http/ConnectorsGate: no hay ningun paquete adapter/crypto ni ninguna clase llamada ConnectorCipher en todo el repositorio. El guarda nuevo de scripts/project.test.mjs afirma la lista literal de cadenas, incluidas esas dos, comparando texto con texto, de modo que pasa en verde sin comprobar que la clase exista.

**Consecuencia.** Al ejecutar 'bin/harness mutate external_calendar-backend', PIT no genera ni un mutante para el cifrado de secretos de la feature 28 (nonce de 12 bytes, etiqueta GCM de 128 bits, propietario como AAD) ni para el llavero de rotacion. La puntuacion de la campana sale calculada sobre el resto del corte y se puede superar el umbral de 80 con la pieza criptografica sin cubrir. Nada falla ni avisa: el alcance vacio se reporta como si estuviera cubierto. Y como la clase si esta dentro de githubConnectorClasses via el comodin 'com.apptolast.organization.adapter.connectors.*' (build.gradle.kts:87), el perfil por defecto la muta y esconde el agujero del alcance por feature.

**Arreglo.** Corregir las dos entradas del val externalCalendarClasses a 'com.apptolast.organization.adapter.connectors.AesGcmSecretCipher*' y 'com.apptolast.organization.adapter.connectors.ConnectorKeyRing*' (o eliminar la de ConnectorCipher, que nombra una clase inexistente), y actualizar en el mismo commit la lista literal del guarda de scripts/project.test.mjs. Conviene ademas anadir al guarda una comprobacion de que cada cadena del alcance corresponde a un fichero .java existente, para que un alcance apuntando a la nada vuelva a ser ruidoso.

**Correccion del verificador.** TITULO: El alcance PIT de external_calendar apunta al paquete borrado adapter.crypto: el cifrado AES-GCM y el llavero de rotacion quedan con cero mutantes y el guarda de scripts lo da por bueno.

QUE PASA DE VERDAD: al unificar SecretCipher entre las features 27 y 28 se borraron adapter/crypto/AesGcmSecretCipher y adapter/crypto/ConnectorCipher (documentado en progress/tdd_external_calendar.md:489) y se quedo el adaptador de la 27 en adapter/connectors. Lo que no se actualizo fue el alcance de mutacion: backend/build.gradle.kts lineas 397-398 de la rama siguen nombrando 'com.apptolast.organization.adapter.crypto.AesGcmSecretCipher*' y 'com.apptolast.organization.adapter.crypto.ConnectorCipher*', dos globs que no casan con ninguna clase del repositorio. Como el when de targetClasses hace 'externalCalendarOnly -> externalCalendarClasses' (sustituye, no suma core) y en esa lista no hay ninguna entrada de adapter.connectors, la campana 'bin/harness mutate external_calendar-backend' no genera mutantes ni para AesGcmSecretCipher (nonce de 12 bytes, etiqueta GCM de 128 bits, propietario como AAD) ni para ConnectorKeyRing (rotacion), y ambas clases las MODIFICA esta misma rama respecto a main (se les quita el byte de version y se devuelve Optional). El guarda nuevo scripts/project.test.mjs:2046-2081 afirma la lista literal de cadenas comparando texto con texto, incluidas esas dos entradas muertas, asi que pasa en verde y congela el error.

ESTADO: aun no ha enganado a nadie. No existe progress/mutation_external_calendar.md y la feature 28 sigue "status": "in_progress" en feature_list.json: el riesgo se materializa en el cierre, cuando la puntuacion se calcule sobre el resto del corte y pueda superar el 80 con la pieza criptografica sin cubrir.

RELACION CON LA FUSION: git NO lo marca. externalCalendarClasses es un bloque nuevo del fichero y se fusiona limpio con main y con las otras dos ramas; el guarda pasa en verde. Es una correccion manual posterior a la fusion, no un conflicto.

ARREGLO: sustituir las dos cadenas por 'com.apptolast.organization.adapter.connectors.AesGcmSecretCipher*' y 'com.apptolast.organization.adapter.connectors.ConnectorKeyRing*' en backend/build.gradle.kts y en la lista esperada de scripts/project.test.mjs, y reejecutar la campana de la 28 antes de cerrarla. Conviene ademas reejecutar github_connector tras la fusion: su puntuacion en main es anterior a estos cambios del cifrado.

GRAVEDAD: media-baja (no media). Las dos clases si caen dentro de githubConnectorClasses via el comodin 'com.apptolast.organization.adapter.connectors.*' (build.gradle.kts:79 en main) y del perfil por defecto, asi que existe cobertura de mutacion real aunque con puntuacion obsoleta; lo que falla es la puerta por feature de la 28 y la honestidad del guarda.

### 22. [MEDIA] Las features 25 y 30 reclaman en la spec la misma ranura de navegación: «detrás de API para integraciones»

- **Dimension:** contratos
- **Ramas:** claude/webhooks y claude/automations (contrato en main)
- **Rutas:** project-spec.md:2044, project-spec.md:2461, features/webhooks.feature:441-446, features/automations.feature:499-500, project-spec.md:2263

**Evidencia.** project-spec.md:2044 (feature 25): «Ruta privada /webhooks, H1 «Webhooks», entrada de navegación tras «API para integraciones» sin desplazar Hoy», reforzado por el escenario @s36 (features/webhooks.feature:443): «abre Webhooks desde la entrada de navegación situada tras API para integraciones». project-spec.md:2461 (feature 30): «Ruta /automatizaciones, encabezado «Automatizaciones», entrada de navegación tras Integraciones». La 28, en cambio, solo pide «entrada «Calendario externo» en la navegación principal» sin posición (project-spec.md:2263) y su escenario solo exige `aria-current` (features/external_calendar.feature:500), por eso pudo colocarse antes de «Apariencia» sin romper nada.

**Consecuencia.** Dos secciones del contrato aprobado piden literalmente el mismo hueco. Al fusionar, una de las dos entradas queda en segunda posición tras Integraciones y su texto de spec (y, en el caso de la 25, su escenario @s36, que es normativo y tagueado) deja de describir el producto. Un juez que verifique C6 sobre features/webhooks.feature encontrará el @s36 desalineado con la navegación real si «Automatizaciones» acaba insertada entre medias.

**Arreglo.** Fijar el orden definitivo (por ejemplo API para integraciones → Webhooks → Automatizaciones) y editar project-spec.md:2461 para decir «tras Webhooks» en lugar de «tras Integraciones», dejando :2044 y el @s36 como están; o al revés, pero escribiendo la decisión en un solo sitio y ajustando el escenario que pierda.

**Correccion del verificador.** Título correcto: «Las features 25 y 30 se insertan en el MISMO punto del nav (tras «API para integraciones») y sus suites fijan esa posición de forma mutuamente excluyente».

Alcance real (más que contratos: conflicto textual + suite imposible):
- frontend/src/workspace.tsx — mismos dos hunks en claude/webhooks y claude/automations: la unión de tipos `section` (línea 21, «Webhooks» vs «Automatizaciones») y el `<RouteLink>` añadido inmediatamente tras el de «API para integraciones» (línea ~114). Conflicto de merge garantizado.
- frontend/src/App.tsx — las TRES ramas (también claude/external-calendar) reescriben la misma cadena ternaria de `section` insertando su rama al principio. Conflicto a tres bandas, aunque semánticamente trivial de resolver.
- Incompatibilidad de aserciones (esto es lo que hace que resolver el conflicto textual NO baste):
  · claude/webhooks/frontend/src/webhooks-route.test.tsx:44 (@s36) exige adyacencia estricta: `names[api+1] === "Webhooks"` y que ese enlace lleve aria-current.
  · claude/automations clava la última posición por índice en tres ficheros que la 25 acaba de desindexar: App.test.tsx (`links[len-1]` = «Automatizaciones», `links[len-2]` = «API para integraciones», `links[len-3]` = «Importación»), export-data.test.tsx (`at(-1)`/`at(-2)`), appearance.test.tsx (`at(-1)`/`at(-2)`).
  Ningún orden del nav fusionado deja verdes ambas suites. Además esos tres ficheros de prueba son a su vez zona de conflicto textual entre las dos ramas (la 25 los reescribió a comparación por nombre; la 30 los reescribió a índices duros).
- Colateral ya rojo en cada rama por separado (ninguna lo arregló): e2e/export-data.spec.mjs:61 y e2e/import-data.spec.mjs:65 afirman `nav.getByRole("link").last()` con nombre accesible «API para integraciones». Tanto la 25 como la 30 añaden un enlace DESPUÉS de ese, así que esas dos aserciones E2E ya fallan en cada rama aislada y siguen fallando tras cualquier fusión. Ninguna rama tocó esos ficheros (`git diff main...<rama> -- e2e` solo añade specs nuevas).

La 28 queda fuera: inserta «Calendario externo» entre «Revisión semanal» y «Apariencia» (hunk en workspace.tsx línea ~76), no colisiona con estos dos hunks y no rompe las cuentas `at(-N)` porque inserta por delante; su único choque es la cadena ternaria de App.tsx.

Qué hay que decidir antes de fusionar (no es mecánico): elegir el orden definitivo de las dos entradas terminales, y en consecuencia (a) reescribir las aserciones por índice de la 30 al estilo relativo por nombre que ya introdujo la 25, (b) si «Automatizaciones» acaba entre medias, corregir el paso normativo features/webhooks.feature:444 y project-spec.md:2044 — lo que reabre la puerta de aprobación humana sobre el .feature — o bien colocar «Webhooks» primero y ajustar project-spec.md:2461, y (c) actualizar las dos aserciones `.last()` de los E2E.

Gravedad: ALTA, no media. Motivo: no es solo prosa desalineada; garantiza suite roja en frontend tras la fusión hagas lo que hagas con el conflicto textual, toca cinco ficheros compartidos (workspace.tsx, App.tsx, App.test.tsx, export-data.test.tsx, appearance.test.tsx) más dos specs E2E, y obliga a modificar un .feature aprobado por la puerta humana o a cambiar la spec de la 30.

### 23. [BAJA] application.properties de la 28 declara app.connectors.key y key-previous dos veces

- **Dimension:** spring-wiring
- **Ramas:** claude/external-calendar contra main
- **Rutas:** backend/src/main/resources/application.properties:19-20 y :26-27 (external-calendar)

**Evidencia.** El fichero completo en claude/external-calendar tiene `app.connectors.key=${APP_CONNECTOR_KEY:}` en la linea 19 y otra vez en la 26, y `app.connectors.key-previous=${APP_CONNECTOR_KEY_PREVIOUS:}` en la 20 y otra vez en la 27; las lineas 24-25 son el comentario de la feature 27 que main ya traia. El diff contra main confirma que la rama las ANADE (`+app.connectors.key`, `+app.connectors.key-previous`, `+app.connectors.allow-private-addresses`) sin borrar las de main: es un residuo del reasentamiento b53df2b, cuando main ya las habia introducido con la feature 27. El diff fusiona limpio, o sea que el duplicado sobrevive a la fusion sin conflicto que lo delate.

**Consecuencia.** No rompe el arranque (java.util.Properties se queda con la ultima ocurrencia, y aqui las dos tienen el mismo valor), pero deja el bloque de conectores partido en dos sitios del fichero con un comentario que ya no describe lo que hay debajo, y el ConnectorSettingsTest que la propia 28 anade (:40-45) lee con `Properties.load` y por tanto tampoco lo detecta. La proxima vez que alguien cambie el valor por defecto de una de las dos, editara la ocurrencia equivocada y el cambio no tendra efecto.

**Arreglo.** Al resolver la fusion de backend/src/main/resources/application.properties, borrar las lineas 19-20 duplicadas y dejar un unico bloque de conectores: el comentario de las lineas 24-25 (ampliado a «features 27 y 28»), las dos claves y `app.connectors.allow-private-addresses=${APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES:false}`. Ampliar ConnectorSettingsTest para que afirme que cada clave de conector aparece exactamente una vez en el texto del fichero, no solo que su valor resuelto empieza por `${`.

**Correccion del verificador.** La colision es real y esta bien calibrada como gravedad baja. Unica correccion factual: la atribucion del origen. No es un residuo del reasentamiento b53df2b. Las tres lineas (app.connectors.key, app.connectors.key-previous y app.connectors.allow-private-addresses) las introdujo el commit 3cbb643 "wip(external-calendar): preservar trabajo en curso al aparcar el carril", anterior al reasentamiento; lo que hizo b53df2b fue reasentar sobre una main que ya traia las dos primeras sin deduplicarlas. Segunda matizacion util para la fusion: de las tres lineas anadidas solo dos son duplicado; app.connectors.allow-private-addresses:21 es genuinamente nueva de la 28 y debe conservarse. La correccion al fusionar es borrar las lineas 19-20 de la rama y mover la 21 junto al bloque de conectores de main (tras la linea 24 de main), dejando una sola declaracion de cada clave bajo el comentario. Ninguna de las otras dos ramas (claude/webhooks, claude/automations) toca application.properties, asi que no hay tercera parte implicada.

### 24. [BAJA] Dos conectores nuevos crecen el menu principal y quedan fuera del indice de integraciones, contra la razon de ser declarada de ese indice

- **Dimension:** frontend
- **Ramas:** claude/webhooks y claude/automations contra main (feature 27)
- **Rutas:** frontend/src/integrations-index.tsx:3-6 y :12-28, frontend/src/workspace.tsx:114-119 en ambas ramas

**Evidencia.** El docstring de IntegrationsIndex dice literalmente: «Indice de integraciones. Existe para que cada conector tenga donde vivir sin que el menu principal crezca una entrada por cada uno», y la lista solo enumera «Conector de GitHub» (/integraciones/github) y «API para integraciones» (/integraciones/api). webhooks registra /webhooks y automations /automatizaciones como entradas de primer nivel del menu, y ninguna de las dos anade su ficha a integrations-index.tsx.

**Consecuencia.** Tras el merge el menu principal pasa de 10 a 13 entradas y el indice de integraciones sigue listando dos, aunque los webhooks (con su secreto y sus entregas) y las automatizaciones que notifican webhooks son conectores de pleno derecho. Ningun test se pone rojo por esto: es incoherencia de navegacion, no fallo. El test @s42 de github-connector-routing.test.tsx:40-54 esta escrito a prueba de esto (compara el menu consigo mismo en dos rutas, no contra un numero fijo) y sobrevive.

**Arreglo.** Decidir en la integracion si /webhooks y /automatizaciones van al indice de integraciones o al menu principal, y en el primer caso anadir sus dos entradas a integrations-index.tsx; si se quedan en el menu, actualizar el docstring para que deje de prometer lo contrario.

**Correccion del verificador.** Titulo correcto: webhooks y automations insertan su entrada de menu en el mismo ancla y automations fija posiciones de cola que webhooks invalida (tres tests rojos tras el merge).

Ramas: claude/webhooks y claude/automations entre si (main solo aporta la linea base).

Rutas y hechos verificados:
- frontend/src/workspace.tsx: ambas anaden un valor a la union de `section` justo tras `| "API para integraciones"` y un <RouteLink> justo tras el de /integraciones/api. Conflicto textual add/add en los dos hunks; resolucion trivial (conservar ambos), pero hay que decidir el orden.
- frontend/src/App.tsx: ambas reescriben por completo la misma cadena ternaria de `section` (mismo bloque de ~30 lineas) y ambas insertan su rama delante de `githubConnector && username`. Conflicto grande aunque semanticamente compatible.
- frontend/src/App.test.tsx @s33: webhooks lo reescribe a comparacion por nombre y orden relativo; automations lo deja posicional y anade `links[links.length-1]` = "Automatizaciones". Con las dos entradas presentes, la version de automations falla (Importacion pasa a len-4).
- frontend/src/export-data.test.tsx:397-407 @s22: igual — webhooks pasa a orden por nombre; automations desplaza indices y afirma `links.at(-1)` = "Automatizaciones".
- frontend/src/appearance.test.tsx:785-808 @s20: igual — webhooks pasa a orden por nombre; automations desplaza at(-5)..at(-1) y afirma `at(-1)` = "Automatizaciones".

Consecuencia real: git marca conflicto en los cinco ficheros. Si al resolver se toma el lado de automations en App.test.tsx, export-data.test.tsx o appearance.test.tsx, esos tres tests quedan ROJOS de forma determinista tras el merge. La resolucion correcta es quedarse con las versiones por nombre/orden relativo de webhooks en los tres tests y anadir en ellas la comprobacion de "Automatizaciones", no con las posicionales de automations.

Lo que NO es un hallazgo: que webhooks y automations no aparezcan en integrations-index.tsx. Esta mandado por las specs aprobadas en main (webhooks.feature:444, automations.feature:500), main ya tiene «API para integraciones» simultaneamente en menu e indice, y el contrato verificado del @s42 solo obliga al conector de GitHub y sigue verde. Como mucho es deuda de UX opcional, no una colision de merge.

Gravedad: media-alta (tests rojos deterministas y conflicto en cinco ficheros), no baja.

### 25. [BAJA] application.properties de la 28 declara app.connectors.key y key-previous dos veces

- **Dimension:** entorno-e2e
- **Ramas:** claude/external-calendar contra main (feature 27 ya fusionada)
- **Rutas:** backend/src/main/resources/application.properties:19-21 y 26-27 (rama claude/external-calendar)

**Evidencia.** El fichero de la rama contiene 'app.connectors.key=${APP_CONNECTOR_KEY:}' y 'app.connectors.key-previous=${APP_CONNECTOR_KEY_PREVIOUS:}' en las lineas 19-20 y otra vez, bajo el comentario '# Conector de GitHub (feature 27)...', en las lineas 26-27; main solo las tiene una vez (lineas 23-24).

**Consecuencia.** Duplicado que sobrevive a la fusion (git no lo marca como conflicto porque main no cambio esas lineas). Hoy es inocuo porque ambos valores coinciden, pero deja dos definiciones de la misma clave separadas por otras propiedades: cualquier rotacion futura que edite una sola de ellas queda sin efecto y el fallo es dificil de ver.

**Arreglo.** Borrar la pareja duplicada de las lineas 19-20 y dejar solo app.connectors.allow-private-addresses como propiedad nueva, junto al bloque comentado de conectores que ya existe en main.

**Correccion del verificador.** Descripción corregida y afinada:

QUÉ ES: no es una colisión que nazca al fusionar, sino un artefacto de reasentamiento que YA existe en la punta de claude/external-calendar (b376c8c) y que la fusión se limita a propagar a main. El commit wip 3cbb643 añadió las tres líneas app.connectors.* justo antes de spring.session.*; el reasentamiento b53df2b trajo de main el bloque de la feature 27 que ya declaraba dos de esas tres claves. Nadie limpió el solapamiento.

RUTAS EXACTAS: backend/src/main/resources/application.properties en claude/external-calendar, líneas 19-20 (copia añadida por la rama) y 26-27 (copia heredada de main); en main, líneas 23-24. La tercera línea añadida por la rama, app.connectors.allow-private-addresses (línea 21), es genuinamente nueva y NO está duplicada: no hay que tocarla.

COMPORTAMIENTO REAL: java.util.Properties.load hace que gane la ÚLTIMA ocurrencia, así que las definiciones efectivas son las de 26-27 y las muertas son las de 19-20. La descripción original decía 'cualquier rotación futura que edite una sola de ellas queda sin efecto', lo cual solo es cierto para la primera copia; editar la segunda sí surte efecto. El fallo silencioso concreto es: quien edite las líneas 19-20 no verá ningún cambio de comportamiento.

GIT: no marca conflicto porque main no cambió esa zona; el duplicado sobrevive en silencio. Esto lo hace de las que hay que anotar antes de fusionar, no de las que git recordará por su cuenta.

ARREGLO: borrar las líneas 19-20 en claude/external-calendar antes de la fusión (dejar solo app.connectors.allow-private-addresses en el bloque nuevo, o moverla junto al bloque de la 27). Es una supresión de dos líneas, no genera conflicto con las otras dos ramas (ni webhooks ni automations tocan el fichero) y no rompe ningún test: ConnectorSettingsTest solo exige que cada clave exista y empiece por '${'.

GRAVEDAD: muy baja / cosmética-latente, incluso por debajo de la 'baja' afirmada. Cero impacto funcional hoy (mismos valores); el único coste es una definición muerta que puede confundir en una rotación de claves futura. No debe bloquear la fusión ni consumir tiempo de integración: es una limpieza de un minuto que se puede hacer antes o después de fusionar.

## Descartadas por el verificador

- **(migraciones) La feature 25 reserva V23, por debajo de las V24 y V25 que ya estan aplicadas en main: Flyway se niega a arrancar en cualquier base viva** — Los hechos mecanicos son correctos, pero la consecuencia no se sigue: no existe ninguna base de datos con V24/V25 aplicadas y sin V23.

VERIFICADO Y CIERTO:
- claude/webhooks anade backend/src/main/resources/db/migration/V23__webhooks.sql y conserva V24__calendar_feed_tokens.sql y V25__github_connector.sql de main. main va V22 -> V24 (sin V23). external-calendar aporta V26, automations V28; nadie ocupa V27.
- No hay configuracion de Flyway: solo backend/build.gradle.kts:19-20. application.properties (leido entero, 25 lineas) no contiene ningun spring.flyway.*, ni en main ni en las tres ramas. Valen los defaults outOfOrder=false, validateOnMigrate=true.
- docker-compose.yml declara el volumen con nombre postgres-data y lo registra en volumes:. scripts/e2e.mjs usa proyecto propio (organizationweb-e2e-<pid>) y hace down --volumes en el finally: cada campana E2E arranca virgen.

POR QUE SE REFUTA (la consecuencia es falsa):
1. La produccion NO tiene V24 ni V25. docs/mvp-delivery-plan.md, primera linea (actualizado el 8 de septiembre de 2026): "Las funcionalidades 1-23 estan desplegadas y verificadas en https://organizacion.apptolast.com. Importacion23 esta aceptada en produccion; 24-30 siguen pendientes de implementacion." La feature 23 es importacion = V21__import_receipts.sql; el mismo documento describe la copia restaurada del servidor con "18 migraciones". Las features 24 (V22), 26 (V24) y 27 (V25) nunca se han desplegado.
2. progress/current.md:37 en main lo confirma: "El despliegue productivo de 24-30 no puede hacerse desde este equipo" (SSH: Permission denied (publickey)). README.md:111: "No se ha desplegado OrganizationWeb en el servidor". No existe ningun workflow de despliegue en .github/workflows (solo harness-ci, harness-mutation, guard-sensitive-paths, autonomous-evolve).
3. Sobre una base a V21, Flyway aplica V22, V23, V24, V25, V26 y V28 en orden ascendente: todas pendientes y por encima de la version actual. No hay condicion out-of-order y no se lanza ninguna FlywayValidateException. El "el arranque muere en el despliegue" no ocurre.
4. La unica base que podria llevar V24/V25 sin V23 es el volumen postgres-data de la pila local de algun desarrollador levantada desde que la 26 y la 27 entraron en main (ayer/hoy). Se arregla con docker compose down -v: molestia local de un minuto, no bloqueante, y no afecta a las pruebas (Testcontainers y E2E arrancan limpios).
5. Ademas V23 no es un numero robado por descuido: progress/brief_backend.md:42 documenta la reserva de todo el proyecto — "Reserva para 25-30: V23 webhooks, V24 ics_calendar, V25 github_connector, V26 external_calendar, V27 additional_connectors, V28 automations". El hueco V23 en main existe precisamente porque estaba reservado para la feature 25; fusionar webhooks lo rellena segun lo previsto.

Residual (no es la colision afirmada, y no bloquea la fusion de hoy): el hueco V27 queda libre porque la feature 29 no entra. Cuando la 29 llegue con V27 sobre una base que ya tenga V28 aplicada, ahi si habra un out-of-order real; conviene decidir entonces si se renumera la 29 por encima de V28 o se activa spring.flyway.out-of-order.

- **(migraciones) El destrozo de fixtures de la feature 27 sigue abierto en main: 24 clases truncan sin CASCADE y las tres ramas solo parchean una** — REFUTADA: la afirmacion describe un estado de main que ya no existe. Trabaja sobre 9d81c17 (la base de rebase de las tres ramas), no sobre el main actual.

1) main HEAD es e3c08a3, "test: limpiar las fixtures por el punto unico en vez de listas a mano". Ese commit es exactamente el trabajo que la afirmacion dice que "esta disenado, no hecho". La afirmacion vio 1d03160 (que solo anade TestDatabase.java + su test + el doc) y concluyo que ahi acababa la historia; el commit de continuacion existe y es el HEAD de main.

2) La evidencia clave citada es directamente falsa. La afirmacion dice: "git grep -l TestDatabase main -- backend/src/test solo devuelve TestDatabase.java y TestDatabaseTest.java: ninguna fixture lo usa". Ejecutado sobre main devuelve 31 ficheros: las 29 clases de integracion mas los dos de soporte. Todas las fixtures lo usan.

3) Las lineas concretas citadas dicen lo contrario de lo alegado. En main:
   - ProjectApiTest.java:69 -> "TestDatabase.empty(jdbc);" (no la lista TRUNCATE a mano)
   - TaskApiTest.java:51 -> "TestDatabase.empty(jdbc);"
   - WeeklyReviewPersistenceTest.java:710 -> "TestDatabase.empty(jdbc);"
   - AuthenticationHttpTest.java:47 -> "TestDatabase.empty(jdbc);" (la linea 48 rota ya no existe)

4) En main NO quedan 24 TRUNCATE a mano. "git grep -n TRUNCATE main -- backend/src/test" devuelve solo 5 aciertos: dos usos legitimos y quirurgicos de "TRUNCATE outbox_events" en mitad de un test de HistoryQueriesPersistenceTest (1210 y 1581, tabla unica sin FK entrante, no son limpiezas de @BeforeEach) y tres menciones en comentarios/Javadoc de TestDatabase.java y TestDatabaseTest.java. La lista de 24 clases con su linea exacta corresponde punto por punto al estado de las RAMAS, no de main.

5) La migracion V25 si dice lo alegado (issue_import_receipts.project_id REFERENCES projects(id) en :23, task_external_links.task_id REFERENCES tasks(id) en :55) y TestDatabase.java:32 si construye "TRUNCATE ... RESTART IDENTITY CASCADE" descubriendo tablas en information_schema. Pero eso es la prueba de que el problema esta RESUELTO, no abierto.

6) La consecuencia alegada tampoco se sigue. Las tres ramas parten de 9d81c17 y no tocan esas fixtures (salvo un caso), asi que al fusionar git toma la version de main -que ya es TestDatabase.empty- para todas ellas. No hay 23 clases cayendo en @BeforeEach tras la integracion.

Unico residuo real, y es trivial y git lo marca solo: claude/webhooks es la unica rama que modifica WeeklyReviewPersistenceTest.java:710, anadiendo " CASCADE" a la lista vieja. main reescribio esa misma linea a TestDatabase.empty(jdbc). Eso es un conflicto de fusion de una linea que git senalara y cuya resolucion es quedarse con main. No es una colision oculta ni de gravedad alta, y ademas es el patron ya conocido del enunciado (destrozo de fixtures de la 27), no un hallazgo nuevo.

Nota al margen, fuera de esta dimension: webhooks y external-calendar anaden AMBAS un fichero nuevo con la misma ruta, backend/src/test/java/com/apptolast/organization/application/AddressPolicyTest.java. Eso si es una colision real add/add que git marcara, pero pertenece a otra dimension y no sostiene la afirmacion evaluada aqui.

- **(nombres-java) Dos cifrados de secretos distintos alimentados por la MISMA clave y las MISMAS propiedades, con formatos de sobre incompatibles** — Los hechos citados son literalmente exactos (verifiqué AesGcmWebhookSecrets.java:20/:92-125, WebhookSecrets.java:11-34, el bean webhookSecrets anadido en ApplicationConfiguration, ConnectorConfiguration.java:31-37 en main y los diffs de SecretCipher/AesGcmSecretCipher/ConnectorKeyRing en external-calendar), pero la colision no se sostiene por cuatro razones. (1) Los datos son disjuntos: los secretos de webhook viven en webhook_endpoints.secret_ciphertext (V23__webhooks.sql) y solo los tocan WebhookSecrets/PostgresWebhookWork/ManageWebhook; los de conectores viven en connector_connections y external_calendar y solo los toca SecretCipher. git grep confirma que la rama webhooks no anade ninguna referencia a SecretCipher y que external-calendar no menciona WebhookSecrets en ningun sitio. La consecuencia alegada (leer una fila de webhook_endpoints con el cifrado de conectores y fallar en la etiqueta GCM) no se sigue de ninguna ruta de codigo existente: es un escenario hipotetico de una unificacion futura opcional. (2) Son conceptos legitimamente distintos, no la duplicacion 27/28: alli dos ramas crearon el MISMO nombre cualificado para el MISMO concepto; aqui difieren nombre, paquete, tabla y AAD, y el AAD difiere por dominio (hay N secretos por propietario, uno por endpoint, de ahi endpointId; en conectores hay una conexion por propietario y proveedor, de ahi solo ownerId). La divergencia de AAD que se presenta como impedimento es justo lo que demuestra que no son el mismo puerto. (3) Nada rompe al fusionar: beans de tipos y nombres distintos (webhookSecrets:WebhookSecrets frente a secretCipher:SecretCipher), sin ambiguedad para Spring; ConnectorsGate.PREFIX es /api/v1/me/external-calendar con comparacion exacta o prefijo con barra, asi que no filtra ninguna ruta de webhooks; WebhookConnectorStartup solo emite una linea de auditoria. Compilacion y arranque intactos. (4) Compartir la clave AES-256 entre dos esquemas GCM con nonce aleatorio de 96 bits por escritura y AAD distinto no es un problema criptografico a esta escala. Ademas la premisa "git no marca nada" es falsa en la zona relevante: ambas ramas anexan sus bloques de beans justo tras la linea 429 de ApplicationConfiguration.java y ambas ANADEN application/AddressPolicy.java (add/add), asi que git marcara ambos casos y quien resuelva vera los dos cableados juntos. Lo que queda es deuda de diseno advisory (dos puertos de secretos), no una colision silenciosa de gravedad alta.

- **(nombres-java) El catalogo de tipos de evento del outbox queda triplicado, con las doce cadenas copiadas literalmente en dos ramas y en main** — REFUTADA como colisión de fusión. (1) git no tiene nada que resolver: ninguna de las dos ramas toca domain/OutboxMessage.java (verificado con `git diff --name-only main...claude/webhooks|automations`), y WebhookIntent.java y AutomationEventType.java son ficheros nuevos con nombres distintos. La fusión es limpia, compila y no deja un solo test rojo: hoy las tres listas son idénticas elemento a elemento. (2) El patrón de duplicación YA existe en main por partida doble: además de OutboxMessage.validationCode(), adapter/broker/RabbitBrokerPublisher.java enumera los mismos doce tipos a mano tres veces (switch de 'kind' en líneas 44-55 y dos cadenas ternarias de routing keys en 60-95). Añadir un decimotercer evento ya hoy, antes de fusionar nada, exige editar OutboxMessage + RabbitBrokerPublisher (tres bloques) + sus tests. La fusión no crea el riesgo de mantenimiento: lo hereda de un estilo preexistente del repo. (3) Un dato de la evidencia es falso: alega que 'automations lanza UnknownEventTypeException desde AutomationEventType.of (línea 25-27)'; esas líneas son `return PUBLISHED.contains(raw) ? Optional.of(raw) : Optional.empty();` — of() devuelve Optional y no lanza; el orElseThrow está en domain/AutomationDraft.java:21. (4) Los dos catálogos son decisiones de negocio legítimamente distintas: a qué puede suscribirse un endpoint HTTP externo vs. a qué puede reaccionar una regla interna; que coincidan hoy no los hace el mismo concepto, y cada uno está fijado por su propio test (WebhookIntentTest:100, AutomationEventTypeTest:25-27). (5) La consecuencia alegada (guardar una regla o un endpoint sobre un tipo que el outbox marcará UNSUPPORTED_EVENT) es imposible con el código actual porque los tres conjuntos son iguales; es un riesgo hipotético condicionado a una edición futura descoordinada, del mismo tipo que el que ya se corre con RabbitBrokerPublisher. Es deuda técnica para un ticket de refactor (extraer un EventCatalog único del que dependan OutboxMessage, RabbitBrokerPublisher, WebhookIntent y AutomationEventType), no un bloqueante del plan de integración de hoy.

- **(nombres-java) application.CalendarFeed (feature 28, descargar un ICS ajeno) choca por prefijo con la familia CalendarFeed* de la feature 26 (publicar NUESTRO ICS) en el mismo paquete** — REFUTADA. Los hechos citados son literalmente ciertos, pero no constituyen una colision de fusion no resuelta.

1) NO ES EMERGENTE DE LA FUSION. claude/external-calendar esta reasentada sobre un main que YA contiene toda la familia de la feature 26. Comprobado en el tip de la rama (git ls-tree -r claude/external-calendar .../application/): alli conviven ya CalendarFeed.java, CalendarFeedAddress.java, CalendarFeedLink.java, CalendarFeedStatus.java, CalendarFeedTokens.java, ManageCalendarFeed.java y ManageCalendarFeedUseCase.java. La coexistencia que se denuncia existe HOY dentro de la propia rama y ya paso su TDD/juez alli. Fusionar no introduce nada nuevo: no hay conflicto de git, no hay ruptura de compilacion y no hay ninguna prueba que pase de verde a roja. main sigue sin tener application/CalendarFeed.java (verificado con git cat-file), asi que el fichero entra limpio.

2) NO ES EL PATRON IcsCalendar. Aquel caso era un choque de nombre SIMPLE identico en el mismo espacio de nombres, con ambiguedad real de import. Aqui ningun nombre simple coincide: CalendarFeed (puerto de descarga) frente a CalendarFeedLink / CalendarFeedAddress / CalendarFeedTokens / CalendarFeedStatus / ManageCalendarFeed / CalendarFeedSecret. Son identificadores distintos; el compilador no puede confundirlos y un import equivocado no compila con otro tipo. La consecuencia alegada ("un import equivocado o un autocompletado descuidado mezcla las dos mitades") no se sigue: no hay dos tipos intercambiables. Tampoco hay choque de beans de Spring: ApplicationConfiguration define manageCalendarFeed(...) (26) y calendarFeed(...) (28), nombres distintos.

3) LA CONSECUENCIA SOBRE ExternalCalendarIsolationTest NO SE MATERIALIZA. Lei el fichero completo. SLICE_NAMES es ".*\\.(ExternalCalendar|ExternalEvent|IcsFeed|CalendarFeed|FeedFetch|FeedError|SyncExternalCalendar|SecretCipher).*" y se usa dentro de un noClasses(...).should().dependOnClassesThat(): capturar de mas solo ENDURECE la regla, nunca la afloja. Y las cuatro clases aisladas (ReadToday, PlanBlock, TodayController, BlockController) no referencian CalendarFeed en absoluto (grep sin resultados), asi que no hay falso rojo ni falso verde. El unico efecto real es que la lista de exclusion del segundo test tolera que clases de la 26 dependan de ExternalCalendarStore/SyncExternalCalendar/ReadExternalCalendarEvents: un hueco teorico en una regla arquitectonica interna del carril 28, sin dependencia real que lo explote, y es higiene de test dentro de una sola rama, no una colision entre ramas.

En suma: es a lo sumo una nota de estilo de nomenclatura ya integrada y revisada en la rama 28, no algo que la fusion vaya a romper ni que exija intervencion en la integracion de hoy. Perseguirla costaria tiempo de integracion sin evitar ningun fallo.

- **(nombres-java) Segunda politica de reintentos, con nombre generico en domain, junto a la que main ya tiene incrustada en el publicador del outbox** — Los hechos citados son literalmente correctos, pero no constituyen una colision de fusion.

1) Verificado el texto: `claude/webhooks:backend/.../domain/RetrySchedule.java` existe tal cual se describe (MAX_ATTEMPTS = 6, esperas 1m/5m/30m/2h/24h, javadoc "Fixed back-off after each failed attempt; the sixth failure exhausts the delivery"). Y `main:.../application/PublishOutbox.java` contiene en publishBatch la formula inline `completedAt.plusSeconds(message.attempts() >= 6 ? 60 : 1L << message.attempts())`. Ambas citas son fieles.

2) No hay colision de ningun tipo que git o el compilador vean. `git diff --name-status main...claude/webhooks` marca RetrySchedule.java como **A** (fichero nuevo); no existe ningun `RetrySchedule` en main ni en claude/external-calendar ni en claude/automations (`git ls-tree -r` en las tres ramas solo devuelve ficheros de progress/ con "retry" en el nombre, ninguno Java). Y `git diff --stat main...claude/webhooks -- .../PublishOutbox.java .../PublicationAttempt.java` devuelve **vacio**: la rama de webhooks no toca ninguno de los dos ficheros de main. Fichero nuevo sin homonimo + cero solapamiento = fusion limpia sin intervencion.

3) Son dos conceptos legitimamente distintos, no una duplicacion. `RetrySchedule` solo se invoca desde un sitio: `domain/WebhookDelivery.java:45` (`RetrySchedule.nextAttemptAt(tried, now)`), mas su test. Gobierna la entrega HTTP a endpoints de terceros: escala de minutos a dias, con agotamiento, porque el destino es un servidor ajeno que puede estar caido 24h. La formula de PublishOutbox gobierna la publicacion al broker interno: escala de segundos con techo de 60s y sin agotamiento, porque el broker es infraestructura propia que siempre acaba volviendo. Curvas opuestas porque los dominios son opuestos; unificarlas seria el error, no la duplicacion. Que el numero 6 aparezca en las dos es coincidencia numerica sin acoplamiento (en una es el limite de intentos, en la otra el punto donde 1L<<n supera los 60s, es decir un artefacto de la propia formula).

4) La consecuencia alegada no se sigue. El escenario es "alguien en el futuro editara RetrySchedule creyendo que gobierna el outbox y no cambiara nada": es una hipotesis de confusion futura, no un efecto de esta fusion, y ademas no seria silenciosa (RetryScheduleTest y las pruebas de webhook cambiarian de color mientras las del outbox no se moverian, lo que senala de inmediato el alcance real de la clase). Ademas la clase vive rodeada de WebhookAttempt, WebhookDelivery, WebhookCursor, WebhookEndpoint en el mismo paquete domain, lo que acota su lectura en la practica.

En resumen: es a lo sumo una nota de gusto sobre nomenclatura (un `WebhookRetrySchedule` se leeria mejor), no una colision que la fusion de hoy deba perseguir. Ante la duda, y siendo el criterio explicito que es peor gastar una hora en una colision inexistente, se refuta.

- **(spring-wiring) El worker de webhooks queda apagado en todos los perfiles: app.webhooks.enabled no existe en ninguna propiedad ni variable de entorno** — Las citas literales son correctas (WebhookConfiguration.java:13 lleva @ConditionalOnProperty sin matchIfMissing y la rama 25 no toca application.properties, .env.example, docker-compose.yml ni scripts/), pero NO es una colision de fusion y la consecuencia esta mal deducida. (1) Que el worker este apagado sin la variable es el comportamiento ESPECIFICADO de la propia 25: project-spec.md:2036 y progress/proposal_webhooks.md:44 dicen literalmente «(env APP_WEBHOOKS_ENABLED, por defecto false)» y el Gherkin aprobado features/webhooks.feature:399 exige «app.webhooks.enabled ausente -> ninguna entrega cambia». WebhookScheduleTest.s32 cubre los tres estados. El estado es identico en la rama sola y tras fusionar: la fusion no lo provoca ni lo empeora. (2) No hay interaccion alguna con 28 ni 30: la 25 no modifica ninguno de esos cuatro ficheros, la 28 modifica .env.example, application.properties y docker-compose.yml y la 30 no toca ninguno, asi que git aplica los cambios de 28 limpiamente, sin conflicto ni sobreescritura. (3) La bandera es alcanzable HOY sin tocar codigo: SystemEnvironmentPropertySource de Spring mapea app.webhooks.enabled a APP_WEBHOOKS_ENABLED en getProperty y OnPropertyCondition resuelve por el Environment, luego APP_WEBHOOKS_ENABLED=true en el entorno del contenedor publica el bean aunque no haya linea en application.properties. La linea del publisher (app.publisher.enabled=${OUTBOX_PUBLISHER_ENABLED:false}) es convencion, no requisito, y ademas resuelve tambien a false por defecto: mismo estado final. (4) El agravante que alega sobre la 28 no existe: ConnectorSettingsTest es un @ParameterizedTest sobre exactamente APP_CONNECTOR_KEY, APP_CONNECTOR_KEY_PREVIOUS y APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES, no afirma nada exhaustivo sobre .env.example, luego la ausencia de APP_WEBHOOKS_ENABLED no puede ponerlo rojo. (5) Ninguna prueba ni E2E se pone en rojo: e2e/webhooks-ux.spec.mjs intercepta rutas y sirve entregas fijas con status «succeeded» (:30-34), nunca necesita el worker vivo. Lo unico que queda es el pendiente B10 ya registrado por la propia rama en progress/security_review_connectors.md:51 con severidad «baja»: documentar APP_WEBHOOKS_ENABLED en .env.example/docker-compose.yml y seguir la convencion ${...:false} en application.properties. Es deuda interna de la feature 25 previa a la fusion, no una colision entre ramas, y desde luego no gravedad alta.

- **(spring-wiring) Las tres ramas insertan su bloque de beans en el mismo ancla del final de ApplicationConfiguration** — La evidencia citada es literalmente cierta (la verifique toda: main:432 lineas terminando en readImportReceipt+llave; cabeceras @@ -429,4 +429,113/90/113 @@; 0 borrados en las tres; ExternalCalendarController pide ...UseCases.Sync en el ctor linea 50 y AutomationController pide CreateAutomationUseCase en la linea 36). Incluso simule la fusion con git merge-file sobre blobs extraidos al scratchpad (sin escrituras en el repo, sin checkout): webhooks+external-calendar da exit=1 con UNA sola region de conflicto (lineas 433-626). Pero la colision no merece seguimiento, porque su consecuencia esta mal planteada en los tres puntos que la harian grave. (1) Git la marca a gritos y con la forma menos ambigua que existe: un unico hunk con marcadores estandar <<<<<<< / ======= / >>>>>>>. La afirmacion de que no hay 'ninguna marca que los separe entre si' es falsa: las fusiones son por pares, nunca te enfrentas a tres bloques solapados, sino a dos, resuelves, y luego a dos otra vez, siempre con marcadores. (2) Los bloques son totalmente disjuntos, asi que 'quedarse con ambos lados' es la resolucion completa y correcta: cruce todos los beans anadidos por las tres ramas buscando pares tipo+nombre duplicados y el resultado es cero (webhooks anade webhookStore/webhookSecrets/webhookDestinationGuard/..., external-calendar anade connectorAddressPolicy/outboundGuard/externalCalendarStore/...Sync/..., automations anade automationStore/automationMatcher/createAutomation/...). No hay nada que reconciliar ni nada entre lo que elegir: concatenar es la respuesta, y es mecanico. (3) El modo de fallo que alega esta del reves: a un @RestController escaneado por componentes que le falte una dependencia de constructor le revienta el contexto de Spring ENTERO, asi que pondria en rojo de golpe todas las pruebas que cargan contexto del backend, no 'solo en los @SpringBootTest de esa feature'. Es el fallo mas ruidoso posible, no uno sutil. Aplicando la regla de que es peor perder una hora persiguiendo una colision que dejar pasar una que git marcara igualmente al fusionar: esto es el caso de manual.

- **(spring-wiring) Un tercer puerto de cifrado (WebhookSecrets) cableado sobre app.connectors.key, con formato de sellado incompatible con el SecretCipher que la 28 acaba de unificar** — La rama claude/webhooks NO toca ninguno de los ficheros que la 28 reescribe: `git diff --name-only main...claude/webhooks` sobre adapter/connectors/, application/SecretCipher.java y adapter/config/ConnectorConfiguration.java devuelve vacio, asi que git los fusiona sin intervencion. No es el patron 27/28 (dos ramas creando el MISMO nombre cualificado application/SecretCipher): aqui los tipos se llaman WebhookSecrets y SecretCipher, en paquetes y ficheros distintos, y ningun fichero de la 25 referencia SecretCipher ni ninguno de la 28 referencia WebhookSecrets. Ademas son puertos legitimamente distintos y no unificables tal cual: WebhookSecrets.encrypt(String ownerId, UUID endpointId, String secret) liga el dato autenticado adicional a ownerId+"|"+endpointId (enmienda B6, para que una fila movida a otro endpoint no abra), mientras SecretCipher.encrypt(String ownerId, String plaintext) solo liga el propietario. Las dos consecuencias alegadas no se siguen: (a) rotar NO exige dos procedimientos, porque ambas implementaciones leen las mismas dos propiedades y aceptan vigente+anterior — AesGcmWebhookSecrets.decrypt itera `known` = {current, previous}, asi que una fila sellada con la clave vieja abre con APP_CONNECTOR_KEY_PREVIOUS igual que el llavero unificado, con un solo movimiento de rotacion para las dos; (b) "un cambio futuro de formato en el puerto unificado deja los secretos de webhooks ilegibles" esta invertido: precisamente porque webhooks no pasa por SecretCipher, un cambio de formato alli no puede alcanzar las filas de V23, que solo escribe y lee su propio codigo. Tampoco hay "dos definiciones de conector deshabilitado": ConnectorKeyRing.of (ausente -> enabled() false; mal formada -> excepcion al crear el bean) y AesGcmWebhookSecrets.from (ausente -> WebhookSecrets.DISABLED; mal formada -> IllegalStateException al arrancar) implementan la misma politica. Y el `secrets::decrypt` de ApplicationConfiguration:471 apunta a la interfaz funcional propia de webhooks, no a SecretCipher, asi que el paso a Optional<String> no lo alcanza. Nota lateral, ajena a esta colision: ambas ramas anaden su bloque de beans en la cola de ApplicationConfiguration.java (@@ -429,4 +429,113 @@ en webhooks, @@ -429,4 +429,90 @@ en external-calendar), lo que si produce un conflicto textual trivial de tipo "conservar los dos bloques" que git marcara de todos modos. Lo unico real que queda es duplicacion de la mecanica AES-GCM entre dos adaptadores: deuda de mantenimiento, no colision de fusion.

- **(seguridad) El matcher de ConnectorsGate ignora el context path y rompe el convenio que main usa en sus otros tres matchers** — El texto citado es exacto, pero no es una colision de fusion y la consecuencia no se sigue. (1) ConnectorsGate.java es un fichero nuevo que SOLO anade claude/external-calendar: git grep ConnectorsGate sobre las tres ramas confirma que ni claude/webhooks, ni claude/automations, ni main lo mencionan. Git lo fusiona limpio, no hay zona compartida ni dos versiones que reconciliar; es una nota de revision intra-rama disfrazada de colision. (2) El supuesto convenio de main no existe: en main conviven al menos cuatro estilos de matcher — ExportHeadersFilter:18 concatena el context path, CalendarPaths:15 lo recorta, ApiCredentialController:157-159 usa endsWith sobre la URI cruda, y SessionAccessDeniedHandler:27 usa getServletPath(). La afirmacion escoge tres casos e ignora los que la contradicen. (3) La consecuencia exige un server.servlet.context-path que ninguna de las tres ramas introduce (revisado .env.example, docker-compose.yml y application.properties del diff de external-calendar): es una hipotesis sobre un despliegue futuro, no un efecto de esta fusion. (4) Aun bajo esa hipotesis no hay fallo de seguridad: el gate se registra en SecurityConfiguration:101 y :163 con addFilterAfter(AuthorizationFilter.class), o sea despues de autenticacion y autorizacion, asi que como mucho un usuario ya autenticado veria 500 en lugar de 503 en un despliegue inexistente. Ni git la marcara, ni se manifiesta hoy, ni la provoca juntar estas tres ramas.

- **(seguridad) La ConnectorsGate anadida a la cadena Bearer es codigo inalcanzable: el allowlist de la feature 24 corta antes** — Las citas del código son correctas, pero la conclusión no. Tres motivos independientes para refutar:

1) NO es una colisión de fusión. Todo lo descrito existe ya hoy dentro de claude/external-calendar, que está reasentada sobre main. `git diff --name-status main...claude/webhooks` y `...claude/automations` sobre SecurityConfiguration.java y ApiCredentialBearerFilter.java devuelven VACÍO: ninguna de las otras dos ramas toca esos ficheros. Las tres líneas que añade la 28 (`.addFilterAfter(new ConnectorsGate(...), AuthorizationFilter.class)` dentro del bean `bearerSecurity`) son una adición limpia sobre main que git fusiona sin conflicto ni interacción semántica con las otras ramas. Si esto fuera un defecto, sería un defecto interno de la 28 que ya pasó su propio juez, no algo que aparezca al fusionar.

2) La consecuencia alegada está invertida. La ConnectorsGate en el canal Bearer es redundancia defensiva deliberada, y es precisamente lo que evita que una ruta de conector añadida mañana a PERMISSIONS se salte la puerta. El escenario "empezaría a ejecutarse de golpe con semántica nunca probada" es al revés: si NO estuviera ahí, esa ruta futura sí quedaría sin puerta. Su presencia es la mitigación, no el riesgo.

3) La semántica sí está cubierta. ConnectorsGate es agnóstica a la cadena: `shouldNotFilter` devuelve true salvo en `/api/v1/me/external-calendar[/...]`, y `doFilterInternal` siempre responde 503 CONNECTORS_DISABLED. Ese comportamiento exacto está probado en backend/src/test/java/com/apptolast/organization/adapter/ExternalCalendarDisabledApiTest.java sobre la cadena de sesión @Order(2), donde la 28 añade la MISMA instancia de la misma clase (diff, línea +159). No hay "semántica nunca probada"; es la misma clase, el mismo predicado y la misma respuesta.

Además, en materia de seguridad no hay hueco: el canal Bearer responde 403 API_SCOPE_DENIED a las rutas de conector, que es más restrictivo que el 503 de la puerta. El resultado neto para el atacante es idéntico o mejor. Como mucho queda una línea de código redundante en un canal donde nunca se dispara — observación de limpieza, no colisión de integración, y no debe consumir tiempo del día de fusión.

- **(mutacion) Inventario de configuraciones de Stryker que anclan por linea:columna: 14 hay que recalcular, 8 de ellas no las toca ninguna rama** — El inventario numérico es correcto, pero la tesis, la consecuencia y la gravedad no se sostienen. Verificado en main:

1) NO ES UNA COLISIÓN DE FUSIÓN. Los 8 ficheros "olvidados" no los toca NINGUNA de las tres ramas (`git diff --name-status main...<rama>` lo confirma: webhooks no toca ni un stryker.*.json; external-calendar toca appearance + ics-calendar; automations toca esas 6). Al no tocarlos nadie, git los fusiona limpiamente y no hay nada que resolver en la fusión. Y, sobre todo, LOS 8 YA ESTÁN DESFASADOS EN MAIN HOY, antes de fusionar nada — no "varios", los ocho. En main, App.tsx:10-21 son todo imports:
   - weekly-review `App.tsx:15:8-15:69` → línea 15 = `import { ExportData } from "./export-data";` (42 chars, col 69 no existe)
   - custom-views-fields `App.tsx:17:24-17:49` → línea 17 = `import { Calendar } from "./calendar";` (37 chars)
   - split-task `App.tsx:10:0-14:200` y split-task.replay `10:0-10:200` → imports
   - availability `App.tsx:11:0-11:200`, `14:0-16:200` → imports
   - today `App.tsx:10:0-49:1` → arranca en un import
   La fusión no crea ni empeora nada aquí: es deuda preexistente del repo, idéntica antes y después.

2) CUATRO DE LOS OCHO NI SIQUIERA SON EJECUTABLES. `scripts/project.mjs` en main solo referencia 18 configs: appearance, close-work-session, custom-views-fields, end-time-notification, export-data, github-connector, history, ics-calendar, import-data, integration-api, pause-resume-session, reschedule, schedule-block.replay, start-work-session, today, today.final, today.replay, weekly-review. NO hay target para availability, availability-replay, split-task ni split-task.replay. Son ficheros huérfanos/archivo: ninguna campaña los ejecutará jamás, así que la consecuencia alegada ("Stryker muta el código equivocado / 0 mutantes = puntuación perfecta") es imposible para la mitad de la lista.

3) EL EJEMPLO ESTRELLA ESTÁ AL REVÉS. La afirmación dice que `stryker.today.replay.config.json` "es la campaña más expuesta y nadie la ha revisado". Es exactamente lo contrario: sus rangos están CONGELADOS A PROPÓSITO contra un snapshot histórico. `scripts/project.test.mjs:677-699` define `historicalTodayFile()`, que lee `progress/today_frontend_historical_snapshot.json` con `sourceCommit = 1f7090eb6a114194622d45184a84be4904f302bb` y verifica cada fichero por sha256. El guarda de la línea 1058 (`today replay preserves 63 reviewed identities...`) exige `config.mutate === manifest.selection.map(item => item.range)` y valida cada rango con offsets calculados sobre el App.tsx HISTÓRICO, no el vivo (líneas 1111-1136). Lo mismo el guarda de today en 1146-1191 (`config.mutate` idéntico a `progress/today_frontend_mutation_scope.json` + sha256 del snapshot). "Recalcular" esos rangos a las líneas actuales rompería los guardas y destruiría el sentido del replay: son identidades de mutantes archivadas, no un apuntador al código actual.

4) LA AFIRMACIÓN SOBRE LOS GUARDAS ES FALSA. Dice "solo 4 rangos protegidos por un guarda; los 10 restantes no tienen ni siquiera un guarda que romper". custom-views-fields SÍ tiene guarda (`project.test.mjs:180-200`, contrastado contra `progress/export_frontend_scope_nodes.json`, más otro en 446-470). Y el detalle clave: TODOS estos guardas comparan la config contra manifiestos JSON congelados en `progress/`, nunca contra el App.tsx vivo — por eso ningún guarda se pondrá rojo al desplazarse las líneas. Confirma que son artefactos de archivo por diseño.

En resumen: no hay conflicto de git, no hay test que se rompa, no hay novedad inducida por la fusión, la mitad de los ficheros no son ejecutables y el caso emblemático (today.replay) exige lo contrario de lo que se propone. Perseguir esto es una hora perdida recalculando coordenadas de campañas de mutación ya cerradas y archivadas.

NOTA APARTE (no es esta colisión, y probablemente sea otro hallazgo): el riesgo real inducido por la fusión está en las 6 configs que las ramas SÍ tocaron. automations y external-calendar modifican ambas `stryker.appearance.config.json` e `stryker.ics-calendar.config.json` — eso sí dará conflicto textual —, y cada rama calculó sus rangos contra su propio App.tsx aislado, así que tras fusionar las tres (webhooks incluida, que desplaza App.tsx/workspace.tsx sin actualizar ninguna config) esos rangos recién actualizados quedarán mal. Eso es lo inverso de esta afirmación.

- **(mutacion) Los alcances por feature dejan de ser disjuntos: external_calendar y automations capturan clases de la feature 25 por comodin** — La colisión, tal y como se formula (dimensión mutación / pérdida de disyunción de alcances), no se sostiene. Cuatro comprobaciones, cada una suficiente por sí sola:

1) LA PROPIEDAD QUE DICE ROMPERSE NUNCA EXISTIÓ. El guarda citado, scripts/project.test.mjs:25, es `test("import PIT keeps complete disjoint classes and dedicated tests while extending the default", ...)` y su cuerpo (líneas 26-105) solo compara los TRES subalcances de import_data (Reader / Http / Persistence) entre sí. Es un grep sobre `main:scripts/project.test.mjs`: no hay ninguna otra aparición de "disjoint" en el fichero, y ninguna mención de external_calendar, automations ni webhook. No hay ninguna afirmación de disyunción entre features. Al contrario: el repositorio comparte clases entre alcances a propósito y de forma explícita — `com.apptolast.organization.adapter.config.ApplicationConfiguration*` figura simultáneamente en exportPersistenceClasses (main), en externalCalendarClasses (rama 28) y en automationsClasses (rama 30). Meter una clase compartida en el alcance de la feature que la usa es el patrón establecido, no una violación.

2) LA CONSECUENCIA NO SE SIGUE, PORQUE EL CONJUNTO DE PRUEBAS YA ES LA SUITE ENTERA. En backend/build.gradle.kts de ambas ramas, `targetTests` para los nuevos alcances es `setOf("com.apptolast.organization.*")` (externalCalendarOnly -> …, automationsOnly -> …). Es decir, la campaña ya ejecuta TODAS las pruebas del proyecto contra las clases objetivo. Si tras unificar AddressPolicy gana la implementación de la 25, los mutantes de sus máscaras de bits los mata el propio AddressPolicyTest de la feature 25, que estará en la suite tras la fusión. La afirmación "su umbral break=80 se juzgará sobre pruebas que no son las de la 28" describe un diseño que estas campañas no tienen.

3) LA MITAD DE 'WebhookEndpoint*' ES UN NO-OP MEDIBLE. backend/src/main/java/com/apptolast/organization/application/WebhookEndpoints.java (rama webhooks) es `public interface WebhookEndpoints` con cinco métodos abstractos y cero cuerpo. PIT no genera mutantes sobre métodos abstractos: el comodín `application.WebhookEndpoint*` de automations lo capturaría y produciría exactamente cero mutantes adicionales. Sus otros dos capturados son igual de vacíos (WebhookEndpointLookup es un @FunctionalInterface de una línea, WebhookEndpointNotFoundException una excepción). Además WebhookEndpoints (puerto CRUD de la 25) y WebhookEndpointLookup (puerto de solo lectura de la 30, con javadoc que dice explícitamente "Port over the webhook endpoints of feature 25") son conceptos legítimamente distintos, no una duplicación.

4) EL GUARDA LITERAL SOLO EXISTE EN UNA DE LAS DOS RAMAS. En claude/automations no hay ningún test que fije la lista literal de automationsClasses (grep de "WebhookEndpoint|automationsClasses" en su scripts/project.test.mjs: cero resultados); solo hay "automations backend invokes only its fixed PIT scope", que comprueba el comando. La frase "el guarda que fija la lista literal seguirá en verde" solo aplica a external_calendar (líneas 2049-2069 de su project.test.mjs), y aun ahí seguir en verde es lo correcto: la cadena y la clase que captura son las que la feature 28 realmente usa.

LO QUE SÍ HAY DEBAJO, Y NO ES ESTO: claude/webhooks y claude/external-calendar añaden AMBAS backend/src/main/java/com/apptolast/organization/application/AddressPolicy.java con formas incompatibles (interfaz de 24 líneas con factorías estáticas frente a `public final class` de ~100 líneas con máscaras IPv4/IPv6 y constructor privado), y AMBAS añaden backend/src/test/java/com/apptolast/organization/application/AddressPolicyTest.java. Eso es un conflicto add/add en dos rutas, exactamente el patrón ya materializado hoy con SecretCipher y con IcsCalendar; git lo marcará a gritos en la fusión y nadie puede pasarlo por alto. Reportarlo como una colisión "de dimensión mutación" con la disyunción de alcances de fondo es recalificar mal un conflicto textual evidente y añadirle una consecuencia que las campañas, tal y como están configuradas, no producen. (Aparte, sin relación con lo afirmado: externalCalendarClasses lista `adapter.crypto.AesGcmSecretCipher*` y `adapter.crypto.ConnectorCipher*` mientras el fichero real vive en adapter/connectors/ — comodines muertos dentro de la propia rama, no una colisión entre ramas.)

- **(entorno-e2e) El worker de webhooks nunca existe en ninguna pila: app.webhooks.enabled no esta definido ni en application.properties, ni en el compose, ni en .env.example, ni en el arnes E2E** — Los hechos citados son ciertos pero no constituyen una colision de fusion, y las dos consecuencias graves no se siguen.

1) NO ES COLISION. Ninguna de las tres ramas toca application.properties/docker-compose.yml/.env.example/scripts/e2e.mjs de forma que choque con la 25 en este punto (la 28 toca application.properties, docker-compose.yml y e2e.mjs, pero por lineas de conectores/calendario, no por webhooks). Git fusiona limpio. El hueco de configuracion existe ya, identico, dentro del worktree aislado de claude/webhooks: no aparece "al fusionar", es un pendiente intra-rama de la feature 25 y esta ademas ya registrado por la propia rama en progress/security_review_connectors.md:51 (item B10, gravedad "baja": ".env.example no declara APP_CONNECTOR_KEY, APP_WEBHOOKS_ENABLED...").

2) LA CONSECUENCIA E2E ES FALSA. C:/Users/vhurt/OneDrive/Escritorio/Proyectos/OrganizacionWeb -> git show claude/webhooks:e2e/webhooks-ux.spec.mjs intercepta page.route("**/api/**") y simula TODAS las respuestas (/api/session, /api/v1/me/webhooks GET/POST, entregas...). Su cabecera lo dice literalmente: "This audits real browser UI; it does NOT replace backend acceptance, which the filtered JVM suite covers". Ninguna prueba E2E despacha ni observa una entrega real, asi que la ausencia del worker en la pila E2E no rompe nada ni deja ningun escenario sin cubrir mas alla de lo que el propio disenno declara.

3) LA CONSECUENCIA SOBRE LA 30 ES FALSA. claude/automations no tiene NINGUN worker propio (git grep de @Scheduled en su backend/src/main/java solo devuelve el PublisherSchedule heredado de main). progress/tdd_automations.md:5 y 472-474 y 506-510 dicen que la fase 1 "no ejecuta ninguna regla: solo las declara, las simula y expone su historial", y que NOTIFY_WEBHOOK queda "detras del punto de extension WebhookEndpointLookup, cuyo bean responde false para todo" hasta la fase 2 (AutomationWiringTest.s5_theWebhookExtensionPointRejectsEveryEndpointUntilFeature25Exists). Es decir: tras fusionar, la accion NotifyWebhook no llega a destino porque la 30 no ejecuta reglas todavia, no por la propiedad ausente. Encender app.webhooks.enabled no cambiaria nada para la 30.

4) EL COMPORTAMIENTO ES EL DISENNO ACORDADO, NO UN OLVIDO. project-spec.md:2036 (ya en main) especifica "@ConditionalOnProperty(app.webhooks.enabled) (env APP_WEBHOOKS_ENABLED, por defecto false)", el escenario @s32 de features/webhooks.feature (ya en main) exige que sin la propiedad no se despache nada, y WebhookScheduleTest lo verifica en los tres estados. Ademas replica exactamente el precedente vivo de main: app.publisher.enabled esta en application.properties con default false, docker-compose.yml fija OUTBOX_PUBLISHER_ENABLED: "false" en el compose base, y el worker solo se enciende con el overlay deploy/compose.publisher.yml (OUTBOX_PUBLISHER_ENABLED: "true"). Un worker opcional apagado en el compose base es el patron establecido del repositorio.

Lo unico real que queda es de bajisimo calado y ya inventariado: el compose base reenvia una lista blanca explicita de variables, asi que hoy no hay palanca soportada para encender el worker en despliegue (haria falta declarar APP_WEBHOOKS_ENABLED en application.properties/compose/.env.example, o un overlay analogo al del publisher). Eso es deuda de acabado de la feature 25 -gravedad baja, ya anotada como B10 por su propio revisor-, no una colision de fusion de gravedad alta. Perseguirla como bloqueante de la integracion de hoy es tiempo perdido.

- **(entorno-e2e) El E2E del calendario externo sale a Internet real, y el mecanismo de contenedor de pruebas que documenta .env.example no existe en el compose ni en el arnes** — No es una colision de fusion. (1) Ninguna de las otras dos ramas toca los ficheros implicados: `git diff --name-status main...claude/webhooks` y `...claude/automations` sobre scripts/e2e.mjs, docker-compose.yml, .env.example y playwright.config.mjs devuelven vacio. La rama 28 anade UNA linea al objeto env de scripts/e2e.mjs sobre un main sin cambios: git fusiona limpio y sin interaccion semantica. La situacion existe igual en la rama 28 aislada, antes y despues de fusionar; la fusion ni la crea ni la agrava. (2) Es una decision deliberada y blindada por la propia rama 28: scripts/project.test.mjs:2110-2126 tiene un test llamado "the end to end stack enables the connectors with an explicit key and keeps the SSRF guard" que asserta doesNotMatch(harness, /APP_CONNECTORS_ALLOW_PRIVATE_ADDRESSES: "true"/). No hay nada "sin resolver" entre ramas: la 28 eligio conservar la guardia y renunciar al contenedor de fixtures que planeaba project-spec.md:2291. (3) La consecuencia alegada esta mal calibrada: HttpCalendarFeed.TIMEOUT = Duration.ofSeconds(5), usado como connectTimeout y como timeout de peticion, y cableado asi en ApplicationConfiguration (new HttpCalendarFeed(HttpCalendarFeed.TIMEOUT)). El coste maximo son unos segundos en un solo caso; no hay estado compartido que contaminar y workers:1 solo significa serializado, no acoplado. Sin salida a Internet la resolucion DNS de la guardia falla antes y el caso termina FAILED rapido. (4) El escenario de sincronizacion correcta SI esta cubierto, en HttpCalendarFeedTest, SyncExternalCalendarTest, ExternalCalendarApiTest y las fixtures backend/src/test/resources/ics/*.ics; lo que queda flojo es solo la assertion E2E ["OK","FAILED"], que es una pega de calidad intra-rama para el juez de la 28. Lo unico cierto y accionable es que el comentario de .env.example ("Existe solo para que la suite de extremo a extremo pueda servir un feed desde un contenedor de prueba") quedo obsoleto respecto a la implementacion final, heredado de progress/proposal_external_calendar.md:81: correccion de una linea de documentacion dentro de la rama 28, no un bloqueante de integracion.

- **(entorno-e2e) La lista de comprobacion sintactica del arnes solo registra las specs E2E de la 30; las de la 25 y la 28 quedan fuera** — REFUTADA por dos motivos independientes, cada uno suficiente.

1) La consecuencia no se sigue: la lista de lint NUNCA ha sido la lista de todas las specs E2E. En main hay ~50 ficheros en e2e/ y el bloque 'if (task === "lint")' nombra exactamente DOS de ellos (e2e/create-project.spec.mjs y e2e/github-connector.spec.mjs). Quedan fuera ~48 specs preexistentes, entre ellas e2e/integration-api.spec.mjs (feature 24) y e2e/ics-calendar.spec.mjs (feature 26), features que main YA contiene. Es decir, la "asimetria" que se alega no la crea la fusion: es la convencion vigente del repo desde antes. Las tres specs de la 25 y la 28 quedarian en el mismo saco que otras 48 que ya estan asi hoy, y un error de sintaxis en specs no listadas ya es la situacion normal en main. No hay regresion en el momento de fusionar. La rama que se sale de la convencion es la de automations, que anade las suyas.

2) Git lo resuelve solo, sin intervencion. Solo claude/automations toca el bloque de lint. claude/external-calendar toca scripts/project.mjs unicamente en otras dos zonas (el array de targets validos ~linea 73 y un nuevo bloque de mutate ~linea 202) y claude/webhooks no toca scripts/project.mjs en absoluto. El bloque de lint fusionado sera exactamente la version de automations: sin conflicto, sin hunk perdido, sin decision manual.

Los hechos citados si son correctos (main lista esos 6 ficheros; automations anade sus dos specs; las otras dos ramas no tocan ese bloque), pero de esos hechos no se sigue una colision entre ramas: es una convencion preexistente, no una incompatibilidad materializada por la fusion.

Nota aparte, y que NO es lo que afirma esta colision: el array de targets validos de la linea 73 si recibe adiciones concurrentes de automations y external-calendar en lineas contiguas, y project.test.mjs arrastra el churn conocido de rangos linea:columna de App.tsx (patron ya catalogado). Pero son elementos distintos y el primero lo marcaria git por si mismo.

- **(contratos) Tras fusionar la 25, la 30 sigue declarando por código y por documento que «la feature 25 no existe» y NOTIFY_WEBHOOK queda imposible** — Comprobado en el código: las citas son literalmente correctas (ApplicationConfiguration.java de `claude/automations` sí tiene `webhookEndpointLookup() { return (owner, endpointId) -> false; }` con ese javadoc, el puerto `WebhookEndpointLookup` existe, y `AutomationWiringTest.s5_theWebhookExtensionPointRejectsEveryEndpointUntilFeature25Exists` ancla el stub). Aun así REFUTO, porque no es una colisión de fusión sino un escalonado fase1/fase2 planificado y ya registrado, y la consecuencia central que alega («la contradicción pasa desapercibida») es falsa:

1) No hay nada que git tenga que resolver ni nada que se ponga rojo por esta causa. `claude/webhooks` no toca `WebhookEndpointLookup` ni ese bean (ni siquiera conoce el puerto: no aparece en su árbol); sus beans se añaden al final del mismo fichero, de modo que el único roce es un choque textual de apéndice contiguo (ambos hunks arrancan en `-429`), que es otra colisión distinta y mecánica. Tras fusionar, el comportamiento es exactamente el que ya tiene hoy la rama 30 en solitario: fusionar la 25 no crea el hueco, solo vuelve obsoleta la redacción del javadoc y del nombre del test.

2) El hueco está trazado por escrito en cuatro sitios que entran en main con la propia rama: `feature_list.json` (feature 30, `status: "in_progress"`, descripción «Fase 2 (worker y NOTIFY_WEBHOOK) depende de 25», aceptación «Cumplir @s1–@s43 … antes del cierre»), `progress/current.md` (carril A «25 webhooks → después 30 automations fase 2», carril E «fase 1 … fase 2 tras 25», y «No demostrado: la fase 2 entera (worker y NOTIFY_WEBHOOK real, dependen de 25)»), `progress/tdd_automations.md` («Sustituir ese único bean por el adaptador real de 25 es todo el cambio de la fase 2; ningún escenario de webhook se declara verde por vacuidad») y `progress/integration_plan_25_30.md` (orden 25 → 30 precisamente por esa dependencia). La puerta de cierre sigue intacta: la 30 no puede marcarse `done` con la fila E del @s5 sin cumplir, porque su aceptación exige @s1–@s43. Nadie va a «creer» que NOTIFY_WEBHOOK funciona.

3) Que romper `AutomationWiringTest:99-111` al enchufar el adaptador real sea «un daño» está mal planteado: ese test es explícitamente el test de contrato de la fase 1, y cambiarlo es el trabajo previsto de la fase 2, no una víctima colateral de la fusión.

4) La atribución de `automation_runs.delivery_id` siempre null al stub es incorrecta: la fase 1 no ejecuta ninguna regla (no hay worker), así que ese campo sería null aunque el bean fuera el real.

En suma: quedan unas frases obsoletas («Feature 25 has not shipped webhook endpoints», `...UntilFeature25Exists`) y una tarea de fase 2 ya calendarizada. Gravedad real: baja/documental, no alta, y no requiere ninguna intervención en el momento de la fusión. Perseguirla como colisión haría perder tiempo en trabajo que ya está planificado como fase 2 de la feature 30.

- **(contratos) La fase 2 de la 30 no tiene costura en la 25: encolar una entrega mueve el cursor del endpoint y rompe la invariante «una sola entrega de outbox en vuelo»** — Refutada por tres motivos independientes, cualquiera de ellos suficiente.

(1) La premisa central es falsa. La 25 SI tiene un camino de escritura de entregas que no toca el cursor: WebhookDeliveries.enqueuePing esta respaldado por PostgresWebhookStore.insertDelivery (PostgresWebhookStore.java:145-174), un INSERT generico sobre webhook_deliveries que toma delivery.eventId(), delivery.eventType() y un body arbitrario, y que no escribe nada en webhook_endpoints. Solo el NOMBRE del metodo del puerto es especifico del ping; el adaptador no lo es. La fase 2 necesita una sobrecarga/renombrado aditivo en un puerto de aplicacion (enqueuePing -> enqueue(owner, endpointId, delivery, body)), no un cambio de contrato. La disyuntiva (a) «mover indebidamente el cursor» no existe.

(2) La consecuencia (b) no se sigue. PostgresWebhookWork.claimNext (lineas 49-81) reclama cualquier fila pending de un endpoint active SIN filtrar por e.event_types, asi que la entrega insertada por la automatizacion se despacha y llega a estado terminal con normalidad; no «congela» nada. La pausa de readyEndpoints() es exactamente la serializacion «una en vuelo» que la 25 ya se impone a si misma: la entrega propia siguiente espera un ciclo, igual que espera detras de cualquier entrega propia. Un receptor caido ya detiene el encolado de ese endpoint durante todo el calendario de reintentos por diseno; la automatizacion no introduce una clase de fallo nueva.

(3) No se materializa al fusionar hoy, ni por conflicto de git ni por compilacion ni por pruebas. WebhookEndpointLookup.java de la 30 es un stub documentado que devuelve false («phase one ships a stub that always answers false; phase two plugs the real adapter»), y progress/tdd_automations.md:5 difiere explicitamente a fase 2 los escenarios @s15-@s17, @s20, @s22-@s26 y todas las filas NOTIFY_WEBHOOK reales. En el arbol fusionado ninguna regla NOTIFY_WEBHOOK puede siquiera guardarse, asi que nada ejerce la supuesta incompatibilidad. Ficheros disjuntos, sin solape.

Ademas, «ninguno de los dos carriles lo sabe» esta desmentido por el propio repo: progress/current.md:23 fija la secuencia «A: 25 webhooks -> despues 30 automations fase 2 (worker compartido y NOTIFY_WEBHOOK)», y tdd_automations.md:508 acota el cambio de bean a «el unico cambio de la fase 2 en la parte de guardado», mientras la linea 509 declara el worker entero como no demostrado. La afirmacion ataca un hombre de paja: el carril de la 30 nunca dijo que toda la fase 2 fuera sustituir un bean.

Lo unico real que queda es una nota de redaccion para el futuro: cuando la fase 2 aterrice habra que matizar el texto del @s20 de la 25 («en ningun momento existen dos entregas pendientes derivadas de la outbox»), porque una entrega insertada por automatizaciones podria coexistir con una pendiente del ciclo de la 25. Eso es trabajo planificado y ya secuenciado sobre una dependencia conocida, no una colision de fusion de hoy, y no justifica gravedad alta.

- **(contratos) El puerto genérico de cola de eventos que la spec obliga a compartir entre 25 y 30 no existe en ninguna rama: quedan dos lectores de la outbox y dos cursores** — Comprobado rama por rama; la colisión no se sostiene por cuatro vías independientes.

1) Los dos elementos son conceptos legítimamente distintos, no un puerto duplicado. `application/AutomationEventTail.java` (rama automations) es exactamente esto: `List<AutomationEvent> recent(String owner, int limit)` — «los eventos más recientes no bloqueados de este propietario, del más nuevo al más viejo». Su único implementador es `PostgresAutomationEvents.recent`, cuya consulta es `... ORDER BY occurred_at DESC, event_id DESC LIMIT ?`, y su único consumidor es `SimulateAutomation`. Es el lector de la SIMULACIÓN que la propia spec exige («evalúa ... contra los últimos 100 eventos del propietario ... orden descendente»), no un lector de cola por cursor. `WebhookOutbox.after(ownerId, cursor, horizon)` es orden ASCENDENTE por tupla, con horizonte y avance de cursor transaccional. No comparten nombre, firma, orden, propósito ni fichero.

2) `AutomationRunCursor` tampoco es un cursor de cola: es `record AutomationRunCursor(UUID ruleId, Instant executedAt, UUID id)`, el cursor opaco de paginación de `GET /api/v1/me/automations/{id}/runs`. Nada que ver con el cursor de outbox.

3) La consecuencia alegada («main queda con dos lectores independientes de outbox_events y dos semánticas de cursor conviviendo») es falsa en el árbol fusionado. `git grep -l outbox_events` sobre backend/src/main da, en la rama automations, exactamente los mismos ficheros que en main más `PostgresAutomationEvents` (lectura descendente sin cursor); el único lector por cursor de la outbox tras la fusión es `PostgresWebhookOutbox` de la 25. La 30 NO trae worker: `git grep "Scheduled|fixedDelay|app.automations.enabled"` sobre backend/src/main de la rama automations solo devuelve el `PublisherSchedule` preexistente. `progress/tdd_automations.md` lo dice: «Lo que NO está demostrado: los escenarios de ejecución del worker (fase 2). La fase 1 no ejecuta ninguna regla: sólo las declara, las simula y expone su auditoría». Y `feature_list.json` de esa rama marca la 30 como `in_progress` con «Fase 2 (worker y NOTIFY_WEBHOOK) depende de 25». La tabla `automation_cursors` de V28 existe pero ningún Java la referencia (solo aparece en la limpieza SQL de los dos .spec.mjs de e2e): es una tabla vacía sin uso, no un segundo mecanismo en funcionamiento.

4) El argumento de la ventana de gracia de 5 s se cae con lo anterior: la 30 no tiene lector por cursor al que aplicarle o no esa ventana; el lector que sí tiene (simulación) no avanza cursor, no entrega nada y no debe aplicarla. La «promesa falsa» no existe porque nadie la implementa todavía.

5) Además, git no marcaría nada: son ficheros distintos con nombres distintos, sin solape textual entre `WebhookOutbox.java` y `AutomationEventTail.java`, ni entre V23 y V28.

6) La PREGUNTA ABIERTA de project-spec.md:2407 está de hecho contestada y bien: `progress/gherkin_automations.md:59` la resuelve leyendo la propuesta de 25 («La propuesta de 25 no define ese puerto: guarda el cursor dentro de webhook_endpoints»), y V23 confirma esa lectura (`cursor_occurred_at`, `cursor_event_id` en `webhook_endpoints`, sin tabla de cursores por consumidor). La deuda queda registrada explícitamente en tdd_automations.md:504-505 y en la descripción de feature_list. Es deuda planificada y anotada de la fase 2, no una colisión de fusión que rompa nada hoy.

Lo único cierto del hallazgo es que el texto de project-spec.md quedará desalineado con el código (menciona `EventTail`/`OwnerEventCursor`, que no existen). Eso es un ajuste de documentación de una feature aún `in_progress`, no una colisión de gravedad alta entre ramas.

- **(contratos) La feature 25 se fusiona sin `docs/webhooks.md`, que su propia spec y la enmienda B4 exigen y al que la interfaz debía enlazar** — Los hechos son ciertos pero NO son una colisión de fusión, y perseguirla durante la integración es tiempo perdido.

Comprobado uno a uno:
- `git cat-file -e claude/webhooks:docs/webhooks.md` → no existe; `git ls-tree -r --name-only claude/webhooks -- docs/` devuelve exactamente el mismo listado de 25 ficheros que main (la rama no toca docs/). Cierto.
- project-spec.md:2044 y :2058 en main dicen literalmente lo alegado («ayuda que enlaza `docs/webhooks.md`» y «Documento para receptores `docs/webhooks.md`…»). Cierto.
- La enmienda B4 (project-spec.md, sección «Enmiendas de seguridad a las features 24–28») es en efecto puramente documental. Cierto.
- progress/tdd_webhooks.md:40-44 y progress/gherkin_webhooks.md:66/78/87 dicen lo alegado. Cierto.
- frontend/src/webhooks.tsx no contiene ninguna cadena «docs/». Cierto.

Por qué se refuta igualmente:

1. No hay colisión entre las tres ramas ni con main. Los cuatro refs (main, webhooks, external-calendar, automations) contienen project-spec.md:2044/:2058, progress/proposal_webhooks.md, progress/gherkin_webhooks.md y progress/security_review_connectors.md con **bytes idénticos** (el grep devuelve el mismo texto en las cuatro). No hay dos versiones que git tenga que reconciliar: hay un fichero que nadie ha escrito nunca. Fusionar las tres ramas ni crea, ni empeora, ni revela este hueco; existe igual si no se fusiona nada.

2. Ningún código lo referencia, así que no hay enlace roto que la fusión active. La única mención en artefactos ejecutables sería un enlace desde webhooks.tsx, y ese enlace tampoco existe: no hay `<a href="docs/webhooks.md">` que quede apuntando al vacío. No rompe compilación, ni test, ni Stryker, ni E2E.

3. La feature 25 sigue `"status": "in_progress"` en feature_list.json de la rama (línea 289) y el diff `main...claude/webhooks -- feature_list.json` está **vacío**: la rama no la marca `done`. Por tanto no se está saltando ninguna puerta del arnés al fusionar; el documento es trabajo pendiente de una feature abierta, no una entrega incumplida al cerrarla. La puerta de `done` del coordinador lo atrapará de todos modos.

4. El propio criterio de aceptación de feature_list.json es «Cumplir @s1–@s42 de features/webhooks.feature». En features/webhooks.feature la única mención a docs es `docs/ux-requirements.md` en la cabecera: **ningún escenario del contrato Gherkin aprobado exige docs/webhooks.md ni el enlace desde la UI**. La deuda es contra la prosa de la propuesta, no contra el contrato verificable.

5. La propia bitácora de la rama ya lo tiene registrado como pendiente (progress/tdd_webhooks.md:40-44 lo lista como «B4 (media) — Solo documentación»). Es deuda conocida y trazada, no un hallazgo que la integración deba destapar.

Resumen: es una deuda documental real de un carril inacabado, correctamente anotada en su propia bitácora, que git fusionará sin un solo conflicto. No pertenece a la lista de colisiones de integración; pertenece a la lista de pendientes para cerrar la feature 25.

- **(contratos) Ninguna de las tres ramas toca feature_list.json: no hay conflicto, pero main quedará afirmando que 25, 28 y 30 siguen «in_progress» sin ninguna anotación de cierre** — No es una colisión: es una observación de higiene de proceso, y además la consecuencia que alega no se sigue.

1) Los datos son correctos, pero prueban lo contrario de lo que se afirma. `git rev-parse <ref>:feature_list.json` devuelve fb84f454663f4fe7de3c06a772046be228757dcf en las cuatro referencias, y `git diff --name-status main...<rama> -- feature_list.json CHECKPOINTS.md progress/` confirma que ninguna rama toca feature_list.json ni CHECKPOINTS.md (webhooks solo añade progress/tdd_webhooks.md y progress/ux_webhooks.md; external-calendar solo progress/tdd_external_calendar.md; automations progress/current.md, progress/export_frontend_scope_nodes.json y dos ficheros nuevos). Fichero byte a byte idéntico en las cuatro puntas = cero conflicto, cero interacción entre ramas, cero sensibilidad al orden de integración. No hay nada que git tenga que resolver ni nada que un humano tenga que arbitrar al fusionar. Una "colisión" que consiste en que las tres ramas hicieron exactamente lo mismo (nada) en un fichero no es una colisión.

2) La consecuencia principal es falsa: el árbol fusionado NO incumple C5. C5 (CHECKPOINTS.md:41) pide que «la última feature trabajada está reflejada en su estado correcto», no que esté marcada `done`. Y `in_progress` ES el estado correcto: la propia entrada de cada feature en feature_list.json declara en `acceptance` «con revisión, pruebas y mutación antes del cierre», y `git ls-tree -r <ref> progress/ | grep -E '(judge|mutation)_(webhooks|external_calendar|automations)'` devuelve vacío en las cuatro referencias. Sin juez ni mutación, `done` está prohibido por CLAUDE.md — cosa que la propia afirmación reconoce. Es decir: el manifiesto dice la verdad, y `bin/harness status` daría un resumen correcto, no falso. El hallazgo se autorrefuta.

3) El estado que describe como daño post-fusión ya existe hoy en main, sin fusionar nada. main contiene el código completo de las features 26 y 27 y sus juicios (progress/judge_ics_calendar.md, progress/judge_github_connector.md están en main), y aun así ambas siguen `in_progress` en feature_list.json. Es decir, «código integrado con manifiesto en in_progress» es el régimen establecido y aceptado del repositorio, no algo que estas tres fusiones introduzcan. Fusionar 25, 28 y 30 no degrada nada respecto a lo que main ya afirma.

4) Lo que queda es una tarea de cierre de sesión rutinaria (registrar la integración en progress/current.md e history.md, lanzar juez y mutación para 25/28/30 cuando toque), no una colisión que buscar antes de fusionar. Perseguirla en la fase de detección de colisiones desvía tiempo del riesgo real. La única coincidencia con potencial de conflicto textual en esta zona es progress/current.md, que solo modifica claude/automations — una sola rama, por tanto tampoco colisiona.

- **(contratos) progress/current.md solo lo actualiza la 30, con un hash de reasentamiento equivocado, y su tabla de carriles queda desfasada tras la fusión** — REFUTADA. Comprobaciones sobre el repo:

1) NO es una colisión entre ramas. `git diff --name-status main...<rama> -- progress/current.md` devuelve 'M' solo en claude/automations; webhooks y external-calendar no lo tocan. Un único escritor no colisiona con nadie.

2) Lo único que git marcará es un add/add trivial al final del fichero, y lo marcará solo. La base de fusión es 9d81c17, donde current.md tiene 119 líneas. Main añadió después la sección «Deuda identificada, aplazada a propósito» (@@ -117,3 +117,34 @@) y automations añadió «Carril claude/automations — feature 30, fase 1» (@@ -117,3 +117,18 @@): ambos apéndices al EOF desde la misma base. Git lo señalará por sí mismo y la resolución es «conservar los dos textos», sin análisis previo. Es exactamente el caso que el propio encargo dice que no vale la pena perseguir.

3) La consecuencia alegada ya existe en main HOY, antes de fusionar nada. La tabla «Siete carriles activos» (main:progress/current.md:102-116) ya lista 26 `claude/ics-calendar` y 27 `claude/github-connector` como carriles activos aunque ambas están integradas (a08d3be «merge: integrar la feature 27»; 9c2dbe4 registra el dictamen de 27 y la mutación del ICS). Esa desactualización es deuda preexistente de main, no algo que produzcan estas tres fusiones; fusionar las tres no la empeora ni la crea.

4) La evidencia de progress/tdd_automations.md:252-253 está mal atribuida. Esas líneas dicen «**`App.tsx` NO dio conflicto**: ningún otro carril ha aterrizado todavía su entrada de navegación en main. La mía es la única añadida; cuando lleguen calendario, webhooks y conectores habrá que conservarlas todas». Hablan de las entradas de navegación de App.tsx, no de progress/current.md. No «reconocen el arrastre» de nada relativo a current.md.

5) «Las bitácoras de 25 y 28 no aparecen mencionadas en ninguna parte de current.md» es falso en lo que importa: ambos carriles figuran nominalmente en la tabla (`25 | claude/webhooks`, `28 | claude/external-calendar`). Y current.md no nombra el fichero tdd_*.md de NINGÚN carril (ni el de 26 ni el de 27, ya integrados): solo referencia brief_backend.md, brief_frontend.md, proposal_<name>.md, darkmode_audit.md y judge_integration_api_final.md. No hay regresión de localizabilidad introducida por la fusión.

6) El «hash de reasentamiento equivocado» no es equivocado, es histórico. 7ea682d existe y es «feat(integration_api): cerrar la feature 24 como done»; era origin/main en el ciclo 16, tal y como documenta tdd_automations.md:245. Los reasentamientos posteriores (be03141 sobre a6164e4, 97861b3 sobre 9d81c17) lo dejan viejo, pero además main ya está en e3c08a3, así que cualquier hash escrito ahí nace caducado. Es una nota de bitácora desactualizada, no un contrato roto.

En suma: cero colisión de contratos entre ramas, el único choque textual lo marca git solo y se resuelve conservando ambos párrafos, y el resto es higiene documental preexistente con dos piezas de evidencia mal leídas.

