# Revisión independiente del núcleo y PostgreSQL16

**APPROVED para este paquete.** No encontré un defecto contractual accionable ni un hueco diferenciable que justifique otra prueba antes del gate. Revisión de lectura, sin ejecutar Gradle/PIT ni modificar producto o tests. Excluye mis adaptadores HTTP/publicador y no constituye cierre global de16.

Corte contrastado en79109d: los19 archivos de `close_work_backend_hashes.json` coinciden con el árbol. Los ocho XML preservados en `close_work_backend_xml` contienen115 pruebas, cero fallos y cero errores; son evidencia del autor, no una ejecución nueva. Se leyeron el mapa de `tdd_close_work_backend.md`, `review_close_work_implementation.md`, contrato s1–s27, fuentes y pruebas concretas de migración, carreras, replay y rollback.

- **Precedencia e intención:** Store:65–82 bloquea la sesión propia, verifica la identidad completa del token y busca replay antes de invocar negocio. Receipt:25–41 compara sesión/acción/revisión y las dos notas normalizadas; Store:141 reutiliza la misma comparación después del rollback por colisión. WorkSessionState:20–25 conserva revisión, compatibilidad terminal y agotamiento antes del reloj. Un CLOSE histórico no depende del estado actual ni de otra sesión abierta. Notas inválidas se rechazan al construir el valor, sin recortar texto; la frontera JSON/HTTP queda fuera de esta revisión.
- **Tiempo y terminalidad:** ChangeWorkSession:31–52 captura una vez y trunca a microsegundos, suma sólo el tramo running y conserva SessionStart. El rango válido completo cabe en long de microsegundos; no se convierte a nanosegundos ni Number. El fallback abarca sólo la resolución de ZoneId; desbordamiento del año local sigue siendo error. ReadWorkSessionState deja fijo el neto de closed incluso si el reloj retrocede. PAUSE/RESUME no aceptan closed por su compatibilidad exacta compartida.
- **Locks y atomicidad:** un único `FOR UPDATE` de sesión ordena decisiones; cerrar no consulta elegibilidad, preferencias ni bloquea proyecto/tarea. Las carreras observan ambos workers esperando locks reales antes de liberarlos. Los dos intercalados de inicio mantienen la transacción exterior sin commit y distinguen confirmación de rollback. Supresiones, SQL tardío y fallo al commit verifican conservación de filas y plaza. La colisión owner/key se recupera en otra transacción, mientras otras restricciones quedan como fallo de almacenamiento.
- **Durabilidad y upgrade:** V17 sólo añade el índice parcial de un CLOSE por sesión. El test de upgrade16→17 compara filas completas de inicios, intervalos, recibos y outbox y luego cierra running/paused. La serialización omite closure ausente para preservar JSONB15 y su constructor de seis campos. F verifica propiedad y recibo dentro de un snapshot read-only/RR, sin reloj ni catálogo; el caso concurrente confirma que no mezcla ambas consultas con un cierre recién cometido.

Acepto la evidencia compuesta declarada para s3 (cero microsegundos), s12/s13 (guardas comunes), s20 (ausencia de elegibilidad en el camino compartido) y s24 (rango de reloj compartido). La lectura no muestra una rama nueva sin cubrir en esas conexiones que exija duplicar filas por su etiqueta. La recuperación tardía de colisión con intención idéntica sigue siendo un límite explícito: el lock de esa misma sesión serializa el recorrido natural; el test inyecta sólo la omisión inicial del lookup para observar UNIQUE real y rollback, sin fingir dos sesiones abiertas. No reclasifica mutantes ni sustituye la campaña pendiente.
